## 04

对这张完整黑金人物置换效果图只进行以下定向修正，全部照片和布局保持不变。左侧任务中心数字3改成2。右侧两个张工职业标签统一改「乡墅设计师」，不是建筑设计师。当前场景设计讲解统一改「设计室讲解」。底部「确认首帧并继续」按钮下方加一行小字「下一步选择复刻编辑或图生视频，不会立即生成」。其余所有内容保持原样，特别是原始画面人物和置换后的张工是两张不同图片，庭院与姿态一致。

## 05

精准编辑这张完整AI视频生成页面，仅调整右侧「生成数量」这一行：去掉原来的减号、数字、加号，以及「按条数计费」注解。改成三个横向分段选项「1个」「2个」「4个」，「1个」金色选中，其余两个深色未选中，不能出现3个。数量区域尺寸不扩大，不挤压其他控件。保留当前时长、分辨率、七种比例、所有素材和图片、提示词内容、当前一二级页签、提交前费用说明和主按钮完全不变。左侧任务中心数字2。不得出现新的金额或积分报价。

## 06

精准编辑这张完整AI视频生成页面，仅调整右侧「生成数量」这一行：去掉原来的减号、数字、加号，以及「按条数计费」注解。改成三个横向分段选项「1个」「2个」「4个」，「1个」金色选中，其余两个深色未选中，不能出现3个。数量区域尺寸不扩大，不挤压其他控件。保留当前时长、分辨率、七种比例、所有素材和图片、提示词内容、当前一二级页签、提交前费用说明和主按钮完全不变。左侧任务中心数字2。不得出现新的金额或积分报价。

## 07

精准编辑这张完整AI视频生成页面，仅调整右侧「生成数量」这一行：去掉原来的减号、数字、加号，以及「按条数计费」注解。改成三个横向分段选项「1个」「2个」「4个」，「1个」金色选中，其余两个深色未选中，不能出现3个。数量区域尺寸不扩大，不挤压其他控件。保留当前时长、分辨率、七种比例、所有素材和图片、提示词内容、当前一二级页签、提交前费用说明和主按钮完全不变。左侧任务中心数字2。不得出现新的金额或积分报价。
