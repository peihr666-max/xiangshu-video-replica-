# V1.3 制作检查与交付边界

日期：2026-09-05。结论：28 张新增/修订效果图已出齐；业务结构和主要交接状态完成制作自检，交由用户审核。用户审核状态全部保留为“待审核”，没有代替用户批准。

## 1. 交付物

- 28 张独立 PNG，每张对应一个页面或关键状态，全部与生成原图的 SHA-256 一致。
- 00-打开审核.html：本地静态审核图册，可筛选模块、按业务链看图、填写并导出意见。
- 00-模块衔接与跳转需求.md：F01–F30 共 30 条关系，包含入口、携带对象、返回、失效和禁止行为。
- 99-页面清单.json：页面编号、模块、参考来源、修订次数、尺寸、字节数、SHA-256 和最终生成来源。
- 01-审核意见模板.md：28 页空白审核模板。
- 六份出图/修订提示词记录，保留设计可追溯性。

图片尺寸为 1586×992（26张）、1577×997（1张）和1585×992（1张），总计约43.7 MiB。生成器输出尺寸存在少量差异，未通过拉伸、裁切或覆盖文字加工图片。

## 2. 本轮重点修正

| 检查点 | 修正结果 |
|---|---|
| 文案二创到数字人口播 | 08、20E、20C 使用同一张工建房预算 V3 正文；带入确认版本，不要求重复提取/改写 |
| 成片到发布 | 20D、12 承接同一28秒成片；发布标题可编辑，但不改视频中已经生成的字幕与正文 |
| IP 场景照片到置换 | 15B→04A带入目标人物与场景版本；原始画面独立提供，缺失时不能提交 |
| 置换到视频 | 04为置换首帧；03仅带入所选镜头，静态首帧不伪装成已生成视频播放器 |
| 补资产返回 | 20F为李总试听确认声音后返回的原草稿，不混接张工声音页，不重复转写/改写 |
| 草稿与任务 | 声音待确认标为未提交草稿；已提交任务显示真实执行阶段，避免混称收费任务 |
| 失败续作 | 09A复用已完成配音与数字人结果；已知失败环节重试须重新确认费用，受理不明先核对 |
| 人物资产边界 | 五视图为一张拼合图；多场景照片、可驱动口播形象、声音档案分开；肖像与声音授权分别校验 |
| 爆款内容 | 抖音/视频号各30条设计示例；提供两种创作用途，视频简介不冒充完整口播文案 |
| 前端与后台 | 用户页不放采集设置、API参数、密钥或云端运维配置 |
| 参数一致性 | 05–07均保留七种画面比例、1/2/4生成数量；能力范围待用户提供的接口资料验证 |

## 3. 视觉检查

主制作人逐页检查28张最终效果图，并对文字、人物、场景、入口和任务状态进行定向修订。独立复核覆盖13页，意见已结合业务场景采纳或复核。

工作台的两个快捷流程入口是“视频复刻 / 数字人口播”；四个完整功能标签在“视频创作”页面内，不要求工作台再复制四个标签。

本轮使用 imagegen-frontend-web 的单页出图与参考图约束，并按 imagegen 要求通过图像生成工具定向修订；没有用代码覆盖生成图文字。visual-verdict 技能在当前会话不可用，因此采用逐页人工视觉检查及独立复核，未声称执行该技能。检查记录位于仓库 .omx/state/ai-jichuang-v13/ralph-progress.json；没有启动 OMX 执行工作流。

已知非阻断项：12页发布账号旁的小头像仍是示意，不应被作为张工人物资产的正式来源；实施时须读取绑定账号的真实头像。正式界面还应使用同一份官方矢量Logo与受控人物资产，避免生成图片细节漂移。以上不影响两条主要业务链的页面审核。

## 4. 已执行验证

- 静态清单检查：28个唯一页面、PNG签名/尺寸、HTML引用完整，通过。
- 生成来源核对：28张交付PNG均与原始生成文件逐一哈希匹配，通过。
- 本地HTTP核对：28张图片、审核HTML、模块需求文档共30个资源返回成功，响应内容与本地文件逐字节一致。
- JavaScript语法检查：审核图册1段内联脚本解析通过；检查脚本执行通过。
- 需求编号：F01–F30共30项，无重复。
- 浏览器加载：28/28张缩略图完成加载且有有效原始尺寸。
- 大图操作：工作台起点、下一张、关闭、原图链接与两条指定业务链的完整顺序已核对。
- 模块筛选：人物库显示6/28页；结论筛选与测试意见记录一致。
- 意见保存：临时审核意见在刷新后仍保留；测试内容随后清除，恢复28页待审核。
- 意见导出：实际下载Markdown并读取核对28个页面段落；其空白版本已放入交付目录。
- 文本疑似密钥扫描：交付目录未发现疑似真实密钥或凭证。
- 正式代码：git diff --stat为空；未修改已跟踪的正式业务代码或覆盖既有V1.1/V1.2包。

## 5. 没有执行、不能据此宣称完成的事项

本次仅包含图像、需求文档和审核工具，不是正式前后端业务开发。未调用真实视频/数字人生成、转写、云上传、收费、采集或发布接口，没有生产操作，也未进行上述功能的真实链路验收。

不增加任何软件依赖；因此没有依赖变更扫描。本轮未运行与静态交付无关的业务服务全量pytest、PG、Rust、正式前端构建或CI；这些检查不能由静态图册结果代替。

文生/图生/参考生视频界面参数为待确认设计目标，不能当作当前H3接口能力证明。数字人口播费用未确定，20C与09A展示待报价且不能提交。正式实现须补齐有效报价、过期、余额不足、重复提交防护、资产授权和草稿返回等验收。

图册里的“按链审核”仅调整看图顺序，不代表页面内真实业务路由已实现。用户确认设计后，再按模块衔接文档实施与验收。
