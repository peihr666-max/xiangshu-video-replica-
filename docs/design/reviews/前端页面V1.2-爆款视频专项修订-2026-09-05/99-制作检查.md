# 制作检查记录

版本：V1.2 爆款视频用户端专项修订。用户审核状态：待审核。

## 核对范围

- 17A 抖音列表：原图视觉检查通过；独立复核通过。前端无采集/API/云端运维设置，6 张卡均属于抖音，30 条按 6 条/页分为 5 页。
- 17B 视频号列表：原图视觉检查通过；独立复核通过。平台内容未混排，数量与分页一致，无明显文字裁切。
- 17C 视频详情：原图视觉检查通过；独立复核通过。与 17A 首卡的标题、作者、时长和互动数字保持一致。
- 沿用黑金色、众墅之家与 AI 即创命名、已整合的侧栏和乡墅行业文案；三张图均不展示用户端采集设置。

## 图册检查

浏览器已检查三张原图加载、大图显示与翻页。审核意见保存并重新打开页面后保留，筛选能只显示“需调整”条目；自检意见已清空，再次打开确认 3 张均为“待审核”、意见为空。1280 宽视口未发现横向溢出；大图弹窗按原有方式允许纵向滚动。

本专项 4 项自动检查全部通过：3 页与审核存储键隔离、全部本地资源存在、PNG 尺寸/字节数/SHA-256 与清单一致、脚本语法与最新前后台分工声明。旧版 V1.2 的 4 项回归检查也全部通过。

图册复用原 V1.2 结构，只替换本模块页面与文案、使用独立浏览器存储键，未增加依赖。原 V1.2 的 4 项文件/资源/哈希/脚本检查通过，原 19 张效果图保留未改。

## 制作方式与边界

采用内置 imagegen，每页独立生成；本轮用户改变分工之前的采集设置稿及同步稿均不进入审核包。详情初次请求长时间未返回，使用内置工具单张重试后取得新图。生成源路径、尺寸和 SHA-256 见“99-页面清单.json”，提示词见“99-出图规格与提示词.md”。

当前未提供 visual-verdict 技能，本轮以原图检查、独立复核和浏览器实际打开作为视觉证据；不声称调用了不可用工具。

只改本次设计审核材料，未修改 server/client 业务代码，未运行或承诺通过产品全量 CI。安全特征检查未发现疑似硬编码密钥，未新增依赖。本轮未调用真实采集 API、云存储或付费视频服务；实际视频播放和复刻业务尚未接入，图中内容为设计示例。

所有采集 API 参数以用户提供为准；本轮不再搜索，也不把此前搜索所得参数写成确定的接口契约。
