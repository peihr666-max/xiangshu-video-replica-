【页面主题】激活码列表（改版：补有效期、批次、发放时间列），面包屑：客户运营 / 激活码。
【页面结构】
顶部筛选栏：搜索框（掩码码/绑定用户名）、状态下拉（全部/待启用/可使用/使用中/已暂停/已撤销/已过期，共 7 项）、批次下拉（零额度授权码-202608 等）、「显示已归档」复选框、查询/重置按钮。
表格列：激活码（掩码等宽字体 + 复制图标）、状态徽章（使用中绿/可使用蓝/待启用灰/已暂停橙/已撤销红/已过期深灰）、绑定账号（customer-a1b2c3d4e5f6 或「未绑定」灰字）、关联设备（「2 台」蓝色可展开，带下箭头）、有效期至（2027-08-31 正常黑字；其中一行 2026-08-31 显红色+「已过期」角标）、批次（零额度授权码-202608 小灰字）、发放时间、最近活跃、操作（使用中行显示「暂停」「撤销」灰边按钮；已过期行操作列灰禁用）。
示例 6 行覆盖不同状态，其中 1 行为已过期（有效期红色、绑定账号仍显示但灰字），1 行未绑定。展开行示例（第 1 行下方缩进展开）：子表「绑定设备」列：设备名/平台/槽位/状态/绑定时间/最近活跃/操作（解绑）；旁边小卡「待批准配对 1」：设备「新笔记本·Windows」·申请于 10 分钟前，按钮「批准设备」「替换现有设备」，下方红字小注「槽位 1 已解绑时才可批准」。右上角「共 1,024 枚」，分页器。
