你的唯一任务：调用你内置的图像生成工具（Image Gen），按下面【图像提示词】生成一张图片，然后把生成的 PNG 保存到当前工作目录，文件名必须严格等于下面指定的名字。要求：
1) 图片为宽幅横版（优先 1536x1024）。
2) 只做两件事：生成图片、保存文件；不要读取仓库其他文件、不要改任何代码、不要运行测试。
3) 如果工具产物在别的目录（如生成图片缓存目录），用复制的方式保存到当前目录并重命名。
4) 完成后最终回复只写一行：OK <实际保存的文件名>。

【图像提示词开始】
【统一风格要求】
现代企业级中文 SaaS 管理后台网页界面效果图，桌面端宽屏浏览器（16:9 横版构图，约 1536x1024）。浅色主题：页面背景浅灰 #F5F6F8，内容区为白色圆角卡片（8px 圆角、1px 浅灰边框、极轻投影）；主色品牌蓝 #2563EB；徽章语义色：成功/在线=绿，警告/待处理=橙，失败/危险/异常=红，中性/离线=灰，处理中=蓝。左侧固定导航栏（宽约 220px、白底），菜单分三组——「运营概览」总览仪表盘/客户与钱包/充值订单/账务流水；「客户运营」客户详情/激活码/发码中心/设备/生成记录/会话管理；「系统治理」调账记录/审计日志/支付与价格/服务配置。本页对应的菜单项高亮为蓝色。顶部栏含面包屑、全局搜索框、右侧管理员身份（头像 + admin-phlong · 管理员）。界面所有文字为简体中文，示例数据时间在 2026-09-03 前后，用户名形如 customer-a1b2c3d4e5f6，激活码掩码形如 XS04-7K2M***-***-***-9WX4。表格信息密度较高但留白克制，类 Ant Design 企业中后台的专业风格。只呈现这一个网页界面，不要手机、不要多余装饰。

【页面主题】发码中心（简化改版：创建即发放，生成与发放两步合一），面包屑：客户运营 / 发码中心。页面顶部两个页签：「批次与发码记录」（当前高亮蓝）、「快速发码」。页签下方一条浅蓝色信息条：「生成即发放：填写发放信息后一次完成创建与发放；明文码仅在生成后一次性展示，下载与复制均记入审计」。
【页面结构——主体为「批次与发码记录」tab，右侧叠加一个快速发码抽屉表单】
主体上半部分卡片「批次列表」：右上角蓝色主按钮「+ 快速发码」。表格列：批次名（零额度授权码-202609）、创建时间（2026-09-01 10:15）、经手人（admin-phlong）、数量（100）、已激活（79 绿色数字）、未激活（21）、渠道（客服交付 蓝色小标签）、有效期至（2027-08-31）、操作（「查看码」「下载记录」蓝色链接，下载记录旁带小锁图标）。示例 4 行。
主体下半部分卡片「发码记录（生成即发放）」：列：发放时间（2026-09-03 15:40:12）、激活码（XS04-7K2M***-***-***-9WX4 等宽掩码）、批次（零额度授权码-202609 小灰字）、渠道徽章（客服交付蓝/线下交付灰/渠道分销紫）、外部单号（SO-2026-0857 等宽小字或 —）、收件人（「李女士·微信」或 —）、经手人（admin-phlong）、原因（小字「客户续费交付」）、明文下载（绿色对勾「已下载·09-03 15:41」或灰字「未下载」）。示例 5 行，其中 1 行未下载。
右侧抽屉浮层（宽约 420px，背景压暗，抽屉白底带阴影，标题「快速发码 · 生成即发放」加关闭×）：表单字段自上而下——数量（数字输入框，默认 10）、有效期至（日期选择控件显示 2027-09-03，旁注「可配置，不再固定一年」）、初始条数（数字输入框 0）、批次名（自动填充「零额度授权码-20260903」可编辑）、发放渠道（下拉框展开样式：客服交付/线下交付/渠道分销）、外部单号（输入框，标注「选填」灰字）、收件人（输入框，标注「选填」）、操作原因（多行文本框，标签带红色星号*，示例文字「客户王先生续费，客服渠道交付」）、复选框「我已确认发放」（已勾选）；抽屉底部两个按钮：左「取消」白底灰边、右「生成并发放」蓝色主按钮；按钮下一行小灰字「生成后明文码仅显示一次，请立即交付客户」。

【图像提示词结束】

保存文件名：07-code-center.png
