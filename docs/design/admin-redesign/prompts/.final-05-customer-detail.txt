你的唯一任务：调用你内置的图像生成工具（Image Gen），按下面【图像提示词】生成一张图片，然后把生成的 PNG 保存到当前工作目录，文件名必须严格等于下面指定的名字。要求：
1) 图片为宽幅横版（优先 1536x1024）。
2) 只做两件事：生成图片、保存文件；不要读取仓库其他文件、不要改任何代码、不要运行测试。
3) 如果工具产物在别的目录（如生成图片缓存目录），用复制的方式保存到当前目录并重命名。
4) 完成后最终回复只写一行：OK <实际保存的文件名>。

【图像提示词开始】
【统一风格要求】
现代企业级中文 SaaS 管理后台网页界面效果图，桌面端宽屏浏览器（16:9 横版构图，约 1536x1024）。浅色主题：页面背景浅灰 #F5F6F8，内容区为白色圆角卡片（8px 圆角、1px 浅灰边框、极轻投影）；主色品牌蓝 #2563EB；徽章语义色：成功/在线=绿，警告/待处理=橙，失败/危险/异常=红，中性/离线=灰，处理中=蓝。左侧固定导航栏（宽约 220px、白底），菜单分三组——「运营概览」总览仪表盘/客户与钱包/充值订单/账务流水；「客户运营」客户详情/激活码/发码中心/设备/生成记录/会话管理；「系统治理」调账记录/审计日志/支付与价格/服务配置。本页对应的菜单项高亮为蓝色。顶部栏含面包屑、全局搜索框、右侧管理员身份（头像 + admin-phlong · 管理员）。界面所有文字为简体中文，示例数据时间在 2026-09-03 前后，用户名形如 customer-a1b2c3d4e5f6，激活码掩码形如 XS04-7K2M***-***-***-9WX4。表格信息密度较高但留白克制，类 Ant Design 企业中后台的专业风格。只呈现这一个网页界面，不要手机、不要多余装饰。

【页面主题】客户详情 360°（全新页面：一个客户的全部关联信息聚合），面包屑：客户运营 / 客户详情 / customer-a1b2c3d4e5f6。
【页面结构】
顶部客户头卡：头像占位方块 + 用户名 customer-a1b2c3d4e5f6 + 姓名李女士 + 客户ID usr_01H8XK（等宽小字）+ 状态徽章「活跃」（绿）+ 注册时间 2026-08-15 14:22；右侧三个操作按钮：后台加款（蓝主按钮）、发放免费条数（白底蓝边）、修改售价（白底蓝边），按钮下方一行灰字小标注「写操作需填写原因与来源单号，全程留痕审计」。
头卡下一行 4 个指标卡：可用条数 128、冻结条数 2、累计生成 356、成功率 96.7%。
主体左右两栏。左栏（约 55% 宽）上下两卡：卡1「激活码信息」——掩码 XS04-7K2M***-***-***-9WX4（ACTIVE 绿徽章）、激活时间 2026-08-15 14:25、有效期至「已绑定·长期有效」、所属批次「零额度授权码-202608」、发放记录「客服渠道·08-14」（每行标签+值排列）；卡2「绑定设备（2/2 槽位）」——两条设备行：槽位1「办公电脑·Windows」绿点在线·心跳 12 秒前·绑定 2026-08-15·右侧「解绑」灰边按钮；槽位2「备用笔记本·Windows」灰点离线·最近活跃 2 天前·右侧「强制退出」按钮。右栏（45% 宽）上下三卡：卡1「最近充值订单」3 行小表（订单号/金额/状态/支付时间）；卡2「最近调账」2 行（时间/操作人 admin-phlong/来源单 CS-20260903-014/+10 条）；卡3「生成趋势」迷你柱状图（近 14 日，绿色为主两根红色）。每张卡右上角均有「查看全部 →」链接。

【图像提示词结束】

保存文件名：05-customer-detail.png
