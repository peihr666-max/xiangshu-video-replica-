# 管理后台改版工作任务清单 v2（2026-09-05 定稿）

> 蓝本：native-style-v4 效果图（导航 7 项、页签式、众墅之家品牌、按秒计费口径）+ 2026-09-05 四项裁决 + 「创建激活码即视为可激活、无发放动作、无批次/发码记录」决议。
> 每项含【现状核对】：✅=后端已具备（前端改动即可）；🟡=数据已有、接口需扩展；🔴=不具备、需新开发（接口/迁移/新页）。
> 流程：接续账本 T 编号、从 main 切分支、测试先行、单任务单 PR。本文取代 v1（ADM-01~26 按旧 12 页结构编制，已被 v4 页面结构取代）。
> 2026-09-05 收尾口径：W3–W17 已完成本地实现、自动化与浏览器对照，达到 `AUTOMATED_VERIFIED`。最终 `npm.cmd run check` 退出 0：前端 746 通过；后端 1633 通过、1 项因缺少 ffmpeg 跳过。133 个受检源码指纹复核一致。后台 12 页及客户端 3 个组件的对照见[浏览器验收](../../evidence/admin-revamp/browser-qa.md)。真实支付、真实 Provider、生产迁移与发布未执行；W4 的具体赠送浏览器写入仍受审批限制。最新业务裁决优先于参考图示例。

## 阶段 0 · 基础

**W1 品牌替换　【已完成 2026-09-05，含 index.html 标题补漏】**
范围：侧栏品牌区、登录页、页面标题、Tauri 窗口/安装包元数据、客户端品牌区。
核对：文案硬编码于 AdminApp.tsx / App.tsx / src-tauri ✅（改动点明确）；🔴 金色屋形 Logo 素材待产出（所有者提供或 AI 生成后定稿）。
类型：前端+素材。

**W2 导航合并与页签骨架　【已完成 2026-09-05，含设备页 h1 降级修复】**
范围：AdminApp tabGroups 16→7（总览仪表盘/经营分析/资金流水/客户管理/生成记录/审计中心/系统设置）；新建页签 TabBar 组件（复用现有样式变量）；旧页面容器迁入对应页签。
核对：现有 10 个页面组件全部保留复用 ✅；🔴 TabBar 组件、页签路由状态为新增。
类型：前端。

## 阶段 1 · 客户管理聚合页

**W3 客户列表页签　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：用户名、状态、注册时间和余额筛选，钱包可用/冻结余额、设备槽位、秒数口径及详情入口。
类型：接口扩展+前端。

**W4 激活码页签（创建即激活，无批次/发码记录）　【本地自动化与浏览器验收完成】**
实施：码列表已返回有效期并派生过期状态；快速发码固定 `auto_issue=true`，支持数量、有效期、经二次确认的初始免费秒数和必填原因。062 仅允许显式授权的免费秒数，激活时通过零金额 `FREE_GRANT` 调账及钱包流水闭环，不计作收入。已完成本地页面交互验证；未执行对外发码或生产验证。
类型：前端为主+1 个接口小改。

**W5 设备与会话页签　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：设备用户名/激活码/在线状态、平台筛选、汇总、会话卡片，以及带 epoch CAS 和幂等重放的管理员结束会话端点。
类型：接口扩展+前端。

**W6 客户详情 360°　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：将账户、单价、设备、会话、订单、流水和调账接口聚合为客户详情视图。
类型：前端为主。

## 阶段 2 · 资金与经营

**W7 资金流水（充值订单｜额度流水两页签）　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：订单/流水筛选、关联项和余额前后值。最终选择 063 的不可变流水序号支持确定性重建，没有为历史记录猜测顺序。
类型：前端+接口+小迁移。

**W8 经营分析·利润总览（全新）　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：每日对外售价、写契约、标准收入与成本聚合、利润页面及 CSV。历史无成本数据仍按裁决标注口径起点，不回填。
类型：全新（后端+前端）。

**W9 经营分析·成本明细（全新）　【本地自动化与浏览器验收完成】**
实施：059 增加提交时费率快照与追加式成本记录；视频使用 Provider 返回的真实输出秒数，解析使用参考视频秒数，图片与 Context IR 使用实际调用量。Provider 未返回用量时状态保持 `UNKNOWN`，不以请求量伪造实际成本。成本日聚合、CSV 与前端 KPI/图表/明细均已接入。
类型：全新（后端+前端）。

**W10 系统设置·费率管理（全新）　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：7 个上游成本科目、2 个对外售价档、读写端点、历史 old→new、数值边界、大幅调价确认和提交时快照。
类型：全新（后端+前端）。**本项是 W8/W9/W11 的公共底座，最优先。**

## 阶段 3 · 按秒计费与生成扩展

**W11 按秒计费切换（最大后端项）　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：预留与结算按提交档位秒数，钱包/流水/订单/调账/客户列表统一秒数口径；H3 `usage.output_seconds` 只用于成本和统计。
类型：后端大项+迁移+前端联动。

**W12 生成参数扩展（比例/数量）　【已实现，真实 Provider 待验】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：7 档比例、数量 1/2/4、逐任务计费和只返回对外售价的试算接口。参数已透传到 Provider 请求代码；真实 Provider 支持度仍需授权验证。
类型：后端+客户端。

**W13 反推提示词编辑与另存　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：编辑态、另存端点、scope/来源/作者元数据、审计和“我的提示词”选择器。
类型：前端+1 端点。

**W14 客户端秒数化　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：额度秒数、768P/2K 元每秒价格、充值换算、流水明细和提交前费用预估。
类型：前端。

## 阶段 4 · 收尾

**W15 总览仪表盘　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：今日生成/秒数/成本/毛利、上海自然日 7 日趋势、待办与费率告警、槽位和快捷入口。
类型：新端点+前端。

**W16 审计中心（审计日志｜调账记录）　【本地自动化与浏览器验收完成】**
实施：已支持 actor/target 用户名筛选、全局调账查询及确定性余额前后值；审计 UNION 只白名单提取单价变更主题与 old/new 字段，绝不整体暴露 metadata。历史缺失旧值保持未知，不补 0、不虚构。
类型：接口+前端+小迁移。

**W17 生成记录页　【本地自动化与浏览器验收完成】**
原缺口（当前实现见[实现证据](../../evidence/admin-revamp/implementation.md)）：用户、状态、类型和时间筛选下推及统一分页。设备维度按裁决永久移除：任务表没有可审计归属，历史不可回填。
类型：接口重构+前端。

## 依赖与顺序

W1/W2 先行（骨架）→ W10（费率底座）→ W9+W11（成本落库与按秒切换）→ W8（利润）→ W15（仪表盘含成本毛利 KPI）。
W3–W7、W13、W14、W16、W17 可与上述并行穿插。
外部依赖：W12 需真实 Provider 调用授权；W1 需 Logo 素材定稿。

## 遗留决策（仅 1 项）

- 生成记录设备归属：建议永久不做（数据层不存在、历史不可回填、按秒计费下价值低）；如坚持要做需 generation_batches 加 device_id 且仅新任务生效。默认按"不做"执行。
