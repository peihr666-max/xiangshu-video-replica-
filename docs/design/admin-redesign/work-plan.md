# 管理后台改版工作任务清单 v2（2026-09-05 定稿）

> 蓝本：native-style-v4 效果图（导航 7 项、页签式、众墅之家品牌、按秒计费口径）+ 2026-09-05 四项裁决 + 「创建激活码即视为可激活、无发放动作、无批次/发码记录」决议。
> 每项含【现状核对】：✅=后端已具备（前端改动即可）；🟡=数据已有、接口需扩展；🔴=不具备、需新开发（接口/迁移/新页）。
> 流程：接续账本 T 编号、从 main 切分支、测试先行、单任务单 PR。本文取代 v1（ADM-01~26 按旧 12 页结构编制，已被 v4 页面结构取代）。

## 阶段 0 · 基础

**W1 品牌替换　【已完成 2026-09-05，含 index.html 标题补漏】**
范围：侧栏品牌区、登录页、页面标题、Tauri 窗口/安装包元数据、客户端品牌区。
核对：文案硬编码于 AdminApp.tsx / App.tsx / src-tauri ✅（改动点明确）；🔴 金色屋形 Logo 素材待产出（所有者提供或 AI 生成后定稿）。
类型：前端+素材。

**W2 导航合并与页签骨架　【已完成 2026-09-05，含设备页 h1 降级修复】**
范围：AdminApp tabGroups 16→7（总览仪表盘/经营分析/资金流水/客户管理/生成记录/审计中心/系统设置）；新建页签 TabBar 组件（复用现有样式变量）；旧页面容器迁入对应页签。
核对：现有 10 个页面组件全部保留复用 ✅；🔴 TabBar 组件、页签路由状态为新增。
类型：前端。

## 阶段 1 · 客户管理聚合页

**W3 客户列表页签**
核对：用户名筛选、生成统计五项、掩码码、credits_spent ✅已返回；🟡接口缺：status/注册时间/余额区间筛选参数、display_name、钱包可用/冻结余额、设备槽位占用（join wallets + BOUND 子查询）；🔴前端：秒数口径列、行点击进详情。注意 credits_spent=SETTLE 笔数非金额。
类型：接口扩展+前端。

**W4 激活码页签（创建即激活，无批次/发码记录）　【已完成 2026-09-05，浏览器实测真实发码通过；初始秒数暂限 0（审计红线），>0 随 W11 开放】**
核对：码列表接口字段（掩码/状态/绑定账号/设备内嵌/待批配对/total）✅；「创建即激活」= generate 接口 auto_issue=true（同事务翻转 ISSUED+DELIVERED 事件）✅后端已具备，前端固定传参并去掉发放概念；批次有效期可配置 ✅（接口本来就收 activation_expires_at，前端从硬编码一年改为输入项）；🟡接口缺：expires_at 字段（join 批次表）+派生"已过期"（EXPIRED 状态从不落库，须查询派生）；🔴前端：快速发码表单（数量/有效期/初始秒数/必填原因，去自动原因）、复制明文码补真实 reason、列表去掉批次/发放时间列（改"创建时间"）、操作按钮保留暂停/撤销。
类型：前端为主+1 个接口小改。

**W5 设备与会话页签**
核对：设备接口已支持 user_id/activation_code_id/status 筛选 ✅（现 UI 未用）；会话 live 接口字段（心跳/租约/epoch/槽位）✅；🟡接口缺：设备列表 join username/激活码掩码、在线状态（join 会话租约）、platform 筛选参数、汇总计数；🔴前端：左右分栏（设备表+在线会话卡）、租约进度条；可选：仅踢会话端点（复用 revoke_session）。
类型：接口扩展+前端。

**W6 客户详情 360°**
核对：设备(？user_id)/订单/流水/调账/会话/单价各接口齐 ✅可前端拼装；🟡可选：按客户生成趋势聚合小端点（迷你图）。
类型：前端为主。

## 阶段 2 · 资金与经营

**W7 资金流水（充值订单｜额度流水两页签）**
核对：订单时间/渠道/第三方单号字段 ✅已返回（纯前端补列）；流水时间/关联订单/任务 ✅；🟡接口缺：订单渠道/时间筛选参数、流水时间范围参数；🔴数据层：流水变动后余额（balance_after 加列+按零差额不变量回填，或查询期窗口函数——推荐加列）。
类型：前端+接口+小迁移。

**W8 经营分析·利润总览（全新）**　【已完成 2026-09-05，9005a58】
核对：🔴全部需开发——每日对外售价实体（date 唯一+768P/2K 价+录入人）、录入端点（写契约）、日利润聚合端点（收入=每日售价×结算秒数，成本=W9）、前端页（录入卡+日利润表+CSV）。口径已裁决：标准收入主口径；历史无成本数据标注口径起点不回填。
类型：全新（后端+前端）。

**W9 经营分析·成本明细（全新）**　【后端成本落库已完成（665b67b 内）；成本明细页签聚合查询待接入】
核对：🔴全部需开发——成本落库：任务终态写 actual_cost=输出秒数×费率快照（按分辨率分档），解析类任务落参考视频秒数（秘塔按参考视频同价计秒费，防成本低估）；日成本聚合端点（天×科目：768P/2K/解析/图片）；前端页（KPI+堆叠图+日明细+CSV）。历史无 actual_cost 标注口径起点。
类型：全新（后端+前端）。

**W10 系统设置·费率管理（全新）　【已完成 2026-09-05：迁移 056+读写端点+审计历史+前端 RatesManager，浏览器实测调价通过】**
核对：设置基础设施（写契约/审计/auditor 只读）✅；🔴需开发——费率配置实体：上游成本 7 科目（视频生成 768P/2K 元/秒、解析参考视频 768P/2K 元/秒、首帧图片、人物五视图、Context IR）+对外售价档（768P/2K 元/秒）；读写端点；前端区块（表格+调整对话框+历史变更旧值→新值）；数值边界校验+变动>50% 二次告警+试算预览；任务提交时费率快照。
类型：全新（后端+前端）。**本项是 W8/W9/W11 的公共底座，最优先。**

## 阶段 3 · 按秒计费与生成扩展

**W11 按秒计费切换（最大后端项）**　【已完成 2026-09-05，665b67b：按提交档位计秒、057 迁移、全链路口径切换】
核对：现计费=每任务 1 条（迁移 022 形状 CHECK 锁 RESERVE=-1/+1）；🔴需开发——计费模型改「预留=结算=提交档位秒数」（4/15s 档，已裁决不透支：实际输出秒数仅进成本统计）；新迁移（055+，PG 重建 CHECK+SQLite 表重建，冻结迁移不碰，downgrade 拒绝）；钱包/流水/订单/调账/客户列表全链路条→秒口径；测试号余额数值平移（已裁决免补偿）；H3 usage.output_seconds 仅作成本与统计口径。
类型：后端大项+迁移+前端联动。

**W12 生成参数扩展（比例/数量）**
核对：分辨率 768P/2K ✅、时长 4–15s ✅前端已有；ratio 服务端硬锁 adaptive（generation.py:5553）🔴需放开（7 档：自动/21:9/16:9/4:3/1:1/3:4/9:16）——**需真实链路验证，涉及真实 Provider 调用授权**；数量 1/2/4 🔴需开发（N 任务入同一批、逐任务幂等/计费/归档）；费用预估（对外价×秒×条）🔴前端+1 个试算接口（只返回对外价，不返回成本）。
类型：后端+客户端。

**W13 反推提示词编辑与另存**
核对：versions 表+prompt_version_id FK+prompt_snapshot_json ✅底座现成；🔴需开发——分析工作台编辑态、「另存为新提示词」端点（versions 加 scope/来源/作者，写审计）、生成器「我的提示词」选择器。
类型：前端+1 端点。

**W14 客户端秒数化**
核对：钱包/生成面板组件现成 ✅；🔴前端——额度单位秒、价目展示（768P/2K 元/秒）、充值档位换算秒数、流水计费明细、提交前费用预估。
类型：前端。

## 阶段 4 · 收尾

**W15 总览仪表盘**
核对：对账 5 指标 ✅、在线数=live 会话 total ✅；🔴需开发——统计聚合端点（今日生成/秒数/成本/毛利、7 日趋势、待办四项+费率未配置告警、槽位占用；generation_tasks.created_at_utc 索引已备 ✅）；前端 KPI/组合图/待办。
类型：新端点+前端。

**W16 审计中心（审计日志｜调账记录）**
核对：actor 用户名反解 ✅、时间筛选 ✅、调账按客户列表 ✅；🟡接口缺——target 用户名反解、actor/target 按用户名检索、调账全局列表+操作人/来源单类型/时间筛选、操作人 username；🔴数据层：调账前后余额（admin_adjustments 加列，创建响应值落库）。变更明细（旧值→新值）：新写入点落 metadata+UNION 扩取，历史事件显示动作描述不虚构。
类型：接口+前端+小迁移。

**W17 生成记录页**
核对：全部展示字段 ✅、耗时=前端算 ✅、图像类扣费恒 0（口径注明）；🔴接口缺——任何筛选参数（user/status/type/时间，现仅 limit/offset，且 5 表内存合并分页需重构为过滤下推）；设备维度**不做**（任务表无归属字段、fence 证据不可逆、历史不可回填——建议永久移除该列，从图 v12 中忽略设备筛选）。
类型：接口重构+前端。

## 依赖与顺序

W1/W2 先行（骨架）→ W10（费率底座）→ W9+W11（成本落库与按秒切换）→ W8（利润）→ W15（仪表盘含成本毛利 KPI）。
W3–W7、W13、W14、W16、W17 可与上述并行穿插。
外部依赖：W12 需真实 Provider 调用授权；W1 需 Logo 素材定稿。

## 遗留决策（仅 1 项）

- 生成记录设备归属：建议永久不做（数据层不存在、历史不可回填、按秒计费下价值低）；如坚持要做需 generation_batches 加 device_id 且仅新任务生效。默认按"不做"执行。
