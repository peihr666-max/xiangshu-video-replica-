【页面主题】账务流水（v3：秒数口径与计费明细），面包屑：运营概览 / 账务流水。
【页面结构】
顶部筛选栏：账号搜索、类型下拉（全部/充值/预留/结算/释放）、时间范围、查询/重置、导出 CSV。
表格列：时间、账号、类型徽章（充值绿/预留金/结算紫/释放灰）、可用变动（+300 秒 绿色 / -15 秒 红色，带符号）、冻结变动、变动后余额（加粗，如 1,280 秒）、关联订单/任务（金色链接）、备注（「充值 300 秒」「视频生成·2K·15s」「任务失败退回 12 秒」「客服工单补偿 +60 秒」）。
示例 7 行覆盖四类型。底部合计条：当前筛选合计 +285 秒。表下小字：「1 条 = 1 秒视频额度；余额为秒数；历史按秒口径自 2026-09 起记录」。
