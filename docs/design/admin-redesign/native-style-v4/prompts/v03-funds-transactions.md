【页面主题】资金流水·额度流水页签，面包屑：运营概览 / 资金流水。
【页面顶部页签】面包屑下方页签行：「充值订单」「额度流水」（当前金色高亮）。
筛选栏：账号搜索、类型下拉（全部/充值/预留/结算/释放）、时间范围日期选择器、查询/重置、右侧「导出流水 CSV」。
表格列：时间、账号、类型徽章（充值绿/预留金/结算紫/释放灰）、可用变动（+300 秒 绿 / -15 秒 红）、冻结变动、变动后余额（加粗，如 1,280 秒）、关联订单/任务（金色链接）、备注（「充值 300 秒」「视频生成·2K·15s」「任务失败退回 12 秒」）。示例 7 行覆盖四类型。底部合计条：当前筛选合计 +285 秒。
