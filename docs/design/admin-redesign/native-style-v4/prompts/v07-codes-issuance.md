【页面主题】客户管理·激活码页签（取消独立发码中心，发码并入本页），面包屑：客户运营 / 客户管理。
【页面顶部页签】面包屑下方页签行：「客户列表」「激活码」（当前金色高亮）「设备与会话」。
页签行右侧金色主按钮「+ 快速发码」（生成即发放：填数量、有效期、渠道、收件人、必填原因）。
主体上半部分「激活码列表」表格，列：激活码（掩码等宽+复制图标）、状态徽章（使用中绿/可使用金/待启用灰/已暂停橙/已撤销红/已过期深灰）、绑定账号（或「未绑定」灰字）、有效期至（过期红色角标）、批次（小灰字）、发放时间、最近活跃、操作（暂停/撤销金边小按钮）。示例 5 行覆盖多状态。
主体下半部分「批次与发码记录」两段小表：批次表（批次名/创建时间/经手人/数量/已激活/未激活/渠道/有效期至/操作「查看码」「下载记录」带小锁图标）3 行；发码记录表（发放时间/激活码掩码/渠道徽章/外部单号/收件人/经手人/明文下载状态「已下载·09-05 15:41」或「未下载」）4 行。
