【页面主题】客户管理·激活码页签（最终版：创建即激活，无批次与发码记录概念），面包屑：客户运营 / 客户管理。
【页面顶部页签】面包屑下方页签行：「客户列表」「激活码」（当前金色高亮）「设备与会话」。
页签行右侧金色主按钮「+ 生成激活码」（填数量、有效期至、初始秒数、必填操作原因；小字标注：创建即可激活并交付客户，无发放动作）。
主体上半部分「激活码列表」表格，列：激活码（掩码等宽+复制图标）、状态徽章（使用中绿/可使用金/已暂停橙/已撤销红/已过期深灰）、绑定账号（或「未绑定」灰字）、有效期至（过期红色角标）、批次（小灰字）、发放时间、最近活跃、操作（暂停/撤销金边小按钮）。示例 5 行覆盖多状态。
主体下半部分「待批准配对（2）」小卡：设备名+平台+申请时间，按钮「批准」「替换」，小字「首设备不可用时转管理员审批」。
页面无批次、无发码记录、无发放动作概念：激活码生成后即为「可使用」，可直接交付客户激活。
