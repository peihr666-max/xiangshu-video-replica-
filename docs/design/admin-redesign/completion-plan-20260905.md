# 管理后台改版收尾执行计划（2026-09-05）

基线：`feat/customer-v3-admin-revamp` / `67cf008`。用户授权完成未完成清单，并通过浏览器逐项对照原效果图调整。

当前状态：W3–W17 已达到 AUTOMATED_VERIFIED，后台 12 页和客户端 3 个组件已完成浏览器成对对照；稳定源码上的最终 `npm.cmd run check` 退出 0。生产、真实支付、对外发码及真实 Provider 调用均未验证。

## 范围与验收

- 以 `work-plan.md` v2、最终裁决和 `native-style-v4` 12 张效果图为准；客户端仅补 W12/W13/W14，保持已裁决的工作台结构范围。
- W9/W10/W11：提交时成本费率快照；客户按提交秒数结算；实际用量用于成本；未提供真实用量时明确未知/估算，不能伪造实际成本；覆盖解析、图片与 Context IR 的实际调用点。
- W8/W15：统一成本与日界口径，补齐利润、成本、秒数、告警、趋势和 CSV。
- W3/W5/W6/W7/W16/W17：完成筛选、分页、详情、余额快照和审计展示；生成记录不增加已裁决排除的设备归属。
- W12/W13/W14：比例、数量、客户价格试算、提示词另存与选择、秒数单位与充值展示；保留鉴权、幂等、fencing 与逐任务计费。
- 每张效果图均需实际浏览器页面、截图对比、交互结果和复验结论；示例数据只在隔离本地验证库生成。

## 清理与回归顺序

1. 先用失败测试锁定成本快照、筛选分页、余额/审计、参数与提示词行为。
2. 复用现有写契约、查询组件和主题；删除调试输出、旧单位和占位内容，不增加依赖。
3. 按职责独立实现：经营计费、客户资金审计、生成提示词与客户端；主线程负责整合、公共样式、浏览器对照及最终门禁。
4. 迁移仅追加，实际链路为 `058 → 059 → 061 → 062 → 063`：059 经营成本与任务费率快照；061 个人提示词范围/来源/作者；062 经明确确认的激活初始免费秒数；063 新钱包流水不可变序号。原计划预留的 060 未创建，管理余额前后值由 063 支持确定性重建；不得篡改已发布迁移。
5. 专项验证使用各自隔离测试库；禁止并行全量 pytest；最终源码稳定后由主线程运行一次完整 `npm.cmd run check`，失败先修复。
6. 独立复核最终 diff，按逻辑增量中文提交；更新清单与证据。未验证的真实供应商、生产环境、安装包如实保留，不以本地浏览器替代。

## 视觉复验

逐页检查字体、间距、配色、图片与内容；将参考图和浏览器截图置于同次比较输入。发现差异后先记录 verdict，再修改并重截。记录写入 `.omx/state/admin-revamp/ralph-progress.json`，完整证据保存在 `docs/evidence/admin-revamp/`。当前没有启动 OMX ralph 运行时。

已保存 v01–v12 的最终截图，逐页差异、实际操作及范围边界见 `docs/evidence/admin-revamp/browser-qa.md`。总览和利润图按最新要求改为圆滑曲线；钱包报价错误与正常状态分别留证。视觉实现以最新用户需求、真实数据状态和安全/审计约束为先；AI 参考图中的批次、发放、虚构金额或不可实现交互不构成产品契约。

## 收尾门禁

- 已读取的专项最终结果：管理后端 402 项、SQLite 导入和客户链路 40 项、首帧与成本 50 项、钱包 5 项通过；Ruff、Mypy（78 个源文件）、Biome、`tsc -b` 与 `git diff --check` 通过。
- 最终全仓门禁退出 0：前端 746 通过；后端 1633 通过、1 项因本机缺少 ffmpeg 跳过，耗时 919.85 秒。密钥扫描、Biome、TypeScript、Cargo fmt/check、Ruff 和 Mypy 全部通过。
- `docs/evidence/admin-revamp/source-manifest.json` 记录所有受检代码指纹，收尾复核 133 个文件一致。
- 明确保留：真实 ZPay、真实 Provider、生产数据库迁移、安装包和公网发布均未执行，不得提升为真实链路或生产证据。
