你的唯一任务：调用你内置的图像生成工具（Image Gen），按下面【图像提示词】生成一张图片，然后把生成的 PNG 保存到当前工作目录，文件名必须严格等于下面指定的名字。要求：
1) 图片为宽幅横版（优先 1536x1024）。
2) 只做两件事：生成图片、保存文件；不要读取仓库其他文件、不要改任何代码、不要运行测试。
3) 如果工具产物在别的目录（如生成图片缓存目录），用复制的方式保存到当前目录并重命名。
4) 完成后最终回复只写一行：OK <实际保存的文件名>。

【图像提示词开始】
【统一风格要求 —— 必须严格遵循的现有系统视觉规范（warm-ledger 暖账本深色主题）】
这是名为「镜序 Studio」的中文管理后台网页界面效果图，深色主题、金色强调、大圆角面板，专业克制，桌面端宽屏（16:9 横版，约 1536x1024）。
整体布局：暖炭黑画布（#131313）。顶部细横栏（约 64px 高、深色）：左侧为面包屑「分组名 / 页面名」，右侧为日期、操作员姓名与角色小字、一个透明底细边框的「退出登录」小按钮。顶栏下方两栏：左侧固定侧边栏（宽约 232px，更深的近黑 #0d0d0e），右侧主工作区。
左侧栏自上而下：品牌区（金色小 logo 标记 + 「镜序 Studio」加粗 + 小字 OPERATIONS）；「当前模块」极小灰字标题 + 当前页名 + 一行灰色辅助说明；导航分组（每组一个极小的分组标题，其下若干导航卡，每张卡两行：菜单名 + 更小的灰色说明文字，卡片为大圆角深色矩形）；当前页所在导航卡带金色描边（#c6a45f 半透明）与淡金渐变底；侧栏底部显示操作员姓名与「管理员 · 安全会话」小字。导航分组固定为三组：「运营概览」总览仪表盘/客户与钱包/充值订单/账务流水；「客户运营」客户详情/激活码/发码中心/设备/生成记录/会话管理；「系统治理」调账记录/审计日志/支付与价格/服务配置。
配色规范：主强调色金色 #c6a45f（高亮 #e0c17c）；面板底 #181818、凸起面板 #1d1c1a；边框暗棕灰 #2e2c29、浅分隔线 #242321；正文暖白 #ece8df，次级文字灰褐 #a49e95；重要数字用亮金 #e0c17c。
组件规范：内容区为大圆角（18-26px）深色卡片带柔和投影；主按钮金色系（金色描边或淡金渐变底），次按钮透明底+细边框+浅灰字；表格深底、行间细分隔线、表头为小号浅灰文字，编号/订单号/激活码掩码/请求ID 用等宽字体；状态徽章为小号胶囊（约 11px 加粗，深色底+彩色描边+彩字，低饱和）：正常/成功/在线=柔和绿、待处理/暂停=金、失败/危险=红、中性/离线=灰蓝；筛选控件与分页器为深色圆角小控件。页面规格中若出现「蓝色」按钮/链接/高亮/标签等描述，一律改用本系统的金色强调实现；语义色（绿/红/橙）仅用于状态徽章。
字体：Avenir Next / PingFang SC 系无衬线中文界面字体。界面所有文字为简体中文，示例数据时间在 2026-09-03 前后，用户名形如 customer-a1b2c3d4e5f6，激活码掩码形如 XS04-7K2M***-***-***-9WX4。只呈现这一个网页界面：不要手机、不要多余装饰、不要浅色背景、不要浏览器地址栏。

【页面主题】客户与钱包（客户列表改版），面包屑：运营概览 / 客户与钱包。
【页面结构】
顶部筛选栏：用户名/客户ID 搜索框、状态下拉（全部/活跃/已暂停/已撤销）、注册时间范围（日期选择器）、余额区间（最小-最大输入框）、「查询」金色主按钮与「重置」按钮、右侧「导出 CSV」次按钮。
表格列（表头依次）：用户名、姓名、客户ID、激活码（掩码+状态小徽章）、注册时间、状态、设备占用、可用条数、冻结条数、已结算消耗、生成情况（成功/失败/进行中分三个小数字）、待关注、操作。
示例 6 行数据：第 1 行 customer-a1b2c3d4e5f6、姓名「李女士」、ID usr_01H8…、XS04-7K2M***-***-***-9WX4（ACTIVE 绿）、2026-08-15 14:22、活跃（绿）、2/2·1 在线、128、2、356、356/12/3、2、「查看详情」金链接；第 2 行 customer-b3c4…、备用机客户、1/2·0 在线、58、0、102、102/3/0、0；第 3 行状态已暂停（橙）可用 0；第 4 行待关注 5（橙）；其余行数据自然变化。行首有复选框，「查看详情」按钮为金色文字链接。表格右上角显示「共 512 位客户」，底部右对齐分页器。

【图像提示词结束】

保存文件名：02-customers.png
