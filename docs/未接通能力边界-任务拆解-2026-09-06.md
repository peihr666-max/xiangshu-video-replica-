# 未接通能力边界 · 任务拆解（2026-09-06）

> 来源：2026-09-06 全前端盘点（内部工作台 / studio V1.4 / customer / admin 四线 + 前后端接口对齐）。
> 与 `outputs/frontend-audit-2026-09-05/前端逐页功能审查清单.md`（425 项）、`docs/evidence/FRONTEND-V14-IMPLEMENTATION.md` 的结论一致；
> 本文件把"未接通能力"收敛为可排期的工作任务，作为后续排期与销项的依据。
>
> **接口层前提**：`api.ts` / `api.admin.ts` 与 `server/app/*_routes.py` 已逐一比对，前端已写调用而后端无路由的为 **0 个**。
> B 组接口全部成对存在、只差凭据/环境；C 组是后端能力缺失（前端按"未接通即禁用"红线处理），需要立项开发后联调。

## 用户决策记录（2026-09-06）

| 项 | 决策 |
|---|---|
| C2 独立创作接口 | ✅ 已确认：**复用现有 metaso 通道 + 钱包计费**（RESERVE/SETTLE + 公平队列） |
| C7 云端草稿 / C8 人物 IP 定位 / C10 首页统计·通知偏好 | ✅ 已批准：**先行开发** |
| C1 数字人口播整链 | ✅ 已批准开发：按**飞影 API 说明文档**完善前后端。✅ 文档已获取（https://api.hifly.cc/hifly.html ），集成要点快照见 `docs/飞影数字人API-V2-集成参考.md`；第一砖已落地（hifly provider 客户端 + 迁移 056）；实施切片见该文档 §4/§5 |
| C3 链接解析 | ✅ 已确认：**参考 `oral-ip-agents-research` 项目的实现方案**。调研结论（2026-09-06）：该项目调用付费第三方服务"抖音豆 Douyidou"（`gateway.diadi.cn/api/parse`，MD5 签名，超时 60s 仅 5xx 重试），支持抖音/快手/小红书/B站/微博，**视频号不支持**（走文件上传）；最小移植集 = `url_resolver.py` + `douyidou.py` + 配置适配。⚠️ 需用户提供 Douyidou app_id/secret（付费服务，按红线需授权）后才能真实联调；代码移植可先行 |
| C4 爆款视频参考库 | ⏸ 用户决策：**暂缓**（爆款页保持空态） |
| C5 发布能力 | 🔨 **开发中（第一阶段：账号授权，CW-068）**。原 2026-09-06 决策为「⏸ 暂缓（发布页保持禁用；发布管理先不做）」，**2026-09-11 owner 决策变更为纳入并分两阶段**（范围签认见 `docs/evidence/CW002-SCOPE-DECISIONS.md` §3/§5/§7 追加行，专用验收矩阵见其 §8）。<br>**三项选型结论**：① 路线＝**云端 Fernet 密文落库 + 逆向 HTTP 直连**（不走平台开放平台 OAuth/申请开发者资质，凭据由用户手工粘贴 Cookie 提供，抖音额外需 security_sdk）；② 供应商＝**项目所有者自有仓库**（`github.com/phlong026/douyin_publisher`@`f61418d`、`github.com/phlong026/weixinshipinhao_publisher`@`ee4babe`），无第三方授权问题，源码内联进 `server/app/publishers/vendor/` 并排除出本仓 lint/format/mypy 口径；③ 交付切分＝**第一阶段只做账号授权**（抖音/视频号连接、密文落库、异步登录态探测、前端「发布账号」tab 接线），**正式发布链路**（`publish_records`/worker publish round/封面/定时/`published_total`/`publishing` 页）留第二阶段另立任务。<br>**小红书本轮暂缓**（三平台中唯一无自有实现），另立后续任务。<br>**泄露 Cookie 安全事件已闭环**：用户确认原仓库泄露的视频号登录态已作废、仓库已删除或转私有（GitHub API `users/phlong026/repos` 返回 `[]`）；本次沿用该裁剪结果，不重新引入含 PEM 模板的 `ucenter_ticket.umd.js`。<br>**真实平台链路仍按红线**：探测/发布真实链路需真实凭据人工授权，本阶段全部用例使用合成凭据且不触网，证据层级上限 AUTOMATED_VERIFIED，不得标 PRODUCTION_GO。 |
| C6 数据看板 | ✅ 已确认：**先接平台侧真实统计**——只统计本平台真实任务量/成片量/发布动作，播放/互动等外部数据不伪造；与 C10 首页统计合并实施 |
| C9 素材库 | ✅ 已批准并完成代码闭环：统一列表/按 ID 恢复、图片/音频/视频上传、分页搜索、来源筛选、预览下载、重命名/分组/隐藏、创作引用与权限隔离；媒体按永久保存执行。真实 COS 与客户身份浏览器验收归 B3/联调证据。 |
| 执行分支 | 沿用 `codex/frontend-v1.4`（前端 V1.4 线），测试先行；合并 main 前跑全量门禁 |

---

## A 组：纯前端收尾（无需确认，本轮直接实现）

| 编号 | 任务 | 位置 | 说明 | 状态 |
|---|---|---|---|---|
| A1 | 客户会话状态接线：`HeartbeatStatus` / `LeaseCountdown` 中文化并接入个人中心 · 设备管理 | `customer/HeartbeatStatus.tsx`、`customer/LeaseCountdown.tsx`、`useCustomerSession.ts`、`CustomerProfilePanel.tsx`、`RootApp.tsx`、`CustomerWorkspace.tsx`、`App.tsx`（customerAccount 类型） | T30/T31 交付的两个组件一直是孤儿（仅测试引用）。hook 心跳结果此前被丢弃、租约时间未暴露。本轮：hook 暴露 `sessionRuntime`（lastHeartbeatAt + leaseExpiresAt，取自登录/心跳响应）与 `sendHeartbeatNow()`；组件中文化；沿 customerAccount 链路接入个人中心设备页。**不改** RootApp 已审核的三个中文终态屏。 | ✅ 本轮实现 |
| A1 附注 | `SessionDisplacedNotice` 处置 | `customer/SessionDisplacedNotice.tsx` | 其职责已被 RootApp 内联中文终态屏（session-replaced）覆盖且文案更完整；因 T30 证据账本登记过该文件，**保留不删、不再接线**，避免双实现漂移。 | ✅ 已登记 |
| A2 | 充值建议金额透传修复 | `studio/LiveWorkspacePanel.tsx:132,145` | `CustomerWalletPanel.onRechargeRequested(amountYuan)` 与 `CustomerProfilePanel.onRecharge(amountYuan?)` 传出的建议金额被丢弃，`CustomerRechargeDialog.suggestedAmountYuan` 永远为空。本轮接上。 | ✅ 本轮实现 |
| A3 | 死代码 / 过时注释清理 | `customer/DeviceManagementPage.tsx:14`（`onError` prop 声明未用）、`useWorkspaceReadiness.ts:192-194`（"GenerationComposer split" 过时注释） | 移除 + 修正，行为零变化。 | ✅ 本轮实现 |
| A4 | studio 工作台任务数据静默轮询 | `studio/live.ts`、`studio/StudioWorkspace.tsx` | 新壳进入时一次性拉取，生成中的批次进度不会自动更新（旧 TaskRecordsPanel 是 2 秒轮询）。本轮：导出 `reloadTasks`，壳层每 20s 静默刷新 tasks 切片（页面隐藏 / 面板忙时跳过，失败静默，不打扰显式重试路径）。 | ✅ 本轮实现 |

---

## B 组：环境与凭据联调（前后端代码已就绪，需用户凭据/授权）

> 依据 AGENTS.md 硬红线：**真实 ZPay / 付费 Provider / 生产 COS 变更必须先取得用户明确授权**，因此全部需要确认。

| 编号 | 任务 | 前端证据 | 未配置时的表现 | 需要用户提供 |
|---|---|---|---|---|
| B1 | metaso 视频生成凭据 + `VITE_GENERATION_PROVIDER` 核查 | `api.ts:1067-1075`（默认 `metaso`，仅显式 env 才走 `fake_h3`） | 成片标"测试模式"（`VideoResultStage.tsx:290,864`） | metaso API key；确认构建环境未设 `VITE_GENERATION_PROVIDER=fake_h3` |
| B2 | apilio 凭据（Gemini 拆解 + 图像生成） | `api.ts:347`；`AnalysisWorkspace.tsx:1408-1411`；`FirstFrameSelection.tsx:596-603`；`CharacterLibrary.tsx:604-608` | 拆解"演示数据"、首帧"模拟输出"、人物图"本地占位结果"、源画面语义评分降级（`SourceFrameSelection.tsx:229`） | apilio API key |
| B3 | COS 生产存储配置 | SettingsPanel 四类 provider | 资产走本地对象通道 | COS 密钥（生产变更需授权） |
| B4 | ZPay 商户配置与支付回调 | `WalletPanel.tsx:316-332`、`CustomerRechargeDialog`（下单→二维码→轮询→网关表单三通道，代码完整） | 充值无法闭环 | ZPay 商户 pid/key（真实付费，需明确授权） |
| B5 | Tauri 桌面端打包联调 | `api.ts:1272-1411`（4 个下载命令，浏览器模式已优雅降级）、`src-tauri/src/lib.rs:21` | 需打包后真机验证 | 打包环境/签名决策 |

---

## C 组：需后端立项开发 + 联调（需产品/供应商决策，放最后确认）

| 编号 | 任务 | 前端位置（已就绪的 UI） | 后端缺口 | 审核清单索引 | 外部依赖 |
|---|---|---|---|---|---|
| C1 | **数字人口播整链**（优先级最高） | `studio/CreationPages.tsx:772-1019`（双模式表单 100%）、`StudioWorkspace.tsx:662`（提交提示未接入）、`PeoplePages.tsx:456,580`（分身制作/声音克隆按钮 disabled） | 分身列表（avatars）、声音列表（voices）、音频上传、声音克隆、分身制作、口播生成任务、报价计费——全链无接口；全仓无"飞影数字人"域接口 | I08 / F244 / F251 / F274 / F275 | 数字人供应商选型（付费 Provider，需授权） |
| C2 | 独立创作接口（文生/图生/参考生视频 + 独立人物置换）<!-- 2026-09-07：视频生成/复刻/人物替换均已完成拆分实现，见 docs/视频生成独立创作-设计与实施-2026-09-07.md 与 docs/视频复刻与人物替换-模块拆分实施-2026-09-07.md --> | `studio/CreationPages.tsx:595-752`（参数表单）、`StudioWorkspace.tsx:653-693`（"确认费用并提交"永久 disabled）；置换页 `frameConfirmed` 生产无置 true 路径 | 脱离项目上下文的创作 API：报价、批次创建、参数组合（首尾帧/参考/分辨率/时长/比例/数量）提交路径 | F225-F228 / F239 / F253 / F255 | 可复用现有 metaso/H3 通道，需设计确认 | ✅ 视频生成已实现（2026-09-07，`POST /api/independent/video-tasks` + 前端接线 + 预览区进度，见 `docs/视频生成独立创作-设计与实施-2026-09-07.md`）；尾帧/T2V/R2V 真实提交由 `h3_extended_modes_enabled` 门禁待供应商核对；人物置换未动 |
| C3 | 视频链接解析（抖音/视频号/小红书） | `studio/MainPages.tsx:39-42`、`CreationPages.tsx:340` | 无解析接口 | — | 第三方解析服务选型 |
| C4 | 爆款视频参考库 | `studio/ContentPages.tsx:108-374`；`live.ts:321` `videos: []` | 无内容查询接口（示例数据仅 DEV `/review/v1.4`） | — | 内容来源决策（采集/合作/自建） |
| C5 | 发布能力 | `studio/ContentPages.tsx:565-855`（草稿编辑器已成形）、`MainPages.tsx:658-661`（连接发布账号 disabled）、`:754`（封面上传 disabled）、`:843-849`（正式发布 disabled） | 平台账号授权、发布、封面托管、发布状态回传；草稿目前仅会话内存 | F068 / F074 | 各平台开放平台授权 → **2026-09-11 选型改为逆向 HTTP 直连 + 用户手工粘贴 Cookie，不再依赖开放平台授权**（见上文用户决策记录 C5 行） |
| C6 | 数据看板数据源 | `studio/AnalyticsPage.tsx`（生产空态"数据接口尚未接通"） | 无播放/互动统计接口 | I12 | 平台数据授权或自埋点 |

> **C6 销项回填（2026-09-07）**：平台侧真实统计已接通——后端新增 `GET /api/studio/analytics`（北京日界成片趋势/任务类型分布/最近成片，口径镜像 `/api/studio/stats`），前端看板页接真数（成片趋势/类型占比/最近成片，播放/互动等外部指标按决策红线删除不伪造）；见 `docs/数据统计模块-现状分析与开发计划-2026-09-07.md`。外部播放/互动数据仍依赖 C5 发布/平台授权，维持不伪造。

> **C5 进展回填（2026-09-11，CW-068 第一阶段开工）**：暂缓决策已变更为分两阶段纳入，详见上文用户决策记录 C5 行与 `docs/evidence/CW002-SCOPE-DECISIONS.md` §8 专用验收矩阵。第一阶段只交付**账号授权**（`publish_accounts` 表 + 4 个 accounts 端点 + verify worker 轮次 + 前端「发布账号」tab），故本行「封面上传 / 正式发布」两个 disabled 入口与 `ContentPages.tsx` 草稿编辑器**仍保持禁用不变**；C10 首页「累计已发布」维持 `—` 诚实降级（`published_total` 属第二阶段）。泄露 Cookie 安全事件已闭环（原仓库视频号登录态作废、仓库已删除/转私有）。
| C7 | 云端草稿 / 我的文案 | `studio/StudioWorkspace.tsx:375-396`（仅内存，刷新即丢） | 草稿 CRUD；项目内文案版本接口（`/projects/{id}/scripts`）已存在，可评估复用 | — | 无（内部） |
| C8 | 人物 IP 定位保存 | `studio/PeoplePages.tsx:277-279`（"正式保存待接通"） | 定位字段保存接口 | F264 | 无（内部） |
| C9 | 素材库/口播音频上传领域接口 | `studio/ContentPages.tsx`、`studio/StudioWorkspace.tsx`、`studio/live.ts` | ✅ 已实现统一素材领域接口、通用上传、列表/恢复、管理动作、口播音频引用及 owner/type 权限修复；DIRECT 成片保持供应商托管标识，不伪装为云归档 | F244 | 无（内部）；真实 COS/客户浏览器 UAT 待 B3 |
| C10 | 首页统计 + 通知偏好 | `MainPages.tsx:82-97`（"今日成片/累计已发布"恒 `—`）、`:694-701`（通知偏好只弹 toast） | 统计聚合接口；偏好存储 | — | 无（内部） |

> **C10b 销项回填（2026-09-07）**：通知偏好已接通——迁移 `076_studio_notification_preferences`（按用户一行 JSON 偏好）+ `GET/PUT /api/studio/notification-preferences`（未保存过按默认开启返回）；个人中心开关进页拉取、乐观保存失败回退，审核模式只演示不落库。C10a 统计此前已由 `68ea2e5` 销项。

### C 组建议排序
1. **C1、C2**：前端表单/确认弹窗已 100% 就绪，后端补齐后联调面最小、商业价值最高；C1 涉及付费 Provider 选型与计费接入（复用钱包 RESERVE/SETTLE + 公平队列），需先授权。
2. **C7、C8、C10**：无外部依赖的内部小接口，可随 V3 任务线快速排期。
3. **C3、C4、C5、C6**：依赖外部平台能力（解析服务、平台授权、内容来源），建议逐个立项；上线前保持"隐藏或禁用"红线（`前端页面V1.4-完整审核包-2026-09-05/99-制作自检与交付记录.md:62`）。

---

## 执行约定

- A 组改动仅前端（`client/`），测试先行（红→绿），不触碰已审核的 RootApp 终态屏与 V1.4 禁用红线。
- B/C 组任何一项动工前，按 AGENTS.md 取得用户明确授权；涉真实凭据只进服务端密钥存储，不入码、不入库、不入 PR。
- 每项销项后回填本表状态，并在 `docs/CUSTOMER-TASK-EVIDENCE-V3.md` / 对应 evidence 文件登记证据。
