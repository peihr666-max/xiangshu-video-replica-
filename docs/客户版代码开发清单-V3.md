# 客户版代码开发清单 V3

## DESKTOP-ACTIONS-20260917 文件边界

- 新增 `.github/workflows/desktop-build.yml`：Windows x64、macOS arm64 / x64 原生打包、上传可下载 Artifact。
- 新增 `client/src-tauri/tauri.macos.conf.json`：复用客户配置的 macOS app/DMG 与 ad-hoc 签名覆盖项；不改 Windows 配置和应用身份。
- 新增 `scripts/release/collect-desktop-artifacts.mjs`：复用现有 API 地址校验，归档安装包、源码 SHA、签名渠道与 SHA256。
- `server/tests/test_build_contracts.py`：追加真实临时文件上的归档行为测试及工作流合同；不新增依赖。
- `README.md`：自动构建入口、下载、平台选择与签名限制。独立证据为 `docs/evidence/DESKTOP-ACTIONS-20260917.md`；任务、认领和证据账本同步登记本增量。
- 原 `.github/workflows/ci.yml`、品牌 PR #136 及业务模块不属于本任务改动范围。

## PUBLISH-LOGIN-RECOVERY-20260917 文件边界

用户报告添加发布账号无法取得二维码，打开官方窗口扫码后不回写、不关窗。本次复用现有桌面与云端流程，不新增后端接口、迁移或依赖。后续实机反馈确认 Mac 已手机确认抖音仍不保存，而视频号两个账号正常保存；因此同任务追加抖音服务端登录确认修复。

- `client/src/studio/localPublishAccounts.ts` / `cloudPublishAccounts.test.ts`：仅 Windows Tauri 调用本地账号命令，Mac 桌面沿用加密云端扫码；覆盖三平台扫码流、取消、列表/解绑及 Windows 路径保持。
- `client/src/studio/LocalPublishAccountsPanel.tsx` / `MainPages.test.tsx` 的发布账号段：打开官方窗口后恢复检测，取消或切换后的迟到响应不恢复旧会话。
- `server/app/publish_browser_engine.py` / `server/tests/test_publish_browser.py`：抖音成功码类型兼容、迟到身份响应隔离、同源后台页面的主动身份复核与扫码后停止发送页面片段；保持视频号及小红书既有流程。
- `docs/发布账号扫码部署与验收.md`：运行平台及恢复路径；独立证据固定为 `docs/evidence/PUBLISH-LOGIN-RECOVERY-20260917.md`。认领、任务及证据账本仅登记本增量。
- 并行 `REPLICA-SIMPLE-FLOW-20260917` 的复刻业务不在本任务范围；相同测试文件只改发布账号段。

## PUBLISH-QR-REUSE-20260915 文件边界

- 前端：`client/src/studio/LocalPublishAccountsPanel.tsx`、`localPublishAccounts.ts`、新增 `publish-accounts.css`；`client/src/api.ts` 增加可取消的认证流请求；同步 `client/src/generated/api.ts`。
- 桌面：`client/src-tauri/src/publish_accounts.rs`、新增 `publish_login.js`、`lib.rs` 命令登记；`client/public/licenses/social-auto-upload-MIT.txt` 随前端资源分发，Tauri 外部 resources 保持为空。
- 服务端冻结新增：`server/app/publish_browser.py`（加密账号）、`publish_browser_engine.py`（官方浏览器）、`publish_browser_routes.py`（认证流）；`main.py` 路由登记。MIT 来源见 `server/app/publishers/vendor/social_auto_upload/{LICENSE,VENDOR-NOTES.md}`。
- 迁移冻结新增：`server/migrations/versions/20260915T1200_browser_accounts.py`，单 parent `20260914T0000_local_joint_merge`；manifest 和现有五个测试模块的 HEAD 断言同步，历史迁移不改。
- 历史离线兼容：`server/scripts/reconcile_customer_billing.py` 登记两个仅 PG 的空表；非空云端账号仍拒绝导入覆盖。`test_publish_accounts.py` 的旧 Worker 无 records 表守卫明确限定其两个协议表，不误计独立浏览器账号表。
- 回归：新增 `client/src/publishLogin.test.ts`、`client/src/studio/cloudPublishAccounts.test.ts`、`server/tests/test_publish_browser.py`；扩展 `MainPages.test.tsx`、`client/src/api.test.ts`（网页会话请求头与流取消信号）与 Rust 单测。PG 模块登记在 shard-1，与共用 fixture 的账号测试串行。
- 运行与证据：Playwright 依赖及锁文件、`deploy/customer/bootstrap-base-image.sh`、`scripts/publish-qr-smoke.py`；[部署验收说明](发布账号扫码部署与验收.md)、[任务证据](evidence/PUBLISH-QR-REUSE-20260915.md)。
## VIRAL-COPY-CACHE-20260915 业务文件边界

复用 server/app/script_from_audio.py 与 server/tests/test_cw030_worker_pg_matrix.py，实现可信视频关联、缓存读写、等待与逐请求结算。新增证据路径冻结为 docs/evidence/VIRAL-COPY-CACHE-20260915.md；迁移及迁移清单范围见本文件同任务迁移登记。更新任务账本、证据索引、认领；不修改前端或其他任务业务文件。


## FIX-ASR-400-20260914 文件边界

修改既有 `server/app/asr.py`、`server/app/script_from_audio.py`、`server/app/viral_import_routes.py`、`server/app/viral_import.py`、`server/app/viral_link.py` 和相邻测试，统一本地短音频直传、Flash 请求参数及抖音/小红书完整视频音轨选择。新增文件冻结为 `docs/evidence/FIX-ASR-400-20260914.md`、`docs/媒体云端可访问性排查-20260914.md`。同步本映射、认领、任务账本与证据索引；不修改迁移、CI、云存储配置和其他任务业务代码。

## FE-VIRAL-ACTIONS-20260913 文件边界

自检追加既有 `client/src/studio/StudioWorkspace.tsx` 与相邻测试：文案入口传入新项目/素材 ID，但上下文实现忽略参数而读取旧草稿；修复明确来源交接并回归验证空草稿/已有旧来源。沿用已有转写 API，不改变供应商或计费。

用户要求爆款各页移除视频复刻操作，卡片主操作改为查看详情与提取文案，并列出前端仍未完成的任务。实现调整 `client/src/studio/ContentPages.tsx`、`MainPages.tsx`、`StudioWorkspace.tsx` 与相邻测试，样式限定 `content.css` / `studio.css`；复用现有 copy 导入与文案提取，不修改媒体 API、数据库、供应商或采集路径。收藏保留为卡片封面图标；首页精选一并移除用它复刻。

新增文件冻结为 `docs/evidence/FE-VIRAL-ACTIONS-20260913.md` 与 `docs/前端剩余工作任务清单-20260913.md`，同步任务账本、认领和证据索引。已有 VIRAL-CLOUD-MEDIA 任务的 ContentPages 改动仅为 RefreshStatisticsButton 标签，MainPages 改动为首页精选筛选；本任务避开两处，合并前复查语义及冲突。独立 main 分支与测试资源，不操作其他任务的本地联调配置和服务。


## FE-CHARACTER-SCENES-20260913 文件边界

追加用户截图反馈：在既有 `ContentPages.tsx` / `content.css` 统一爆款卡片标题、标签占位和底部操作对齐；复用内容页回归与独立预览，不新增页面或生成请求。

用户确认素材按套收纳、正面图作封面、展开选择其他角度。为隔离素材详情的视角签名/切换状态，新增文件冻结为 `client/src/studio/CharacterMaterialViews.tsx`，回归复用 `ContentPages.test.tsx`；样式补充现有 `content.css`。后端在既有 MaterialItem 合同中附加封面 ID 与已发布视角，不新增接口或迁移。

人物库体验维护：复用 `client/src/CharacterLibrary.tsx`、`client/src/api.ts` 的持久化场景任务；`client/src/studio/PeoplePages.tsx`、`StudioWorkspace.tsx`、`LiveWorkspacePanel.tsx`、`types.ts`、`live.ts` 与既有素材适配层完成定向入口、结果同步与合成图展示。样式限定 `client/src/studio/people.css`、`studio.css`、`client/src/legacy-panels.css`；后端素材投影复用 `server/app/materials.py`，不增加数据库迁移或生成调用。

回归复用各既有相邻测试（包括 `api.test.ts`）与 `server/tests/test_cw058_content_asset_pg_matrix.py`；新增文件冻结为 `client/src/CharacterScenePanel.tsx`、`client/src/CharacterScenePanel.test.tsx` 以及 `docs/evidence/FE-CHARACTER-SCENES-20260913.md`。共享弹窗复用 `studio/ui.tsx`；素材合成标记同步 `generated/api.ts`、`studio/ContentPages.tsx`。同步任务账本、证据索引与认领登记；截图与本地日志在仓库外 outputs/character-scenes-20260913，不提交用户照片或运行时凭据。

## VIRAL-CLOUD-MEDIA-20260913 文件映射

全量回归兼容：既有 `server/scripts/reconcile_customer_billing.py` 仅登记新增 PG 专用表/列，保留已有目标非空拒绝和所有导入算法；复用 `test_sqlite_to_postgres.py`、`test_cw040_internal_release_exit.py`、`test_cw056_supported_head_matrix.py`、`test_db_pg.py`、`test_postgres_migrations.py` 验证。没有实际迁移历史业务或切换环境。

全聊天需求覆盖审计追加修复：复用 CustomerWalletPanel 及其既有测试、LiveWorkspacePanel 集成测试统一积分单位；script_rewrite/asr/script_from_audio 保留已确认供应商用量，内容失败仍退回用户积分；generation_worker 对项目副本提交回执不确定时保留对象。测试追加到现有 test_usage_billing、test_asr_provider、test_cw030_worker_pg_matrix 与 test_cw043_viral_import_pg，不新增文件。用户已确认采集仅向管理端状态为已激活且未停用的客户结算，余额不足记终态失败，不透支、不自动补扣。

追加后台采集按接口次数向客户结算：新增文件冻结为 `server/app/viral_collection_billing.py`（独立批次、价格与客户快照、逐请求扣分幂等、采集账单查询）和 `client/src/admin/ViralCollectionBilling.tsx`（采集批次与客户扣分明细）。复用 billing_meter、usage_billing、billing_reports、billing_routes、BillingEconomics、BillingRatesManager 与现有 test_usage_billing 测试；同一未发布 migration 追加批次/账单关联。供应商成本只记一次，客户收入分别入账，数据库读取、云存储不产生采集收费。首页隔离复用 MainPages、StudioWorkspace（含主线新来源交接测试的精选 fixture）、types、viral_tikhub 及相邻测试，test_db_pg 收紧已移除自动采集路径的数据库边界。

用户补充独立视频库页面：新增 `client/src/admin/ViralVideosPage.tsx` 与 `client/src/admin/ViralVideosPage.test.tsx`，由 `client/src/AdminApp.tsx` 导航接入；管理查询/首页展示/逻辑删除复用 `admin_runtime_routes.py` 和 `api.admin.ts`。客户首页通过 `client/src/api.ts`与 `studio/live.ts` 增加精选查询；追加同一未发布 migration 的首页精选和删除标记，删除不影响项目副本。前端测试新增文件不影响服务端 pytest 分片清单。

新增文件冻结：`server/app/viral_media_preparation.py` 为 PG 共享租约与不可变媒体发布；`server/app/viral_collection.py` 为每天或每周后台采集、断点续做与成品批次发布；`server/migrations/versions/20260913T1600_shared_viral_media.py` 追加媒体引用、采集配置、进度及发布字段；`docs/evidence/VIRAL-CLOUD-MEDIA-20260913.md` 为独立证据。生产入口为既有 generation_worker 的 `--viral-collection`，随默认客户镜像交付。复用 storage、viral_decrypt、viral_media、viral_import、viral_routes、viral_import_routes、viral_store、viral_refresh、viral_keywords、media、generation_worker、billing_viral_routes；管理配置复用 admin_runtime_routes、ViralRuntimeSection 与 api.admin，前端 ContentPages/BillingModulePrices 将统计刷新改为读取已采集数据。部署复用 deploy/customer/compose.yaml、README、customer-git-rollout.sh、package.json。回归追加到现有 storage/viral_media/viral_link_media/cw043_viral_import_pg/customer_registration/cw058_content_asset_pg_matrix/admin_session_routes/cw032_delivery_package/build_contracts 与 ViralRuntimeSection 专项；现有 head 常量、CW056 schema 和 migration manifest 同步。云存储不向用户计费，主动解析保留原独立计费与失败退款。



## FIX-VIDEO-LINK-20260913 文件边界

测试覆盖登记补充：`scripts/ci/test-shards/shard-0.txt` 登记新增 `test_viral_link_media.py`，保留全量分片覆盖，不变更 CI 门禁或工作流。

复用 `server/app/viral_media.py` 的公网解析与固定连接，兼容系统 DNS 返回的 Fake-IP；`server/app/viral_import_routes.py` 将网络解析失败归为可理解的网络错误。回归落在现有 `server/tests/test_viral_media.py`，必要的链接预检专项冻结为 `server/tests/test_viral_link_media.py`；任务证据冻结为 `docs/evidence/FIX-VIDEO-LINK-20260913.md`。只新增上述测试与证据文件；同步任务账本、证据索引和认领登记，不改凭据、计费、迁移或 CI。

真实缓存复验补充：源音频实际为 M4A，预检需兼容并保留真实 MIME；`server/app/viral_import.py` 同步导入源文件扩展名。`server/app/media.py` 修复 Windows 下 ffprobe 无法读取仍被打开的临时文件；回归同归上述已冻结测试文件，不增加供应商调用。

`client/src/studio/MainPages.tsx` 与其现有测试仅处理明确终态的 DNS 失败重试键；未知解析结果仍保留原幂等键，不自动重试。

## ADMIN-R01-20260913 增量文件映射

复用 `client/src/api.admin.ts`（写合同、401、错误解析）与 `client/src/api.ts`（管理过期通知）；回归放在既有 `client/src/api.admin.test.ts`、`client/src/AdminApp.test.tsx`。独立证据为 `docs/evidence/ADMIN-R01-20260913.md`；同 PR 更新任务账本、证据索引、认领登记。无新增业务路由或数据库文件。

## BILLING-20260913 用户授权维护文件映射

| 新文件 | 职责 |
| --- | --- |
| `server/app/billing_catalog.py`、`usage_billing.py` | 科目、冻结计价、资金来源、预留与结算 |
| `server/app/billing_meter.py`、`billing_viral_routes.py` | 实际供应商调用、显式数据刷新与平台成本 |
| `server/app/billing_reports.py`、`billing_routes.py`、`billing_evidence.py` | 管理配置、逐次/周期报表、只追加核对依据 |
| `server/migrations/versions/20260913T1100_itemized_billing.py` | 新账务结构；不迁移历史业务或转换余额 |
| `client/src/admin/BillingRatesManager.tsx`、`BillingEconomics.tsx`、`BillingEvidenceForm.tsx` | 成本售价、经营分析、凭据核对 |
| `client/src/studio/BillingModulePrices.tsx` | 前端模块适用价格说明 |
| `server/tests/test_usage_billing.py`、`client/src/admin/BillingRatesManager.test.tsx` | 计价、资金、周期、权限、重试与配置测试 |
| `docs/evidence/BILLING-20260913-EVIDENCE.md` | 功能/API 对照、测试、评审与 PR 证据 |

既有文件的变更边界：`analysis/analysis_routes`、`first_frames/image_tasks/character_image_generation`、`script_rewrite/script_from_audio`、`generation/generation_worker/internal_billing`、`oral/oral_worker/oral_routes`、`viral_import_routes/viral_routes/viral_tikhub` 接入逐项受理、实际成本与交付结算；`customer_pricing/customer_pricing_routes/admin_write_contract` 维护显式售价发布、锁顺序与 Decimal 写入幂等；`operation_costs`、`wallet_routes/recharge_routes` 接入明细与资金来源；`main` 注册接口。

客户界面 `useGenerationDrafts`、`ProjectDetailFlow`、`GenerationComposer` 专项及 `studio/StudioWorkspace/ContentPages` 使用后端积分别价并保留退款、重试与幂等；管理端 `SystemSettingsPage/AnalyticsPage/CustomerPricingManager` 注册新页；`api/api.admin/generated/api` 和客户账单显示新科目。既有 worker、口播、微信回调、钱包、头结构、注册/激活和归档工具测试按新结构及明确收费夹具更新；`scripts/ci/test-shards`、迁移 manifest 同步。`server/scripts/sqlite_to_postgres` 与 `reconcile_customer_billing` 仅维持已登记离线归档契约，历史输入不会产生新资金来源；没有执行业务迁移或切换。



## 2026-09-11 开工准备执行证据登记

维护任务 `COORD-PREFLIGHT-EXECUTION-20260911`：执行用户已授权的开工准备清单，跳过其他工作区占用项。以下证据只记录检查结果与交接差额，不改变业务任务验收状态；不修改 059/061 实现或 044 现有盘点。

| 路径 | 职责 / 消费者 |
| --- | --- |
| `docs/evidence/COORD-PREFLIGHT-EXECUTION-20260911.md` | 顺序执行结果、043 缺口映射、044 交接边界、测试资源安排和 worktree 处置；供后续 Owner/Reviewer 使用 |
| `docs/evidence/COORD-PREFLIGHT-EXECUTION-20260911.json` | 固定 SHA 下的测试函数/分片核对及脱敏 PR、worktree、资源快照；作为上述记录的可核验证据，不含环境变量或凭据 |

## 2026-09-11 开发进展复核文件登记

维护任务COORD-PROGRESS-AUDIT-20260911：从最新main创建独立worktree，更新当前进展与后续思路。仅派生分析，不改变业务完成状态；#35、059、061正在修改的账本/快照/认领文件由原任务处理。

| 路径 | 职责 |
| --- | --- |
| `docs/客户云版开发进展与后续思路-2026-09-11.md` | 合并与在制证据、CI覆盖缺口、061/059优先顺序及pre-GA/GA路线 |
| `docs/evidence/COORD-PROGRESS-AUDIT-20260911.md` | 本次维护范围、核对与验证记录 |
| `docs/客户云版开发顺序排班与Worktree协作清单.md`、本文件 | 最新分析入口与文件登记，保留正式依赖和开发规则 |

## 2026-09-10 排班与 Worktree 协作文件登记

用户要求建立开发顺序、前置、并行、开工查重、远程提交和合并后 worktree 清理规则。本次是 `COORD-SCHEDULE` 文档维护任务，不新增 CW 编号、不关闭任何业务验收。

| 路径 | 职责 |
| --- | --- |
| `docs/客户云版开发顺序排班与Worktree协作清单.md` | 25项候选排班、后续条件/验收任务、前置和并行边界、worktree全生命周期 |
| `docs/客户云版任务认领登记.md` | 任务占用、负责人、分支、worktree、PR及交接；不替代任务账本完成状态 |
| `docs/evidence/COORD-SCHEDULE-EVIDENCE.md` | 本次文档维护的范围、校验、远程提交与未完成验证记录 |
| `docs/客户云版新任务开工前工作清单.md` | 2026-09-11 COORD-PREFLIGHT维护：当前准备事项、逐任务开工检查、main来源命令及记录模板；入口接入AGENTS |
| `docs/evidence/COORD-PREFLIGHT-20260911.md` | COORD-PREFLIGHT文档维护范围、来源与验证；不关闭业务任务 |
| `AGENTS.md`、`docs/ChatGPT网页端开发交接提示词-V3.md` | 每次新任务必执行的开工与交付规则 |
| `docs/客户版任务清单-V3.md`、`docs/客户版开发计划-V3.md`、本文件 | 入口链接、规则优先级与文件登记，保留原业务DoD及状态 |

> 当前执行清单已更新为[本地实现去重V3](../outputs/customer-cloud-convergence-analysis-2026-09-08/v3/客户版收敛剩余任务清单与验收完工标准-V3.md)：57项剩余排程，复用既有代码；原60项及CW-006/008/011保留追溯，当前状态仅见任务账本§18。此更新不代表代码或数据迁移已完成。

> 2026-09-08 PostgreSQL 全面统一增量：用户已确定开发、业务数据库测试、CI、staging、生产均使用 PostgreSQL；SQLite 仅限精确登记的离线历史输入、归档与兼容工具。
> 实施与验收以[唯一数据库规范](PostgreSQL唯一数据库实施与验收规范.md)及 CW-001—060 为准。此前仅客户生产 PG、默认开发 SQLite、SQLite 业务测试可作为当前验收的口径不再适用。
> 本次更新只确认规范和任务定义；原代码仍有 SQLite 分支，历史任务/测试记录保留原文，不据此声明实际迁移或生产切换已完成。

> 目标：把现有内部版代码改造成客户版“一人一码、双设备、单在线”方案。
> 说明：以下是代码清单，不是执行结果。
> 状态：`READY_AFTER_USER_CONFIRMATION`。除明确标记为已有的复用项外，复选框均未完成。
> 关联任务：`docs/客户版任务清单-V3.md`
> 状态边界：本文件的复选框只表示文件范围；完成状态只允许在 `docs/客户版任务清单-V3.md` 更新。
> 人物与首帧链路：问题校核见 `docs/人物与首帧业务链路问题清单-2026-08-29.md`，原子发布、回滚与保留风险见 `docs/人物与首帧四工作包发布计划-2026-08-30.md`。

## 2026-09-05 管理后台改版增量文件映射

本增量按 `docs/design/admin-redesign/work-plan.md` 的 W3–W17 实施，沿用既有客户鉴权、钱包、租约和审计边界。下表登记扩展文件范围；状态见任务账本和改版清单。

| 范围 | 文件 |
| --- | --- |
| 成本与费率快照 | `server/app/operation_costs.py`、`server/migrations/versions/059_operation_cost_records.py`、`server/tests/test_operation_costs.py` |
| 个人提示词元数据 | `server/migrations/versions/061_saved_prompt_metadata.py`；复用 `generation.py`、`generation_routes.py` 与 `versions` |
| 激活初始免费秒数 | `server/migrations/versions/062_activation_initial_free_seconds.py`；复用 `admin_activation_routes.py`、`activation_code_routes.py` 与免费调账审计 |
| 钱包流水顺序 | `server/migrations/versions/063_wallet_ledger_sequence.py`；复用 `admin_customer_routes.py` 和 SQLite→PG 导入/对账工具 |
| 成本明细页面 | `client/src/admin/CostDetails.tsx`、`CostDetails.test.tsx`、`api.economics.ts`、`economics.css` |
| 管理页局部样式 | `client/src/admin/admin-activation.css`、`admin-audit.css`、`admin-customer-detail.css`、`admin-sessions.css` |
| 生成/提示词/钱包样式 | `client/src/generation-controls.css`、`prompt-markdown.css`、`customer/customer-wallet.css` |
| 正式图标 | `client/src/assets/icons/`；替换页面内临时符号 |
| 验证记录 | `docs/evidence/admin-revamp/implementation.md`、`browser-qa.md`、`source-manifest.json`、最终日志与截图 |

## 2026-09-09 客户版收敛 CW-007 增量文件映射

按 V3 收敛清单（CW-007 补齐 PG 测试隔离、客户种子与缺库硬门）登记新增测试基建文件；状态见任务账本 §18。

| 范围 | 文件 |
| --- | --- |
| PG 测试统一基建（preflight 硬门、测试库 allowlist、2客户3设备种子、共享套件文件锁） | `server/tests/pg_test_kit.py`（新增） |
| 硬门/种子可复用/中断重跑/双库隔离/锁互斥验收测试 | `server/tests/test_pg_test_kit.py`（新增） |
| ce40db5 整合扩展：迁移链单 head 断言前进 + viral PG 用例缺库 skip→硬门 | `server/tests/test_postgres_migrations.py`（HEAD_REVISION 076→080，改）、`server/tests/test_viral_refresh.py`（#102 新增后迁硬门，改） |

## 2026-09-09 客户版收敛 CW-009 增量文件映射

| 范围 | 文件 |
| --- | --- |
| CW-009 安全矩阵机器可核销校验器（requirement→test 引用存在性） | `server/tests/test_cw009_security_matrix_export.py`（新增） |

## 2026-09-11 客户版收敛 CW-029 增量文件映射

按 V3 收敛清单 CW-029（复用账务核心并核验扣费、支付差额）登记；状态见任务账本 §18。账务核心零生产代码改动，全部实现落在已冻结的 `internal_billing.py`/`zpay_payments.py` 复用路径内；新测试文件按 §15 规则在此登记后创建。

| 范围 | 文件 |
| --- | --- |
| 支付回调+任务状态 TEST-PG 多连接矩阵（四套账差额断言） | `server/tests/test_cw029_billing_pg_matrix.py`（新增，7 用例；纯测试模块，不被应用代码 import） |
| 验证记录 | `docs/evidence/CW029-EVIDENCE.md`（新增）；`docs/CUSTOMER-TASK-EVIDENCE-V3.md`（登记） |

## 2026-09-11 客户版收敛 CW-028 增量文件映射

按 V3 收敛清单 CW-028（迁出共享设置工具并核销旧路由消费者）登记；状态见任务账本 §18。共享工具迁移全部落在既有冻结文件内（settings.py / settings_routes.py / control_routes.py），不新增应用源文件；新测试文件按 §15 规则在此登记后创建。

| 范围 | 文件 |
| --- | --- |
| 共享 Provider 配置/测试工具中性实现宿主（九符号单一实现；hifly 调用期惰性导入破环） | `server/app/settings.py`（改，追加共享栈） |
| 路由模块改为导入中性实现（仅保留自身消费的六符号再导出） | `server/app/settings_routes.py`（改，删除重复定义） |
| 管理控制面改为自中性模块导入（源码零 settings_routes 引用） | `server/app/control_routes.py`（改，导入块） |
| 既有设置回归的 monkeypatch 目标与测试器类导入改指 app.settings | `server/tests/test_settings.py`（改） |
| 共享工具合同矩阵（34 用例：行为合同+单一实现身份+零反向依赖） | `server/tests/test_cw028_shared_settings_contract.py`（新增；纯测试模块，不被应用代码 import） |
| 验证记录 | `docs/evidence/CW028-EVIDENCE.md`（新增，含 §3 逐端点消费者清单与兼容允许清单）；`docs/CUSTOMER-TASK-EVIDENCE-V3.md`（登记） |
## 2026-09-11 客户版收敛 CW-030 增量文件映射

按 V3 收敛清单 CW-030（收敛各类Worker的PG调度与恢复差额）登记；状态见任务账本 §18。生产改动全部落在任务冻结文件内（generation_worker.py / generation.py 等），不新增应用源文件；新测试文件按 §15 规则在此登记后创建。

| 范围 | 文件 |
| --- | --- |
| 删除 SQLite 轮询入口（run_sqlite_worker_round/run_forever+孤儿导入） | `server/app/generation_worker.py`（改） |
| mark_task_submission_uncertain 迟到标记守卫（SUBMITTING/RUNNING 条件转换+条件释放） | `server/app/generation.py`（改） |
| 逐类 PG 调度/恢复矩阵（29 用例：独立/首帧/联系表/人物图/源帧/文案改写/ASR/混合/两设备） | `server/tests/test_cw030_worker_pg_matrix.py`（新增；纯测试模块，不被应用代码 import） |
| 测试库 allowlist 登记 cw030_worker_matrix_test | `server/tests/pg_test_kit.py`（改） |
| 被删入口引用移植（run_worker_once 直驱/不存在断言/SQLite 降级用例移交矩阵） | `server/tests/test_generation.py`、`server/tests/test_db_pg.py`、`server/tests/test_source_frames.py`（改） |
| 验证记录 | `docs/evidence/CW030-EVIDENCE.md`（新增，§3 矩阵、§4 CW-002 限制登记）；`docs/CUSTOMER-TASK-EVIDENCE-V3.md`（登记） |

## 2026-09-11 客户版收敛 CW-027 增量文件映射

按 V3 收敛清单 CW-027（复用管理后台并收口权限差额，复验）登记；状态见任务账本 §18。核验结论为「矩阵无漏接」，零生产代码改动，不新增应用模块；新测试文件按 §15 规则在此登记后创建。

| 范围 | 文件 |
| --- | --- |
| 84 条管理路由权限静态矩阵 + admin/auditor/customer/CSRF/过期吊销/旧proxy 动态底线网格 | `server/tests/test_cw027_admin_permission_matrix.py`（新增，8 用例；纯测试模块，不被应用代码 import） |
| 验证记录 | `docs/evidence/CW027-EVIDENCE.md`（新增）；`docs/CUSTOMER-TASK-EVIDENCE-V3.md`（登记） |

## 2026-09-11 客户版收敛 CW-026 增量文件映射

按 V3 收敛清单 CW-026（退出内部认证并复验客户fencing）登记；状态见任务账本 §18。实现全部落在已冻结的 `server/app/auth.py` 映射路径内，不新增任何应用模块；新测试文件按 §15「先冻结准确路径，再写代码」规则在此登记后创建。

| 范围 | 文件 |
| --- | --- |
| 收敛通道身份矩阵 + 迟到写 fencing + 幂等重放再授权 + 271 条 method-path 授权矩阵（漏项=0）专项 | `server/tests/test_cw026_converged_auth.py`（新增，14 用例；纯测试模块，不被任何应用代码 import，由 `pg_test_kit.require_pg_or_explicit_skip()` 把关；禁 `sqlite3.connect` 于 PG 用例） |
| `authenticate_request` 收敛（删除非客户生产 PG 内部 Bearer 豁免与 dev identity 可达路径） | `server/app/auth.py`（已冻结映射文件，改） |
| 旧行为测试迁移（断言语义不变，认证方式改客户会话/依赖覆盖） | `server/tests/test_internal_access_tokens.py`、`server/tests/test_independent_creation.py`、`server/tests/test_oral_domain.py`（已冻结测试文件，改） |
| 验证记录 | `docs/evidence/CW026-EVIDENCE.md`（新增）；`docs/CUSTOMER-TASK-EVIDENCE-V3.md`（登记） |

## 2026-09-11 客户版收敛 CW-024 增量文件映射

按 V3 收敛清单 CW-024（补齐安全升级和唯一签名发布流程，承接 CW-011 安装升级合同）登记；状态见任务账本 §18。复用 CW-021 交付的 NSIS 失败阻断与 CI 无签名构建/SHA 归档，仅做剩余差额：旧安装路径全覆盖、数据保护前置（运行守卫→归档→校验→卸载）、无签名制品内部测试标注、唯一签名发布流程与运行手册。真实签名实机验收归 CW-046，真实旧数据切换归 CW-051；新文件按 §15「先冻结准确路径，再写代码」规则在此登记后创建。

| 范围 | 文件 |
| --- | --- |
| 安全升级/签名/回滚合同专项（唯一签名发布 fail-closed、CI 无签名标注、hook 顺序合同；纯静态断言无 PG） | `server/tests/test_cw024_signed_release_upgrade_contracts.py`（新增，9 用例；纯测试模块，不被应用代码 import） |
| 安装钩子数据保护改造（双旧安装路径覆盖：运行实例守卫→完整归档→校验+清单→静默卸载，任一失败 Abort 且可恢复） | `client/src-tauri/customer-installer-hooks.nsh`（改） |
| CI 无签名制品内部测试标注（归档目录 `customer-cloud-internal-test-unsigned` + `RELEASE-CHANNEL.txt` 渠道标签） | `.github/workflows/ci.yml`（改） |
| 唯一签名发布流程入口（Authenticode fail-closed：无签名材料/无有效签名不出制品；产物记版本/平台/签名/SHA） | `scripts/release/build-customer-signed-release.ps1`（新增）、`package.json` `release:customer`（改） |
| 升级/卸载运行手册（支持版本×旧路径矩阵、升级行为矩阵、回滚与恢复、签名发布步骤） | `docs/客户版桌面升级与签名发布手册.md`（新增） |
| 验证记录 | `docs/evidence/CW-024-EVIDENCE.md`（新增）；`docs/CUSTOMER-TASK-EVIDENCE-V3.md`（登记） |

## 2026-09-10 客户版收敛 CW-054 增量文件映射

按 V3 收敛清单 CW-054（修复 PG 批量操作、查询类型与异常差额）Segment 1/N 登记。依 §14 “若实现确需新的测试/legacy 模块，先在本节登记确切文件名、消费者、入口/打包边界和责任再创建”要求，先冻结再落地；沿用 CW-007/CW-009 先例格式。本 Segment **不新增任何应用模块**：实现全部落在 §14「CW-025/054」映射行已登记的 `db_portable.py` 路径内，不新建泛化多后端抽象。状态只在本清单 §18 更新。（引用用行内容锚点而非绝对行号：共享文档上游插入会使行号漂移。）

| 范围 | 文件 | 消费者 / 入口边界 / 责任 |
| --- | --- | --- |
| CW-031 正式服务存储围栏与跨实例专项（**TEST-PG**） | `server/tests/test_storage_cross_instance.py`（新增，8 组 27 用例） | 消费者：pytest（PG 硬门车道），`require_pg_or_explicit_skip()` 把关 + `shared_suite_lock()` 串行共享库，不被应用代码 import。责任：钉住 `get_media_storage` 双闸门（active_storage_provider 主 + is_customer_production 兜底，缺 COS 配置 503 `STORAGE_PROVIDER_FORBIDDEN`）、`storage_for_asset` 历史 local URI 客户生产拒绝、API A→API B/Worker 三独立连接共享 COS 后端可读且落库 `cos://` 100%、COS put/get/sign 故障本地持久零新增、签名跨用户/篡改/过期拒绝与重获、本地缓存目录删除后 COS 恢复、bootstrap 启动期写路径探测；禁 override `get_media_storage` 本体；不新建 PG 测试库（`pg_test_kit.py` 零改动） |
| CW-054 PG-lane 查询/行/异常契约专项（**TEST-PG**） | `server/tests/test_cw054_pg_portable_contract.py`（新增，9 类 49 用例） | 消费者：pytest（PG 硬门车道），由 `pg_test_kit.require_pg_or_explicit_skip()` 把关，不被任何应用代码 import。入口/打包边界：纯测试模块，不进任何构建产物。责任：钉住 executemany 真实批量与 rowcount、iterdump 真实 INSERT、set_trace_callback 生效、行按名/位置与 `dict(row)`/`keys()`、金额/布尔/NULL/UTC/JSONB roundtrip、UNIQUE/FK/CHECK/NOT NULL 四类约束映射与 SQLSTATE 携带、`%%` 字面百分号约定；并反向钉住 CW-055 边界（commit/rollback/close/transaction 的 PG no-op **不得**被本任务改动）。禁 `sqlite3.connect`/`SQLiteBackend`/`DB_PATH`/`:memory:`；`sqlite3` 仅作为异常基类引入以验证双继承 |
| 既有门面单测扩充（**TEST-LOGIC**，PG-free） | `server/tests/test_db_portable.py`（19→57 用例，改） | 消费者：pytest（无库车道）。入口/打包边界：纯测试模块。责任：新增 27 条 `_NamedRow` vs **真实** `sqlite3.Row` 逐轴 parity 矩阵 + 4 具名 + 7 异常契约；其中 mapper 的 SQLSTATE 四路映射因 psycopg 把 `sqlstate` 作为类属性而可 PG-free 覆盖，`constraint_name` 因 `diag` 为只读属性不可 stub 而留给 TEST-PG。已实测验证：指向死端口 DSN 仍 57 passed |
| PG-lane 实现（§14「CW-025/054」映射行已登记路径，无新模块） | `server/app/db_portable.py`（437→650 行，改） | 消费者：全仓业务服务与路由经 `BusinessConnection` 门面。入口/打包边界：既有应用模块，无新增导出面。责任：新增 `IntegrityConstraintError`（双继承 `sqlite3.IntegrityError` + `psycopg.IntegrityError`，过渡性设计，CW-042 退役 SQLite lane 后摘除 sqlite3 基类）、`_map_integrity_error`、`_pg_literal`、`_pg_iterdump`；修 `PostgresBackend.execute`/`executemany`、`BusinessConnection.executemany`/`set_trace_callback`/`iterdump`、`_NamedRow` 三处行语义 |
| 测试库 allowlist 追加专用库 | `server/tests/pg_test_kit.py`（+4 行，仅追加） | 消费者：上述 TEST-PG 的 module-scoped fixture。入口/打包边界：既有测试基建，无行为改动。责任：登记 `cw054_contract_test` 使 create/drop 经 `assert_safe_test_database` 守卫；专用库满足 PG-05「独立专项不互删」。**本机与全量 suite 的隔离靠端口/容器而非套件锁**：`SHARED_SUITE_LOCK` 默认 POSIX 路径且 `fcntl` 在 Windows 被 shim，本机共享套件锁实为空操作，真实互斥语义由 CI Linux 承载（AGENTS.md 标准工作流第 8 条：每 worktree 隔离运行端口和 PG 资源，未隔离的共享 PG 全量测试保持串行） |

## 1. 当前可复用的代码

| 文件 | 复用点 | 说明 |
| --- | --- | --- |
| `server/migrations/versions/001_core.py` | `users`、基础账户结构 | 继续作为用户主表 |
| `server/migrations/versions/022_internal_billing.py` | `wallets`、`wallet_transactions`、`recharge_orders` | 继续作为账务底座 |
| `server/app/recharge_routes.py` | ZPay 充值入口 | 后续改造成客户 session 鉴权后复用 |
| `server/app/wallet_routes.py` | 钱包查询与流水 | 继续复用 |
| `server/app/generation.py` | 任务领取、租约、状态流转 | 需要改为用户公平轮转，但大结构可用 |
| `server/app/storage.py` | 存储抽象 | 客户生产强制私有 COS，可保留抽象层 |
| `client/src/SettingsPanel.tsx` 等现有工作台组件 | 基础 React 组件结构 | 工作台仍可复用一部分布局和样式体系 |

## 2. 必须替换的现有文件

| 文件 | 为什么要换 | 预期替换方向 |
| --- | --- | --- |
| `client/src/App.tsx` | 当前是内部令牌登录 + 工作台，不支持客户激活/设备状态机 | 改成客户激活、登录冲突、切换、heartbeat、设备管理主容器 |
| `client/src/AdminApp.tsx` | 当前只有账号钱包、订单、支付设置 | 扩成激活码、客户、设备、会话、审计总控台 |
| `client/src/api.ts` | 只有现有内部 API 类型 | 增加 customer/control 激活码、设备、会话、新类型 |
| `server/app/auth.py` | 当前是桌面身份 / 内部 token | 保留内部控制面，另建客户 session 认证层 |
| `server/app/control_auth.py` | 当前只有单一控制身份 | 改为真实 admin/auditor actor 与审计输入 |
| `server/app/main.py` | 当前没有客户激活码路由 | 挂载客户版 customer 路由与管理端 control 路由 |
| `server/app/db.py` | 当前是 SQLite 专用 | 抽象出 PostgreSQL 运行方式与迁移入口 |
| `server/app/generation.py` | 当前按全局 FIFO 领取 | 改成按 `user_id` 公平轮转 |
| `deploy/systemd/video-replica-api.service` | 只适合单 API | 改成多实例可部署模板 |
| `deploy/systemd/video-replica-worker.service` | 只适合单 Worker | 改成多 Worker 模板 |

## 3. 唯一实现文件映射

本节是 V3 的规范文件映射。任务清单、开发计划和执行计划必须引用这里的名称，不得再创建同义模块；实施时若确需改名，必须先同步 V3 交付包全部材料及上游 PRD/测试规格。

### 3.1 数据迁移

迁移主题和顺序固定如下；`025`–`030` 仅基于当前 head `024` 的建议编号，实际编号以实施当日 Alembic head 为准，不得覆盖既有 revision。

| 建议文件 | 固定主题 |
| --- | --- |
| `server/migrations/versions/025_postgres_runtime_compatibility.py` | PostgreSQL 运行兼容、基础方言和索引修正 |
| `server/migrations/versions/026_customer_security_and_billing.py` | 用户/账务扩展、管理员会话、共享限流所需数据 |
| `server/migrations/versions/027_activation_code_catalog.py` | 激活码批次、码、发放、一次性导出事实 |
| `server/migrations/versions/028_customer_devices_and_activations.py` | 激活事实、两个当前设备槽、配对与解绑历史 |
| `server/migrations/versions/029_customer_sessions_and_idempotency.py` | 单在线租约、epoch fencing、幂等请求和恢复信封 |
| `server/migrations/versions/030_user_fair_queue.py` | 按用户公平调度游标、并发约束和热路径索引（预留 T25，未创建） |
| `server/migrations/versions/031_admin_write_idempotency.py` | 管理端写幂等快照层与下载审计列（T12；编号倒挂见 M2 评审 §6，链序 `027→031→028→029→032`） |
| `server/migrations/versions/032_security_rate_limits.py` | 共享限流计数器与失败审计表（T15） |
| `server/migrations/versions/033_batch_creation_audit.py` | 批次创建审计列 creation_reason/creation_request_id（M2 评审 H1 修复） |
| `server/migrations/versions/034_device_fingerprint_canonical.py` | 设备指纹跨版本探测键与部分唯一索引（M2 评审 M1 修复） |
| `server/migrations/versions/035_export_ciphertext_purge.py` | 导出密文可清除化（列 nullable 化 + purged_at 耦合 CHECK；M2 评审 M2 修复） |
| `server/migrations/versions/036_low_review_constraint_guards.py` | 026 约束矩阵收口（channel/provider、paid_at/PAID、admin_sessions 时区安全 CHECK）+ 三张 append-only 审计表的 TRUNCATE 守卫（M1/M2 评审 LOW 修复） |
| `server/migrations/versions/037_device_pairing_requests.py` | 第二设备一次性配对请求（四态状态机形状 CHECK + 部分唯一活动索引；T17/DEV-02，PG-only） |
| `server/migrations/versions/038_admin_device_operations.py` | 管理员核验批准/解绑/凭据撤销（approved_by_admin_user_id 双批准列互斥 lineage + admin_device_events append-only 审计表；T18，PG-only） |
| `server/migrations/versions/039_admin_adjustments.py` | 后台调账审计（append-only + source_document_type 枚举 CHECK + TRUNCATE 守卫；T23/BILL-02，PG-only） |
| `server/migrations/versions/040_fix_provider_settings_constraint.py` | 修复 002 遗留 provider_settings 自动命名 CHECK（zpay/deepseek 无法入库；drop 两个重建五 provider 约束；T22 撞出修复，注意 downgrade 会删除 zpay 配置行） |
| `server/migrations/versions/041_user_fair_queue.py` | T25 公平队列追加式修复（从 040 接链，避免已发布 032–040 数据库跳过原预留 030） |
| `server/migrations/versions/042_t37_observability_indexes.py` | T37 告警探针热索引（含新 epoch LOGIN 后旧 epoch heartbeat）、跨主机共享告警状态、`admin:exchange:ip` / append-only `session:fencing` 审计维度、成功写事务的 expected/verified epoch 去重证据表，以及 append-only `customer_authorization_evidence` 摘要对表（PG-only；成功的 actor≠owner 即 `cross_user_access` P1，拒绝审计仍按 action/time 索引；以 UTC 一次回填并索引 session/audit/task 的 typed absolute-time companion 列，探针不在查询时强转 legacy TEXT） |
| `server/migrations/versions/043_admin_password_login.py` | 管理员日常账号密码登录：逐操作员 scrypt 哈希、会话认证方式标记；ASX1 仅保留为首次设置/找回密码入口（PG-only） |
| `server/migrations/versions/044_customer_unit_prices.py` | 按激活客户设置单条视频销售价：用户价格覆盖表、管理员留痕，并解除客户售价不得低于内部基础价的旧约束（PG-only） |
| `server/migrations/versions/045_async_analysis_tasks.py` | 视频拆解持久化异步任务：待执行/执行中/成功/失败状态、任务租约、失败原因与可重试标记（SQLite/PG） |
| `server/migrations/versions/046_async_image_tasks.py` | 人物拼合图与人物置换首帧持久化异步任务：请求 hash、活跃任务幂等、worker 租约、终态恢复与不确定提交隔离（SQLite/PG） |
| `server/migrations/versions/047_async_source_frame_tasks.py` | 源画面抽帧持久化异步任务：API 快速入队、FFmpeg/COS 在事务外执行、活跃任务幂等与跨页面终态恢复（SQLite/PG） |
| `server/migrations/versions/048_async_script_rewrite_tasks.py` | AI 口播稿改写持久化异步任务：API 快速入队、DeepSeek 在事务外执行、结果跨页面恢复与不确定提交隔离（SQLite/PG） |
| `server/migrations/versions/049_async_generation_reconcile.py` | H3 不确定提交对账异步化：复用操作幂等账本补充 Worker 租约，服务商查询/下载/COS 归档脱离 HTTP 与数据库长事务（SQLite/PG） |
| `server/migrations/versions/050_activation_license_zero_credit.py` | 激活授权与充值解耦：允许正常批次零面值/零初始额度、约束金额与额度形状一致，并允许激活事实不关联充值单（PG-only） |
| `server/migrations/versions/051_identity_owner_backfill.py` | T44 存量人物 owner 回填与强约束：项目关系优先、创建人兜底，冲突/无归属 fail closed；owner 改为 NOT NULL + RESTRICT（PG-only） |
| `server/migrations/versions/054_admin_free_grant_adjustments.py` | FREE_GRANT 免费条数发放：admin_adjustment 订单允许 0 金额（免费发放不虚构收入），来源单枚举追加 FREE_GRANT；存在零金额发放时拒绝降级（PG-only） |
| `server/migrations/versions/053_activation_code_archive.py` | 已撤销激活码软归档：日常列表隐藏，保留设备、充值与审计关联；存在归档历史时拒绝降级（PG-only） |
| `server/migrations/versions/052_character_scene_look_tasks.py` | 场景造型任务持久化：允许同一人物版本按场景与服装描述生成独立五视图，并保留发布所需的场景造型快照（SQLite/PG） |

### 3.2 后端业务模块

- `server/app/customer_models.py`
- `server/app/activation_code_service.py`
- `server/app/activation_code_routes.py`
- `server/app/customer_device_service.py`
- `server/app/customer_device_routes.py`
- `server/app/customer_session_service.py`
- `server/app/customer_session_routes.py`
- `server/app/customer_idempotency.py`
- `server/app/customer_auth.py`
- `server/app/customer_queue.py`
- `server/app/security_rate_limit.py`
- `server/app/recharge_routes.py`（T44：内部充值通道拒绝 customer/auditor）
- `server/app/characters.py`、`server/app/rbac_routes.py`（T44：legacy 人物归属与缓存读取门禁）
- `server/app/image_tasks.py`、`server/app/storage.py`（T44：幂等重放前鉴权与对象键拒绝歧义路径）
- `server/app/simple_character.py`（T44：无需人工审核的系统自动发布策略及审计语义）
- `server/app/activation_code_routes.py`、`server/app/customer_session_routes.py`、`server/app/customer_device_routes.py`、`server/app/customer_idempotency.py`（T45：完整码恢复、重放状态复核、账号 scope、keyed digest、预认证/重置限流）
- `server/app/media_routes.py`、`server/app/storage.py`、`server/app/permissions.py`、`server/app/generation_routes.py`、`server/app/image_tasks.py`、`server/app/simple_character_routes.py`（T45：本地/COS 可吊销应用签名、跨用户资源 404 同型与审计保留）
- `server/app/zpay_payments.py`、`server/app/payment_routes.py`（T45：CLOSED 迟到支付和启用渠道回调）
- `server/app/admin_auth_routes.py`、`server/app/admin_customer_routes.py`（T45：空闲/上下文撤销与自助调账/改价 SoD 审计）
- `scripts/customer_release_preflight.py`（T45：客户生产身份红线和 metrics token 文件检查）
- `server/app/admin_auth_routes.py`
- `server/app/admin_write_contract.py`（2026-09-02 评估 A6：管理端写契约 + 031 幂等快照层公共模块，activation/customer/device/control 共用）
- `server/app/admin_activation_routes.py`
- `server/app/admin_customer_routes.py`
- `server/app/admin_device_routes.py`
- `server/app/admin_session_routes.py`
- `server/app/admin_audit_routes.py`
- `server/app/admin_runtime_routes.py`（M4/M5 评审 M2：公平队列开关的生产控制面读写，AdminWriter + CSRF + 审计）
- `server/app/ops_metrics.py`（T37：进程本地指标、统一 request id、结构化请求日志；注册在 CORS 外层，preflight 同样可观测；未处理 500 也回传关联 id）
- `server/scripts/check_ops_alerts.py`（T37：advisory-lock 单所有者集群异常探针；以 042 的 typed timestamp companion 列作索引查询，队列探针从超时任务 left join 游标以覆盖缺失游标）
- `server/scripts/pitr_recovery_facts.py`（T38：捕获并逐笔复核 100 条 activation/order/CHARGE/session epoch 恢复事实；仅受控 0600 manifest，绝不输出凭据或客户原始记录）

现有 `server/app/control_routes.py` 仅保留兼容入口并逐步瘦身，不继续承载所有管理业务。

### 3.3 服务端测试

- `server/tests/test_postgres_migrations.py`
- `server/tests/test_sqlite_to_postgres.py`
- `server/tests/test_activation_code_service.py`
- `server/tests/test_activation_code_routes.py`
- `server/tests/test_customer_activation.py`
- `server/tests/test_customer_devices.py`
- `server/tests/test_customer_sessions.py`
- `server/tests/test_customer_fencing.py`
- `server/tests/test_customer_idempotency.py`
- `server/tests/test_customer_recharge.py`
- `server/tests/test_customer_queue_fairness.py`
- `server/tests/test_admin_auth.py`
- `server/tests/test_admin_activation_routes.py`
- `server/tests/test_customer_security.py`
- `server/tests/test_customer_ha_smoke.py`
- `server/tests/test_real_chain_contracts.py`
- `server/tests/test_ops_metrics.py`
- `server/tests/test_ops_alerts.py`
- `server/tests/test_customer_pitr.py`

## 4. 必须新增的前端文件

### 4.1 客户端

- `client/src/customer/ActivationPage.tsx`
- `client/src/customer/LoginPage.tsx`
- `client/src/customer/DevicePairingPage.tsx`
- `client/src/customer/SessionConflictDialog.tsx`
- `client/src/customer/DeviceManagementPage.tsx`
- `client/src/customer/useCustomerSession.ts`
- `client/src/customer/customer-state.ts`

### 4.2 管理端

- `client/src/admin/ActivationCodeBatchesPage.tsx`
- `client/src/admin/ActivationCodesPage.tsx`
- `client/src/admin/DeliveriesPage.tsx`
- `client/src/admin/CustomersPage.tsx`
- `client/src/admin/DevicesPage.tsx`
- `client/src/admin/SessionsPage.tsx`
- `client/src/admin/AuditEventsPage.tsx`
- `client/src/admin/AccountsPage.tsx`
- `client/src/admin/OrdersPage.tsx`
- `client/src/admin/PaymentSettingsSection.tsx`
- `client/src/admin/QueueModeSection.tsx`
- `client/src/admin/ui/vocabulary.ts`
- `client/src/admin/ui/DataTable.tsx`
- `client/src/admin/ui/PageBanner.tsx`
- `client/src/admin/ui/Pagination.tsx`
- `client/src/admin/ui/StatusBadge.tsx`
- `client/src/admin/ui/ConfirmDialog.tsx`

### 4.3 Tauri

- `client/src-tauri/src/customer_credentials.rs`
- `client/src-tauri/tauri.customer.conf.json`
- `scripts/require_customer_api_base.mjs`

### 4.4 前端测试

- 与页面同目录的 `client/src/customer/*.test.tsx`
- 与页面同目录的 `client/src/admin/*.test.tsx`
- 与组件同目录的 `client/src/admin/ui/*.test.tsx`
- `client/src/api.customer.test.ts`
- `client/src/api.admin.test.ts`
- `client/src/e2e_customer_contract.test.tsx`

### 4.5 构建入口与制品（CW-019）

本清单 §4.1—4.4 冻结的是业务组件、API 与测试文件名，未覆盖构建入口层。CW-019
规格「仅做剩余」第 1 条要求在已登记路径实施客户/管理独立入口与构建输出，物理上
必须新增入口文件，故在此追加登记，使本清单继续作为唯一文件映射真源。

- `client/admin.html` — 管理端独立入口 HTML（挂载 `#root`，引用 `admin-main.tsx`）
- `client/src/admin-main.tsx` — 管理端独立挂载文件（只挂载 `AdminApp`，不引用 `RootApp`）
- `client/vite.admin.config.ts` — 管理端独立构建配置（`base: "/admin/"`、`outDir: dist-admin`、`copyPublicDir: false`）
- `client/src/entryContract.test.ts` — 源码级入口合同测试（客户依赖图不认识管理域/内部域的模块引用）
- `scripts/verify_customer_bundle.mjs` — 产物级排除断言（构建后扫 `client/dist` 的禁止文件名与特征串 + `client/dist-admin` 阳性对照 + 两份 SHA-256 清单）

构建输出目录（产物，不入库，已在 `.gitignore` 登记）：`client/dist`（客户；目录名
被 `client/src-tauri/tauri.conf.json` 的 `build.frontendDist: "../dist"` 依赖，
`tauri.customer.conf.json` 无 `build` 键而继承同一值，故不可改名）、
`client/dist-admin`（管理，`base` 为 `/admin/`）。

命名若经 owner 评审要求变更，按指定名称重做，并同步更新本节登记与
`scripts/verify_customer_bundle.mjs` 内的禁止文件名断言。

### 4.6 构建默认翻转为唯一客户（CW-020）

CW-020 规格要求「把现有客户配置和命令变成唯一默认，删除内部 default feature/config
对正式发行的影响」。据此桌面构建默认翻转：`client/src-tauri/tauri.conf.json`（base）
由内部 edition 改为**客户 foundation**（identifier `com.xiangshu.video-replica.customer`、
CSP 仅 HTTPS、`bundle.resources: []`、窗口 `url: "customer"`），`tauri.customer.conf.json`
瘦身为仅承载发行专属的 `installerHooks`（不能进 base，否则内部 overlay 会继承旧内部版
卸载迁移钩子）。内部 edition 按用户决策「保留为显式 opt-in」，物理上必须新增一个
overlay 文件承载，故在此追加登记，使本清单继续作为唯一文件映射真源（循 §4.5 CW-019
先例）。

配套命令合同（`package.json`）：无后缀的 `check:tauri` / `tauri:build` / `tauri:dev`
统一为客户默认；`tauri:build:customer` 保留为 `npm run tauri:build` 别名以维持 CI 与
既有引用连续。

### 4.7 删除桌面本地后端启动与管理资源（CW-021）

CW-021 承接 CW-020 的「内部 edition 退役」：本地 sidecar 启动链自源码与制品两个
层面整体移除，桌面端只剩客户云版一个产品，内部发行不能被有意或无意重建。

删除登记（文件自仓库移除，不再存在）：

- `client/src-tauri/tauri.internal.conf.json` — CW-020 引入的内部 opt-in overlay，
  随内部 edition 退役删除（不再有第四份桌面配置）。
- `client/src-tauri/resources/start-backend.sh`、`start-backend.bat` — 打包进内部
  安装包的本地后端启动器，删除后客户安装包无任何启动脚本可携带。
- `package.json` 的 `check:tauri:internal` / `tauri:build:internal` /
  `tauri:dev:internal` — 内部 opt-in 构建入口随 feature 一起移除。

修改登记：

- `client/src-tauri/src/lib.rs` — 删除 `local-sidecar` feature 门控的全部启动链
  （`BackendProcess`、`LOCAL_API_ADDR`/`127.0.0.1:8000` 端口探测、
  `VIDEO_REPLICA_BOOT_COMMAND` 启动命令查找、`start_local_services` 启动、退出
  杀进程）；凭据与下载 handler 原样保留。
- `client/src-tauri/Cargo.toml` — 删除整个 `[features]` 表（`default = []` 与
  local sidecar feature 一并移除），任何构建都无法再编译回本地后端。
- `.github/workflows/ci.yml` — Windows job 移除内部构建/记录/归档/隔离步骤，仅
  构建客户云安装包；制品检测由「排除本地启动器」扩大为「排除本地后端分发」：
  禁止文件名（启动脚本、`pyvenv.cfg`、`ffmpeg.exe`、`ffprobe.exe`）、禁止扩展名
  （`.db`/`.sqlite`/`.sqlite3`/`.pyd`）、禁止目录（`server/`、`.venv/`、`ffmpeg/`）、
  二进制标记串（`start-backend`、`VIDEO_REPLICA_BOOT_COMMAND`、`127.0.0.1:8000`，
  ASCII+UTF-16 双编码）。
- 守卫测试同步重写：`server/tests/test_build_contracts.py`（launcher 执行用例改为
  「启动器已删除」契约；CI 合同改为仅客户制品 + 扩大检测）、
  `server/tests/test_customer_ha_smoke.py::test_customer_desktop_build_is_the_sole_default_target`
  （内部 overlay/feature/脚本断言翻转为「必须不存在」）、
  `server/tests/test_desktop_artifact_no_pg_dsn.py`（三配置布局改为双配置布局）。
- `resources/ffmpeg/` 仍在仓库但无任何构建引用（客户默认 `resources: []`）；
  其物理清理按 CW-001 P1 决策随 CW-040/042 执行，本任务不动。

## 5. 文件级开发顺序

### 5.1 后端优先

1. 迁移和 PostgreSQL 基础。
2. 激活码批次和首次激活。
3. 设备绑定和 session 状态。
4. 客户认证和幂等层。
5. 管理端控制 API。
6. 公平队列改造。

### 5.2 前端随后

1. 客户激活页。
2. 第二设备配对与冲突页。
3. session 失效和切换提示。
4. 管理端激活码、客户、设备和会话页。

### 5.3 持续并行验证

1. 从第一个行为变更起同步补失败测试、回归测试和文档，不在开发末期集中补齐。
2. 从 T03 起持续执行安全扫描；认证、账务和并发变更在每个任务关闭前独立评审。
3. PostgreSQL 基座可运行后逐步增加多实例 smoke test，T36 再完成类生产拓扑验收。
4. API 变更同提交更新 OpenAPI 和生成类型；E2E 随竖切逐层从 Fake 扩展到 staging/真实链路。

## 6. 需要修改的调用关系

| 现有调用 | 改造后 |
| --- | --- |
| `getCurrentUser()` | 区分内部控制面用户与客户 session 用户 |
| `setInternalAccessToken()` | 客户端不再作为主登录方式 |
| `SESSION_EXPIRED_EVENT` | 客户 session 失效统一事件，名称可保留但语义升级 |
| `getControlAccounts()` | 补充客户激活码、设备、会话、审计查询 |
| `syncControlRechargeOrder()` | 保留给 ZPay 续充和补偿，不处理零额度激活 |
| `acquire_generation_task_lease()` | 改为按 `user_id` 调度，不按全局 FIFO |

## 7. 代码提交粒度建议

每个提交只解决一个闭环，不要把大重构揉在一起：

1. `postgres` 迁移基础。
2. 激活码批次和表结构。
3. 首次激活事务。
4. 设备和会话。
5. 客户前端。
6. 管理前端。
7. 公平队列。
8. 部署与验证。

每个闭环都要能单独回滚、单独测试、单独解释。

## 8. PostgreSQL 运行时迁移清单

本轮扫描到 33 个 `server/app` 文件直接使用 `sqlite3`。下面是生产运行路径迁移范围，不允许只改新客户模块后就宣称 PostgreSQL 完成；最终数量和行号以 T04/DB-02 执行时生成的清单为准。

### 8.1 基础设施

- [ ] `server/pyproject.toml`：经依赖评审后固定 PostgreSQL driver/连接池版本，并更新 lock；不引入 ORM、Redis 或队列框架。
- [ ] `server/app/db.py`：改为 `DATABASE_URL`、连接池、事务上下文、映射行；客户生产拒绝 SQLite。
- [ ] `server/migrations/env.py`：当前发布仅支持经验证的PostgreSQL在线迁移与CI URL；移除默认SQLite入口。离线SQL受现有长revision限制，不交付为可执行升级脚本。
- [ ] `server/app/bootstrap.py`：启动迁移、ready 检查、生产 fail-closed。
- [ ] `server/tests/conftest.py`：增加独立 PostgreSQL 数据库 fixture、事务清理和并发连接。
- [ ] `server/tests/test_postgres_migrations.py`：空库 upgrade/downgrade、约束和索引存在性。

### 8.2 直接 SQLite 依赖移除

- [ ] 认证与控制：`auth.py`、`control_routes.py`、`internal_accounts.py`、`permissions.py`、`rbac_routes.py`。
- [ ] 账务与支付：`internal_billing.py`、`payment_routes.py`、`recharge_routes.py`、`wallet_routes.py`、`zpay_payments.py`。
- [ ] 项目/人物/素材：`analysis.py`、`analysis_routes.py`、`characters.py`、`character_asset_review.py`、`character_identity.py`、`character_image_generation.py`、`character_reference_matching.py`、`first_frames.py`、`first_frame_routes.py`、`media.py`、`project_character_selection.py`、`simple_character.py`、`source_frames.py`、`source_frame_routes.py`。
- [ ] 任务/Worker：`generation.py`、`generation_routes.py`、`generation_worker.py`、`repositories.py`、`script_rewrite.py`。
- [ ] 配置/备份：`settings.py`、`settings_routes.py`、`backup.py`；SQLite backup 只保留为旧数据导入工具，客户生产改用 PostgreSQL 备份/PITR。
- [ ] 消除生产路径中的 `BEGIN IMMEDIATE`、`?` 参数、`INSERT OR IGNORE`、`datetime('now', ...)`、`sqlite3.Row/Error` 等方言依赖。
- [ ] 全量服务端测试在 PostgreSQL 下通过后，才允许客户入口打开。

### 8.3 数据迁移工具

- [ ] 新增 `server/scripts/sqlite_to_postgres.py`：维护窗导入、断点安全、禁止双写。
- [ ] 新增 `server/scripts/reconcile_customer_billing.py`：表行数、主键集合、关键哈希、资产引用、PAID/CHARGE 和钱包重算对账。
- [ ] 新增 `server/tests/test_sqlite_to_postgres.py`：成功、重复执行、部分失败和回滚验证。
- [ ] 更新 `server/app/backup.py` 或拆出 `legacy_sqlite_backup.py`，避免生产误用 SQLite 备份命令。

## 9. 客户域后端清单

### 9.1 激活码与独立充值

- [ ] `activation_code_service.py`：CSPRNG、格式校验、HMAC/key version、掩码、状态机。
- [ ] `activation_code_routes.py`：`POST /api/customer/activate`，公共错误脱敏与限流。
- [ ] `admin_activation_routes.py`：批次、生成、发放、暂停、作废、加密导出和下载审计。
- [ ] `customer_idempotency.py`：operation/scope/key digest、request hash、同键冲突、AEAD 响应恢复、过期密文清理。
- [ ] 首次激活事务创建 user、零余额 wallet、activation、slot 1、session 和审计；正常零额度码不创建 PAID order/CHARGE，历史正额度码仅作兼容。
- [ ] `recharge_orders.provider` 扩展 `activation_code/admin_adjustment`，按 provider 增加形状约束。
- [ ] 任何第二设备、登录、heartbeat、logout、switch 路径均断言不会写 CHARGE。

### 9.2 设备与 session

- [ ] `customer_device_service.py`：两槽、当前绑定部分唯一索引、解绑历史、凭据撤销。
- [ ] `customer_device_routes.py`：enroll、pairing approve、list、delete。
- [ ] `customer_session_service.py`：login、heartbeat、logout、显式 switch、同设备恢复。
- [ ] `customer_session_routes.py`：稳定错误码和幂等响应。
- [ ] `customer_auth.py`：验证 user/code/device/session/token/lease/epoch；只返回最小 `CustomerSessionContext`。
- [ ] 所有客户写路由在业务事务内重新检查 session id/epoch/lease；不得只依赖 FastAPI dependency 的早期检查。
- [ ] 设备撤销、码暂停、用户禁用时原子使当前 session 失效。
- [ ] session event 只追加，旧 logout/heartbeat 不得覆盖新 session。

### 9.3 管理鉴权与审计

- [ ] `control_auth.py`：保留内部 P0 兼容，但客户生产使用真实 per-operator admin session。
- [ ] 新增 `admin_auth_routes.py`，实现 admin session/exchange 和 CSRF；操作员凭证只保存摘要。
- [ ] 将 `control_routes.py` 瘦身并拆为 `admin_activation_routes.py`、`admin_customer_routes.py`、`admin_device_routes.py`、`admin_session_routes.py`、`admin_audit_routes.py`。
- [ ] 管理写操作统一要求 actor、reason、Idempotency-Key、request id 和二次确认字段。
- [ ] auditor 全部只读；普通客户不能访问任何 control 路径。

### 9.4 公平队列

- [ ] 先新增 `.omx/plans/adr-user-fair-queue-v1.md` 或等价 ADR，冻结候选用户和任务领取事务。
- [ ] 新增 `customer_queue.py` 或从 `generation.py` 提取最小调度服务；不重写 Provider 状态机。
- [ ] 用户候选、用户运行数检查、任务租约、`last_dispatched_at` 更新同事务完成。
- [ ] 使用 `FOR UPDATE SKIP LOCKED`；Provider 全局槽位与用户并发分别控制。
- [ ] 保留现有 `SUBMISSION_UNCERTAIN` 防重复付费语义。
- [ ] 增加 10,000 排队任务 explain/锁等待/连接池证据。

## 10. 客户端与 Tauri 清单

### 10.1 React 客户入口

- [ ] `client/src/App.tsx`：把登录 shell 与工作台拆开，删除客户输入内部访问 token 的入口。
- [ ] `client/src/api.ts`：拆分 customer/control API，支持 401/409 结构化错误；逐步降低 2,500 行单文件耦合。
- [ ] `client/src/generated/api.ts`：由最新 OpenAPI 自动生成，增加漂移测试。
- [ ] 新增 `client/src/customer/ActivationPage.tsx`。
- [ ] 新增 `client/src/customer/LoginPage.tsx`。
- [ ] 新增 `client/src/customer/DevicePairingPage.tsx`。
- [ ] 新增 `client/src/customer/SessionConflictDialog.tsx`。
- [ ] 新增 `client/src/customer/DeviceManagementPage.tsx`。
- [ ] 新增 `client/src/customer/useCustomerSession.ts`：30 秒 heartbeat、页面隐藏/恢复、401 清理、租约倒计时。
- [ ] `WalletPanel.tsx`：保留 ZPay 续充，明确不提供第二主激活码入口。
- [ ] `SESSION_EXPIRED_EVENT` 拆分为 expired/replaced/revoked，避免所有 401 都显示同一句话。

### 10.2 Tauri 安全存储

- [ ] `client/src-tauri/src/lib.rs`：客户构建移除自动启动本机业务后端，或仅保留在明确开发 feature。
- [ ] 新增 `client/src-tauri/src/customer_credentials.rs`：设备实例、设备凭据和 session token 的读写删除命令。
- [ ] `client/src-tauri/Cargo.toml/Cargo.lock`：安全存储方案单独依赖审查；不得把 secret 写普通文件。
- [ ] macOS Keychain 和 Windows DPAPI/系统凭据存储测试；实际重启后凭据仍可恢复。
- [ ] JS 侧禁止 LocalStorage、sessionStorage 和持久明文日志。

### 10.3 管理端

- [ ] `AdminApp.tsx` 改为路由/页面容器，而不是继续把全部页面塞进单组件。
- [ ] 新增 `admin/ActivationCodeBatchesPage.tsx`、`ActivationCodesPage.tsx`、`DeliveriesPage.tsx`。
- [ ] 新增 `admin/CustomersPage.tsx`、`DevicesPage.tsx`、`SessionsPage.tsx`、`AuditEventsPage.tsx`。
- [ ] 高风险操作统一确认对话框、原因字段、幂等键和操作结果 request id。
- [ ] CSV/导出做公式注入防护；激活码明文只允许受控一次性密文导出流程。

## 11. 测试清单

### 11.1 服务端

- [ ] `test_activation_code_service.py`：熵、HMAC、状态机、无明文。
- [ ] `test_activation_code_routes.py`：客户激活和管理端激活码 API 合同、权限与错误码。
- [ ] `test_customer_activation.py`：100 并发、全回滚、用户名冲突、响应丢失、零额度无入账、陌生设备强制配对。
- [ ] `test_customer_devices.py`：slot 2 竞态、第三设备、解绑复用、配对审批、enroll 响应丢失。
- [ ] `test_customer_sessions.py`：普通 409、显式 switch、同设备恢复、heartbeat、timeout、迟到 logout。
- [ ] `test_customer_fencing.py`：旧请求已过中间件但事务提交前被切换，必须无业务写入。
- [ ] `test_customer_idempotency.py`：请求哈希、同键恢复、同键异参冲突、密文过期和清理。
- [ ] `test_customer_recharge.py`：ZPay 续充不改变码、设备、session、并发。
- [ ] `test_customer_queue_fairness.py`：A1000/B100/C10、公平轮转、每用户 running<=1、四 Worker 无重复。
- [ ] `test_admin_auth.py`：per-operator session、真实 actor、RBAC、CSRF 和客户生产 fail-closed。
- [ ] `test_admin_activation_routes.py`：批次、发放、二次确认、幂等、审计和 request id。
- [ ] `test_customer_security.py`：枚举、限流、IDOR、CORS/Host、token/码日志脱敏。
- [ ] `test_customer_ha_smoke.py`：双 API、四 Worker、API/Worker 重启和 PG failover。
- [ ] `test_real_chain_contracts.py`：ZPay、COS、Provider 的人工授权证据门，不默认执行真实付费。

### 11.2 客户端/E2E

- [ ] 首次激活成功/失败/重复提交组件测试。
- [ ] 第二设备等待批准、批准、已满和管理员核验组件测试。
- [ ] 登录冲突、切换确认、被踢下线和租约过期组件测试。
- [ ] heartbeat fake timer、页面恢复和网络抖动测试。
- [ ] 管理端批次、发放、解绑、强制下线、审计页面测试。
- [ ] 新增真实浏览器 Playwright E2E；现有 `e2e_contract.test.tsx` 仅是组件合同，不能替代双实例浏览器链路。

## 12. 部署、配置和运维文件

- [ ] `deploy/customer.env.example`：PG、COS、code/device/session/idempotency/admin 密钥、公开 origin；只放占位符。
- [ ] `deploy/nginx/customer.conf.example`：LB upstream、TLS、真实 IP、Host、CORS、客户与管理路径边界。
- [ ] `deploy/systemd/video-replica-api@.service`：无状态 API 模板。
- [ ] `deploy/systemd/video-replica-worker@.service`：多 Worker 模板，不依赖 API service 存活。
- [ ] `deploy/systemd/video-replica-maintenance.service/timer`：幂等密文清理、对账和过期状态维护。
- [ ] `deploy/systemd/video-replica-ops-alerts.service`：单所有者 P1 异常探针。
- [ ] `deploy/systemd/video-replica-ops-alerts.timer`：每分钟触发并保留 fired/resolved 状态。
- [ ] `deploy/systemd/video-replica-pitr-backup.service`：PostgreSQL 物理 base backup 定时服务；客户生产不得安装旧 SQLite timer。
- [ ] `deploy/systemd/video-replica-pitr-backup.timer`：PostgreSQL 物理 base backup 定时器；客户生产不得安装旧 SQLite timer。
- [ ] `deploy/postgres/pitr-preflight.sh`：PG16、WAL archive 和异地可取 WAL 前置校验。
- [ ] `deploy/postgres/pitr-backup.sh`：`pg_basebackup` + manifest 校验 + 异地 base copy 校验。
- [ ] `deploy/postgres/pitr-fetch-wal.sh`：PostgreSQL restore_command 专用 WAL 取回器。
- [ ] `deploy/postgres/pitr-restore-drill.sh`：仅 staging、显式确认的隔离恢复演练。
- [ ] `deploy/postgres/`：备份、恢复演练和迁移窗口脚本；不得把真实凭据提交 Git。
- [ ] `docs/客户版部署与灰度手册.md`：部署顺序、健康检查、迁移、回滚、灰度。
- [ ] `docs/客户版验收证据包模板.md`：自动化、staging、真实链路、生产四类证据。

## 视频直链交付与桌面下载反馈补充（2026-09-03）

按最新用户需求取消成片后期处理；不新增依赖或上传服务。追加账号级列表隐藏表，保留原始生成与财务记录；现有生成/任务/界面文件复用，新增文件登记如下。该补充已达到 `AUTOMATED_VERIFIED`，尚未部署或打包验收：

| 文件 | 职责 |
| --- | --- |
| `client/src-tauri/src/video_downloads.rs` | Windows 保存位置、受控下载授权、真实完成事件与打开文件夹；模块内原生回归测试 |
| `client/src-tauri/capabilities/download-events.json` | 仅本地 main 窗口的事件监听/解除监听权限 |
| `client/src/videoDownload.test.ts` | 下载前选择/取消、真实完成、失败、浏览器降级和清理回归 |
| `docs/evidence/VIDEO-DIRECT-DELIVERY-DOWNLOAD-EVIDENCE.md` | 本轮清理计划、范围、验证与未验证项 |
| `server/migrations/versions/055_customer_batch_visibility.py` | 按账号隐藏客户生成批次，保留后台任务与财务数据；可回滚的新增表 |

## 13. 每次提交的完成检查

- [ ] 先有失败测试或明确的回归锁定。
- [ ] Ruff、format、mypy、服务端相关测试通过。
- [ ] Biome、TypeScript、Vitest、构建通过。
- [ ] 数据迁移有 upgrade/downgrade 或不可逆 ADR 和恢复脚本。
- [ ] 认证、账务、并发变更经过独立专项评审。
- [ ] `git diff` 无调试输出、死代码、真实密钥和无关格式化。
- [ ] OpenAPI、生成类型、配置样例、README/CHANGELOG 同步。
- [ ] 中文 Lore 提交，一个提交只关闭一个任务 ID。

## 14. PostgreSQL 全面统一增量文件映射（2026-09-08）

原表中SQLite/PG表示历史迁移支持情况，不再是未来业务模块双后端要求；已发布revision字节保持，后续业务和数据库测试只针对PG。当前源码仍含兼容分支，以下是新增/修订范围，未标为已实现。

| 工作项 | 现有文件/范围 | 交付与限制 |
| --- | --- | --- |
| CW-053 | server/app、tests、scripts、migrations；scripts；deploy；client/src-tauri | 全量消费者、测试类别和精确历史例外 |
| CW-025/054 | db.py、db_pg.py、db_portable.py、auth.py、main.py、bootstrap.py及业务调用者 | 全环境PG入口、SQL/row/异常/真实批量写入 |
| CW-055 | db_pg.py、db_portable.py、customer_fence.py、auth.py、generation_worker.py及PG测试 | 外层提交权、连接池/异常回收/时间和竞态 |
| CW-056 | server/migrations/env.py与versions；test_postgres_migrations.py；deploy/postgres/migrate.sh | 空PG和受支持旧PG升级、schema/序列；只交付已验证online迁移 |
| CW-057 | server/scripts；gate1_bootstrap.py、gate1_e2e.py、backup.py；scripts；deploy/systemd、postgres | 管理/种子/对账/维护/备份统一PG |
| CW-058 | server/tests内容、资产、版本、工作台、爆款等测试及fixture | TEST-PG真实执行；全部旧断言映射 |
| CW-059 | server/tests账务、支付、成本、生成、独立、口播、图片、拆解、改写、ASR、权限等测试 | TEST-PG多连接及约束/锁/账务/恢复 |
| CW-060 | backup.py、db.py、sqlite_to_postgres.py、reconcile_customer_billing.py、历史迁移/导入测试及打包 | 独立operator工具；封存后原件只读，规范化只写副本 |
| CW-007/043/044/045 | conftest.py、server/pyproject.toml、pg-fixture.sh、package.json、.github/workflows/ci.yml | TEST-*分类、缺PG失败、SQLite业务替代0、历史检查分栏 |
| 本轮新增规范 | docs/PostgreSQL唯一数据库实施与验收规范.md | 新增数据库增量合同，状态仍只在V3账本 |
| 本轮评审与定义 | outputs/customer-cloud-convergence-analysis-2026-09-08/PostgreSQL全面统一文档评审报告.md；详细任务MD/CSV；task-list-validation.json | 文档评审与校验，不计代码/数据迁移验收 |

优先复用上述现有文件和工具，不新建泛化多后端抽象。若实现确需新的测试/legacy模块，先在本节登记确切文件名、消费者、入口/打包边界和责任再创建；不得改写既有revision绕过登记。

## 15. 本地去重V3的定义与文件登记

当前执行定义位于 outputs/customer-cloud-convergence-analysis-2026-09-08/v3/：客户版收敛剩余任务清单与验收完工标准-V3.md、客户版收敛剩余任务清单-V3.csv、60项任务本地实现核查与剔除报告-V3.md、60项任务本地实现核查对照表-V3.csv。机器定义为 task-definitions.json，基线为 baseline.json，校验为 validation.json，分域源码证据与专项日志在 audit/。

本轮只新增/修订文档证据，沿用§14已有业务文件路径，不新增任何应用模块名。57项保留任务直接复用原映射；CW-006登记责任归各项通用完工要求，CW-008/011的剩余测试按§18说明并入对应实现。实施确需新文件时仍先冻结准确路径，再写代码。

### 15.1 CW-012 类型解耦新增叶子模块冻结（2026-09-09）

CW-012（解耦 WorkspaceShell 类型依赖）为纯前端类型/映射重构，确需新增 2 个应用叶子模块。按§15“实施确需新文件时仍先冻结准确路径，再写代码”要求，先在此冻结确切文件名、导出、消费者、入口/打包边界与责任，再落地代码。二者均为纯叶子（无运行时副作用、不回指消费者），不新增数据库/PG/legacy 模块，不涉及§14映射。

| 冻结文件路径 | 导出符号 | 消费者（确切位置） | 入口/打包边界 | 责任 |
| --- | --- | --- | --- | --- |
| client/src/workspace-shell.ts | `WorkspaceShellProps`（type） | App.tsx（WorkspaceShell props 注解）；studio/StudioWorkspace.tsx（`Props` 派生）；studio/LiveWorkspacePanel.tsx（`CustomerAccount`/`CustomerWallet` 派生） | 纯类型叶子，无运行时代码，不进打包产物；仅 import type 自 ./api 与 ./customer/useCustomerSession | 抽出 WorkspaceShell 内联 props，打断 App ↔ studio 的 import type 环；WorkspaceShell 组件本体仍留 App.tsx |
| client/src/customer/customerToCurrentUser.ts | `customerToCurrentUser`（纯函数） | customer/CustomerWorkspace.tsx:304 | 纯函数叶子，仅 import type 自 ../api 与 ./useCustomerSession | 从旧入口 RootApp.tsx 迁出客户身份映射，解除“客户域 → 旧入口”反向依赖；回退链逐字节等价 |

配套测试 client/src/customer/customerToCurrentUser.test.ts（锁定三条 `??` 回退链，迁移前红、建模块后绿）。冻结生效后：App.tsx 不再从 useCustomerSession import `CustomerCredentialStore`/`CustomerSessionRuntime`，RootApp.tsx 不再 import `CurrentUser`/`CustomerProfile`/`CustomerWorkspaceUser` 且不再导出 `customerToCurrentUser`；studio 两文件不再经 `ComponentProps<typeof WorkspaceShell>` 反向派生。

## 2026-09-11 客户版收敛 CW-057 增量文件映射

按 V3 收敛清单 CW-057（统一剩余维护与种子CLI的PG入口）登记。沿用「先登记再创建」规则：本节登记全部新增/改动文件、消费者与边界；`sqlite_to_postgres.py`、`reconcile_customer_billing.py` 归 CW-060 零触碰。状态只在任务清单 §18 更新。

| 范围 | 文件 | 消费者 / 入口边界 / 责任 |
| --- | --- | --- |
| 统一 PG CLI 入口（新增函数，附加式） | `server/app/db_pg.py`（+`CliDatabaseConfigError`、`resolve_cli_pg_dsn`） | 消费者：4 个维护 timer CLI、app.gate1_bootstrap、app.gate1_e2e 及其测试；不被任何路由/Worker 引用。入口：`--database-url` 优先、env 回退走既有 `resolve_database_config`；sqlite://、DB_PATH 残留、错误 scheme 连接前拒绝。责任：PG-01 在 HTTP 外命令面的唯一 DSN 解析点 |
| 维护 timer CLI 统一入口替换 | `server/scripts/purge_idempotency_envelopes.py`、`purge_expired_export_ciphertexts.py`、`purge_stale_rate_limit_counters.py`、`reconcile_dangling_billing_reservations.py` | 消费者：`video-replica-maintenance.timer`（customer.env 注入 DSN）。行为与退出码（缺配置=1、计数输出、--dry-run 只读）不变，仅 DSN 解析块收口 |
| 种子 CLI PG 化 | `server/app/gate1_bootstrap.py` | 入口 `python -m app.gate1_bootstrap`；消费者：gate1_e2e、test_gate1_bootstrap.py。仅接受已迁移 pristine PG 库；advisory lock + 非 pristine 拒绝；摘要 DSN 经 redact_postgres_dsn 脱敏 |
| Gate-1 harness PG 化 | `server/app/gate1_e2e.py` | 入口 `python -m app.gate1_e2e`（npm run test:gate1）；消费者：e2e/gate1 Playwright。进程内 alembic upgrade head 仅作用于 Gate-1 专用库；子进程环境 DATABASE_URL 取代 DB_PATH |
| historical 注册 | `server/app/backup.py`（仅 docstring 横幅） | 零代码改动；声明 internal-P0-only 分类与退役路径（CW-060/040/042），客户 PG 备份指向 deploy/postgres/pitr-* |
| CW-057 专项（新增，TEST-PG/TEST-LOGIC） | `server/tests/test_cw057_cli_pg_entry.py`（新增） | 消费者：pytest；不被应用代码 import。命令×分类矩阵注册表完备性守卫 + 拒绝矩阵（无 PG 环境零文件创建）+ 有效 PG 成功/只读/幂等 + 客户链零 SQLite backup 引用 + CW-060 边界反向钉住 |
| 既有测试 PG 替代 | `server/tests/test_gate1_bootstrap.py`（重写）、`server/tests/test_gate1_e2e.py`（更新） | 旧 SQLite 用例逐条对应 PG 替代，无删测；专用库 `cw057_gate1_seed_test` 已按 CW-054 先例登记进 `pg_test_kit.RECORDED_TEST_DATABASES`（纯增量） |

## 2026-09-11 客户版收敛 CW-060 增量文件映射

按 V3 收敛清单 CW-060（隔离既有历史SQLite工具与兼容测试，PG-09）登记。历史算法（sqlite_to_postgres/reconcile_customer_billing/app.backup/app.db 及其既有测试）零重写零字节改动；本节只登记边界与制品面新增。状态只在任务清单 §18 更新。

| 范围 | 文件 | 消费者 / 入口边界 / 责任 |
| --- | --- | --- |
| operator 制品构建器（新增） | `deploy/operator/build_operator_package.py` | 消费者：发布/数据操作员与 test_cw060_operator_isolation.py；仅标准库，不 import app/scripts。责任：组装 PG-09 注册闭包为可复建制品；manifest 绑定 commit+唯一 Alembic head+文件 SHA-256 |
| 客户镜像排除 | `deploy/customer-git-rollout.sh`（build 段） | 构建上下文移除 `server/app/backup.py`；违禁路径循环 fail-closed；镜像内 `! test -e` ×3 + rglob 实物扫描；运行时 import 检查不受影响（backup.py 零导入方） |
| 隔离契约专项（新增） | `server/tests/test_cw060_operator_isolation.py` | 消费者：pytest；不被应用代码 import。可复建/绑定/排除/消费者注册表/TEST-IMPORT/HISTORY 硬门七类断言；全离线 TEST-LOGIC |
| 登记不裁剪 | `server/app/db.py`、`server/app/backup.py`、`server/scripts/sqlite_to_postgres.py`、`server/scripts/reconcile_customer_billing.py`、`server/migrations/**` | 零字节改动；反向消费者注册表在 test_cw060（9+1 文件）冻结，裁剪清单交 CW-042；制品哈希清单见 docs/evidence/CW060-EVIDENCE.md |

## 2026-09-11 客户版收敛 CW-032 增量文件映射

按 V3 收敛清单 CW-032（补齐可重建后端交付包及配置检查）登记。新增唯一默认交付包目录 deploy/customer/，rollout 改为消费库内 compose；internal-P0/SQLite backup 面单仅排除登记不物理删除（归 CW-040/042）。状态只在任务清单 §18 更新。

| 范围 | 文件 | 消费者 / 入口边界 / 责任 |
| --- | --- | --- |
| 唯一默认部署包（新增） | `deploy/customer/compose.yaml` | 消费者：customer-git-rollout.sh（COMPOSE 默认源）与空白环境 docker compose up；服务集=rollout SERVICES∪{db,migrate}；migrate 唯一 DDL 角色；池预算注释即契约（test_cw032 机器断言） |
| 空白环境首镜像（新增） | `deploy/customer/bootstrap-base-image.sh` | 消费者：空白宿主机操作员/README §1；干净基底+锁定依赖+与 rollout 相同镜像内检查+CW-060 实物扫描 |
| 重建手册（新增） | `deploy/customer/README.md` | 前置、重建序列、首管 provision-empty-customer、角色/池预算表、备份恢复与健康检查、制品哈希清单 |
| rollout 消费登记制品 | `deploy/customer-git-rollout.sh`（COMPOSE 默认源 + compose.sha256） | CUSTOMER_COMPOSE= 兼容旧主机文件；发布备份新增 compose 哈希；SERVICES 与迁移步骤不变 |
| 交付包契约专项（新增） | `server/tests/test_cw032_delivery_package.py` | 消费者：pytest；全离线 TEST-LOGIC。拓扑/DDL 单角色/池预算活值断言/三方法 wiring/封闭清单与排除语句/fail-fast 矩阵落点 |

## 2026-09-12 CW-077 用户新增账号门禁范围

准确新增路径及接口边界见 [CW077-ACCOUNT-GATE.md](evidence/CW077-ACCOUNT-GATE.md) 的“文件与接口冻结”；已在创建业务源文件前登记。用户明确授权公开入口、登录门禁、六位客户密码和统一正式 Logo，旧激活制约束按该增量调整。

## 2026-09-12 用户授权五任务批次

用户明确要求每 5 个 UC 任务作为一批，从最新 main 创建独立 worktree，先开发、再评审、提交 PR 并合并。该批次规则替代本功能“一任务一 PR”的旧执行粒度，质量门及生产操作边界保留。首批 UC-01—05（CW-077 登录闭环）新增批次证据路径 `docs/evidence/UC-BATCH01-ACCOUNT-ACCESS.md`；后续 Token、积分子迁移随所属批次交付，不提前混入首批。
# 2026-09-12 批次 01 用户追加范围

用户要求工作台链接同时支持抖音与小红书。冻结新增迁移文件：
`server/migrations/versions/20260912T1910_xiaohongshu_link_import.py`。
仅扩展链接解析、导入、媒体及已存素材的收藏/可见性平台约束，不扩展发布账号或自动采集平台。


## FIX-W15-20260912 / 第一组 W15 / ADM-08、ADM-09：上海业务日与一致的 CSV 导出

文件映射：`client/src/internalBillingApi.test.ts`、`client/src/AdminApp.test.tsx`、`client/src/AdminApp.tsx`、`client/src/admin/AccountsPage.test.tsx`、`client/src/admin/AccountsPage.tsx`、`client/src/admin/OrdersPage.test.tsx`、`client/src/admin/OrdersPage.tsx`、`client/src/admin/ProfitOverview.tsx`、`client/src/admin/ui/vocabulary.test.ts`、`client/src/admin/ui/vocabulary.ts`、`client/src/api.ts`、`client/src/generated/api.ts`、`server/app/admin_audit_routes.py`、`server/app/admin_customer_routes.py`、`server/app/admin_dates.py`、`server/app/admin_profit_routes.py`、`server/app/control_routes.py`、`server/app/main.py`、`server/tests/test_admin_customer_routes.py`、`server/tests/test_admin_profit_routes.py`、`server/tests/test_internal_admin.py`；无新迁移或服务端测试文件。 [任务证据](evidence/FIX-W15-20260912.md)。

## FIX-W19-20260912 / Worker 启动实例与发布消费者

实现前冻结：新增 `server/app/worker_identity.py`（每次 CLI 启动唯一标识）；`server/app/generation_worker.py` 仅 CLI 标识/日志；`server/app/publish_worker.py` 启动标识、停止领取与信号清理；复用 `server/tests/test_customer_ha_smoke.py`、`server/tests/test_publish_accounts.py`。证据 `docs/evidence/FIX-W19-20260912.md`。不新增迁移、服务端测试文件、部署编排或指标标签；现有 lease_token 围栏保持原语义。

## FIX-W20-20260912 文件映射

server/app/first_frames.py、viral_media.py；server/tests/test_apilio_image_provider.py、test_viral_media.py。无新增测试文件、迁移或API字段。 [任务证据](evidence/FIX-W20-20260912.md)。

## FIX-TESTBASE-20260912 / 独立前置：日期敏感测试夹具

文件映射：`server/tests/test_viral_routes.py`；无新迁移或服务端测试文件。 [任务证据](evidence/FIX-TESTBASE-20260912.md)。

## FIX-W18-20260912 / W18

文件映射：`server/tests/test_storage_cross_instance.py`、`server/app/storage.py`、`server/app/media.py`、`server/app/media_routes.py`、`server/app/materials.py`、`server/app/material_routes.py`、`server/app/character_identity.py`、`server/app/upload_cleanup.py`、`server/tests/test_storage.py`、`server/tests/test_media.py`、`server/tests/test_character_identity_api.py`、`server/tests/test_cw058_content_asset_pg_matrix.py`；新增 app/upload_cleanup.py 为PG维护入口，无新迁移和服务端测试文件。 [任务证据](evidence/FIX-W18-20260912.md)。

## FIX-WALLETSTATUS-20260912 / 钱包提示竞态前置修复

文件边界：`client/src/customer/CustomerWalletPanel.tsx`、`client/src/customer/CustomerWalletPanel.test.tsx`；无 API、服务端、钱包余额、充值结算或数据库改动。[任务证据](evidence/FIX-WALLETSTATUS-20260912.md)。

## FIX-ADM02-20260912 文件映射

server/app/admin_profit_routes.py、server/app/admin_write_contract.py、server/tests/test_admin_profit_routes.py；docs/evidence/FIX-ADM02-20260912.md。无新增测试文件、迁移或公开 DTO。

## FIX-W13-20260912 文件映射

server/app/admin_auth_routes.py；client/src/AdminApp.tsx、AdminApp.test.tsx、api.admin.ts、generated/api.ts；server/tests/pg_test_kit.py 及现有十二个相关测试文件，详见任务证据。无新测试文件、无迁移。

## UC-BATCH-02 / UC-06—10 文件与接口冻结

基线 main@37a2633（前置 PR #79 已 squash 合并、三门禁通过）。用户明确要求前端与后端同批交付，沿用融合设计和正式品牌。
新增文件：`client/src/customer/CustomerCenterPage.tsx`、`client/src/customer/customer-center.css`、`client/src/customer/CustomerCenterPage.test.tsx`、`server/tests/test_customer_center.py`、`server/migrations/versions/20260912T2200_customer_token_lifecycle.py`、`docs/evidence/UC-BATCH02-PERSONAL-CENTER.md`。
修改既有 API key service/routes、customer_fence/auth、钱包查询、StudioWorkspace/MainPages、client API 和现有充值/设备组件；按需要增加 Token 凭据版本、默认标记和账务来源的 PG 字段，更新迁移 manifest、head 矩阵和生成类型。
接口：POST /api/customer/api-keys/default，POST /api/customer/api-keys/{key_id}/rotate；既有 GET/POST/DELETE 管理接口补幂等/默认/分组版本元数据，GET /api/customer/center-summary 返回真实账户积分及消费汇总。密钥管理必须使用账号会话，公开元数据不可包含摘要或历史明文。

UC-BATCH-02 补充冻结：`docs/evidence/design-qa-20260917.md`（根目录、本地设计验收记录，不含测试凭据）。按用户 2026-09-12 最新反馈，取消概览重复页，五页签中仅 Token 管理展示 Token；个人中心不显示设备管理。

## UC-BATCH-03 / UC-11—15 文件与接口冻结

基线 main@791fd66，第二批 PR #86 三门禁全部成功并合并；第三批从该主分支创建独立 worktree。范围：后台配置/前端展示接口价格、积分计价与充值兑换、Token 来源贯通、账号钱包并发扣减、消费记录筛选与统计。新建/轮换不建立钱包，预扣与结算的 Token/user 联合归属在数据库和事务内约束。

新增：server/app/customer_pricing.py、server/app/customer_pricing_routes.py、server/tests/test_customer_pricing.py；client/src/customer/CustomerPricesPage.tsx、client/src/customer/CustomerPricesPage.test.tsx、client/src/admin/CustomerPricingManager.tsx、client/src/admin/CustomerPricingManager.test.tsx；server/migrations/versions/20260912T2330_customer_credit_pricing.py；docs/evidence/UC-BATCH03-POINTS-PRICING.md。

接口：GET /api/customer/pricing；GET/PUT /api/control/settings/customer-pricing；扩展既有报价、受理/预扣、充值和钱包流水契约。价格源必须为服务端配置，不能从上游成本推测客户售价。历史未知 Token 来源不归新默认 Token；已受理任务不跟随新价重算。

## FIX-TESTREADY-20260912 / 组件就绪与草稿测试前置

ACTIVE；Owner Codex（01a094e2-9d59-73e3-8b2d-280723c19e7e）；独立只读 Reviewer review_w13。直接从最新 origin/main@791fd6646288c59a84dbed1f1ec26528e862a06d 创建 fix/test-ready-state-20260912，独立 worktree 位于仓库上级 .worktrees/FIX-TESTREADY-20260912。PR #87 实际失败在两个前端用例，未进入后端步骤；分支、开放PR、claim、UC第二批合并以及第三批/物理清理当前文件范围均已只读核对，无重复维护任务。

实现前冻结：client/src/studio/StudioWorkspace.test.tsx 的口播就绪准备与原业务断言，client/src/AnalysisWorkspace.test.tsx 的 F06/F07 初始化/本地保存契约；先不变更产品文件，若独立诊断确认产品问题再登记边界。既有失败日志在 outputs/remediation-20260912/W19-ci-first-failure.log；不删测试、不取消断言、不通过延长全局超时掩盖原因。真实人工联合调试仍归第二部分。[证据](evidence/FIX-TESTREADY-20260912.md)。


2026-09-13 W15整合续记：W19已正常合并PR #87，当前7ce8502三门禁全部成功，squash e8445c4。上文未合并状态为历史记录。W15合入该已合并主干，四份共享文档分别保留两个任务的事实，不按勾选并集推定完成；业务文件没有文字冲突，整合专项与当前提交CI待记录。

## UC-BATCH-04 / UC-16—20 文件与接口冻结

从最新 origin/main@40e261c0d936df044023b03be0eae5afd73135ae 创建，前置 PR #90 三门禁通过且已合并。复用既有账号钱包、管理员写契约与统一支付结算；企业员工 Token 延期。
新增：server/app/account_admin_routes.py、server/app/account_migration_routes.py、server/app/credit_conversion.py；server/tests/test_account_credit_operations.py、server/tests/test_account_migration.py；client/src/admin/AccountCreditPanel.tsx、client/src/admin/LegacyCreditPolicyManager.tsx 及同名 .test.tsx；client/src/customer/AccountPasswordSetup.tsx 及 .test.tsx；server/migrations/versions/20260913T0630_account_credit_operations.py；docs/evidence/UC-BATCH04-ACCOUNT-CREDIT-OPERATIONS.md、docs/evidence/UC-ACCOUNT-PRELAUNCH-REPORT.md。
修改既有 admin_customer_routes、customer_auth/auth_routes、recharge_routes、wallet DTO、main、SettingsRepository 消费者、前后台账号及支付组件、api.admin/api/generated、迁移 head/manifest/schema 矩阵、离线工具精确兼容及分片清单。接口：GET /api/control/customers/{user_id}/account-summary；GET /api/control/settings/customer-payments；PATCH 同路径 /zpay、/billing；POST /api/control/customers/{user_id}/recharge-orders/{order_no}/reconcile；GET/PUT /api/control/settings/legacy-credit-policy；GET/POST /api/control/customers/{user_id}/credit-conversion；GET/POST /api/customer/account/password。全部写操作按既有身份/CSRF或session fencing、幂等、原因和审计约束。旧账号设置密码保持原 user_id；历史余额转换只执行一次并保留差额账本。

第四批联调补齐既有文件边界：客户详情原生 GET /customers/{user_id}/recharge-orders、/wallet-transactions；CustomerCenterPage 的余额刷新与回归；customer-center.css 充值按钮样式覆盖；并发赠送/支付/Token 扣费专项。主分支已退休测试不恢复，分片以 91 个实际文件重新生成。


## FE-PRELAUNCH-20260913 文件与接口映射

基线最新 main@abc96b7，独立 worktree FE-PRELAUNCH-20260913。新文件随用户授权的审计改进批次登记，复用现有域与依赖，不修改迁移。

- `client/public/platforms/douyin.ico`
- `client/public/platforms/sources.json`
- `client/public/platforms/wechat_channels.ico`
- `client/public/platforms/xiaohongshu.ico`
- `client/src-tauri/Cargo.lock`
- `client/src-tauri/Cargo.toml`
- `client/src-tauri/src/customer_credentials.rs`
- `client/src-tauri/src/lib.rs`
- `client/src-tauri/src/publish_accounts.rs`
- `client/src-tauri/src/publish_identity.js`
- `client/src/RootApp.sessionNotice.test.tsx`
- `client/src/RootApp.tsx`
- `client/src/api.ts`
- `client/src/customer/CustomerCenterPage.test.tsx`
- `client/src/customer/CustomerCenterPage.tsx`
- `client/src/customer/customer-access.css`
- `client/src/customer/useCustomerSession.ts`
- `client/src/generated/api.ts`
- `client/src/publishIdentity.test.ts`
- `client/src/studio/ContentPages.test.tsx`
- `client/src/studio/ContentPages.tsx`
- `client/src/studio/CreationPages.tsx`
- `client/src/studio/LocalPublishAccountsPanel.tsx`
- `client/src/studio/MainPages.test.tsx`
- `client/src/studio/MainPages.tsx`
- `client/src/studio/PlatformLogo.tsx`
- `client/src/studio/StudioWorkspace.tsx`
- `client/src/studio/StudioWorkspace.test.tsx`（完整门发现既有重试测试未等待新报价按钮可用，沿用现有就绪 helper，保留全部幂等/单飞断言）
- `client/src/studio/WorkspaceFeatures.test.tsx`
- `client/src/studio/WorkspaceNotifications.tsx`
- `client/src/studio/WorkspaceSearch.tsx`
- `client/src/studio/content.css`
- `client/src/studio/live.ts`
- `client/src/studio/localPublishAccounts.ts`
- `client/src/studio/types.ts`
- `client/vite.config.ts`
- `scripts/verify_customer_bundle.mjs`（正式品牌/平台图标原文件校验，审核图片排除断言）
- `server/app/simple_character.py`
- `server/app/studio_draft_routes.py`
- `server/app/studio_drafts.py`
- `server/app/studio_routes.py`
- `server/app/studio_search.py`
- `server/tests/test_cw058_content_asset_pg_matrix.py`
- `docs/evidence/FE-PRELAUNCH-20260913.md`；任务、认领及证据账本同 PR 更新。

接口：GET /api/studio/search；GET /api/studio/saved-scripts/{script_id}（含超过最近 50 条的历史文案，仍按用户隔离）；GET /api/studio/notifications；POST /api/studio/notifications/read；扩展 /api/studio/drafts/{kind} 支持 publishing 与 expected_revision；Tauri 本机扫码/列表/验证/取消/解绑/官方页面命令。不承诺自动发布队列或平台统计已交付。

## FE-VIDEO-PREVIEW-20260913 文件冻结（2026-09-13）

新增 `client/src/VideoPreview.tsx`（导出 VideoPreview，所有视频/视频帧入口共用）、`client/src/video-preview.css`（9:16 原比例前景及模糊填充）、`client/src/VideoPreview.test.tsx`（播放/背景/错误/销毁回归）、`docs/evidence/FE-VIDEO-PREVIEW-20260913.md`。组件仅依赖 React 与自己的 CSS，不调用业务 API，不改变视频编码比例。

既有文件边界：client/src/studio/{ui,ContentPages,CreationPages,MainPages,StudioWorkspace}.tsx 及 {content,creation,studio}.css；client/src/{VideoResultStage,TaskRecordsPanel,SourceFrameSelection,FirstFrameSelection}.tsx、legacy-panels.css；相关现有测试；任务账本、认领登记、证据索引。消费者为视频海报、播放器、源帧/首帧与生成/口播预览；人物照片、普通图片素材继续按原图用途展示。

## LOCAL-JOINT-20260913：本地联调与管理操作调整

用户持续联调反馈纳入本任务：品牌统一、自动积分单号、去设备/激活码/在线会话展示、客户详情精简和 ID 复制、经营筛选布局、单输入充值换算、API 成本售价表格、客户供应商信息隐藏、响应式导航与字体、管理员默认支付通道及官方支付标识。新增 H3 多账号轮询，每个账号独立填写并发额度，新账号不预设 10；任务与提交账号持久绑定。文件与验证记录见 [LOCAL-JOINT-20260913](evidence/LOCAL-JOINT-20260913.md)。

本地四服务运行正常；两次增量迁移和重启保留账号、会话、积分余额及凭据。最终 Linux 静态门与前端 1382 项通过。后端最终四片全量 1959 passed / 1 skipped，93 个测试文件全部覆盖，每片独立 PostgreSQL；此前导入与分片登记失败均已修复，原始记录保留。相关 32 项及迁移守卫通过。本地功能已交付，远程 CI 与评审待 PR。Owner 为当前 Codex 任务；Reviewer 待 PR 评审，不冒称独立评审。不改变既有 CW 生产验收结论，不进行真实支付、付费生成或公网发布。

本地实现提交 `cc247c1`；推送被自动审批阻止，待用户明确确认远程发送后创建 Draft PR。未推送、未合并，本地联调服务保持运行。

### 管理端表格、积分单位与支付配置收尾（2026-09-13）

客户表格精简为用户名、客户 ID、注册时间、状态、可用额度、累计消耗、生成情况和操作；删除姓名、冻结和待关注展示。筛选按提交应用，导出使用同一条件，迟到请求不覆盖新结果。成本与售价统一为积分，成本按充值换算展示并保留货币底账；提交校验换算版本，避免旧比例写入。表格正文 14px、辅助标签 13px，窄屏客户表格与其他横向滚动数据表分别适配。

支付默认通道可连同当前商户信息一起保存，空密钥保留；缺少公网 HTTPS 回调域名仅显示部署提示，真实充值下单仍严格检查。支付、客户售价和运行配置弹窗直接确认，自动记录操作说明，失败显示在弹窗内。已在本地浏览器验证保存 ZPay 默认通道成功，没有创建支付订单或调用外部网关。用户当前配置页面和持久化数据保留。

完整静态门及前端 **1390 passed**；后端四片 **1961 passed / 1 skipped**，官方清单覆盖全部 93 个测试文件。0、3 片在中断前完成；后端代码内容核对未变后补跑 1、2 片，均独立 PostgreSQL 和锁文件，中断日志未算作通过。最终日志与代码审查详见 [任务证据](evidence/LOCAL-JOINT-20260913.md) 和 [管理端前后端分析](evidence/LOCAL-JOINT-20260913-ADMIN-REVIEW.md)。

本轮实现提交 `c06dcde`；无新增迁移，api.ts / api.admin.ts 保持不变。仪表盘与经营分析核算来源不一致、八个旧入口模块、列表聚合范围和 CSS 叠加等后续事项已分级记录，不冒称全部清理。状态为本地 AUTOMATED_VERIFIED；远程发送仍待此前的明确授权，未推送、未开 PR、未合并。Owner 当前 Codex 任务，Reviewer 待 PR 独立评审。


### 报告建议第一阶段：核算读取统一（2026-09-13）

用户批准按管理端分析报告逐步修改，本阶段继续 LOCAL-JOINT 原任务与工作树。开工 fetch 后 origin/main 为 f401a0b，开放 PR 列表为空；PR #96 已合并，其他在制人物/视频链接任务不重叠。仪表盘复用经营分析读取层，显式保留历史未关联成本/SETTLE 覆盖和待核对状态；移除闲置设备/配对/激活码统计，成本待配置转向当前逐项价格表。api.admin.ts 仅更新返回类型，无新增迁移。详见 [分析报告](evidence/LOCAL-JOINT-20260913-ADMIN-REVIEW.md) 与 [阶段证据](evidence/LOCAL-JOINT-20260913.md#报告建议第一阶段核算读取统一)。

完整 Linux 静态门与前端 **1391 passed**；后端四片 **1964 passed / 1 skipped**，93 个测试文件全部覆盖。 实现本地提交 `d9c746e`，本阶段为本地 AUTOMATED_VERIFIED。此前远程推送的自动审批拒绝仍未解除，本阶段不推送、不创建 PR。P2 查询、旧模块退役、商户配置和 P3 样式仍为后续阶段。


## LOCAL-JOINT 授权环境同步 2026-09-14

本次仅本地整合；新增迁移文件冻结为 `server/migrations/versions/20260914T0000_local_joint_merge.py`，连接已存在的 shared_viral_media 与 h3_account_pool 两个版本头，不改历史迁移。验证与运行切换记录保存在工作区 `outputs/local-joint-sync-20260914/`。

## VIRAL-COPY-CACHE-20260915 迁移文件登记

| 冻结路径 | 用途 |
| --- | --- |
| `server/migrations/versions/20260915T1600_viral_copy_cache.py` | PostgreSQL `viral_script_cache`；按平台与视频 ID 复用文案结果，生产任务 ID 不设外键，使缓存独立于项目和任务生命周期；父版本 `20260914T0000_local_joint_merge`。 |

## UI-TYPOGRAPHY-BRAND-20260916 评审修复文件边界

沿用既有文件：client/vite.config.ts、client/src/entryContract.test.ts、client/src/studio/{CreationPages,scriptRewrite,types}及对应测试、scripts/verify_customer_bundle.mjs；server/app/{script_rewrite,analysis,analysis_routes,billing_catalog}.py及既有worker矩阵/计费测试。无新业务源码或迁移文件；独立证据在 docs/evidence/ui-typography-brand/review-fixes.md。新增内部服务 analysis_repair_deepseek 保留旧成本历史，不复用旧供应商费率。

## 2026-09-16 PUBLISH-INLINE-QR-20260916

`client/src/studio/LocalPublishAccountsPanel.tsx`（平台入口、筛选与成功回填）、`client/src/studio/MainPages.test.tsx`（用户流程回归）、`client/src-tauri/src/publish_accounts.rs`（扫码窗口隐藏与状态读取）、`client/src-tauri/src/publish_login.js`（后台主动读取与视频号官方框架刷新）、`client/src/publishLogin.test.ts`（脚本回归）、`client/src/studio/publish-accounts.css`（窄屏账号布局）。文档为本映射、任务清单、认领登记、CUSTOMER-TASK-EVIDENCE、发布账号扫码部署与验收及 `docs/evidence/PUBLISH-INLINE-QR-20260916.md`。无新数据库文件、依赖或接口。

## 2026-09-16 PROMPT-OPTIMIZE-20260916 · 文件映射

用户已授权 Codex 接手 Claude 会话 fbeae78e 的既有独立 worktree/分支，原进程已停止且成果已备份；不重复开发。H01—H14 实现与代码自检完成；本地静态门和后端全量后的全部失败项/新增专项通过，H15 已提交 [PR #125](https://github.com/peihr666-max/xiangshu-video-replica-/pull/125) 并触发 CI，当前为 REVIEW 交付状态。改动范围、规格映射、评审与测试见 [H3 实施证据](evidence/PROMPT-OPTIMIZE-20260916-EVIDENCE.md)。AUTOMATED_VERIFIED（本地）；远程三门禁以 PR 当前 head 的 Checks 为准，未宣称独立评审、真实链路、合并或部署完成。

## 2026-09-17 PUBLISH-DELIVERY-20260917 · 文件映射

| 冻结路径 | 用途 |
| --- | --- |
| `server/migrations/versions/20260917T1000_publish_records.py` | `publish_records` 表（jsonb tags/options/stats、timestamptz、lease 三件套、4 索引）与 `publish_browser_accounts.status/error_message/source`；父 `20260916T2000_prompt_optimization_receipts` |
| `server/app/publish_records.py` | 发布记录领域：创建/列表/取消/重试/删除/同步请求/汇总；worker claim（到点、同账号串行、SKIP LOCKED）、过期租约回收、围栏 finalize |
| `server/app/publish_record_routes.py` | `/api/studio/publish/records` 端点，写走 `BusinessDbDep.write()` |
| `server/app/publish_credentials.py` | storage_state → Cookie 头 / 抖音 security-sdk 的纯函数桥接 |
| `server/app/publish_delivery.py` | 素材流式落盘、API 优先投递、浏览器兜底接缝（PR-B 实装）、`VIDEO_REPLICA_PUBLISH_BROWSER_FALLBACK` |
| `server/app/publish_worker.py` | 每轮追加 claim_publish_work → 解密/落盘/投递 → finalize |
| `server/app/publish_browser.py` / `publish_browser_routes.py` | `POST /accounts/import`（桌面导出）、`upsert_browser_account` 复用、账号响应新增 status/source |
| `server/app/publishers/douyin_adapter.py` | `visibility` 透传；接受 dict 形式 security_sdk |
| `server/tests/test_publish_records.py` / `test_publish_credentials.py` / `test_publish_delivery.py` | 新增专项；`test_publish_browser.py` 追加 import 用例；`test_publish_accounts.py` A12 改写；`pg_test_kit.py` 登记 `publish_records_test` |
| `server/tests/test_cw056_supported_head_matrix.py` 等 6 处 head 常量、`server/migrations/manifest.json`、`scripts/ci/test-shards/*.txt` | 链尾与冻结矩阵/分片清单重算 |
| `client/src-tauri/src/publish_accounts.rs` / `lib.rs` | connected 时导出 cookies + localStorage；`export_local_publish_account_state` 命令 |
| `client/src/api.ts` | `/publish/records` 与 summary 包装 |
| `client/src/studio/localPublishAccounts.ts` / `LocalPublishAccountsPanel.tsx` | 服务端账号列表、导入/重试同步、解绑联动删除 |
| `client/src/studio/ContentPages.tsx` / `types.ts` / `content.css` | 发布页立即/定时、服务端账号选择、`scheduledAt` 草稿字段、时区换算助手 |
| `client/src/studio/PublishRecordsPanel.tsx`（+test） | 发布记录面板与轮询 |
| `client/src/studio/MainPages.tsx` | 首页「累计已发布」读 summary |
| `deploy/customer/compose.yaml` / `deploy/customer-git-rollout.sh` / `deploy/customer/bootstrap-base-image.sh` / `deploy/customer/README.md` / `deploy/systemd/video-replica-publish-worker.service` | `worker-publish` 服务、Node.js、import 冒烟、连接预算 |

## PRELAUNCH-REMEDIATION-20260917 文件边界登记

复用 `deploy/customer/compose.yaml`、`README.md`、`bootstrap-base-image.sh`、
`deploy/customer-git-rollout.sh`、`deploy/customer.env.example`；新增文件冻结为
`deploy/postgres/customer-pg_hba.conf`（TLS-only 数据库入口）、
`deploy/customer/healthcheck.py`（容器 /ready 探针，仍走生产入口校验）、
`docs/evidence/PRELAUNCH-REMEDIATION-20260917.md`（独立证据）。
测试追加既有 `server/tests/test_customer_git_rollout.py` 和 `test_cw032_delivery_package.py`；
无需修改分片清单、迁移、CI 门禁或供应商业务。关联功能修复复用 #126/#129/#130。

## WORKSPACE-STRUCTURE-20260917 资料文件映射

用户确认仅整理目录、资料与导航。六版审核包→`docs/design/reviews/`，设计预览→`docs/design/previews/`，原型→`docs/design/prototypes/`，五张布局图→`docs/design/references/`，商务方案→`docs/business/`，已跟踪视觉记录→`docs/evidence/design-qa-20260917.md`，旧 Skill 发行包→`archive/skill-releases/`。新导航文件为 `docs/README.md`、`docs/development/README.md`、`docs/design/README.md`、`archive/README.md`、`video-replica.code-workspace`，独立证据为 `docs/evidence/WORKSPACE-STRUCTURE-20260917.md`。业务源码、迁移和运行入口不调整；旧资料路径以本段最新映射为准。

## REPLICA-SIMPLE-FLOW-20260917 文件边界

复用 `client/src/studio/CreationPages.tsx`、`ReplicaPreparation.tsx`、`StudioWorkspace.tsx`、`PromptEditor.tsx`、`types.ts`、`creation.css` 及 `client/src/legacy-panels.css`、`client/src/useGenerationDrafts.ts`、`client/src/CharacterSelection.tsx`、`SourceFrameSelection.tsx`、`FirstFrameSelection.tsx`；相邻测试覆盖三步、历史入口、单场景参考、开场帧、提示词与费用保护。后端复用 `server/app/analysis.py`、`analysis_routes.py`、`source_frames.py`、`prompt_rules/analysis.txt`、`first_frames.py`、`h3_prompts.py`、`generation.py`。新增测试 `server/tests/test_analysis_scene_boundaries.py`、`test_batch_titles.py` 并登记 `scripts/ci/test-shards/shard-0.txt`。无新依赖、数据库迁移或付费供应商调用。证据：`docs/evidence/REPLICA-SIMPLE-FLOW-20260917.md`。

2026-09-17 用户澄清增量：`client/src/AnalysisWorkspace.tsx` 内原有完整表格抽取为共享 `client/src/ShotCardEditor.tsx`，旧页与整合页共同使用，保持全部字段与 motion 编辑能力。`ReviewWorkspace.tsx` 仅补充 DEV 示例项目以在完整桌面导航中预览；不新增生产入口。

全量门兼容增量：`client/src/GenerationComposer.test.tsx`、`client/src/ProjectDetailFlow.test.tsx` 更新共享控件精简文案后的集成断言；`client/src/AppGenerationFlow.test.tsx` 更新统一入口后的 App 集成断言，保留文案确认、首帧生成、费用与幂等业务覆盖；不改变这些旧入口的业务实现。

客户入口统一增量：`client/src/studio/LiveWorkspacePanel.tsx` 与对应测试、`StudioWorkspace.tsx` 与对应测试；历史项目入口只保留项目列表并导入统一 replica，移除客户挂载点中的两套旧工作区；内部 `App.tsx` 的兼容消费者不删除。导入失败保留列表和草稿、关闭/切项目后的迟到响应不跳回旧页。

## BRAND-IDENTITY-20260917 文件边界

- 新增 `client/src/BrandIdentity.tsx`、`client/src/brand-identity.css`：统一主工作台、创作页和用户中心的 Logo 与公司/软件文字。
- 修改 `client/src/studio/StudioWorkspace.tsx`（仅品牌渲染）、`studio.css`（仅品牌尺寸/窄侧栏规则）、`client/src/customer/CustomerCenterPage.tsx`、`customer-center.css` 及现有专项测试。
- 更新 `client/src-tauri/app-icon.svg` 与 `icons/` 顶层桌面图标；保留安装产品名、标识符与数据路径。
- 独立证据 `docs/evidence/BRAND-IDENTITY-20260917.md` 与 `docs/evidence/brand-identity-20260917/`；账本只追加本任务状态。
