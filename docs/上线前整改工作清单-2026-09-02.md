# 上线前整改工作清单（2026-09-02）

> 来源：2026-09-02 全仓综合评估（架构 / 业务健壮性 / 代码质量 / 测试与证据链 / 部署就绪）。
> 整改分支：`fix/pre-launch-remediation`（基于 `fix/0.1.12-activation-device-flow` 收口提交 `6989e51` + `29034e9`）。
> 并发排查结论：`codex/admin-redesign-release`、`codex/customer-admin-production-session`、`codex/release-async-image-workpackages` 对 main 均无独有提交（陈旧指针）；本地 `fix/0.1.12-first-frame-status` 与 0.1.12 基线同提交。整改期间工作区出现的 0.1.12 配套文件（CHANGELOG / generated/api.ts / App.test.tsx / 激活码文档）已随基线一并提交，无未合并的并行改动。
> 状态标记：`[x]` 已完成；`[~]` 进行中；`[ ]` 未开始；`[EXT]` 需外部资源/授权，代码侧无法关闭。

## P0 —— 阻断上线

| 状态 | # | 事项 | 类型 | 说明 / 出口 |
| --- | --- | --- | --- | --- |
| [x] | P0-1 | 配对自动轮询 × 激活限流冲突 | 代码 | 客户端每 3s 新幂等键 POST enroll，共享激活预算（5 次/码/300s），约 15s 必 429。修复：服务端对"已存在活跃配对"的轮询免预算（复用 `lookup_active_pairing`，置于限流之前）；客户端幂等键在等待期内保持稳定（丢失的 201 可重放）、到过期时间停止轮询、连续失败退避。回归测试：轮询不耗预算 + 陌生指纹仍受限。 |
| [x] | P0-2 | 收口 0.1.12 未提交工作区 | 流程 | 20 个修改文件 + 迁移 053 + 运行中出现的 4 个配套文件已分两笔提交（`6989e51`、`29034e9`）；专项测试 138+12 通过，ruff/mypy/tsc 干净。注意：**部署时迁移必须先于代码生效**（列表 SQL 依赖 `archived_at` 列）。 |
| [x] | P0-3 | L1 迟到 Worker 终态写 supersession 守卫 | 代码 | M4M5 评审 L1（接受延期、要求不得静默遗忘）：worker 终态写（SUCCEEDED/FAILED）补 `AND superseded_by_task_id IS NULL` 守卫，rowcount=0 视为被顶替、跳过结算；对齐 reconcile 入口（generation.py:2172）写法；回归测试锁定。 |
| [x] | P0-4 | 资金闭环：悬挂预约对账调度 + 退款 SOP | 代码+流程 | 复核更正：`reconcile_dangling_billing_reservations` 已由 b644656 接入 `video-replica-maintenance.service`（每日 04:10 timer，Persistent），原评估"无调度"过时。本次补齐：维护失败告警文案从"仅密文清理"更正为覆盖全部四项清扫与重跑清单；部署手册新增 §10 资金异常处置与退款 SOP（BILL-04 冻结前人工单例：终态悬挂 RESERVE 自动对账、退款走 T23 审计调账 + ZPay 商户后台人工退付、事后 reconcile_customer_billing 差额为零）。 |
| [EXT] | P0-5 | T40 真实链路验收（ZPay/COS/H3/图像 Provider，同 SHA 联合竖切 EXT-03~06） | 授权 | 需真实付费账号与人工授权；未完成不得开放真实流量。 |
| [EXT] | P0-6 | staging 三件套（T36 真实拓扑 / T38 PITR 真实恢复演练 / T39 故障注入） | 环境 | 需目标服务器/LB/PG HA/COS；仓库侧合同已 AUTOMATED_VERIFIED。 |
| [EXT] | P0-7 | DESK-04 Windows 代码签名证书采购 | 采购 | 无签名只能标 `UNSIGNED_INTERNAL_TEST`，Gate D 无法通过。 |
| [EXT] | P0-8 | LEGAL-01 对外销售合规（用户协议/隐私政策/AI 深度合成标识/备案） | 法务 | 公网客户注册前置。 |

## P1 —— 灰度前应闭环

| 状态 | # | 事项 | 类型 | 说明 / 出口 |
| --- | --- | --- | --- | --- |
| [x] | P1-1 | `SESSION_LEASE_SECONDS` 双定义合并 | 代码 | `activation_code_routes.py:117` 与 `customer_session_service.py:74` 各定义 90；统一为单一常量源，防激活租约与心跳租约漂移。 |
| [x] | P1-2 | 配对轮询无过期/退避/停止 | 代码 | 随 P0-1 一并修复（到 `expires_at` 停止并提示重新提交；连续失败指数退避封顶 30s）。 |
| [x] | P1-3 | SessionConflictDialog 全英文 + 无 Esc/焦点管理 | 代码 | 客户可见界面中文化；Esc 关闭、初始焦点、`aria-modal`；更新测试。 |
| [x] | P1-4 | 高风险页面零测试（SettingsPanel/AuditEventsPage/SessionsPage） | 代码 | 补最小回归测试（渲染/加载/提交/错误态）。 |
| [x] | P1-5 | 仓库卫生 | 代码 | `.gitignore` 补 `artifacts/`、`.release/`、`.gstack/`、`.tmp-tools/`、`design-qa.md`、`settings-hotfix-*.tar.gz`；移除 `dist/` 下遗留的旧 Skill 打包跟踪产物。 |
| [x] | P1-6 | AGENTS.md 用例数滞后 + CI-only 检查未注记 | 文档 | 同步为 1285 测试函数（账本口径 1446 通过）并注明 cargo test / npm audit / customer E2E 仅 CI 兜底。 |
| [EXT] | P1-7 | 外部 P1 告警接收器、集中日志、PG 慢查询/备份指标 | 环境 | OPS-02 红线：只有本地 journal 无外部告警时不得开放灰度。 |
| [EXT] | P1-8 | CS-01 客服工单 + BILL-04 退款开票税务流程 | 流程 | 10→100 码扩量前置。 |
| [登记] | P1-9 | R1：COS `get_object`+ffprobe 在 fenced 事务内占连接池 | 评估 | 见本文件"评估登记"节。 |
| [登记] | P1-10 | Tauri 孤儿后端进程 / 无版本协商 / 无更新器 | 评估 | 见本文件"评估登记"节。 |

## P2 —— 技术债（不阻断上线）

| 状态 | # | 事项 | 类型 | 说明 |
| --- | --- | --- | --- | --- |
| [x] | P2-1 | `repositories.py` 死代码删除 | 代码 | `GenerationTaskRepository` 状态机词汇过时（`RETRY_READY` 已不存在）、纯 SQLite 语法、生产零引用；连同 test_db.py 引用一并删除。 |
| [x] | P2-2 | `_fair_queue_enabled` 字符串异常分类脆弱 | 代码 | 改为按 `psycopg.errors.UndefinedColumn` / `sqlite3.OperationalError` 精确分类，保留原文案兜底。 |
| [登记] | P2-3 | 巨型文件拆分（generation.py 6671 / api.ts 4187 / TaskRecordsPanel 1742 等） | 登记 | 上线冻结期内不做大重构（回归风险 > 收益），列入发布后第一优先重构批。 |
| [登记] | P2-4 | worker SQLite/PG 双骨架、三份 HMAC 工具拷贝、`_token_digests` 跨模块私有导入 | 登记 | 同上，发布后合并。 |
| [登记] | P2-5 | CSP `connect-src https:` 通配、Biome type-checked、归档码管理视图、超时回收三连 UPDATE 节流、任务 attempt 全局上限、`locked_until` 时钟源统一 | 登记 | 逐项登记于"评估登记"节。 |

## 评估登记（本轮评估后处置结论）

1. **R1（COS I/O 在 fenced 事务内）**：业务流程分析报告定级 P2 可用性。复核确认媒体预检发生在客户 fenced 写事务内（连接池占用上限 ~8，50MB 下载 + ffprobe 可达数十秒）。整改涉及事务边界重排（预检移出围栏、写回时二次校验），属高风险重构；按"上线冻结期不大动"原则登记延后，缓解措施：池上限 64 ceiling + 超时 30s 兜底、后续单独任务实施。
2. **Tauri 孤儿进程/版本协商/无更新器**：内部版 `start-backend.bat` 拉起的 API/Worker 在应用退出后残留（客户云构建无 sidecar，不受影响）。修复需重排进程树管理（PID 跟踪 + Job 对象），无法在 CI 内验证运行时行为；登记延后至 DESK-03 实机任务一并处理。缓解：安装器钩子升级时清理旧版；`/health` 5s 徽章轮询可暴露异常。无自动更新器 → 发布流程明确"手动发版 + NSIS 升级钩子防双装"（见 T45 发布说明）。
3. **`locked_until` 应用时钟 vs DB 时钟混用**：租约时长受主机漂移影响（≤120s 步进、600s 上限内偏差有限，且过期判定统一 DB 时钟）。改动触及公平队列热路径（10k 压测基准），冻结期内登记延后。
4. **任务级 attempt 全局上限**：现行语义为 provider-failed 终态兜底 + 归档重试上限 5；产品语义需确认后再加上限，登记待产品输入。

## 验证口径

- 每项修复：先失败测试（红）→ 实现（绿）→ 专项复验。
- 本机 Docker Desktop 无法启动（宿主机缺 VirtualMachinePlatform，T45 已知），PG 专属套件本地按 skip；本清单服务端改动以 SQLite 可跑专项 + ruff/mypy 验证，PG 全量留 CI 三门禁兜底。
- 收尾跑 `npm run check`（唯一一次全量）。
