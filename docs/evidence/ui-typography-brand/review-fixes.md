# 本地评审问题修复与复验

日期：2026-09-16。延续 `UI-TYPOGRAPHY-BRAND-20260916` 的独立工作树与分支，基于本轮评审提交 `ea022f0` 修复。用户已授权按评审方案处理。本文更新本目录 `local-review-ea022f0.md` 的未修复状态，历史发现及失败日志保留。

## 修复结果

1. **Logo 打包遗漏**：客户构建钩子复制 `studio/logo-mark.svg`；真实构建钩子测试检查侧栏图形和三种 favicon；生产产物校验脚本新增非空品牌资源断言。仅运行钩子与契约，未将其冒称完整生产构建或安装验收。
2. **文案任务恢复**：按账号/来源作用域合并云端与 session pending；同请求保留已受理任务编号，不允许较旧状态倒退。不同请求用开始时间区分，避免恢复旧任务。云端记录携带请求幂等键，跨设备/会话重试复用同键及原始结果基线；明确不可重试的终态失败清理 pending，修好配置后可重新生成；可重试和不确定结果保留同键。迟到响应受操作编号与作用域保护，人工编辑继续保留为主稿。
3. **长文总超时与租约**：使用项目已有 curl_cffi 非流式传输的总时限240秒，关闭重定向；持续空行保活也不能延长总时限。提交前校验 worker、attempt、状态、租约未过期且尚未发送，重新给予五分钟租约，保留结算窗口。过期租约不能复活，重复提交不能再次调用；网络未知结果仍进入 SUBMISSION_UNCERTAIN，不自动重发。这采用“有界总时限+提交前续期”，无需在请求内无限续期。
4. **纯文本模型统一**：现有生产纯文本调用（文案二创、视频拆解 JSON 修复）统一 DeepSeek。修复专用 JSON 指令、temperature=0、JSON对象输出，随后继续通过原结构校验。视频视觉分析、图片质检保留视觉模型；模板式提示词编译不属于 AI 调用，未宣称新建标题/分析功能入口。

## 配置与成本兼容

- 实际视频分析 provider 在提交前取得 Apilio 与 DeepSeek 两组配置快照，不把数据库连接带入网络等待。仅配置 Apilio 的环境须补齐 DeepSeek 后再提交新分析，避免付费分析后才发现修复通道不可用。未配置时明确报错，不回退其他模型。
- 新内部计量项目 `analysis_repair_deepseek` 不向用户新增收费；旧 `analysis_repair` 及历史供应商成本快照保留。新项目不套用旧 Apilio 单价，未设置实际成本时标记未知，不能当作零成本。
- 完整响应即使截断/格式异常仍记录供应商调用；网络不确定保留未知用量；401/403等配置错误标记不可重试，429/5xx按可重试处理，响应错误不包含密钥或原始请求。
- 文案字数1–5000与8192 tokens输出上限保持不变，仍是近似目标。未进行真实付费5000字生成。

## 测试与独立评审

- 修复前：Logo钩子及旧云端pending恢复两项回归均失败，见 `fix-regressions-red.log`。
- 最终前端：Biome、TypeScript、103文件 **1600 passed**，见 `fix-frontend-check-final.log`。存在原有CSS警告和jsdom scrollTo提示，无失败。
- 后端相关三文件：worker任务矩阵、使用计费、业务前置检查，**148 passed**，见 `fix-backend-regression.log`。
- 后续评审补强的未提交租约过期用例及构建契约：**2 passed / 94 deselected**，见 `fix-final-contracts.log`。这包含重复验证，不与148简单相加为独立用例总数。
- 持续保活测试使用本地HTTP服务、缩短总时限，确认及时终止；供应商测试均为模拟，无付费调用。
- Ruff通过；受影响后端/应用格式144文件通过；mypy151文件通过；e2e lint与秘密扫描通过。Rust源码未变，沿用本任务前轮静态检查，本轮未重复全仓pytest。
- 初轮后端增量测试暴露测试列名、异常导入兼容等问题，修正后通过，原失败日志保留。日志中的测试凭据字面量已缩短脱敏，仅为假数据。
- 独立 Reviewer `review_copy_ip` 复审通过。复审指出的跨设备幂等键丢失、不可重试终态记录未清理、租约测试盲点均已修复并补验。Reviewer只读审查，没有冒称自行运行测试。

## 交付边界

代码层四项问题均已修复并通过本地相关自动化验证。未推送、未合并、未部署、未更新已安装桌面软件；Windows NSIS、正式生产构建和远程CI仍待执行，不宣称发布验收完成。此前远程操作授权/凭据阻碍未在本轮绕过。

测试资源仅本任务 `ui-ip-fix-*`，全部使用 `--rm`，未创建命名卷或新镜像；结束后停止并检查无本任务容器残留。原5206预览服务保留。
