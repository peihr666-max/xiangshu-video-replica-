# FIX-W20-20260912 — 远程图片 DNS 与连接地址一致

任务/工作包：第一组 W20；原评估 BE-R03。

Owner / Reviewer：Codex（01a094e2-9d59-73e3-8b2d-280723c19e7e）/ 独立子代理 review_w13（复审通过）。

分支 / 基线 SHA：fix/w20-image-dns-pin-20260912；origin/main 55220f7e5a84b06e748f98324bfed141f2e6fb94。独立 worktree 位于仓库上级 `.worktrees/FIX-W20-20260912`，从主分支创建。

上游规格段落：上线前评估 BE-R03；去重后第一部分 W20。实际供应商和部署网络联合调试属于第二部分。

改动文件：server/app/first_frames.py、viral_media.py；server/tests/test_apilio_image_provider.py、test_viral_media.py；本任务登记及证据。

失败测试或回归锁定：新回归先证明旧 GET 仍进入 urllib 域名连接路径，且凭据与非标准端口未被提前拒绝。受控 DNS/连接测试覆盖域名只解析一次、私网与混合结果拒绝、peer 不符拒绝、原主机名 Host/SNI、重定向拒绝、声明及实际字节上限、响应和 socket 关闭。独立评审指出首地址不可达的兼容差额，补充同次已验证地址间连接回退及统一连接超时预算。

实现结果：图片 GET 固定到校验通过的 IP，直接连接不读取环境 HTTP 代理，HTTPS 使用原域名 Host/SNI/证书验证。全部候选先校验，连接失败仅在本次已验证候选中尝试；peer 不符立即拒绝。GET 不跟随重定向，保持 20 MiB 上限；共享 peer 检查在 getpeername 抛错时也关闭 socket。供应商 POST 调用保持原接口。

验证命令与通过数：Linux/Python3.12 受控传输专项 test_apilio_image_provider.py + test_viral_media.py：49 passed（W20-reviewed-green.log）；两模块 mypy 与 ruff 通过。无真实 DNS、供应商请求或费用。完整本地质量门与 PR CI 待完成。

证据层级：CODE_PRESENT + 专项通过，完整 AUTOMATED_VERIFIED 待质量门。

安全与可观测性：不透传供应商认证头到图片 GET，不记录带签名的地址。保留已有严格限定的 Apilio 输出主机代理假 IP 兼容规则，未增加其他非公网放行；此部署兼容策略仍需第二部分核对。连接错误保持可重试语义，地址校验错误拒绝。

迁移与回滚：无数据库迁移、公开 API 或依赖变更；回滚本任务代码会恢复 DNS 检查与实际连接分离的问题。

外部授权记录：用户授权分组开发、独立 worktree、独立子代理评审、提交 PR 与合并。未调用真实供应商、发布或修改生产网络。

未测试项：第二部分真实供应商图片地址、部署 DNS/代理与证书网络联合调试。

Lore 提交 SHA：不适用；以 Git 提交和 PR 记录为准。

## 独立评审

首轮一项 P2（多地址回退）已修复并补充回归，独立复审通过，无未解决必须修复项；最终质量门待回填。

## 最终完整门禁复验

完整本地质量门通过：服务端 2858 passed、1 原有 TLS 场景跳过；前端 1344 passed；secret、Biome、TypeScript、e2e lint、Tauri fmt/check、ruff、format、mypy 均通过。服务端四个独占 PG16 分片分别为 794、616、674、774 passed，各退出码 0，覆盖清单检查通过。最终日志为 `W20-final-shard-0.log` 至 `-3.log` 与 `W20-static-complete.log`，保存在工作区 `outputs/remediation-20260912/`。PG 使用临时内存盘，默认同步与事务配置保持开启，不是生产容量或持久性验收。

先前中断、环境或旧夹具失败记录保留，不记为通过；当前证据层级为 AUTOMATED_VERIFIED（本地）。独立评审通过，PR 当前 SHA 的 CI 和正常 squash 合并仍待完成；所有人工联合调试留第二部分。

主干整合复验：完整门在 main@8ae7305 基线上通过，之后合入账号主干 37a2633。独立只读评审确认小红书音视频分派仍经过安全下载入口，DNS 快照、地址回退、peer 校验和异常关闭均保留；当前整合代码的图片供应商、首帧及媒体下载三个文件 89 passed，ruff/format/mypy 通过。日志为 W20-main-verified.log、W20-main-backend-static.log。首次专项因临时父目录未重建失败，修正验证环境后全部通过；生产代码未为此改变。
