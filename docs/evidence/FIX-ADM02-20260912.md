# FIX-ADM02-20260912 — 每日售价幂等重放

任务/工作包：第一组 W12；原评估 ADM-02。

Owner / Reviewer：Codex（01a094e2-9d59-73e3-8b2d-280723c19e7e）/ 独立只读评审 review_w12：PASS，无阻塞问题。

分支 / 基线 SHA：fix/adm02-daily-price-replay-20260912；origin/main 55220f7e5a84b06e748f98324bfed141f2e6fb94。独立 worktree 为仓库上级 `.worktrees/FIX-ADM02-20260912`，没有从其他功能分支派生。

上游规格段落：上线前综合评估 ADM-02；去重后第一部分 W12；账号方案 UC-11 负责积分定价，本任务只修已有每日售价接口。

改动文件：server/app/admin_profit_routes.py、server/app/admin_write_contract.py、server/tests/test_admin_profit_routes.py，以及本任务登记和证据。

失败测试或回归锁定：原实现同键重放对列表调用 get 而报错；新增真实路由和隔离 PostgreSQL 回归先失败。历史列表快照兼容、相同请求的 request_id、异内容冲突及审计不重复均由专项覆盖。

实现结果：新快照内部保存 prices 和 request_id，公开响应仍为原列表；重放层允许读取历史裸列表，路由兼容两种快照。同键同内容重放新快照时保留原 request_id，异内容仍返回 409。历史列表未保存原 request_id，不能凭空恢复，只保证结果兼容与不重复写入。

验证命令与通过数：独立 Linux/Python 3.12/PG16 上相关后台专项 68 passed（日志 outputs/remediation-20260912/ADM02-green-isolated.log）。测试夹具仅清理自己的独立数据库限流计数，避免跨用例登录次数污染；生产限流未修改。完整本地质量门和 PR CI 待完成，不能以专项替代。

证据层级：AUTOMATED_VERIFIED（本地完整质量门）；当前 PR CI 和合并待完成。人工联调、生产验收不在此层级内。

安全与可观测性：幂等键和请求体冲突规则不放宽；审计仍只发生一次；保留请求关联标识；无真实凭据。

迁移与回滚：无 schema 或公开 DTO 变更；旧列表与新对象快照均兼容。可回退本次代码，但回退至有缺陷旧版本会重新暴露重放问题，不建议作为业务恢复方式。

外部授权记录：用户 2026-09-12 要求每五任务一组开发、评审、提交 PR、合并，并逐任务独立 worktree；未执行真实支付或生产发布。

未测试项：用户团队第二部分的真实前后台联合调试；生产环境。

Lore 提交 SHA：不适用（本仓库使用 Git；以本证据所在 PR 的提交记录为准）。

## 代码复核记录

已核对公共列表返回形状、历史快照、新快照请求标识、事务提交边界和冲突分支。此次修复不改写其他后台域的响应结构；既有请求指纹继续包含方法、路由参数和请求体。独立评审 PASS，当前 PR CI 待完成。

## 最终本地质量门

已合入最新主分支 `8ae7305`（前置测试修复 PR #78），仅追加的共享账本逐项保留双方记录。当前代码完整静态门通过：前端 82 文件 1344 passed，TypeScript、Biome、e2e lint、Tauri、ruff、format、mypy 全通过。服务端四个独占 PG16 分片分别为 794、616、676、761 passed，合计 2847 passed、1 原有生产 TLS 场景跳过；覆盖检查和全部退出码均为 0。日志为工作区 `outputs/remediation-20260912/W12-static-final.log` 及 `W12-final-shard-0.log` 至 `W12-final-shard-3.log`。

此前缺少 FFmpeg、测试用户名与密码重复引起的脱敏断言失败、磁盘/网络异常及固定日期夹具失效均按原因处理；旧失败日志保留、不记作通过，以上结果来自更新主分支后的完整重新执行。测试 PG 使用临时内存盘且同步配置保持默认，不能替代生产持久性或灾备验收。跳过的 TLS 场景由现有配置测试覆盖，不把跳过计为通过。
