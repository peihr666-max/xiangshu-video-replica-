# 口播分身与独立声音档案：设计验收

final result: passed

## 对照来源与环境

- 用户选定视觉稿：`C:/Users/Administrator/.codex/generated_images/01a0aa1d-6cdc-7b33-881d-ff4df056f284/exec-312a80f5-117d-42ad-bd8a-dc0b4d77aee4.png`，1668 × 944 px。原稿只包含人物详情页的标签和口播分身区域，没有应用侧栏/人物头部。
- 本地实现：`http://127.0.0.1:5216/review/v1.4#studio/person-avatars?person=zhang`。
- 全景截图：`docs/evidence/oral-video-tts/avatar-desktop.png`；焦点截图：`docs/evidence/oral-video-tts/avatar-region.png`，1598 × 845 px。
- CSS viewport：桌面 1920 × 1080，窄屏 390 × 844；另检查默认窗口 606 × 706。检查结束后已恢复默认 viewport。
- 对照状态：张工、两个可用视频分身、一个创建入口。声音区域按已有测试数据呈现“已确认声音”，同时检查王经理的无声音状态，与原稿的“尚未配置”对应。
- 密度归一：源图是局部视觉稿，没有 CSS/DPR 元数据。实现中区域宽 1558.4 CSS px，标题 30px/40.5px，应用字体为 PingFang SC / Microsoft YaHei / system-ui。浏览器截图服务对部分宽屏捕获约作 0.8 倍内容缩放并保留画布空白；对比采用实际内容区域的宽度归一，不把捕获留白视为应用布局，也不宣称逐像素一致。截图没有后期绘制或修改。
- 源稿与最终局部截图已在同一工具输入中以原始分辨率打开对照；全景、上传弹窗、声音列表、口播创作页也分别打开检查。

## Findings 与修正记录

1. **[P2，已修复] 标题与说明层级偏小。** 早期 `.studio-shell h2` 覆盖局部标题，分身页标题仅 20px。调整为局部 `.studio-shell .oral-library-heading h2` 30px，指南标题 20px、条目标题 16px、正文 14px，避免修改其他任务的全局排版。后验：`avatar-desktop.png`、`avatar-region.png`。
2. **[P2，已修复] 原有照片来源和新视频方案冲突。** 分身库、创建弹窗和创作选择器现只提供真人视频分身；形象照片页保留人物置换操作。后验：分身弹窗截图与自动化回归。
3. **[P2，已修复] 声音弹窗带入旧完整口播音频。** 当前草稿的通用 audioId 曾使 WAV/未验证用途素材显示为克隆样本。现在仅接受含 `voice_clone` 用途的素材；新增回归断言未知用途和 `oral_audio` 不可带入。后验：`voice-create-dialog.png` 显示“请先选择声音样本”。
4. **[P2，已修复] 上传或授权请求返回时人物上下文已改变。** 上传取消、弹窗关闭、人物切换和卸载均有操作标记/AbortSignal；迟到的授权响应不能继续创建错误人物分身。声音流程同样隔离。由相关单元测试验证，未为截图调用实际供应商。

当前没有未解决的 P0/P1/P2 视觉或核心交互问题。

## 五项视觉核对

| 项目 | 结果与边界 |
| --- | --- |
| 字体与排版 | 保留产品既有中文无衬线字体及品牌字体；标题、指南、表单、次要文字层级区分。桌面及窄屏未发现不可读截断。共享标签样式与原稿下划线不同，保留应用统一控件作为实现约束。 |
| 间距与布局 | 左侧分身库、右侧拍摄指南/创建入口；桌面两张卡片和虚线创建卡，约 2:1 主辅区域。窄屏改为单列，创作页也按顺序堆叠。文案长页正常滚动，标签条允许横向滚动；页面本身无横向溢出。 |
| 色彩 | 深黑背景、暖金主按钮/激活状态、灰绿说明与就绪标记；弹窗沿用统一背景遮罩、圆角与边框。没有为此修改全站主题。 |
| 图片 | 两张生成式示例图放入真实卡片，匹配设计室/庭院与黑衣讲解者方向；明确标“示例”。真实资料使用已有媒体组件保留源比例；示例图是静态图片，不伪造播放按钮或时长。 |
| 内容 | 上传真人视频 → 创建分身 → 克隆声音 → 文案口播；声音仍为独立标签。空库直接呈现首个创建入口。口播说明为“成片时长随口播内容确定”，不套用微视频 4–15 秒，也不宣称供应商无限长。 |

## 交互与响应式证据

- 打开/关闭视频创建弹窗，命名、授权与未上传时的禁用状态：`avatar-create-dialog.png`、`avatar-dialog-mobile.png`。
- 声音档案保持独立：`voice-desktop.png`、`voice-create-dialog.png`、`voice-mobile.png`、`voice-empty.png`。
- “使用此分身”进入口播创作，保留所选视频分身；“管理声音”可往返独立声音页。创作页支持字幕切换与保存草稿，出现保存成功反馈：`oral-composer-desktop.png`、`oral-composer-mobile.png`。
- 空分身库：`avatar-empty.png`；窄屏：`avatar-mobile.png`。390px CSS viewport 下 document scrollWidth 为 386px，没有页面横向滚动。
- 形象照片页仍显示基础五视图、场景形象照与“用于人物置换”，不再显示制作照片口播分身按钮。
- 本次浏览器日志检查 error 数为 0。上传/克隆/生成的成功、失败、取消、重复提交等网络状态由自动化测试覆盖；review 页面不调用真实业务接口。

## Implementation Checklist

- [x] 选定稿的库优先布局、实际示例资源、右侧创建指南。
- [x] 保留独立声音档案并统一视觉与表单。
- [x] 口播仅走视频分身 + 已确认克隆声音 + 已确认文案。
- [x] 检查桌面/手机、空态、弹窗、导航、字幕和草稿交互。
- [x] 修复 P2 并重新捕获/对比。

## Follow-up Polish / 未测试边界

- P3：如需更贴近原稿，可进一步调整示例人物的近景构图；当前真实媒体保持原比例，以便用户判断上传视频内容。
- 未运行真实付费飞影克隆与 TTS 成片，未对真实成片的口型同步和音色相似度作视觉结论；这不属于静态设计验收通过的含义。
