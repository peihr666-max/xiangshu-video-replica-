# FIX-W19-20260912 — 唯一 Worker 启动实例与发布消费者退出

任务/工作包：第二组 W19，原 BE-R06 / OPS-P2-01 / OPS-P2-04。重复根因只实施一次；实际部署消费者归第二部分。

Owner / Reviewer：Codex（01a094e2-9d59-73e3-8b2d-280723c19e7e）/ review_w12，独立只读评审及信号修复复审 PASS。

分支 / 基线 SHA：fix/w19-worker-instance-20260912，直接从最新 origin/main@47c9ffb1d3cb14f7d282a1681db06377ef82a4b2 创建；独立目录为仓库上级 .worktrees/FIX-W19-20260912。已 fetch、查询开放 PR #82—85、分支/worktree/claim；CW-030、CW-059、CW-068 已在 main。UC-BATCH-02 与另一物理退出任务在制文件只读检查，未改其目录；generation_worker 仅 CLI 边界，后续主干清理与 W22 按函数整合。

上游规格段落：去重第一部分 W19。原消费者 claim→平台探测→CAS 回写已有真实 PG 回归，不重复实现。新模块与既有测试映射已在代码清单提前冻结。

改动文件：worker_identity.py、generation_worker.py、publish_worker.py、test_customer_ha_smoke.py、test_publish_accounts.py 及本任务文档。

失败测试或回归锁定：原实现 6 项失败，新增启动标识与退出逻辑后 6 项通过。独立评审发现信号回调调用 Event.set() 会在 Event.wait() 持有非重入锁时自锁；补隔离子进程在该持锁窗口发送 SIGTERM 的确定性回归，原实现 10 秒超时失败，修复后与原回归合计 7 项通过。父进程超时会回收子进程；断言信号后不再领取、关闭池及恢复原 handler，不因死锁挂住整个测试进程。

实现结果：每次 CLI 启动生成一次有界可打印的逻辑标签/主机/PID/随机后缀，generation 和 publish 的同次循环复用该 ID，显式 --worker-id 作为逻辑标签。发布循环复用现有领取、平台探测与 CAS 回写；SIGINT/SIGTERM 回调仅更新无锁布尔标记，正常循环用单调时钟保留原空闲间隔，每次等待最多 0.2 秒观察退出，外部 stop_event 仍能唤醒。当前任务完成后停止新轮次，finally 关闭池和恢复 handler。实例 ID 不替代随机租约 token，不增设高基数指标标签；未扩展 generation 的租约或执行循环。

验证命令与通过数：现有 test_customer_ha_smoke.py 和 test_publish_accounts.py 的 W19 专项 7 passed、49 未选择，服务端 ruff/format/mypy 均通过。日志位于仓库上级 outputs/remediation-20260912/W19-{red,green,signal-red,signal-green,signal-static}.log；初次静态检查的未使用导入失败另保留，后续修复通过。已同步最新 origin/main@9bfe593，验证代码提交 d515e02515576551f446527dfe3909b260bf653d。完整静态门与前端1348项通过（W19-final-static.log）；四个独占 PG 分片分别752、692、596、910项通过，总计2950 passed、1原有TLS跳过，覆盖校验及全部退出码0（W19-final-shard-0..3.log）；专属副本 /verify/fast-w19-final，无主机端口，无共享数据库。

证据层级：AUTOMATED_VERIFIED（本地）；独立只读评审及信号修复复审 PASS；main@9bfe593 整合代码 d515e02 完整本地门通过：后端 2950 passed、1 原有 TLS 跳过，前端 1348 passed，secret、Biome、TypeScript、e2e lint、Tauri fmt/check、ruff、format、mypy 全部通过。PR、远程 CI 和合并待完成；全部人工联合调试留第二部分。

安全与可观测性：启动标识使用有界可打印逻辑标签、主机/PID 和随机启动部分；不记录账号凭据或平台响应正文。只读复用已有发布状态机。

迁移与回滚：无数据库迁移；显式 --worker-id 作为可追踪逻辑标签，实际启动标识追加唯一部分；回退会恢复默认容器 PID 重名风险。

外部授权记录：用户授权按五项分组开发、独立主干 worktree、独立子代理只读评审、PR 与合并。首组代码及评审已完成并提交 PR，等待远程门禁期间推进不依赖管理端的 W19。

未测试项：四实例实际编排启动、真实发布平台验证、跨实例联通、监控部署、进程强杀恢复与生产容量均留第二部分人工联合调试。

Lore 提交 SHA：不适用，以 Git 提交及 PR 为准。
