# FIX-W19-20260912 — 唯一 Worker 启动实例与发布消费者退出

任务/工作包：第二组 W19，原 BE-R06 / OPS-P2-01 / OPS-P2-04。重复根因只实施一次；实际部署消费者归第二部分。

Owner / Reviewer：Codex（01a094e2-9d59-73e3-8b2d-280723c19e7e）/ 独立只读评审待完成。

分支 / 基线 SHA：fix/w19-worker-instance-20260912，直接从最新 origin/main@47c9ffb1d3cb14f7d282a1681db06377ef82a4b2 创建；独立目录为仓库上级 .worktrees/FIX-W19-20260912。已 fetch、查询开放 PR #82—85、分支/worktree/claim；CW-030、CW-059、CW-068 已在 main。UC-BATCH-02 与另一物理退出任务在制文件只读检查，未改其目录；generation_worker 仅 CLI 边界，后续主干清理与 W22 按函数整合。

上游规格段落：去重第一部分 W19。原消费者 claim→平台探测→CAS 回写已有真实 PG 回归，不重复实现。新模块与既有测试映射已在代码清单提前冻结。

改动文件：worker_identity.py、generation_worker.py、publish_worker.py、test_customer_ha_smoke.py、test_publish_accounts.py 及本任务文档。

失败测试或回归锁定：待执行；验证同主机/PID/显式标签的四次启动仍不同，同次循环复用标识，发布停止后不再领取、空闲等待可唤醒、退出关闭池并恢复信号处理器。

实现结果：待完成。实例 ID 用于现有 worker_id 和关联日志，不替代随机租约 token，不增设高基数指标标签。

验证命令与通过数：待执行。使用独立 Linux 验证副本与独占 PG，不与其他套件共享数据库。

证据层级：CLAIMED，尚未完成开发或 AUTOMATED_VERIFIED。

安全与可观测性：启动标识使用有界可打印逻辑标签、主机/PID 和随机启动部分；不记录账号凭据或平台响应正文。只读复用已有发布状态机。

迁移与回滚：无数据库迁移；显式 --worker-id 作为可追踪逻辑标签，实际启动标识追加唯一部分；回退会恢复默认容器 PID 重名风险。

外部授权记录：用户授权按五项分组开发、独立主干 worktree、独立子代理只读评审、PR 与合并。首组代码及评审已完成并提交 PR，等待远程门禁期间推进不依赖管理端的 W19。

未测试项：四实例实际编排启动、真实发布平台验证、跨实例联通、监控部署、进程强杀恢复与生产容量均留第二部分人工联合调试。

Lore 提交 SHA：不适用，以 Git 提交及 PR 为准。
