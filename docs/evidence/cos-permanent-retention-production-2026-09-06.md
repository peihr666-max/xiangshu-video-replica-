# COS 永久保存生产变更证据（2026-09-06）

## 变更对象

- Bucket：`zhongshu-lead-platform-1462847756`
- Region：`ap-shanghai`
- 操作身份：腾讯云主账号控制台

## 变更前

目标桶存在两条开启中的 180 天自动删除规则：

1. `expire-project-media-180d`：前缀 `projects/`，对象到期 180 天后删除。
2. `expire-generation-results-180d`：前缀 `generation-results/`，对象到期 180 天后删除。

## 执行与复验

用户在最终生产删除操作前明确回复“确认删除”。随后在腾讯云 COS 控制台删除上述两条规则。删除完成后整页刷新生命周期页面，规则表仍为空，只显示“添加规则”。

生产生命周期状态：`REAL_CHAIN_VERIFIED`。存放在该桶且原本受上述两条规则影响的对象，不再因这两条规则于 180 天后被自动删除。

## 代码与自动化证据

- 永久保存代码提交：`fb687485ad092ad9e8824295b85147126850bf0a`
- 代码保存 COS 配置时会清理上述历史规则，并保留无关生命周期规则。
- 全仓门禁已通过：secret 扫描、Biome、TypeScript、前端 860 个测试、Tauri、Ruff、mypy、服务端 1695 个测试。

## 剩余权限边界

当前应用保存的 COS 凭据调用生命周期 API 仍返回 HTTP 403 `AccessDenied`。这不影响本次通过主账号控制台完成并刷新复验的生产变更；若后续需要应用自动查询或修改生命周期，仍需给对应 API 执行身份补充桶生命周期管理权限。

H3 `DIRECT` 等仅保存在供应商侧、尚未归档进该 COS 桶的结果不受本次变更覆盖；永久保存适用于已经写入目标 COS 桶的对象。
