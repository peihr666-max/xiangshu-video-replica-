# COORD-W6-PHYS-EXIT-20260912 — W6 物理退出批次决策（owner 签认，D5）

> 决策正本第二号（承接 `COORD-W6-UNBLOCK-20260912.md` D1~D4）。owner 于 2026-09-12 会话指令：「已合并，先清理，然后进行下一步 039和0042-b/040/041 物理删除批次等，在一个批次里面进行开发，然后一起提交pr」。

## D5 — 物理删除批次提前执行（修订 D1 的「GA 后」时点）

1. **前置已满足**：D1 为 042-b 设定的前置 =「CW-039 手册定版 + CW-040/041 入口关闭证据」。后者已随 #77 合入（243f1d6）；前者在本批次内先行完成（见 `docs/evidence/CW039-EVIDENCE.md` 与手册正本），链序在批次内成立。
2. **时点修订**：物理删除从「GA 触发后清理批次」提前至 pre-GA 现在执行。依据：数据决议（CW-005）下无历史数据、原环境封存、无真实客户，源码树删除不触及任何生产数据；代码级回滚 = git revert + 重新部署（手册 §3）；P1「先关入口、物理清理另起批次」的次序在本批次内保持（入口关闭证据先行合入）。
3. **不变义务**：039 真实演练维持 GA 冻结（与 CW-033/CW-048 同窗）；CW-051 停写义务不变；手册随重大拓扑变更持续更新。
4. **范围实测修正**（依 §5.3「引用他任务证据前必须实测」教训）：
   - `app/internal_billing.py` **不在删除范围**——名称是历史遗留，实体是客户钱包账务核心（CW-029 复用其 RESERVE/SETTLE/RELEASE 作为充值/结算引擎，generation.py 的 PG worker 扣费全走它）；CW-041 物理退出只删 `app/internal_accounts.py`。CW-044 盘点 §1.2 方向 B 将两者并列已过时。
   - `app/backup.py`、`app/db.py` **保留**——CW-060 operator 制品闭包的登记组件（test_cw060 可重建契约 + test_sqlite_to_postgres 依赖），删之违反「历史算法零重写、E4 历史输入长期保留」红线。db.py 降级为 operator 专用历史模块（零运行时消费者）。
   - `client/src-tauri/resources/start-backend.*` 与 client 内部计费 API 已在早前批次清除（盘点记录过时）。
5. **批次执行方式**：沿用 D4 授权（单 worktree 单分支统一开发后一并提 PR）。

## 签认

| 角色 | 结论 | 日期 |
| --- | --- | --- |
| owner（hlong026 / peihr666-max） | 采纳 D5；#77 已合并（14:01Z）后指令执行本批次 | 2026-09-12 |
