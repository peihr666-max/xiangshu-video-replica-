# 2.0.1 服务端与桌面升级

用户授权将当前主线服务部署到既有宝塔服务器，并生成可覆盖旧版的桌面安装包。代码基线为 `58e147dc`（含 #126–#131 已合并成果），发布分支仅统一版本为 2.0.1，不调整应用标识、产品名称、可执行文件名称或 NSIS currentUser 安装模式。

## 升级合同

- 原版本：2.0.0；目标：2.0.1，版本递增，`allowDowngrades=false` 保持。
- 标识：`com.xiangshu.video-replica.customer`，凭据命名空间保持。
- 产品：短视频复刻客户云工作台，NSIS 通过既有注册项识别安装目录；保留用户自定义路径。
- 本轮不修改卸载/旧版迁移钩子，不清除本机应用数据。
- 签名渠道与实际安装结果记录在最终交付清单；没有证书时不将未签名包冒称正式签名发行。

## 本地验证

- 版本清单、依赖锁内本项目版本与 FastAPI 版本同步；第三方依赖版本不变。
- 构建与升级合同：`test_build_contracts.py` 和 `test_cw024_signed_release_upgrade_contracts.py`，17/17 通过。
- 前端 107 个测试文件、1639 项测试通过；TypeScript、Biome、E2E lint 通过（Biome 有 4 条既有 CSS 告警）。
- 客户与管理端生产构建、客户 bundle 隔离检查通过；桌面包的 API 地址为 `https://video.zszhj.cn`。
- Windows 本地 Release 与 NSIS 构建通过；2.0.1 安装包大小 2,802,240 字节，SHA256 为 `B938E8B431BD459348AA6F0693CE536B80C7F625361710A45EC407F9FE3AF885`，当前签名状态 `NotSigned`，渠道仅为 `internal-test-unsigned`。
- 旧版 2.0.0 已安装在 `D:\Program Files\短视频复刻客户云工作台`。只读核验 `HKCU\Software\Xiangshu Video Replica\短视频复刻客户云工作台` 默认值正是该路径，与新安装脚本 `RestorePreviousInstallLocation` 使用的注册项一致。
- 已生成的 NSIS 脚本保持 `currentUser`、同一应用标识与产品名，升级模式排除删除应用数据；本轮未执行真实安装，不能据此声明实机升级验收通过。
- 原版本合同测试仍固定断言 2.0.0，已同步为 2.0.1 并在 Linux 容器专项复验通过。未放宽版本一致性要求。
- 后端静态门通过：Ruff、371 文件格式检查、159 模块 mypy。
- 本地隔离 PostgreSQL 全量首次为 2498 passed / 3 failed / 1 skipped（1320.34 秒）。失败是上述旧版本断言及 2 项临时 Git 仓库测试；后者继承了容器为 Windows worktree 设置的 `GIT_DIR/GIT_COMMON_DIR/GIT_WORK_TREE`，误向只读主仓库初始化。去除这组测试环境变量后，三个失败项一起专项复验为 3 passed（2.54 秒），未改动 Git 门禁测试或业务实现。汇总覆盖 2501 passed / 1 skipped，并非伪称首次全量全绿。
- 本次测试容器均使用 `--rm`，结束后确认 `release201-*` 容器、网络、命名卷无残留。

## 发布边界

- 用户在确认项后回复“允许”，本次按授权清理无引用旧视频应用镜像和可再生构建缓存，保留在用镜像、最近回退镜像、数据库卷与备份，并沿用旧版未签名交付方式。
- `e57e1d81` 的全部 CI 门禁已通过。上传采用 Git 增量 bundle，服务器导入后核对完整 SHA；发布脚本通过进程级 Git URL 映射读取已验证的本地对象，仓库 canonical origin 保留 GitHub URL，避免服务器到 GitHub 的 TLS 故障。
- 首次基础镜像实测发现 Debian 登录式 Shell 重置 PATH，`sh -lc 'alembic heads'` 报找不到 Alembic。基础镜像构建入口现补充 `/etc/profile.d/video-replica-venv.sh`，恢复与 ENV PATH 相同的虚拟环境；实际镜像中 `sh -lc` 下 Alembic head 与应用导入均成功。该环境修正不改变 `server/app` 或桌面制品内容。
- 修正后的 shell 语法检查通过；交付拓扑合同 12 项覆盖通过（Windows 一项初次因默认 GBK 读取 UTF-8 文档失败，显式 UTF-8 复验通过）。既有服务器拓扑已增补 `worker-publish` 和 `worker-viral`，经 Compose 解析逐项核对原服务、网络和卷配置不变。

- 源码已上传至宝塔既有候选环境；发布前检查通过，数据库、站点、配置已备份，数据库备份清单可读取。
- 已在空间恢复并通过 4 GiB 前置门槛后完成滚动部署，批次 `20260917-001504`（UTC），脚本退出 0、最终状态 `SUCCESS`。
- 线上源码固定为 `e57e1d81f5f1e07461de21ebcb66840d6901f7ec`，应用镜像为 `video-replica-rehearsal-app:e57e1d8-git`，实测 API 版本 `2.0.1`。登录 Shell 环境修正已回填发布分支提交 `14a4ab36`；它只改变基础镜像环境初始化，应用代码与已审计部署源相同。
- 数据库从 `20260914T0000_local_joint_merge` 升至 `20260917T1000_publish_records`。本批次备份为 `/opt/video-replica-candidate/backups/git-e57e1d8-20260917-001504/`，包含可读取的数据库 dump、客户/管理站点、Compose、旧镜像与 SHA 记录。
- 两个 API 的 `/ready` 均返回 ready，数据库为 PostgreSQL、存储为 COS；公网 `/health` 返回 ok。4 个生成 Worker、采集 Worker、发布 Worker 都运行同一新版镜像，新增两个 Worker 的重启计数为 0。
- 发布脚本已核对 `/customer` 与 `/admin/` 静态资产属于同一发布源；发布 Worker 内实际启动 Chromium，内存 HTML 页面标题校验通过，浏览器版本 `151.0.7922.34`。
- 本次交付明确为用户批准的未签名安装包，不冒称 Authenticode 签名发行。交付目录为本地工作区 `outputs/交付/短视频复刻客户云工作台/2.0.1/`，含安装包、升级说明、SHA256SUMS 与来源/签名清单；未代用户执行实机覆盖安装。
- 本文不声明 `PRODUCTION_GO`、真实付费链路或实机升级完成。
