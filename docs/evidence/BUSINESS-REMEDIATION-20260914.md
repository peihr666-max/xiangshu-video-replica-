# BUSINESS-REMEDIATION-20260914 — 真实业务整改与复测

## 2026-09-15 06:25：R34—R42 当前复测与评审增量

管理端已由用户重新登录，并实际核对运行配置和全部三页 43 条额度流水。此前“管理端会话过期”仅为历史快照。H3 扩展模式仍关闭；没有在实际成本未知时新增付费生成。供应商和权限待办继续保留。

| 编号 | 整改与实际验证 |
|---|---|
| R37 评审补充 | 归档网络传输在 PG 事务之外，前后均校验身份/任务归属；最终保存锁住任务并校验原结果，重复保存只形成一个资产及审计，账务不变。HTTP 下载/配置/非法时长失败、结果变化及最后提交会话失效均有回归。另修复隐藏任务后已单独保存的素材被一并隐藏的问题，49 项 CW058 PG 回归通过。 |
| R38 | 前端素材“联调-复刻成片-20260914”分组“联合调试验收”，点击“用于参考生视频”后仍显示最新名称、4 秒视频及生成结果来源；扩展模式关闭说明和提交禁用均明确。 |
| R39 | 发布管理恢复两份草稿。H3 草稿正确恢复标题及“联合调试作品：场景形象替换工地视频中的人物，保持原画面构图与动作。”；实际点击复制后在标签输入框粘贴，内容与标题/描述一致，随后清空未保存测试输入。此前工具剪贴板读空不足以判定失败，本次键盘粘贴已证实成功。 |
| R40 | COS 管理连接诊断从五目录补齐为七目录，新增 viral/cover 与 viral/prepared，避免只测 projects 后错误报告全部可用；42 项设置合同通过。新增目录实际写入仍等待精确权限授权，不冒充已修复云端权限。运行说明明确成片可保存素材库或下载本机。 |
| R41 | 任务详情接口补充授权项目名称，前端无自定义名称时使用项目名；真实详情显示“联调复测上传”，不再显示批次 UUID。 |
| R42 | 历史批次和成片结果统一北京时间格式。真实列表提交时间 2026/9/14 23:20:10，完成时间 23:23:06，与管理流水相同；前端相关 205 项通过。 |

核账：后台只读检查点 `84-ledger-reconciliation.json` 显示 781 可用、0 冻结；最新计费任务仍为 8b7ee9f7-da0a-4f64-b4d4-9f8ad2ff9975，预扣及实扣均 40。素材资产 b1676a5c-8cf2-4251-81e1-ef03399c64e6 为 COS、949665 字节，归档后未产生新计费操作。管理端最新结算 23:23:06，口播 34 预留/20 结算/14 释放，历史分析及首帧失败释放均可追至原任务。

评审结论：当前改动保留客户 fencing、归属校验、审计及历史计费，不通过修改旧账把 UNKNOWN 成本伪装为零；未更改认证期限、供应商密钥或当前共享权限。完整前端 1449 项通过，首次静态门在一条测试 SQL 超长行处停下；该纯格式问题修正后 Ruff/format/mypy（147 文件）通过，剩余门禁继续记录。当前完整后端四物理 PG 与远程当前提交 CI 尚未验完。

实际未完成项：爆款精选仍缺 COS viral/cover 与 viral/prepared 权限；Hifly/Metaso 账单未登录，实际人民币累计及旧 UNKNOWN 对账未完成；T2V/首尾帧/R2V、音频输入口播、链接成功复测和下载落盘仍需独立签收。浏览器已收到下载事件不等于确认最终磁盘路径。只将已跑通路径列为通过，不声明整体验收或合并完成。


## 当前增量：R34—R39（2026-09-15 06:08）

用户最新授权要求覆盖全部业务，适合项目的推荐修复先执行并记录；最终通过真实验收、评审及当前提交 CI 后，允许 PR squash 合并 main。下文“不合并”均为历史授权记录。部署、真实充值付款及对外发布仍未授权。当前仍未完成整体业务验收，PR #108 未合并。

| 编号 | 真实问题 / 推荐整改及原因 | 实现与验证 |
|---|---|---|
| R34 | 人工确认首帧后，H3 编译仍要求 AI 质检来源，返回 409 | 人工确认仅接受当前选择创建人的确认，继续核对候选、场景和资产；拒绝空/他人确认。前端重新编译、确认报价并提交成功 |
| R35 | 管理生成记录混用无时区 UTC 与带时区时间，耗时误显示 0 | 统一解析历史 UTC 时间。真实首帧显示 1 分 2 秒，H3 显示 2 分 56 秒，15 项相关前端测试通过 |
| R36 | 任务轮询丢失直出临时资产，正在播放的视频消失 | 按用户和任务缓存已载入结果，换上下文作废旧请求；已归档结果优先于供应商直出地址。H3 视频跨刷新播放到结束 |
| R37 | 直出成片不能永久保存到素材库，无法继续发布准备 | 新增显式保存接口，仅 GET 已成功结果、验证 MP4/时长并归档；两段事务分别验证权限，网络请求不持锁；重放返回同一资产，不重新生成或扣费。素材投影支持独立/项目生成并避免重复归类 |
| R38 | 素材改名/分组后，跨页复用仍显示旧缓存名称 | 复用时按 ID 替换草稿缓存中的旧元数据，保留唯一条目。真实复现，RED→GREEN 回归；跨页面复验继续进行 |
| R39 | 发布草稿保存原始资产 ID，恢复却按素材引用解析，导致整页 404；补齐引用后又缺预览 URL | 恢复时统一加 asset: 前缀，不重复加前缀；重新按权限签发媒体预览，部分媒体失败不丢弃草稿。历史口播草稿恢复后 9.08 秒完整播放，H3 新草稿保存成功 |

### H3 成片四处对账及资产流转

- 前端：从已确认 9:16 首帧提交 H3 768P、4 秒、1 条。批次 `48ed5375-5870-4bcb-803a-70df96abcc4d`，任务 `8b7ee9f7-da0a-4f64-b4d4-9f8ad2ff9975`，2026-09-14 23:20:10 提交、23:23:06 成功。实际视频 4.458333 秒、768×1344，完整播放结束；不把媒体容器时长直接当作供应商计费秒数。
- 后端：计费操作 `68cc9d05-f499-45ca-8a47-29023d0333d4` SUCCEEDED，预算/实际计费单位均 4，预扣/最终消费 40。管理页面已核对 40 积分及 2 分 56 秒；个人中心 06:00 实际显示预扣 -40/+40、结算 0/-40。余额 781、待结算 0、累计消费 219；06:02 只读快照一致。
- 保存成片：23:50:36 生成资产 `b1676a5c-8cf2-4251-81e1-ef03399c64e6`，949665 字节，SHA256 `8f6428faaa1c5b96e6f21ffd16856dd2d3d2119e3ad762601b013d4d94579595`，COS generation-results/ 私有对象。审计一次，无新增钱包流水。
- 素材管理：前端“视频成片”筛选可见；改名“联调-复刻成片-20260914”、分组“联合调试验收”，离开返回保持。点击下载，浏览器 download 事件已收到；工具不提供下载落盘路径，尚不声称磁盘文件验收完成。
- 发布准备：通过任务结果进入发布管理，保存第二条草稿，选择既有人物图片封面，描述为本轮人物替换测试内容；没有对外发布。已有口播草稿恢复和完整播放通过。复制按钮已点击，但工具剪贴板读不到内容，复制验收暂不签收。

### 评审、自动化与未完成项

R37 评审补测发现缺供应商配置原返回 500、NaN 时长仍可保存；已分别修为可重试 503 与拒绝非有限正数时长。HTTP+真实 PG 检查成功重放只下载一次、下载失败无资产、结果变更 409、最终会话拒绝 401 均不产生新消费。当前内容资产及真实 fencing 套件 112 passed；发布与素材前端 85 passed。先前 R34 PG 109、归档/管理 64、四个前端页面 216 通过。新增代码当前全量静态/PG/CI 仍待运行，不能沿用旧 55d3a77 的门禁结论。

R14 已定位另一处真实前置：COS 子账号不具备 viral/cover/* 与 viral/prepared/* 权限。只读 HEAD 为 403，管理展示操作返回 409，未产生首页展示。已写好最小权限增量方案并向用户请求这两个目录的四项对象权限；尚未收到确认，不改云策略。代码补齐了“已归档但缺封面”说明，并在展示前尝试补齐封面，失败继续拒绝；测试通过不代表真实精选已验收。当前管理会话已过期，已请求用户在既有 5200 页面重新登录；不重设密码。

Apilio 2026-09-14 筛选实际 11 条消费，逐条合计 0.880704 ⚡（六张图片共 0.36，其余五条 Gemini 合计 0.520704）。23:00 图片明细为 1008×1792 / 0.060；21:53/21:59 超时段没有对应消费日志，不将“未发现”当作供应商最终免单证明。Hifly/Metaso 实际人民币计价、历史 UNKNOWN 追加对账、其余视频模式、音频驱动口播、链接导入/精选仍需继续验收。累计授权上限不变：1000 用户积分/100 元供应商实际费用。

本机原始证据：outputs/business-remediation-20260914/65—75 前缀文件；不提交原媒体、私有下载 URL、凭证、完整供应商账单或个人信息。后续增量继续在同一 worktree/PR 内完成。

## R29—R33 最新复测（2026-09-14 21:53—23:08）

用户确认场景形象已经完成外观和服饰设计，首帧只需要按该形象替换原视频人物；进一步要求增加 9:16 等图片画幅。仍在原整改 worktree/PR #108 开发，不合并或部署。

实现：场景模式只使用所选场景版本的图片及不可变快照，保留源图单人校验，取消额外付费候选质检和自动补图；前端对照源画面、场景形象和结果，由用户确认。切换场景或画幅会使旧确认失效。默认跟随源图最接近的供应商支持画幅，也可指定 9:16、16:9、1:1、3:4、4:3。GPT Image 2 使用相应合法像素 size，Nano 使用 aspect_ratio；比例进入冻结请求、幂等校验及结果版本。异于源图画幅时允许扩展原背景以容纳目标比例。

延迟根因与优化：旧同步请求在网络超时后可能再次付费提交。改用供应商官方异步编辑协议，保存回执后只读轮询。回执绑定原请求哈希、账号指纹、模型、数量和原成本记录，保存/恢复均验证执行租约。API 密钥不传给图片 CDN；未知提交状态不盲目再发 POST。供应商文档依据：[异步编辑](https://gpt-best.apifox.cn/api-339685644)、[任务查询](https://gpt-best.apifox.cn/api-356258956)、[尺寸约束](https://gpt-best.apifox.cn/api-447258891)。

真实兼容缺陷：任务 ca4de059-ec76-4d4c-9f6d-59059f35dcc8 的供应商回执 f55ac3c4e90c487f9449bd74b6fe5ef7 已成功，b64_json 却包含 Data URI 前缀；原解析器误判。修复只接受 PNG/JPEG/WebP 白名单 Data URI，严格解码及验证类型。供应商页面实际记录 94 秒、0.060 ⚡。旧任务已退款 2，保留原 SUBMISSION_UNCERTAIN 及 UNKNOWN 成本历史；一次技术修复仅 GET 原结果、写入图片检查点并追加审计，未新增付费请求，未修改钱包。第一次技术恢复尝试的成本状态回写被不可变账务触发器拒绝，事务完整回滚；未绕过触发器。该次存储写入可能留下一个尚未精确定位的无引用测试对象，禁止据此清理整个目录。后续前端重试 193d1617-5e69-4740-97d8-16f364ce85f5 复用归档图片成功，结算 2，无新增供应商 POST。历史 UNKNOWN 成本仍需以追加记录方式对账，不能冒称已完全结清。

画面实测：恢复图片存在模糊填边及字幕损失，未采用；增强保留构图/字幕提示词后的 2631a0aa-50fc-4967-84ca-618d03448bdb 约 91 秒成功、结算 2，但仍为 1024×1024 方图填边，未采用。显式画幅修复后，从真实前端选择 9:16 提交 a8c8dcdb-a39e-4e25-82f9-1ef810d6377e，23:00:03 提交、23:01:05 归档，约 62 秒。供应商回执 20359bfbc8a94f31bd28c05b6259253b：23:00:21—23:00:59，38 秒、成功、0.060 ⚡。前端实际 naturalWidth/naturalHeight 为 1008×1792；人物黄色安全帽、蓝色工装、反光背心与所选场景一致，手持图纸、手势、工地及原文字保留，本样本无方图模糊填边，已从前端人工确认。此为样本验收，非任意画面像素级一致承诺。

数据链：首帧资产 23f04309-1e9d-4f73-9a52-a076f63cb2d0，COS、2,297,545 字节、SHA256 86a78e6a7a5a9201792fe0dfddaff64ed388864fd82c474d7544d7f04df4de31；结果版本 db70a68e-6974-4178-a09f-0e462e6c86fd；计费 5ce0232e-1a6f-42d3-ab0b-c3312a8051c5 仅结算 2；81ae4472-de11-4601-8168-a12934352f22 为 1 张已确认用量。内部配置计算 cost_fen=1 不等于供应商发票。个人中心实际显示 23:00:03 预扣 -2/+2、23:01:05 结算 0/-2，可用 821、待结算 0、累计 179。供应商单位 ⚡ 与人民币的最终支付折算仍待核验，不把内部成本价格当实际现金账单。

验证：新增场景语义、懒加载质检、回执防串用与费用幂等、Data URI 及比例请求测试先红后绿。当前完整四个隔离 PostgreSQL 分片 486+505+612+539=2142 passed / 1 既有 skipped；前端 100 文件/1443 passed。静态门分阶段全部完成：秘密扫描、Biome/TypeScript、e2e lint、Tauri fmt/check、Ruff、344 文件格式检查、Mypy 147 源文件通过。修复了旧 MaterialItem 测试夹具遗漏 composite 的类型错误及容器 cargo PATH；没有降低类型或门禁。之后仅更新界面说明和证据。API 合同由当前 OpenAPI 重新生成，包含之前尚未同步的现存接口字段。

原始本机证据：outputs/business-remediation-20260914/45—60 前缀文件、scene-async/scene-data-uri/scene-ratio 测试日志；原媒体、截图、登录信息与供应商私有下载地址不提交 Git。H3 各模式、R14 精选、下载落盘、历史未知成本及人民币累计对账仍未完成，任务保持进行中。

管理端补验（23:10）：用户重新登录后，实际按账号筛选额度流水，23:00:03 冻结 -2/+2、23:01:05 结算 0/-2，均关联 a8c8dcdb-a39e-4e25-82f9-1ef810d6377e，结算后 821/冻结 0；与前端、数据库、个人中心一致。原始依据 61-admin-ratio-ledger.txt。

## 任务信息

Owner：当前 Codex 任务；Reviewer：执行者代码自检及远程 CI，未进行独立人工评审。分支 `fix/business-remediation-20260914` 从 `origin/main@06a5333` 新建，独立 worktree；未派生自 IMAGE-CONSENT 功能分支。原业务端口 5173/5174/8000 保持不变；复测端口 5199/5200/18099，独立 PostgreSQL 数据副本和素材目录。

用户要求的验收单位是一笔真实业务：**前端完整操作 → 后端任务与资产 → 管理端扣费明细 → 用户个人中心明细**。接口或自动化测试单独成功不能代表四项验收通过。[逐项整改清单](BUSINESS-REMEDIATION-20260914-CHECKLIST.md)保留尚未完成事项。

## 已实现修复及依据

- 本地上传和签名媒体 URL 改为相对 API 路径，由客户端按自身 API 基址解析。经客户 API 代理上传会携带本次用户凭证；第三方预签名上传不带业务 Authorization。没有取消资产归属、签名和会话校验。
- 零字节、缺少哈希的上传占位不能签发成功下载地址，前端不再将非 READY 项目素材作为可播放视频。
- Hifly 兼容真实顶层响应和既有 `data` 包装；余额读取 `left`，照片参数 `image_url`、整数 `aigc_flag`、分身任务返回 `avatar`，上传票据和列表分别按端点验证。依据[供应商官方文档](https://api.hifly.cc/hifly.html)及真实分身、声音、口播成功响应。
- Windows 上传声音探测原来持有 `NamedTemporaryFile`，ffprobe 无法重新读取，合法 MP3 被误判 422。改成关闭文件后探测；同一个 24.085 秒 MP3 修复前失败、修复后真实上传成功。
- 同账号资料定时刷新产生新对象，原先会重建工作区素材，导致场景选择失效和正在播放的成片消失。稳定身份依赖；显式完整刷新仍重新加载人物素材。
- 下载授权原来按账号猜测唯一活跃会话，重新登录时新旧租约重叠会返回 401。下载授权改用本次请求经过事务验证的会话，保留会话撤销后的签名失效能力。
- 本地素材不满足云分析 HTTPS 前置时，手动分析在任务/扣费前拒绝；上传自动分析也检查同一前置，上传本身仍成功。错误提示明确指出存储准备事项，不伪装成素材库错误。
- 业务刷新及任务轮询同步钱包。个人中心新增人物图片、首帧、分析、二创、ASR、链接解析、分身、声音与采集筛选。
- 管理流水统一积分单位，补充业务名称、来源任务和计费操作关联；管理图片记录读取实际结算与调用尝试，重试不重复累乘消费，任何未知调用成本都会保留未知。配置价格估算不宣称供应商账单。
- 声音卡片允许换行，修复真人操作中试听控件与确认按钮溢出。上传提示不再把本地文件误称为已上传云存储。

## 真实业务复测记录

| 业务 | 前端实际操作与结果 | 后端依据 | 个人中心 | 管理页面 |
|---|---|---|---|---|
| 指定 MP4 上传 | 选择用户提供文件，完成上传 | 项目 `8124913e-0691-4fef-b398-d98beea0dbfe`；资产 `0d27c71f-d462-450f-876b-331c16fa23ec`，2,731,966 字节；SHA256 与输入一致 | 上传无费用；旧自动分析失败预扣 2、退回 2 | 管理额度流水已核对 |
| 场景图制作分身 | 人物库选择已授权场景单图，填写名称、确认授权并提交，完成后可选入口播 | 分身 `8dc728db-87e9-4213-b40a-d3d1be1f6d21` READY；计费 `fd3456bf-40f1-48b9-a3ec-1b012500f696` SUCCEEDED | 预扣 2、结算 2，业务筛选返回两条 | 管理额度流水已核对 |
| 合成声音克隆 | 系统合成 MP3 上传、克隆、点击试听和确认；已确认声音带入口播 | 声音 `b1c3e012-4bf3-42d6-98f9-9fe39caa009e` READY；计费 `9b7b0465-8264-467d-a28e-fcd5dc98a4a5` SUCCEEDED；样例媒体 GET 206 | 预扣 2、结算 2，筛选返回两条 | 管理额度流水已核对 |
| 文案口播生成 | 文案工坊输入 33 字、保存版本、确认终稿、带入口播；选择分身与声音，确认预留 34 后提交 | 任务 `2535db12-e8ca-422d-9b94-201ad6c4edc2` SUCCEEDED；计费 `ba111b69-7aa3-4e75-b655-a4fe0d07ef79`，实际 9.08 秒，结算 20，释放 14 | 三条流水为预扣 -34/+34、退回 +14/-14、结算 0/-20，与后台相同 | 管理额度流水已核对 |
| 成片预览 | 点击结果、预览、播放；修复后 `currentTime=duration=9.08`、`ended=true`、`readyState=4`，跨定时刷新仍保留播放器 | 资产 `05c436c5-cd18-4ee4-acc4-f7b6b849442b`，1,466,134 字节，864×1040；签名媒体可读 | 预览不新增消费 | 管理额度流水已核对 |
| 成片下载 | 点击“下载成片”；后台签名和文件请求均 200 | 同一成片资产 | 不新增消费 | 文件落盘路径尚未确认，不标完整下载验收 |
| 发布准备 | 从成片进入发布管理，选择封面、填写发布标题/描述，保存后出现 1 条云端草稿 | 页面确认已保存；后台 studio_drafts revision=1，assetId 和 coverId 与所选素材一致 | 不新增消费 | 未连接桌面发布账号；未对外发布 |
| 原文案二创 | 原环境真实点击按 IP 二创，119 字结果保存确认 | `4577c5cb-12e8-4613-91e2-db8ce8f3c432` 成功；当前未配置二创售价，结算 0 | 不新增消费；须由管理员确认免费定价是否符合预期 | 原环境配置已补齐，独立页面待核对 |

截至上述复测：原环境净消费 143，本次独立副本额外消费 24，累计 **167 积分**；独立副本可用 **833**、待结算 **0**，用户中心相同。复制数据库没有增加测试预算。供应商成本存在未知项，内部冻结价格估算不能代替人民币实际账单；不将未知记为零，也不宣称费用已全面对账。

内置浏览器首次点击试听时崩溃一次；恢复后发现并修复新旧会话误判。声音播放器曾在资料刷新后重置，修复后最终口播视频完整播放已取得证据；不声称完成音色相似度或真人主观听感评审。

## 自动化验证与代码自检

- 场景刷新、上传 API 来源、客户业务筛选、管理单位及错误文案的失败回归均保留。会话签名和 Windows ffprobe 回归修复后通过。
- Linux 受影响后端 55 项通过；新增管理账号流水与个人中心同笔关联测试 1 项通过。使用真实隔离 PostgreSQL，未用 mock 供应商结果冒充真实业务生成。
- 前端完整 99 文件、1429 项通过；TypeScript、Biome、e2e lint、secret 扫描通过。Tauri 使用容器现有 Cargo 路径修正后通过；Ruff 343 文件格式检查和 Mypy 147 源文件通过。
- 后端全量四片 **2094 passed / 1 既有 skip**，97 个测试文件均已登记；四片分别 486、499、612、497 通过。收尾新增声音大小边界测试和受影响口播专项另行复验。首次静态门暴露管理适配器类型遗漏，已改为生成合同类型；另一轮因登录 shell 丢失 Cargo PATH 中断，恢复现有工具链后剩余门禁通过，不抹去失败记录。
- 自检重点：上传凭证不外传；下载签名绑定请求会话；跨用户资产与账务隔离；结算消费与尝试成本分开聚合；旧响应不覆盖新钱包；失败预检不预扣。无历史账务修改、无数据库迁移。

## 尚未完成的验收与整改

独立管理端现使用 `http://localhost:5200/admin/`。用户已通过正规恢复流程亲自设置新密码；已登录并实际查看两页共 23 条额度流水及 6 条生成记录。原管理环境与独立环境使用不同浏览器主机名及独立签名密钥，一次性恢复凭据已删除。未读取或记录新旧管理员密码。

云视频分析、首帧与 H3 各生成模式仍须具备本次独立测试环境的可用云素材及模式配置后实际复测。当前活动存储仍为 local，H3 扩展模式关闭；不把原配置“已保存”当作完整业务成功。原先首帧单人检测失败仍待合适源帧复测。爆款精选的媒体归档/精选前置未完成，不能承诺已经显示。声音克隆 20 MB 供应商上限已在前端上传选择和后端克隆提交前统一拦截；前端人物页 53 项和非媒体工具前置 6 项通过，实际已成功使用的样本为 0.39 MB。

## §14 证据模板

```text
任务编号：BUSINESS-REMEDIATION-20260914
任务目标：按真实前端业务定位并整改媒体、数字人、会话及费用明细问题
代码位置：client/src/{api*,admin,customer,studio}；server/app/{media*,materials,hifly,rbac_routes,control_routes,recharge_routes,analysis_routes}
测试与检查：前端 1429；专项后端 55 + 新管理关联 1；静态门各步骤通过；全量 PG 2094 passed / 1 既有 skip；声音上限增量专项另验
证据层级：部分真实业务已验证，四端整体验收未完成；不标 PRODUCTION_GO
安全与可观测性：独立端口、PG、存储；报告不包含密码、密钥、会话凭证及原视频内容
迁移与回滚：无迁移；未合并/部署；保留独立分支与 worktree
外部授权记录：1000 积分 / 人民币 100 元上限；指定 MP4、AI 场景 Hifly 授权、测试声音授权；允许远程推送及 PR，不合并部署
未测试项：管理页面、云分析/首帧/H3 全模式、下载落盘、真实供应商人民币账单、对外发布
提交 SHA：以本任务 PR 的提交记录为准
```

本机原始证据保存在工作区 `outputs/business-remediation-20260914`；仅本技术报告进入 Git，原媒体、私有配置和运行日志不推送。

收尾补验：20 MB 上限及受影响口播/预检共 20 项通过（53 项未选中），人物页 53 项通过；新增 1 个前端和 1 个后端边界用例，上述新增测试在相应专项中验证（后端全量另有 1 既有 skip）。未重复整仓全量。

## 最后补测与交付

13:43 再次从前端上传同一 MP4，得到新资产 c7a9e3e5-c9a6-4a84-b4f3-1d26894939e6，字节及哈希与输入一致；后台未新增自动分析任务或计费操作，流水仍为 23 条、余额 833、冻结 0。随后点击启动 AI 拆解，页面明确提示配置云端素材存储后重新上传，后台仍无新增扣费。上传自动入口与手动分析入口均已真实验证前置拦截。

真实页面还暴露 PostgreSQL 微秒时间与 +00 短偏移无法被浏览器解析，任务时间显示原始 UTC 字符串；已规范化格式，详情复用时间格式化，并把文案口播来源从错误的项目分镜改为文案口播。失败回归修复后 MainPages 全部 77 项通过，TypeScript 与 Biome 通过。

最新主干 08a4d8b 已整合，公共文档冲突按两侧独立任务保全，管理页专项 47 项通过。整改已推送为 [Draft PR #108](https://github.com/peihr666-max/xiangshu-video-replica-/pull/108)，图像授权独立 [PR #106](https://github.com/peihr666-max/xiangshu-video-replica-/pull/106)。未合并部署；远程 CI 以当前提交检查为准。


## 管理端恢复后的真实验收（19:18 起）

- 管理端两页 23 条流水逐项核对：口播冻结 34、结算 20、释放 14；声音与分身各冻结/结算 2；人物图片结算 5；ASR 结算 136；链接解析结算 2；失败分析/首帧均释放。余额 833、冻结 0，与后端及此前个人中心一致。所有业务流水显示积分、业务名称及来源任务编号。
- 生成记录实际显示口播任务成功，编号 2535db12-e8ca-422d-9b94-201ad6c4edc2、成片资产 05c436c5-cd18-4ee4-acc4-f7b6b849442b，与前端播放资产及后端一致。人物图片记录现显示实际扣 5、估算成本 0.01 元；估算不等同实际供应商账单。
- 真实页面发现生成记录扣费列仍写“秒”，已补修为积分；耗时仍以分/秒显示。失败回归 1 failed / 5 passed，修复后 6 passed；页面已实际显示 20 积分及 5 积分。
- Hifly 管理页面只读检查通过，返回余额 88150 积分，未创建收费任务。腾讯云管理页面连接测试通过。
- 运行设置仍为 local，旧页面却宣称素材保存云端且无启用入口。已增加仅在 local 状态下出现的明确警示和“启用腾讯云存储”按钮，复用既有管理设置写接口；启用时不保存其他未提交的数量草稿。失败回归 1 failed / 17 passed，修复后相关四文件 85 passed；TypeScript 与 Biome 通过。
- 已从管理页面启用腾讯云，后端只读查询 active_storage_provider=cos。随后在前端再次选择同一授权 MP4；上传失败并明确提示跨域规则。新占位资产 d5a56cbe-ce67-423a-8170-468e41d1099f 为 cos、0 字节，项目 9691f8e5-d02e-43b2-a283-eb3704711616；未进入分析、无新增扣费。
- 对已配置桶发起只读 OPTIONS：Origin=http://127.0.0.1:5199，方法 PUT，请求头 content-type；返回 HTTP 403 / AccessForbidden，缺少跨域允许响应头。已请求仅新增该测试来源 PUT/GET/HEAD 的 CORS 规则，保留既有规则及对象权限；当时未改变腾讯云跨域配置；后续授权及执行结果见下文。
- 新发现总览“今日生成 0 / 输出 0 秒”遗漏本次成功口播：后端 admin_dashboard_routes 仅聚合 generation_tasks，billing_reports.video_seconds 仅含两种 H3 服务。当时列入 R22；后续修复及真实页面验收见下文。

本段原始页面证据为本机 outputs/business-remediation-20260914/16—21 前缀文件。管理员恢复、连接检查及本次失败上传未增加用户净消费，累计仍为 167 积分。当前已补齐这些实际收费业务的管理流水验收；云分析、首帧、H3 全模式与供应商实际人民币费用仍未全验收。


## 腾讯云登录后的复测与整改（19:48—20:00）

1. 用户登录腾讯云后，通过控制台在指定桶新增独立 CORS 规则：Origin `http://127.0.0.1:5199`；PUT/GET/HEAD；允许 content-type/range；暴露 ETag、Content-Length、Content-Range、Accept-Ranges；MaxAge 600。原有五个来源及其规则保留，Vary 设置未改变，未调整对象私有权限。保存后的页面与只读 OPTIONS 均确认生效，预检为 200、允许来源精确匹配 5199。
2. 通过前端再次选择用户同一 MP4，跨域上传已通过，但后端 complete 阶段在保存不可变校验对象时抛出 StorageBackendUnavailable，底层为连接重置。首次返回 500，项目 f7d22a41-b52b-4736-823a-75c71d3f51d1、资产 bc752087-0785-4fa7-b312-50205925aa1e。此时不能标上传业务完成。
3. 增加失败测试后，将此类异常映射为脱敏 503，明确提示素材库暂时无法完成校验、稍后重试、本次未启动 AI 拆解。第二次真实前端上传已看到该中文提示；项目 8f50a4f0-7af6-4421-9033-567a2b3898d0、资产 bf9710cf-d911-40ea-924e-df373acb692b 仍为 0 字节占位。独立 SDK 对同一已授权视频的传输诊断也失败；它只用于定位，不算前端业务成功证据。
4. R22 已补齐口播统计：总览日计数和趋势合并口播，客户列表按所属用户独立聚合口播状态，成功输出时长包含 oral 实际秒数。重启本任务 API 后，真实管理页面显示客户成功 1/1、已结算 167、可用 833；总览今日生成 1、成功 1、输出 9.08 秒。未知供应商成本仍显示待核对。
5. R22 三项专项由失败转为通过；本轮完整四个隔离 PG 分片为 486 + 499 + 612 + 498 = 2095 passed / 1 既有 skipped。之后 R25 新增用例 RED 1 failed，GREEN 前置 8 passed，受影响 PostgreSQL 资产矩阵/跨实例/前置 76 passed。Ruff/format 及 Mypy 147 个源文件通过。没有把新增 R25 用例算进此前全量结果。

原始证据：本机 outputs/business-remediation-20260914 的 22—28 前缀文件、cloud-complete 与 admin-count 测试日志。所有上述失败上传均未新增分析任务/计费操作；截至 19:57 仍累计消费 167、余额 833、冻结 0。CORS 与管理统计已整改，R25 存储传输问题、云分析/首帧/H3 全链路、下载落盘及供应商人民币账单仍待完成。无合并、无部署、无对外发布。

最终本地静态质量门通过：前端 99 个文件 / 1436 项测试、TypeScript、Biome、秘密扫描、Tauri、Ruff/format、Mypy。20:00 远程同步发现图像授权 PR #106 已由远程合入 main@71ded39；本任务未执行该合并，整改 PR #108 仍不合并部署。


## 深入定位及根因修复（20:03—20:15）

此前连接重置是症状，根因为腾讯云子账号 `video-replica-cos` 的对象授权只覆盖 projects/、generation-results/、users/、materials/，缺少后端不可变校验对象写入的 verified-uploads/。最小对象对比证实同一身份在 projects/ 写入成功、verified-uploads/ 明确 AccessDenied；大文件遭拒时被 SDK 表现为传输重置。腾讯云控制台策略原文与此完全相符。

用户明确授权后，仅向既有 VideoReplicaCosObjectAccess 策略追加指定桶 verified-uploads/*，沿用 PutObject/GetObject/HeadObject/DeleteObject。20:09 保存为第 5 版，旧四版保留；不开放匿名读写、不扩大到其他桶。

代码同时修复管理连接测试的覆盖缺口：五个实际业务目录分别执行写入、HEAD、GET、内容比对和删除；拒绝时指出目录，且不泄露供应商原始错误。六项失败回归先红，完整设置合同 40 项通过。管理页面说明同步，随后点击真实测试连接成功。

权限补齐后已从前端成功上传同一 MP4。云资产 9aab42f7-161e-4bcc-9fec-26cb57879ede，项目 46a9d37b-11b1-4559-80a6-286b4abd8b6a，2,731,966 字节、SHA256 与输入相同。首次成功上传项目 fd096e75-8357-4c5c-b2ec-5436c701677a 的自动分析 f8993491-45da-40be-ab1b-b0bc8061944f 已成功，计费 eca170c1-3148-4a50-bcae-73facd97b8ea 实扣 2。随后对当前上传来源通过“启动 AI 拆解”继续人工业务复测；状态、最终费用和后续首帧以之后记录为准。

最新主干图像授权整合后，完整前端 100 文件 / 1438 项与静态质量门通过；PG 组合回归正在完成。此处不把中间结果标为整体验收通过。策略原文备份、目录对比及上传界面证据仅保存在本机输出目录，不包含密钥。


## 后续真实分镜与首帧（20:15 起）

当前项目手动分析 a7d6b889-3f7d-45d0-a754-5811471d77b5 已成功，前端显示 3 个镜头（0–3、3–6、6–12.1 秒）及完整提示词。两个成功分析任务各扣 2；累计净消费 171、可用 829、冻结 0。管理页面已核对二者冻结和结算记录，任务关联正确。

人物置换页自动取帧后，AI 评分在 8 秒预算内超时并降级为本地画质推荐；已实际查看五张候选中的单人画面并确认推荐源画面。首次首帧任务 adf7fad0-714f-482a-81ea-46feca85fd76 因旧本地人物参考图被交给当前 COS 适配器读取而失败，冻结 2 后全部释放。此为独立于腾讯云目录权限的新代码缺陷。

R27 修复只读输入的存储解析：非正式环境允许已授权历史 local URI 从本地读取，云 URI 仍严格匹配配置桶；正式客户生产保持禁止历史本地读的规则，新写及成片归档继续使用当前 COS。失败测试分别复现历史输入读取失败及生产限制回归，修复后增加相应防护；中文错误提示替换原始英文。

真实重试首帧任务 10505448-3397-42b1-8dfd-0d0489306a7d 已成功读取输入并进入图片模型调用，当前预留 2。管理流水可用 827/冻结 2，与前端一致；模型完成、人工确认及视频生成仍在继续，不能以提交成功代替最终验收。

主干整合及五目录检测后的完整 PG 回归：486 + 505 + 612 + 503 = 2106 passed / 1 既有 skipped，前端 1438 与静态门通过；之后 R27 按受影响范围另验，未算进上述全量。


## R28：首帧长时间等待与中断恢复（20:52 实测）

任务 `10505448-3397-42b1-8dfd-0d0489306a7d` 于 20:21:44 提交。20:24:20 第一张付费图片已归档；20:28:22 质检网络请求出现 URLError，旧设置单次超时 240 秒、最多两次请求；20:31:27 第一轮质检落盘，身份匹配及整身重建未通过；20:33:11 第二张图片已归档，等待质检。随后本机服务中断，旧进程 42516 已退出，锁却保留至 21:03:11。耗时由生成、质检重试、自动补图和旧锁等待共同组成，不能归为单一图片生成慢。

整改：首帧专用质检改为 60 秒网络超时、单次请求，不改变图像生成服务的 240 秒预算。候选质检不可用时交付已归档图片，quality 保持 null，前端明确提示“自动质检未完成，请查看图片后人工确认”；沿用二次明确确认及后端 allow_unverified 审计，不伪造通过。源画面初次单人检查仍然失败关闭。恢复任务在请求绑定和存储哈希校验后复用检查点，跳过此前已通过的源画面检查；质检阶段锁为 3 分钟，进入再次付费生成前恢复长锁。保持最多两轮生成的既有行为，实际不合格仍可自动修正一次；仅质检不可用不再触发重新生成。

真实复测：核验旧进程已不存在并停止本任务恢复进程后，仅将这一个旧锁的截止时间设为当前时间，保留任务、检查点及钱包。操作记录保存在本地 35-first-frame-recovery.json；这是本次旧锁的人工修复，不声称旧任务自然在 3 分钟内恢复。新 Worker 于 20:51:23 接管检查点，20:52:24 完成，约 61 秒。真实质检请求仍在 60 秒后出现 URLError，系统按新分支正常交付两张图片，没有再次调用图片生成服务。这个 61 秒是恢复后处理耗时，不是新任务端到端生成承诺。

四处证据：前端两张图可见，第一张标记身份/整身质检失败，第二张明确未完成自动质检；后端任务 SUCCEEDED、两张 COS 资产落库；同一原计费操作只结算 2 积分。管理端 20:52:24 流水为可用变化 0、冻结变化 -2，余额 827/冻结 0；个人中心对应预扣与消费一致，累计 173、可用 827、待结算 0。未采用未通过的候选推进视频，不将图片交付成功视为人物一致性验收通过。

验证：新增三类回归先红（3 failed），扩展检查点恢复后相关 PostgreSQL/图片协议/费用套件 76 passed；前端新增未质检标记及二次确认用例。完整静态门通过，前端 1439 passed，Ruff/format/Mypy 与秘密扫描通过。完整四个 PostgreSQL 分片结果另追加。代码自检核对了源检查失败关闭、检查点绑定、旧执行权隔离、生成前长锁恢复、质检 null 与原计费幂等路径。未合并、未部署。


R28 收尾：完整 PostgreSQL 四分片为 486 + 505 + 612 + 510 = 2113 passed / 1 既有 skipped。上一提交远程 Linux 门失败定位为 CharacterLibrary 场景图片异步签名尚未加载，测试提前同步断言；已改为等待实际图片出现，不放宽图片内容断言，人物库与首帧 56 项专项通过。完整静态门此前 1439 项通过，后续仅这项测试等待方式调整。新提交 CI 单独核验，不沿用旧提交失败或成功状态。

## 2026-09-15 06:42：新增实测问题 R43—R45 与 CI 原因

- R43：真实管理生成记录把 H3 0.08 元显示成精确成本。追溯 operation_costs.record_video_generation_cost，数值实际由冻结配置单价乘用量得到；改为 ESTIMATED，页面已实测显示“估算 0.08”。顺带修复视频查询对 projects 的内连接导致无项目的独立任务计数为 1、列表为空，改为左连接与正确空值。两个 PG 断言 RED→GREEN；内容资产/管理员权限/计费专项 88 项通过。没有修改历史金额或供应商实付。
- R44：真实复刻页 Prompt 虽要求保持首帧人物，逐镜头主体却仍重复“穿浅色条纹衬衫的男子”。编译模板升级 h3.prompt.v6，逐镜头主体统一引用“已确认首帧中的人物”，不再插入原人物 subject 外观字段；动作、机位、场景和台词继续来自原结构。新增断言确认旧服饰不出现且动作/背景/台词保留，RED→GREEN，CW030 共 46 项通过。历史已提交任务/锁定快照不改写，新编译采用新模板；新付费视频效果仍须另验。
- R45：返回成熟分镜工作区时，已完成首帧仍被 resume 并标 busy，readOnly 变化触发上游加载/清空绑定，再触发首帧恢复；实测表现为“已等待 27446 秒”及多个图片预览失败。已完成结果改为普通读取并恢复预选，不进入生成中；仍在运行的任务继续恢复轮询，过期绑定不恢复。新增 busy/resume 断言 RED→GREEN，FirstFrameSelection 与 AnalysisWorkspace 63 项通过。原项目页面回归继续核验。
- CI：旧提交 55d3a77 的 Windows/Secret 成功，Linux 唯一失败为 test_generation_capacity_claim_is_atomic_across_oral_and_generation_workers 的 BrokenBarrierError。视频 worker 先持有容量行锁，在 read_runtime_limits 测试屏障等待；口播 worker 隔离阶段在等待同一行锁，造成测试自身死锁。仅把混合并发屏障移至两个连接建立后、进入 worker 前；不改生产锁、不加长等待、不删除容量/账务断言。整个 test_postgres_migrations.py 的 24 项通过。

本轮前一完整门为前端 1449、后端四物理 PG 2154 passed/1 原有 skip；R43—R45 后再次执行最终代码的完整静态与 PG 门，结果回填后才提交。无新供应商生成及客户积分消费。


## 2026-09-15 06:47：R34—R45 当前提交验收记录

完整静态门通过：100 个前端测试文件、1450 项测试；秘密扫描、TypeScript/Biome、E2E lint、Tauri fmt/check、Ruff（344 文件格式）及 Mypy（147 源文件）通过。四个物理隔离 PostgreSQL 分片全部 exit 0：487 + 516 + 612 + 540 = 2155 passed，1 项既有 skipped；不把跳过计为通过。

真实前端继续操作：原项目恢复后角色参考和已确认候选可见，不再显示数万秒的首帧加载；点击“编译视频生成提示词”，模板升级为 h3.prompt.v6，原人物衬衫描述从生成提示词移除，动作、运镜、场景及台词保留。随后点击“锁定 Prompt”，显示 LOCKED、就绪 3/3。未点击付费创建。06:47 数据库只读核对余额 781、冻结 0，最新计费仍为 9 月 14 日 23:20 的 H3 任务，素材仍为已归档 b1676a5c；编译和锁定无新增扣费。

本机原始证据：86-workspace-restored.txt、87-prompt-v6-locked.txt、88-prompt-v6-locked.json。77—80 早期 AX 导出仅包含差异占位，不能作为完整页面证据；相关操作观察以本轮记录为准。81—85 已重新保存完整 AX。

代码自检覆盖归档前后权限与会话 fencing、并发幂等、不重复计费、独立任务空项目、时间换算、提示词版本冻结及首帧恢复状态；未发现当前改动的新阻断缺陷。此为开发者自检，不冒称独立人工评审。PR 当前提交的远程三门禁仍须独立核验。

整体验收仍未完成：爆款 viral/cover 与 viral/prepared 权限扩展待用户确认；Hifly/Metaso 实际账单登录核对、历史 UNKNOWN 费用、音频输入口播、H3 扩展模式及新版提示词生成效果、链接成功链路和下载最终落盘尚未完成。现有 4 秒样本只证明短样本链路，不能证明原视频全长/全部旁白复现。费用累计人民币尚未闭合，继续付费测试前须先核账；保留累计 1000 积分/100 元上限。用户已授权验收完成、CI 通过后正常 PR squash 合并 main；本次先提交已验证修复，不宣称整体完成或部署。


## 2026-09-15 08:05：R46—R50 真实复测与爆款列表重设计

- R46：真实 AUDIO 口播已被供应商生成，但原归档路径 oral/results 不在既有 COS 业务目录内，任务停在 ARCHIVE_FAILED、预扣 50 未结算。成片写入改为 generation-results/oral，声音试听改为 materials/oral/voices。前端点击原任务“重试归档”后成功，不重提供应商、不重复预扣；68 项口播专项通过。
- R47：后台手动精选只更新 homepage_featured，遗漏 collection_published，导致客户普通列表返回 200/0 条。只有通过媒体/封面就绪和可见性检查的手动展示才同步发布到普通目录；取消首页展示保留目录可见性。22 项管理接口专项通过；真实重新展示后客户列表出现 1 条。
- R48（用户新增设计要求）：后台爆款表格长标题挤压其他列，时间/数字频繁换行，删除按钮过于突出。沿用黑金设计，改为四列固定信息层级、两行标题、四项互动数据对齐、归档及首页状态集中、当前页统计、独立详情展开、可聚焦滚动区和固定表头。完整标题/ID/北京时间/存储地址保留；删除移到详情且仍需原因确认。未添加假的缩略图、假统计、批量删除或额外付费动作。搜索 ID、详情展开已真实操作；4 项交互回归通过。当前截图窗口实测 1186×706，页面 scrollWidth=1186、表格宽约 813，无页面横向溢出；viewport 请求 1024 未生效，已 reset，不能据此宣称 1024 实测通过。
- R49：切换 COS 后，旧视频准备记录仍指向 local 目录；独立环境恢复目录又改变缓存范围，导致“归档就绪”但预览返回 409。预览命中持久化的历史 local 记录时，仅在非生产环境按任务 ID/原范围/尝试号重建并核验原对象，逐块校验大小及 SHA256，复制至当前 COS，再用旧状态/范围/URI/尝试号 CAS 更新指针。复制失败保留原指针及原文件；并发新状态不覆盖；生产继续拒绝 local。6 项针对性回归含根目录变化、伪造 URI、上传失败、并发更新和生产阻断。真实管理预览现加载 68.266667 秒、720×1280 视频；原本地文件保留。
- R50：非生产客户鉴权模式配置 loopback 自定义端口时，封面 URL 构建被误当桌面专属并返回 LOCAL_API_BASE_URL_INVALID/503，使列表再次失败。明确 loopback HTTP 仅可在非客户生产环境使用；不修改客户鉴权、会话或生产 HTTPS 要求。成功/外部地址/路径/生产阻断 5 项回归通过；真实客户列表恢复。

### 四端账务与费用边界

音频任务 2febd9a1-2c2b-4c2f-a0ad-5e4df4d83a82；供应商任务 nfTJAZHy2DbcV3WW7TjNyw；操作 85da73bf-5a44-41a2-bca8-5e0b5b4f1262；COS 资产 7dbaf39f-253f-435b-a037-9daf7af732b8。视频 24 秒、864×1040、4102034 字节；前端播放至 ended=true/currentTime=24。07:29:52 原预扣 50 实扣 48、释放 2，余额 733、冻结 0、累计 267；管理端与个人中心各显示同一预扣/结算/退回，总流水 46 条。没有为归档重试新增供应商任务。

飞影实际账单：9/14 500+10+82，9/15 07:17 AUDIO 新增 217，合计 809 供应商积分。只读充值报价 7500 积分/50 元对应约 5.3933 元替代购买成本，非本轮付款。秘塔实际 API 用量 40.8+5.7=46.5 积分，按其展示的 4399 元/500000 积分折算约 0.4091 元；Apilio 9/14 共 11 笔合计 0.880704 算力单位，9/15 查询无记录。供应商币种与现金支付并未完全统一，不将账户充值或历史会员购买全额算作测试成本。后续预算按 Apilio 每单位暂占 10 元，并另留约 20 元未知/小额调用余量，当前保守占用上取整 35 元（预算占用，非实际账单），仍以用户累计 100 元/1000 客户积分为上限。没有充值、支付或对外发布。

### 云权限操作依据

已获用户逐项授权：追加 viral/cover/*、viral/prepared/*；因 CAM 上限 5 版，另获本轮明确授权删除已备份非当前版本 1。完整 JSON 备份已解析验证，控制台只勾选版本 1 删除并保存。当前为版本 6（2026-09-15 07:28:53），保留版本 2—5、原五目录、Put/Get/Head/Delete；没有删除桶对象或开放匿名访问。管理端七目录连接测试通过，既有视频自动补齐封面并展示成功。

### 证据及验收范围

本机原始证据目录 outputs/business-remediation-20260914：97 策略版本 6；98 口播成功和钱包；99 管理结算；100 个人中心；101 爆款展示行；102 改版前、103 改版后、104 展开详情截图。图片使用当前页面实测，不把历史截图冒充当前验收。自动化当前专项：口播 68、管理 22、存储/爆款/管理组合 141；新增目录恢复用例 6 单独通过。前端交互 4 通过、类型和 Biome 检查通过；本次完整静态及四 PostgreSQL 分片正在执行，未完成项不记通过。

评审为开发者自检：重点检查幂等扣费、COS 目录边界、历史文件不删除、失败指针保全、并发 CAS、只读账号、删除确认及表格完整信息访问。未冒称独立评审。仍在 PR #108 同一 worktree，未合并部署；整体业务仍有 H3 新 Prompt 全长/扩展模式、链接导入成功链路、下载落盘与部分个人中心/异常分支待复测，不能宣称全部业务完成。

### 2026-09-15 08:15 提交前复核

R46—R50 完整静态门通过，前端 100 文件 / 1451 项；四个物理隔离 PostgreSQL 分片全部 exit 0，493 + 522 + 614 + 540 = 2169 passed / 1 项原有 skipped。未把跳过计为通过。代码自检未发现新增阻断问题，覆盖展示权限、迁移失败保全与并发更新、媒体地址环境边界以及详情可访问性。真实表格搜索、清空筛选、第二页（2/8、共 177 条）、返回第一页、详情及视频预览通过；客户侧视频完整播放到 68.266667 秒 ended=true，收藏/取消收藏同步，余额仍为 733、冻结 0。最终截图 106-admin-viral-final.png、数据快照 105-viral-ui-and-cloud-retest.json 保存在本机证据目录。当前改动准备更新 PR #108，远程三门禁必须按新提交重新核验。

## 2026-09-15：R51 视频号互动补采与十五秒复测

用户明确授权继续使用已配置的 TikHub API 补齐视频号字段。真实单条核对证明搜索历史值只有点赞 180，评论/分享/收藏为空；详情接口可返回四项统计。后台缺失原因是管理列表未接入既有按需补采服务，而非仅 CSS 隐藏。新增管理员“补齐本页互动”，每条独立幂等请求、串行显示进度；普通 GET/翻页不调用供应商，只读审计员不可触发。24 小时成功缓存、10 分钟失败冷却沿用；真正的 0 保留为 0，接口缺失保持 null，成功响应的缺项显示“未提供”，失败不清空旧值。列表显示最后补采时间。

补采使用已有 TikHub 视频详情 POST /api/v1/wechat_channels/v2/fetch_video_detail（raw=false）：like_count→点赞、comment_count→评论、forward_count→分享、fav_count→收藏。按官方公开契约 https://api.tikhub.io/openapi.json 核对，其价格说明为每请求 0.01 美元；不把库内配置的单价乘次数当成供应商人民币实付。保存供应商返回的精确字符串 object_id，避免64位ID经JavaScript舍入，并供后续在搜索 export_id 过期后继续获取；搜索更新仍保留该元数据。未新增用户积分收费，实际调用继续进入平台 billing_operations/billing_attempts，操作同时进入审计日志。

实际批量操作暴露管理请求默认 5 秒超时、短于上游建议 30 秒：已仅为互动补采配置 45 秒前端等待，普通管理请求超时不变；网络状态不明时同条记录在当前页面保留幂等键。新增模拟6秒响应测试先失败后通过。评审另修复供应商等待期间的锁顺序：使用同视频 PostgreSQL 事务咨询锁串行化补采，不持有视频行锁等待外部接口；采集器可同时更新视频行。并发专项通过。当前自检未发现新增阻断问题，不冒称独立人工评审。

完整静态门已通过：前端100文件/1453项，秘密扫描、类型/Biome、E2E lint、Tauri fmt/check、Ruff及Mypy通过；最终后端四物理隔离PG分片498+523+614+540=2175 passed/1原有skipped。早期静态失败为新增测试的mock返回类型过窄，已修正；不把失败或skip记为通过。表格上一提交06f55d7的远程Secret/Linux/Windows NSIS三门禁全绿；本次R51新提交另核验CI。

真实证据：原始单条接口108、失败样本定向复查111，管理列表回填点赞277/评论3/分享798/收藏279（源点赞在两次查询间变化）；第二次点击没有新增供应商调用，数据库、管理行和审计相符。现有150条视频号按25条/页持续复测，前四页分别21/20/23/13条完整，4/5/2/12条暂未获得有效统计；剩余页及冷却后的失败复测继续进行，不能把已点击补采标为全部完成。本次最多225次TikHub调用（含诊断及失败复测），按每美元暂占10元的保守预算连同既有调用及新H3合计占用上限60元；这是预算留量而非实付账单，仍遵守累计100元/1000客户积分上限。

十五秒H3：前端选择15秒、重新编译 h3.prompt.v6 并锁定，再提交单任务81368a32-2d29-41d5-b018-885d08096dd0，操作6edd2b36-229c-4da2-b14f-e02de28db198。08:18:45预扣150、08:20:12结算150；实际播放15.083333秒、768×1344、ended=true。个人中心48条流水与管理端对应，余额583/冻结0/累计417；秘塔实际新增视频153及Context-IR5.7积分，累计205.2。证据107/109/112—115已保存在本机。此处仅确认播放、造型观察及账务链路，未据此宣称全部台词和原视频逐帧不变。

关于自动转存：正式采集流程会在同一任务中搜索入库、补统计、下载/解密视频及封面并写COS，成功才发布目录；生产compose已有worker-viral。当前独立联调只启动普通生成worker，且采集关键词为0、历史两平台队列均已结束，因此历史“待转存”不会自动上传。此为当前测试环境配置/进程缺口，需受控补做采集到云归档验收；不能把开关开启、互动补采或“展示到首页”当成视频转存完成。存量媒体记录目前仅2条抖音成功，其中1条COS迁移已验证；视频号云归档尚须真实打通。

整体任务继续在PR108既有worktree内；H3其他模式、链接导入、视频号真实归档、下载落盘、部分异常分支及最终成本核对仍待完成。用户授权全部验收及当前CI通过后正常squash合并main，不部署、不充值付款、不对外发布。
