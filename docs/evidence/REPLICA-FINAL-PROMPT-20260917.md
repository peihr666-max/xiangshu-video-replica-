# REPLICA-FINAL-PROMPT-20260917 · 最终提示词与人工选图

## 任务记录（§14）

任务/工作包：BL-01—18 复刻链路调整；首帧三选一与人工验收；文生视频/参考生视频代码链路核验。
Owner / Reviewer：Codex / 01a0aae7-8864-7c41-a255-4c8f62adc5e3；执行者自检，PR 门禁与外部评审待核验，不声称独立代理评审。
分支 / 基线 SHA：fix/replica-final-prompt-20260917 / d95a8db46d1a59ae87def4a345b3800de7298643。
上游规格段落：用户确认的《视频复刻业务逻辑整改问题清单-2026-09-17》BL-01—18；后续明确取消生成图片 AI 质检，一批三张供人工选择，不满意主动再生成。
改动文件：analysis/analysis_routes、generation/h3_prompts/prompt_context/prompt_optimizer、media、first_frames/image_tasks/source_frames；三套复刻前端、共享 PromptEditor/useGenerationDrafts、API 类型及对应回归。
失败测试或回归锁定：最终编译器与普通台词保护先出现 3 项失败；禁止调用图片检查器的回归先失败后通过。旧界面测试改为确认文案、首帧和参数后合成；保留支付、失败恢复、账号隔离原断言。
实现结果：见下方业务清单。
验证命令与通过数：见验证记录；远程三门禁以 PR 当前 head 为准。
证据层级：AUTOMATED_VERIFIED（本地完整范围及失败项补验）；未做真实供应商与生产验收。
安全与可观测性：真实供应商调用零次；任务/版本/幂等回执保留，后端拒绝过期文案、分镜与参数混用；不记录真实凭据。
迁移与回滚：无数据库 migration；通过 PR revert 回滚代码。现有历史版本与质检记录保持可读。
外部授权记录：用户明确授权实现、提交 PR 并合并主分支；未授权真实付费出片、生产开关切换或部署，本任务未执行这些操作。
未测试项：真实供应商出片及质量、生产功能开关当前状态、生产部署、坐姿/站姿真实图像理解。首帧姿态由人工核对，系统仅检查源帧时间和明确的开场衔接方案。
Lore 提交 SHA：不适用；实现提交 28149973，合入 main 的集成提交 f64e1429，本地验证记录 74c11051；[PR #130](https://github.com/peihr666-max/xiangshu-video-replica-/pull/130)。当前为 REVIEW，远程门禁与合并回执以该 PR 为准。

## 最终业务顺序

上传/校验 → 原片事实拆解及初始分镜原子落库 → 文案确认或无口播、首帧制作与确认 → 时长/画幅 → 显式合成最终稿 → 正文/首帧/参数/费用核对 → 原样冻结提交。

文案和首帧可并行准备。内容配置页在未选定首帧时只展示文案与分镜准备，不展示最终提示词框；首帧置换页确认图片后返回最终合成步骤。最终合成为确定性本地服务端逻辑，不再调用一次模型。格式优化仍为可选操作，采用时保留优化任务和上下文来源。

## 问题映射

| 问题 | 实现与边界 |
| --- | --- |
| BL-01/05 | 分析规则只提取原片事实，旧分析正文不自动灌入最终编辑器；三个入口最终就绪门禁共用控件/后端。 |
| BL-02/07 | 确认稿保存独立 script 版本；空稿显式 no_narration；全文只输出一次；提交时对白必须与确认稿一致。迟到分析不覆盖人工编辑。 |
| BL-03 | 新首帧控制身份、服装、配饰和开场状态；不继承源 subject/wardrobe_pose_detail 的混合外观文本。 |
| BL-04 | 候选增加第 0 秒开场帧；选取中段源帧时必须填写明确衔接方案，替代首段动作。保持人工验图，不新增视觉 AI 检查。 |
| BL-06 | 上传回传分析任务回执；继续操作复用任务；明确重新拆解才新建。首页尚未选择用途的上传延后视觉拆解，仅取文案不自动支付视觉分析。 |
| BL-08 | Worker 同事务写入分析、初始分镜、成功回执与审计；新分析形成新版本，旧人工分镜保留；页面只恢复结果。 |
| BL-09 | 消费构图、场景、布景、灯光、摄影机运动、人物/相对运动、手部动作、全片风格、环境声、音乐；ACTION_BEAT 不新增 Shot。 |
| BL-10 | 源时长超过目标时长默认冲突，须明确选择缩放；长文案冲突不删句。 |
| BL-11 | 确认文案独立保护，普通“台词：”也识别；忽略排版空白，不接受实质增删、翻译或改写；优化候选保留迟到保护与撤销。 |
| BL-12/13 | 顶层/嵌套来源均检查最新版本；上游改变使最终稿待更新，当前人工正文保留；采用的优化回执按账号/项目/正文追溯。 |
| BL-14 | 非 Ref2VA 模式显式共享必要段落、Shot、时间戳和对白规范，和校验器一致。 |
| BL-15/16 | 三入口共用最终合成与确认控件；参数在最终合成之前；批次保存用户可见的原文，后端复查素材/版本/对白，旧工作台提前编译按钮由同一控件替代。 |
| BL-17 | 复用持久化分析、图像任务、最终版本及优化回执；提交结果未知时持久化冻结请求和幂等键，刷新后继续原请求。 |
| BL-18 | 移除无调用的首帧/源帧同步包装、失活 JSON 付费修复方法/配置、旧分析 Prompt 回填和过时质检文案。旧 preview API 保留兼容，但不再作为三入口的最终合成路径。 |

## 图片生成

- 置换首帧每批默认 3 张，前端固定 3 张并展示该批报价；接口保留 1—3 的历史兼容范围。
- 一次供应商生成批次后立即归档展示，不再调用源图/候选图 AI 质检，也不因质量不佳自动生成下一轮。
- 用户选择“满意，确认使用这张”；不满意主动“再生成一批”，旧批次和已生成图保持可查。
- 保留文件数量、格式等技术校验，以及供应商提交不确定时的查询恢复；这些检查不会代替人工审美判断。
- 原片候选帧分析、人物库其他素材处理、最终视频质检属于不同链路；本次未笼统删除这些能力。

## 文生视频 / 参考生视频核验

两者已有独立创建接口、模式/素材校验、Worker、供应商协议构造、计费和回执恢复。`h3_extended_modes_enabled` 默认 false；能力接口返回该开关，前端据此限制 T2V/R2V。I2V 基础模式另行开放。

模拟 PostgreSQL/Worker 回归涵盖开关关闭拒绝、文本无素材、参考图/视频/音频角色与数量、持久化请求、幂等及失败结算。不能据此宣称生产已上线或真实供应商能够正常出片。上线前仍需读取目标环境开关，并在获准预算内执行真实 T2V/R2V 验收。

## 验证记录

- 后端唯一一次全量 PostgreSQL：2424 passed / 8 failed / 1 既有 skipped（2741.75 秒）；8 项失败全部完成补验。两项旧链路夹具更新后与最终合成/优化/原子发布共同补验 14 passed；其余旧任务查询测试桩与 Git 元数据映射修正后 23 passed。没有并发执行第二份全量。
- 合并 main 后图像供应商与 Worker：132 passed；覆盖场景替换、默认三张、无 AI 图片质检、归档/恢复/计费及分析原子发布。此前独立视频/H3/优化等专项 209 passed；确定性最终编译纯逻辑 15 passed。
- 合并后完整前端：105 文件 / 1604 passed；AdminApp 文件因 ENOMEM 未启动，单进程补验 24 passed。总计 106 文件 / 1628 项覆盖通过，无未处理失败；新测试验证首帧确认前隐藏最终框、可先进入置换、确认后再合成。
- TypeScript、Biome、Linux/Windows Tauri fmt/check、Ruff/format/mypy 与秘密扫描通过；完整静态命令因上述 ENOMEM 中断，补验管理页并顺序补齐余下检查。没有将首次中断标为一次性全绿。
- 三入口恢复、迟到结果保护、参数冲突、空文案确认、最终正文冻结与幂等均有专项覆盖。真实模型出片质量没有用模拟测试替代。
- 原始日志：本机 `outputs/replica-final-*.log`；证据归档不含密钥或供应商付费数据。

## 并行任务与资源

启动时检查开放 PR #128 CREATION-WORKFLOW-UI 为不同任务；本分支从 main 创建。提交前发现 #128 已合入 main（1f1c1989），在本任务同一分支合入该 main：保留独立视频新布局、口播编辑、场景替换及图片比例展示；按本任务最新用户要求取消其“首帧前生成提示词”的付费入口/下一步门禁，改为选定首帧后确定性最终合成。合并后重新运行完整 Linux 静态门及受影响图像 Worker 专项。

本任务测试容器 `replica-final-pg`、`replica-final-quality`、`replica-final-static`、`replica-final-retest-pg`、`replica-final-retest` 均使用 `--rm`；两套 PG 物理隔离、使用匿名卷。复用现有质量镜像，没有新建镜像 tag。依赖/编译缓存位于 E 盘输出目录；结束前停止自己的全部容器，不触碰其他任务资源。
