# ADMIN-R01-20260913 管理请求与会话契约

任务/工作包：管理端上线梳理 R01，修复会话撤销、登录过期与错误详情。

Owner / Reviewer：Codex / 01a0988d-0a01-7ff2-9b51-515f3dd1237b；独立 Reviewer 待确定，尚未宣称评审通过。

分支 / 基线 SHA：`fix/admin-r01-request-contract-20260913` / `400bbaa3af7fd1290fad7e949b328df349ec8203`（最新 origin/main）。

远端交付：[PR #96](https://github.com/peihr666-max/xiangshu-video-replica-/pull/96)，实现提交 `d019632`。当前头部的 CI 与评审以此 PR 的 Checks/Reviews 为准；独立 Reviewer 尚待确定，未合并。

上游规格段落：AGENTS 标准工作流；任务账本 §12 ADM / §14 / §18；T09 管理会话与 CSRF、T33 会话撤销；2026-09-13 用户授权的管理端上线整改清单 R01。定价 PR #94 已合并 3f77c61，预览 PR #95 已合并 400bbaa，两者 main 三门禁均成功。

改动文件：`client/src/api.admin.ts`、`client/src/api.ts`、`client/src/api.admin.test.ts`、`client/src/AdminApp.test.tsx`；任务账本、文件映射、认领登记、本证据与索引。

失败测试或回归锁定：适配器 RED 为 5 failed / 15 passed，分别复现撤销缺失 CSRF、无 CSRF 仍发送撤销、受保护读/写 401 未通知过期、字符串 detail 被丢弃。自检另用两套适配器复现旧请求迟到的 401 清除新登录，RED 2 failed / 22 passed。登录/恢复凭据失败和匿名恢复负向合同原已通过。

实现结果：撤销复用 adminWrite，保留 session_epoch、confirm、reason、调用者幂等键和 request_id；两套管理请求共用内存清理与过期事件，受保护请求 401 退出管理界面，并按发起时的会话核对归属，旧请求不会清除新登录；认证/初始会话恢复失败交由登录表单处理；保留字符串和结构化业务错误及 HTTP 状态。

验证命令与通过数：最终 `vitest run src/api.admin.test.ts src/AdminApp.test.tsx src/admin --maxWorkers 2` 为 28 文件 / 186 passed；管理壳测试实际调用适配器收到 401，确认回到登录页。`npm run check --workspace client` 为 Biome、TypeScript 和 94 文件 / 1399 passed、exit 0。完整 Linux 等价门禁 `npm run check` 通过（exit 0）：前端 1399、后端 1952 passed / 1 原有 TLS 环境 skip，后端耗时 1051.69 秒；Secret、Biome、TypeScript、E2E lint、Cargo fmt/check、Ruff/format、Mypy 均通过。

证据层级：AUTOMATED_VERIFIED（本地自动验证；独立评审和远端门禁待完成）。

安全与可观测性：管理 CSRF 仅驻内存，写入携带既有确认/原因/幂等合同；不改客户凭据生命周期、服务端鉴权与 session_epoch 栅栏；不记录真实凭据。错误详情以 React 文本呈现。

迁移与回滚：无数据库或配置迁移；通过后续回退 PR 撤销本任务提交，不直接修改 main。

外部授权记录：用户明确授权按独立 worktree 开发、代码评审、CI、PR 和正常合并；后明确允许复用本机已有 Git 凭据助手凭据，仅在进程内操作该仓库。真实付费、生产部署和客户消息不在本任务范围。

未测试项：真实生产会话、现场 UAT；独立评审与当前 PR CI 尚待完成；原有 TLS 环境 skip 不记为真实 TLS 链路通过。

Lore 提交 SHA：本任务 head 与未来 squash SHA 由 PR 和共享 claim 记录，避免在提交内记录自身 SHA。

## 开工记录

- 2026-09-13 实时 fetch；GitHub API 开放 PR 为零，相关分支、worktree 和共享 claim 无重复 R01；相关源任务已完成。
- 本机 claim：Git common dir 下 `codex-task-claims/ADMIN-R01-20260913/claim.json`；worktree 为仓库同级 `.worktrees/ADMIN-R01-20260913`。
- 独立资源：`admin-r01-quality-20260913`、`admin-r01-pg-20260913`、网络 `admin-r01-20260913`；无宿主 PG 端口，无生产依赖。
- 日志归档：仓库上级 `outputs/admin-prelaunch-20260913/`。未占用其他任务端口、PG 实例或工作目录。

## 代码自检与测试环境修正

自检核对两套请求适配器、管理壳及后端会话撤销路由：Cookie/CSRF 车道、请求体及幂等重试合同、错误响应体只消费一次、403 保持登录、认证失败交由表单处理、旧会话响应隔离均有覆盖。服务端既有 `test_revoke_session_keeps_device_and_replays_once` 与 `test_revoke_session_rejects_stale_epoch_and_auditor` 验证真实 PG 的撤销、审计只写一次、epoch 冲突及审计员只读。此记录是 Owner 自检，不能替代待完成的独立评审。

第一次 Linux 完整门禁快照未保留 Windows 工作目录对应的 Git 可执行位，导致 PITR 的 Git mode 合同失败；中断时 1 failed / 1002 passed / 1 原有 skip。诊断日志保留，未记作通过。快照工具已从 `git ls-files --stage` 恢复 15 个可执行位，单独复验该合同 1 passed；其后完整 `npm run check` 重跑为 1952 passed / 1 skipped、QUALITY_EXIT=0；没有修改业务测试或部署脚本以规避校验。当前 679 个代码/测试/构建文件与通过的快照逐项 SHA256 一致。
