# FIX-W15-20260912 — 第一组 W15 / ADM-08、ADM-09：上海业务日与一致的 CSV 导出

任务/工作包：第一组 W15 / ADM-08、ADM-09：上海业务日与一致的 CSV 导出。

Owner / Reviewer：Codex（01a094e2-9d59-73e3-8b2d-280723c19e7e）/ review_w12（两项 P2 修复后只读复审 PASS）。

分支 / 基线 SHA：fix/w15-admin-date-export-20260912；最新 origin/main@55220f7e5a84b06e748f98324bfed141f2e6fb94。独立 worktree 从主分支创建，未从其他任务分支派生。

上游规格段落：去重后第一部分 W15；管理端日期、订单与额度流水导出开发。实际报表交叉核对、真实浏览器和生产时区联合调试归第二部分。

改动文件：client/src/AdminApp.test.tsx、client/src/AdminApp.tsx、client/src/admin/AccountsPage.test.tsx、client/src/admin/AccountsPage.tsx、client/src/admin/OrdersPage.test.tsx、client/src/admin/OrdersPage.tsx、client/src/admin/ProfitOverview.tsx、client/src/admin/ui/vocabulary.test.ts、client/src/admin/ui/vocabulary.ts、client/src/api.ts、client/src/generated/api.ts、server/app/admin_audit_routes.py、server/app/admin_customer_routes.py、server/app/admin_dates.py、server/app/admin_profit_routes.py、server/app/control_routes.py、server/app/main.py、server/tests/test_admin_customer_routes.py、server/tests/test_admin_profit_routes.py、server/tests/test_internal_admin.py；本任务账本和证据。

失败测试或回归锁定：先锁定旧截止日期丢失当日与 CSV 未携带筛选的行为；新增真实 PG 不同时区与 5001 条用例暴露 integer COALESCE 空字符串错误。评审指出全局 CORS 覆盖局部头、售价七天限制仍用 UTC 日期，均补测试修复。

实现结果：统一日期解析，上海日期转 UTC 半开区间；显式时间戳保留兼容，旧无时区存储按 UTC 读取，查询不依赖 PG session timezone。列表和导出共用筛选；最多导出 5000 条，通过同一查询快照返回总数、实际条数和截断标志，并由全局 CORS 暴露。前端按当前列表条件导出，额度流水导出入口放到额度流水页，明确截断/未知总数提示。每日售价和管理端日期显示使用上海日历。修复 billing_round CSV 文本转换，生成类型同步两个导出接口。

验证命令与通过数：原失败回归日志和修复结果保存在工作区 outputs/remediation-20260912/W15-*.log。隔离 PG 日期/5001条/CORS/售价边界专项 8 passed；前端五文件 48 passed，tsc 通过。完整受影响 PG 套件及本地全量质量门待完成；专项计数有重叠，不累计为全量。

证据层级：CODE_PRESENT；完整 AUTOMATED_VERIFIED、远程 PR CI 与合并待验证。

安全与可观测性：没有真实密钥、用户数据或外部生产操作。vs-pg-fix-w15@15515；共享文件与 W12/W13 串行集成，特别保留售价幂等和恢复会话修复。

迁移与回滚：无数据库迁移和依赖变更；通过正常 PR revert 回滚代码，保留既有数据。

外部授权记录：用户已授权独立 worktree、每五个任务一组开发、独立只读子代理评审、提交 PR 及合并；生产和真实服务联合调试由用户团队执行。

未测试项：第二部分：真实浏览器下载、跨报表总额比对、部署与生产时区以及人工数据验收。

Lore 提交 SHA：不适用；提交、PR 和最终 squash SHA 后续回填。

## 收尾复测

完整受影响 PG 初次结果为 112 passed、1 failed、1 error；原因是新增冻结日期样本污染模块共用价格历史，以及共用库的前一测试消耗了登录限流预算。已用 finally 清除本用例合成的未来价格，按用例清理专属测试库的限流计数；没有更改生产限流。完整售价测试文件复测 12 passed。

全量前端验证补齐旧 CSV 响应 mock 的 Headers，并断言无统计头时返回 null；空筛选维持旧 URL。最新完整本地静态门通过：82 文件 1348 passed，TypeScript、Biome、e2e lint、Tauri、ruff、format、mypy 全通过（W15-static-complete.log）。独立评审对这三处补充修改再次 PASS；完整服务端质量门与当前 PR CI 待完成。

## 最终完整门禁复验

完整本地质量门通过：服务端 2879 passed、1 原有 TLS 场景跳过；前端 1350 passed；secret、Biome、TypeScript、e2e lint、Tauri fmt/check、ruff、format、mypy 均通过。服务端四个独占 PG16 分片分别为 798、616、676、789 passed，各退出码 0，覆盖清单检查通过。最终日志为 `W15-verified-shard-0.log`（第零片最终复验）及 `W15-complete-shard-1.log` 至 `-3.log` 与 `W15-main-static.log`，保存在工作区 `outputs/remediation-20260912/`。PG 使用临时内存盘，默认同步与事务配置保持开启，不是生产容量或持久性验收。

先前中断、环境或旧夹具失败记录保留，不记为通过；当前证据层级为 AUTOMATED_VERIFIED（本地）。独立评审通过，PR 当前 SHA 的 CI 和正常 squash 合并仍待完成；所有人工联合调试留第二部分。

主干整合：已合入账号 PR #79 对应 main@37a2633，独立只读复审确认客户查询、日期过滤及 CSV 查询参数/响应头完整保留。上述最终门禁均验证该整合后的 W15 代码。

环境失败处理：W15-final-shard 四片因旧任务临时文件未释放写满 2 GiB 测试盘，清理明确已完成目录后恢复；W15-complete-shard-0 中 2 failed/3 errors 来自复制环境的 editable 导入指向 `/verify/server`，CLI 使用旧源码无法识别主干新迁移。显式 PYTHONPATH 绑定当前任务 server 后，该文件 7 项通过，随后第零片完整 798 项通过。失败日志保留，不放宽生产断言或数据库事务配置。

## 与已合并 W12 的整合复验

同步 main@47c9ffb 后，保留 W12 的快照封装、旧列表兼容及 request_id 重放，以及 W15 上海七天窗口。两项幂等回归、五处稳定上海日期种子和 W15 +7/+8 边界及 finally 清理均保留，限流清理仅一份。独立只读整合复核 PASS。售价、客户查询、审计、内部查询及数据库入口五文件 130 passed，ruff/format/mypy 全通过，退出码均为 0。日志：W15-w12-integration-backend.log、W15-w12-integration-static.log。

PR 可先运行远程门禁；合并前仍须等待 W13 进入主分支，再合入其恢复权限和正常密码会话夹具并完成受影响复验。当前未合入任何未合并任务分支。全部真实人工联合调试仍由用户团队完成。

## 与已合并 W13 及账号主干的最终整合

已通过独立只读复审，完整本地门保留原实测基线；账号主干791fd66整合专项后端134/前端53和静态通过。W13已正常合并PR #84（cd8bccf），本任务合入该主干至1839c86后权限、日期、导出及幂等专项后端322/前端141和静态全部通过。远程PR #85即将更新，必须以更新后当前SHA三门禁成功为合并条件；人工联合调试仍留第二部分。

原远程提交80c2560的PR门禁已全成功，后续因真实账号主干及W13前置合入而更新，旧绿灯不替代更新提交。独立只读review_w12审查1839c86：W13恢复态只能设密、auth_method及密码会话夹具保留；W15上海半开日界、7个CSV筛选字段、统计头及W12幂等快照均无回退。唯一文字冲突为代码清单中空边对W13新增段，保留W13记录；其余双方业务增量均逐项核对。

最新验证日志：W15-w13-backend-final.log（322 passed）、W15-w13-frontend-verified.log（141 passed）、W15-w13-static-verified.log（ruff/format/mypy），全部退出0。前端门含全仓Biome和TypeScript，专项5文件；后端8文件使用独立PG及当前代码PYTHONPATH。最初W15-w13-backend-verified.log因测试命令路径缺server/前缀而收集失败、0项执行，原日志保留，不计通过。此前账号主干专项W15-uc-backend-verified.log为134 passed、W15-uc-frontend.log为53 passed，计数与本次有重叠不累加。
