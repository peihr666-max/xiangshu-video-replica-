# C4 爆款视频数据库与原位播放验证

日期：2026-09-06。工作分支：`codex/frontend-v1.4`。

## 验证范围

本轮保留数据源客户端与既有解密算法，补齐列表数据库复用、真实统计反馈、封面路径、媒体缓存与单视频播放。隔离验证快照只包含 C4 改动，不混入工作区 063 及以后迁移及素材、人物、首页会话。

## 浏览器实际证据

本地 server 8800（desktop），client 5173，使用独立冒烟数据库和已加密的服务端配置。以下为只读 DOM 媒体状态与实际页面操作证据，未把签名 URL、密钥或原始响应写入仓库。

| 操作 | 实际结果 |
| --- | --- |
| 原位启动抖音视频 | readyState=4，播放时间连续前进；视频区域 204.75×273，保持 3:4 |
| 原生暂停 | time=114.893884，paused=true；后续读取时间保持不变 |
| 继续第一条，再播放第二条 | 第一条 time=115.251215、paused=true；第二条 time=20.035309、paused=false、readyState=4 |
| 原生控件继续第一条 | 第一条 time=115.352784、paused=false；第二条 time=28.349203、paused=true |
| 视频号原位播放 | readyState=4，8.875 秒视频完整播放至结束；再次播放与暂停均成功 |
| 视频号统计 | “美德建别墅家合锦墅”样本：点赞 5572、评论 3672、转发 1208、收藏 4481；列表、详情、数据库三处一致 |
| 封面巡检 | 本轮库中 132 条封面 URL 均返回有效图片 |
| 详情页按钮与高度 | 同一条“35万全款建的140平”样本：播放按钮 79.73×41px；左侧 340×480px，右侧 828×498.30px，顶部对齐，无大面积遮罩和下方空白 |
| 新增特殊 ID 封面 | 新一轮视频号结果中含连续斜杠的 ID，封面恢复为 200 image/jpeg、45153 字节；安全缓存键持久化后，第二次读取约 4ms |

两项根因已复现并修复：原播放样式移除了容器宽高比，使绝对定位 video 的高度成为 0；抖音最低清晰度策略曾选入 ByteVC2 非浏览器编码。视频号含 `/` 的 ID 造成封面路由不匹配，含 `=` 的本地媒体对象签名曾重复编码。

## 验证门禁

- 前端 C4 独立快照：69 文件、852 测试通过；TypeScript、Biome 通过。
- 服务端专项：100 测试通过，覆盖缓存与并发去重、源字段异常、下载失败统计反馈。
- 新一轮数据暴露连续斜杠 ID 的缓存路径异常；修复后媒体和路由专项 47 项通过（包含空路径段、点段、反斜杠、NUL 回归），Ruff、格式和 mypy 通过。只为原本不能缓存的 ID 生成安全名称，已有合法缓存键不变。
- PostgreSQL 16 独立数据库实测：upsert、缺失统计合并、TTL、断开重连后持久化均通过。
- 隔离快照 `npm run check` 通过：前端 852 测试、服务端 1718 测试，以及 TypeScript、Biome、Ruff、mypy、Tauri 编译与格式门禁。详情页最后一轮样式调整后，前端 852 测试与静态门禁重新通过；后端没有再次修改。
- CI 三门禁以关联草稿 PR 的最新提交检查为准，不用本地结果替代远端状态。
- 静态凭据扫描通过；真实凭据仅保存在原有服务端加密配置。
- 061 为已发布迁移，未修改其内容。仅在 Ruff formatter 中排除该冻结文件，保留 lint 与迁移升级/回滚检查；其他迁移没有被排除。

## 简化与边界

- 详情页覆盖旧的 700px 固定/最小高度，限定播放按钮定位，避免通用 span 规则把按钮拉伸；播放预览响应式 400–480px，右侧高度按内容。
- 删除播放态 `aspect-ratio:auto`；复用 HTML video 原生控件与现有存储适配器，没有增加播放器或队列依赖。
- 记录保存在服务使用的主数据库；桌面用 SQLite，客户运行模式用 PostgreSQL，没有双写。
- 列表源结果缓存 1 小时，统计成功缓存 24 小时，失败冷却 10 分钟，媒体副本命中直接复用。缓存到期、失败重试或首次准备媒体仍可能消耗上游调用次数。
- 未返回的统计字段显示 `—`；不能以零代替未采集值。
- 本轮互斥覆盖单桌面服务进程；多实例间防重复付费尚需数据库协调，不宣称生产多实例验收完成。
- 代码可通过 Git 撤销；已应用数据库回滚前须保留视频库备份，062 的 downgrade 会删除本轮两张新表，常规应用回退应保留表与数据。


### 客户浏览器门禁修复

- CI 34041926599 的代码检查已通过（前端 852 条、服务端 1725 条通过 / 1 条跳过），安全扫描与 Windows NSIS 构建通过；客户 E2E 仍使用 V1.4 之前的“打开个人中心”入口，四条测试失败。
- E2E 改为真实“用户档案 → 设备管理 / 使用记录”导航，保留激活、设备审批、会话切换、真实本地下单与待支付不加条数断言；支付码仍使用测试拦截，无真实支付。
- 浏览器进一步复现客户“使用记录”误走内部钱包的问题。LiveWorkspacePanel 复用现有 customerSession 为钱包和充值弹窗提供同一客户会话，不改变服务端定价或权限。
- 新增回归测试先红后绿，证明仅传 customerAccount 时读取客户钱包、禁止内部钱包请求并正确带入 100 元充值预设。
- 独立 PostgreSQL 测试库的浏览器实测通过激活、双设备审批和冲突切换；钱包显示 10 元/条、100 元起充、10 元递增，100 元弹窗默认值正确。未触发真实支付网关。
- 修复后本地前端全量 853 条通过（69 文件），TypeScript、Biome 通过。后端代码未变化，完整 CI 将在新提交重新运行，最终状态以 PR 检查为准。
