# 真实业务联调逐项整改工作清单

## 2026-09-15 — 续测：单条归档与失败交互

继续既有 JOINT-RETEST 工作区和 PR；没有新建重复任务。完整账务及媒体标识保留本机。

- R72：管理端可将已有单条爆款提交到持久后台归档队列；同平台忙碌时拒绝，重复提交复用既有任务，管理员写入合同、幂等和审计保持一致。后台复用已转存媒体、补齐封面，不重新搜索整个平台，不创建客户采集计费批次，也不自动修改首页展示。单条失败不进入定时采集的自动重试。
- R73：转存提交失败原因接入确认框，保留操作原因和幂等键供重试；新打开操作清除旧错误。回归先失败后通过，覆盖原先错误被弹窗遮挡的问题。
- 修正先前 CI 中测试查询不支持 exact 参数的 TypeScript 错误；对归档入队查询补齐无返回记录时的显式失败保护。
- 当前验证：前端完整 100 文件、1482 项通过；归档/管理会话 PostgreSQL 专项 32 项通过；TypeScript、Biome、e2e lint、Tauri check、秘密扫描及 Ruff 通过。完整静态命令先在新增查询的 Mypy 检查失败，修正后单独复验 147 个源文件通过。后端完整四分片 2199 passed / 1 skipped；最后的入队空返回保护另作归档/会话专项复验。
- 真实边界：单条视频号归档尚未执行，等待外部上传/可能付费操作的具体批准。已有归档媒体成功加载元数据，点击播放器后本次内置浏览器页崩溃，不能标记完整播放通过。当前启动脚本仅运行普通生成 Worker，需独立采集 Worker 才会消费归档队列；本机已准备单次执行入口，尚未启动。
- 历史素材：仅在隔离联调库修复一条非空素材缺失时长；本机原片 SHA256/大小匹配，FFmpeg 完整解码和 FFprobe 时长均核验，保留修改前快照与审计。不伪造空占位可用性；修复后前端选择/复用仍待登录验收。
- 原有严格画幅、人物替换、历史素材、钱包偶发读取、取消恢复和供应商实际成本验收继续保留。代码自动验证不替代真实媒体验收，也不表示整体发布批准。

## 2026-09-15 — JOINT-RETEST：R52—R71技术交付与验收边界

本节仅包含技术结论。完整测试账号、任务标识、账务及供应商消费证据保留本机，不随本次PR新增外发。开发使用独立worktree与隔离PostgreSQL，未部署、充值或对外发布。当前为部分业务链路复验通过，不代表整体REAL_CHAIN_VERIFIED或PRODUCTION_GO。

| 编号 | 问题及修复 | 验证 |
|---|---|---|
| R52—R57 | 心跳刷新不再误判慢请求会话替换；导入刷新标题；浏览器使用正常HTTP下载；窄屏导航可关闭；完成状态停止等待；过滤未归档虚拟参考并在弹窗显示错误。保留撤销、切换及资产归属边界。 | 真实导入、下载、导航与任务展示复验；相关自动化通过。 |
| R58 | 视频草稿使用工作区保存，避免误调要求正文非空的文案接口。 | 前端保存与重新登录恢复通过。 |
| R59 | 未开放参考模式提供刷新开放状态入口。 | 自动化通过；管理开关翻转后的现场按钮复验待补。 |
| R60 | 允许有权限的已批准人物图及口播音频作为参考，仍要求可用存储与归属。 | 图片、视频、音频混合参考真实创建完成。 |
| R61 | 云归档使用60秒操作等待；响应不确定时只读核对，避免自动重复生成或扣费。 | 真实成片保存成功；慢响应和响应丢失回归通过。 |
| R62 | 混合素材全局编号编译为按媒体类型编号；保留原提示词快照。 | 请求映射、混合顺序及边界回归通过，真实请求已核对。 |
| R63 | 确认摘要展示当前视频提示词；无项目任务正确显示独立创作。 | 真实界面复验通过。 |
| R64 | ASR临时文件改用已有授权项目目录；失败清理保留记录并退避。 | 真实提取、文案保存及终稿确认通过；失败释放与成功结算符合账本。旧目录清理仍保留待办，不冒称完成。 |
| R65 | ASR完成/失败后刷新钱包，保持权限代际保护。 | 真实完成页直接显示最终余额；成功/失败回归通过。 |
| R66 | 选择器将已解析的云图片预览传回创作页；草稿仍保存资产ID。 | 首尾帧恢复可见；即时预览回归通过。 |
| R67 | 批次依子任务状态显示进行中，避免供应商已受理仍出现排队/取消。 | 同一在途真实批次自动恢复正确显示，无重复提交。 |
| R68 | 未命名独立任务显示提示词摘要，手动标题优先；不改写历史数据。 | 真实任务详情及列表通过，PG列表/详情及命名优先级通过。 |
| R69 | 视频上传解码探测并保存实际时长。R2V预扣前验证单段2–15秒、视频/音频各累计15秒、混合12项；前端校验已知累计时长并提供整理及明确错误提示。未知时长由后端拒绝并提示重传。 | 原12秒视频重新上传成功；与4秒成片组合被禁用生成，移除后恢复；选择器拒绝超额追加；旧素材缺时长请求未建批、未预扣。 |
| R70 | 个人中心流水改按数据库记账序号倒序，与管理端一致；历史无序号记录保留原时间/ID回退。修复同秒结算、退回因随机ID而颠倒的问题，不改写账本。 | UI先复现；PG回归先失败后32项通过，覆盖两端同序、分页及业务过滤；原账户两笔同秒流水UI复验已纠正，余额与流水条数不变。 |
| R71 | ASR完成回填时显式保留未保存文案标记，避免切换视频复刻后被空项目脚本覆盖。 | 真实UI发现正文清空；从既有转写记录恢复，无重复转写。新增跨页面集成测试先失败后通过；三文件209项、类型和Biome通过。重新登录恢复原文及正文，往返视频复刻仍保留。 |

实际业务覆盖：链接导入/AI拆解、文生视频、混合参考生视频、上传视频提取文案、首尾帧2K双条生成、完整音频数字人口播、成片播放/归档/浏览器下载、个人明细与后端账本核对。2K两条输出均完整播放，实际4.458333秒、浏览器显示1463×2560；其中本地下载文件编码1440×2560、SAR64:63、DAR4:7，尚非严格9:16。音频口播成片完整播放24秒、864×1040。已在系统默认下载目录找到两条此前由浏览器下载的测试成片（2K及15秒），文件大小与SHA256匹配归档，FFmpeg完整解码通过。

质量边界：R2V仍未满足“只替换人物、背景/构图/字幕保持不变”，不能以生成成功视为质量合格。建议严格场景保持采用人物区域跟踪/局部替换后合成原背景及原音轨；该方案当前尚未实现。没有听觉证据证明口型、台词和音色完全一致。

R69参考约束依据：[MiniMax官方模型卡](https://huggingface.co/MiniMaxAI/MiniMax-H3)。图片产品上限8保持，混合总上限独立为12。新增视频解码探测复用已有组件，不改变音频验证标志，也不伪造或批量补写历史元数据。

验证证据：前端初轮100文件1472项及类型/Biome/e2e lint/Tauri检查通过；最终工作区/创作页/状态208项、API104项通过。后端Ruff、Mypy147文件通过；本轮ASR/状态PG73项通过，R69独立创建/内容资产矩阵79项通过，补充边界后独立创建/请求模型47项通过。早期全量PG分片2176通过、1旧矩阵失败、1原有跳过；矩阵修正相关4项通过，后续按受影响专项复验，未虚报再跑全量。提交门禁以当前PR头为准。代码评审为开发者自检，未冒称独立评审。

管理端已恢复登录，重启期间遗留的配置502在刷新后恢复；新增ASR、2K与音频口播账务已与后端及个人中心逐项核对，临时H3扩展模式已通过管理UI恢复关闭。未保存文稿跨页面保持完整。

剩余工作：严格9:16输出（需校验像素比例并等比补边）；严格人物替换效果；部分历史素材可用性/时长与预览；参考详情信息；取消等异常分支；视频号归档/前端可见及少量缺失统计（已确认历史记录无媒体任务、关键词为空、缺少单条归档入口，不能用全平台重采代替）；供应商实际费用对账。曾观察素材选择后钱包读取失败，实际账本未改变，需继续核查读取链路。主分支合并仅交付已验证修复，不能当作全业务完成。

## 最新执行授权与完整业务验收范围

2026-09-14 23:19 用户要求覆盖全部业务，问题逐项修复、优化、评审和提交；采用适合项目的推荐方案并记录依据，完成后汇总全部修改。用户已进一步明确授权 PR 通过验收和 CI 后合并主分支；下文历史“不合并”记录不再代表当前合并权限。部署、真实充值付款和对外发布仍不在已授权操作中；付费测试继续受累计 1000 积分/人民币 100 元约束。

| 业务 | 必须完成的真实流程 | 当前证据与剩余工作 |
|---|---|---|
| 登录与工作区 | 登录、恢复草稿、离开返回、会话变化提示 | 已多次真实验证；保持单会话约束，不绕过认证 |
| 参考素材与拆解 | 本机上传/云端校验、视频分析、分镜、原文案 | 指定 MP4 上传及三镜头成功；链接解析入口仍需完整成功复测 |
| 文案工坊 | 原文提取、AI 改写、编辑、确认、跨页面使用 | 已生成并保存终稿；需核对复刻和口播最终使用文本 |
| 人物库 | 人物图像授权、多视图、场景形象、发布版本、选择恢复 | 既有授权分支已入主干；本轮场景及口播分身成功，补查常用管理动作 |
| 人物置换 | 源画面推荐/确认、场景选择、画幅、首帧确认 | 9:16 真实生成及四处扣费一致；其他比例合同已自动验证 |
| 视频复刻 | 已确认首帧、编译/锁定提示词、报价、生成、归档 | R34 衔接已修复；4 秒任务成功，4.458 秒媒体完整播放，R37 已归档至 COS，40 积分四端一致 |
| 视频生成 | 文生、首帧图生、首尾帧、参考生视频的可用模式 | 逐项核对 H3 实际能力和管理配置，成功输出及不可用引导均需复测 |
| 数字人口播 | 分身、声音、文案/音频输入、成片 | 9.08 秒文案口播已完整播放；补验音频输入路径及恢复 |
| 素材库 | 预览、筛选、重命名、保存/隐藏、选择复用、下载 | 已有资产流转证据；下载落盘及剩余管理动作待核对 |
| 任务中心 | 进度、失败原因、重试/取消、结果、分页 | 首帧及口播已验证；视频任务取消/失败释放/结果待验 |
| 发布准备 | 成片进入草稿、内容设置、预览、账号状态 | 口播及 H3 两份云端草稿恢复成功，文案复制实测通过；不对外发布，桌面账号连接另验 |
| 个人中心 | 余额、预扣、释放、最终消费、业务筛选 | 最新首帧四处一致；后续每笔生成继续核账 |
| 管理端 | 总览、客户、生成、成本、流水、审计、配置检查 | 口播和首帧记录已核对；历史 UNKNOWN 成本和实际供应商账单待对账 |
| 爆款库与数据看板 | 管理采集、媒体归档、发布就绪、前台精选/搜索与指标 | R14 177 条元数据尚未形成前台精选，继续定位并按现有规则补齐 |

每个流程以真实页面结果和后端依据签收，不能仅以代码测试、提交成功或配置开关代替。全量门禁在各次代码收尾执行，新增真实缺陷另补相关回归，最终当前提交 CI 通过才合并。

## 23:08 增量状态（历史记录保留）

| 编号 | 问题与整改 | 当前验收 |
|---|---|---|
| R29 | 场景形象已经确定外观，旧首帧仍混入原始人物参考和自动服饰/QC；改为仅用不可变场景形象，默认保留源画面检查并人工三图确认，取消场景模式额外 AI 质检和自动补图 | 场景外观、服装、配饰用于置换；真实 9:16 首帧已人工对照并确认 |
| R30 | 同步生图超时后重发付费请求；改为异步提交，先落供应商回执，再只读轮询；绑定用户、请求、模型、账号及执行权，恢复不重复提交 | 回执/费用/租约 PG 回归通过；真实新任务正常完成；旧未知费用仍待追加对账 |
| R31 | 供应商 b64_json 实际带 data:image/png;base64 前缀，原解析器把成功结果判为失败 | 严格允许 PNG/JPEG/WebP Data URI 并验证解码内容；真实正常任务已通过；旧任务仅只读取回付费图片、保留原失败及退款历史 |
| R32 | 提示词不足，旧首帧被输出为方图并模糊填边，部分字幕丢失 | 明确保留背景、动作、道具、字幕；单靠提示词的第二张仍有方图填边，未采用；结合显式画幅的第三张无该填边，人工确认通过本样本 |
| R33 | 首帧无画幅选择 | 前端新增跟随原视频及 9:16/16:9/1:1/3:4/4:3；接口校验、请求冻结、幂等与供应商 size 参数同步。真实 9:16 输出 1008×1792，62 秒，实扣 2 积分，个人中心 821 可用/0 待结算/179 累计 |

仍未完成整体验收：H3 真实视频输出及各可用模式、下载落盘、R14 精选媒体准备、历史未知供应商费用和实际人民币累计对账。新首帧成功不替代这些验收。详细任务、供应商回执与测试证据见同目录主证据。

依据：2026-09-14 浏览器真实业务 B01—B14、后台任务/资产/账本快照及管理页面。原报告位于本机 outputs/real-business-test-20260914/真实业务测试与整改方案.md；最初测试运行基线为 96b2cf8。整改开发从最新 origin/main 06a5333 独立创建，不把新主干已经修复的事项重复开发。

任务：BUSINESS-REMEDIATION-20260914。Owner：当前 Codex 任务；评审：代码自检及远程 CI，未冒称独立人工评审。独立分支 fix/business-remediation-20260914，worktree 同名。用户已明确授权远程推送和创建 PR（包含既有图像授权分支），不授权合并或部署。

## 逐项范围与验收

| 编号 / 优先级 | 真实问题与依据 | 开发整改内容 | 验收条件 | 当前状态 |
|---|---|---|---|---|
| R01 / P0 | B01 用户 MP4 上传 401，意图 URL 8000 与 API 代理 5173 来源不同 | 修复本地上传 URL/认证来源合同，仅对可信 API 上传携带凭证，外部 COS URL 不带业务令牌 | 同一 MP4 实际上传，字节及哈希完整；外域上传不泄露凭证；401 明确可重试 | 已修复，指定 MP4 真实上传成功，前后端合同回归通过 |
| R02 / P1 | B01 留下 0 字节资产；后台反复生成不存在对象的下载地址 | 明确上传中/失败/完成可用性，避免零字节占位被当成可播放素材；失败重试与审计行为可解释 | 失败占位不能进入分析/预览循环；重试形成有效资产，保留历史依据 | 已修复占位预览与签名拦截；新上传资产完整 |
| R03 / P0 | B09 ANALYSIS_VIDEO_URL_UNAVAILABLE，本地素材不能被云服务读取 | 复用现有媒体传输能力，提交前验证供应商可读性；明确本地/COS 前置及操作指引，不能用放宽 URL 安全校验解决 | 不满足已知前置时在计费前拒绝并提示；具备云存储条件时真实拆解有分镜 | 手动及自动入口已做前置校验；云分析成功链路待云存储配置复测 |
| R04 / P0 | B02 已生成场景单图；B03 跳转后照片失效、选择器为空 | 修复人物场景资产共享状态、按人物加载及分页选择；保留人物归属和非合成图限制 | 新场景 → 单图 → 分身入口，离开返回仍可选择；加载失败与空数据区分 | 已修复，同一授权场景已完成分身并用于口播 |
| R05 / P0 | 管理 Hifly 503；只读成功返回顶层 left，适配器读取 data.credit | 按真实响应及供应商文档兼容余额结构，明确单位和异常语义，不臆造余额 | 成功/异常结构测试；只读连接检查能正确解释；分身实际创建另验 | 已修复协议，真实分身/声音/口播均成功；管理只读连接检查已通过（88150 积分） |
| R06 / P1 | B06 二创 503，DeepSeek 未配置 | 复用配置能力，前置展示不可用原因与管理员操作提示；保留服务端拒绝 | 未配置时不给用户虚假可用承诺、不创建或扣费；配置后真实二创另验 | 实际二创已成功，保存 119 字终稿；售价缺省 0 须管理确认 |
| R07 / P1 | B12 文生视频模式关闭、H3 无账号 | 能力与 UI 可操作状态保持一致；已知关闭/未配置状态应在提交前提示 | 模式关闭不能进入无效计费；可用模式按实际配置显示 | 现有 H3 账号已配置，扩展模式仍关闭；全模式真实复测未完成 |
| R08 / P1 | B08 没有声音，口播链路无法继续 | 空状态说明创建声音的输入要求、授权及下一步；不默认克隆第三方视频声音 | 声音为空时给出可执行引导；具备已授权声音与分身后验短口播 | Windows 合法音频被误拒已修复；克隆/确认/文案口播成功；20 MB 上限已统一拦截，前端 53 项与后端边界回归通过 |
| R09 / P1 | B10 AI 评分降级；B11 首帧单人检查失败 | 保留可追溯质检原因，区分多人/无人/无效响应/服务失败；提供换帧或重试，不取消单人约束 | 解析边界测试、失败原因可解释；人工确认适用源画面后真实首帧另验 | 失败提示已明确，保持单人约束；成功首帧仍待复测 |
| R10 / P1 | 场景和 ASR 完成后侧栏余额陈旧，重新登录才更新 | 业务接受、完成、失败时更新统一钱包状态，避免响应竞态覆盖新余额 | 成功扣减、失败释放、并发刷新后余额与账本一致 | 已修复刷新，生成结算后侧栏 833 与个人中心/后端一致 |
| R11 / P1 | B14 图片实际扣 5，管理生成记录显示 0、成本为空 | 统一关联 billing_operations/attempts，区分结算/冻结/释放与未知成本 | 同一任务在生成记录、账本、成本明细一致；未知不视为 0 | 已修复读取结算及未知成本逻辑，PG 回归通过；管理页面已核对 |
| R12 / P1 | 管理流水关联任务为“—”、积分显示“秒”、审计笼统 | 补齐任务/业务关联与单位、审计可读名称；优先复用已合入主干的管理整改 | 从消费明细可追到任务，积分口径统一；记录可核查 | 已修复积分单位与来源关联，生成合同类型同步；管理页面已核对 |
| R13 / P1 | B04 解析中 401、回执 UNCERTAIN；B09 泛化为素材库错误 | 核对长任务会话与不确定回执处理；业务错误码映射为准确中文，避免对已失败业务盲目重提 | 会话变化提示准确，回执与释放可追溯，不重复扣费 | 已修复下载签名猜测会话导致重新登录 401；实际重新登录正常 |
| R14 / P2 | 177 条采集元数据但前台精选为空 | 采集、归档、封面、集合发布与前台展示状态可解释，展示失败/缺失原因和既有重试入口 | 管理可识别哪一层未就绪；满足现有发布条件后前台显示 | 媒体归档与精选前置仍未完成，当前前端为空，待管理端逐项核对 |
| R15 / P1 | 人物照片多视图上传缺少明确授权弹窗 | 沿用已完成 IMAGE-CONSENT-20260914，避免重新开同一功能实现；单独评审和提交 | 默认未勾选、取消不上传、后台拒绝缺失授权并绑定图片/用户/任务；已有本地验证证据 | 独立 PR #106 已由远程合入 main@71ded39；本任务未执行合并，整改 PR #108 仍待评审 |
| R16 / 验收 | 未得到最终可播放视频，不能签收完整链路 | 建立逐步复测记录，区分代码验证、真实供应商、配置和部署前置 | 上传 → 分镜 → 文案 → 人物/首帧 → 视频 → 预览/下载 → 发布准备，前后台明细一致 | 口播 9.08 秒已完整播放并保存发布草稿；下载文件请求成功但落盘未确认；四端整体验收未完成 |

## 开发顺序与完成标准

1. 核对最新主干差额；以 R01/R02 上传、R04 场景照片衔接、R05 Hifly 合同作为首批阻塞修复。
2. 完成 R03/R06—R09/R13 的前置与错误处理，再核对 R10—R12 账务展示和 R14 发布就绪状态。
3. 每项先建立失败测试，再修复；评审鉴权边界、任务幂等、失败退回、数据归属及历史兼容。
4. 独立端口与 PG 完成专项、真实浏览器和全量质量门；记录未测试项，不以模拟供应商测试冒充真实生成。
5. 推送独立分支并创建 PR，验证远程 Secret / Linux / Windows 门禁；不合并、不部署。图像授权分支单独提交，禁止从未合并功能分支派生新任务。

费用继续遵守累计 1000 积分 / 供应商 100 元上限。上一轮净扣 143 积分，部分供应商成本未知；在补齐可核验成本和实际配置前不盲目重复付费调用。已授权 Hifly 场景图和指定 MP4 继续作为同一业务复测输入。

本清单逐项回填代码结果、证据、测试与 PR；配置、真实素材及发布授权前置保留为明确未完成项，不用勾选代替验证。

## 本轮追加问题

- R17：新旧会话重叠时下载签名接口误踢用户；已绑定请求会话并实际复测登录成功。
- R18：资料定时刷新重建素材，使声音/视频播放器中断；已稳定身份依赖，成片跨刷新完整播放。
- R19：声音卡片控件超出卡片宽度；已允许换行，前端静态检查通过。

四项验收逐笔见[复测证据](BUSINESS-REMEDIATION-20260914.md)。累计已用 167 积分，833 可用、0 冻结；供应商实际人民币账单仍待核对，不以内部估算冒充实付费用。

- R20：任务时间显示原始 UTC、文案口播来源误写项目分镜；已修正，77 项专项通过。R03 已追加真实上传与手动拆解前置复测，无新增预扣。交付见 Draft PR #108。


## 管理端真实复测新增整改（19:18 起）

- R21 / P1：生成记录扣费列遗漏单位修正，仍显示 20 秒/5 秒；已修复为积分，耗时保持秒；6 项专项及真实页面复验通过。
- R22 / P1：总览及客户生成数遗漏口播；已补齐数量、状态、趋势和成功输出时长。真实管理页面显示成功 1/1、今日生成 1、输出 9.08 秒；专项 RED→GREEN，完整 PG 2095 passed / 1 skipped。
- R23 / P0：旧 local 配置与“素材上云”说明不一致，管理端没有启用入口；已增加明确启用按钮，保存时不夹带未提交数量草稿。85 项集成测试通过；真实点击后后端为 cos。
- R24 / P0：浏览器上传 OPTIONS 为 403 / AccessForbidden；已获授权通过腾讯云控制台新增 5199 精确来源 PUT/GET/HEAD，保留原规则和私有权限。只读预检现为 200；真实上传已通过跨域，后续云端校验传输故障见 R25。

管理端 23 条额度流水已与后端、个人中心核对；生成记录图片费用为 5、口播为 20，关联任务/资产一致。独立管理页现为 localhost:5200，原 127.0.0.1:5174 管理环境保持独立。当前累计消费仍 167、可用 833、冻结 0。

- R25 / P0：跨域修复后，两次真实上传在后端保存校验对象时连接重置，原返回 500；已改为脱敏 503 和明确重试说明，禁止成功落库及自动分析，前端已复验提示。深入对比小对象后确定为目录权限缺失：projects/ 成功，verified-uploads/ 返回 AccessDenied。用户已授权补齐该目录，策略第 5 版保留原目录及四个操作。真实前端云上传及自动拆解现已成功；76 项资产/跨实例/前置专项通过。

- R26 / P0：管理端连接测试仅验证 projects/，遗漏校验对象目录，造成配置可用假象；已改为五个业务目录分别执行上传、HEAD、GET、内容核验及删除，并返回失败目录。6 项 RED，40 项设置合同 GREEN；权限补齐后真实管理连接测试通过。

- R27 / P0：切换 COS 后首帧任务把历史本地人物图交给云适配器，导致 FIRST_FRAME_INPUT_STORAGE_UNAVAILABLE；已按已授权资产的持久 URI 读取历史本地图片，云输出仍用 COS，且正式客户生产禁止本地遗留读。补充成功读取、不同云桶拒绝、生产限制三项回归；真实重试已进入图片模型生成，最终成片尚待核验。

- R28 / P1：首帧质检单次 240 秒并重试、自动补图及中断后 30 分钟旧锁叠加，造成半小时等待。已改为首帧质检 60 秒单次、不可用时交付未质检候选供二次人工确认、检查点恢复免重复源检查、候选质检锁 3 分钟。真实旧任务接管后 61 秒交付，原结算 2 积分，前端/后端/管理端/个人中心核对 827 可用、0 冻结、173 累计；候选质量尚未验收，不冒充 H3 全流程通过。


## 2026-09-15 06:25 增量状态

2026-09-15 06:25 BUSINESS-REMEDIATION-20260914 / R34—R42：人工首帧确认与视频编译已衔接，H3 真实 4 秒任务成功（实际媒体 4.458 秒，40 积分），成片已保存至 COS 素材库并用于发布草稿。修复任务预览被轮询重置、归档入口缺失、素材复用名称陈旧、发布草稿恢复 404/缺少播放地址、详情显示 UUID、历史时间时区错误。管理端三页 43 条流水已逐页查看，余额 781/冻结 0/累计净消费 219；归档、重命名、分组、草稿及复制未新增生成费用。用户最新明确授权验收及 CI 通过后正常 PR squash 合并主分支，覆盖此前不合并授权；不部署、不真实充值、不对外发布。评审为执行者自检，未冒称独立评审。当前完整前端 1449 项通过，Ruff 与 mypy 147 文件通过，后端四个物理 PG 全量门正在执行；完整静态门剩余段和当前 PR CI 尚待核验。爆款 COS 两目录权限待确认、飞影与秘塔实际人民币账单待登录核对、H3 扩展模式及音频口播真实复测未完成，不能标全部业务验收通过。详见主证据及逐项清单。

R34 人工首帧编译、R35 管理耗时、R36 稳定预览、R37 主动素材归档、R38 复用名称、R39 草稿恢复、R41 详情名称、R42 历史时间已有真实成功证据；R40 七目录诊断代码及自动测试完成，云端两目录权限仍未确认。各项详细依据见主证据当前段。

## 2026-09-15 06:42：新增实测问题 R43—R45 与 CI 原因

- R43：真实管理生成记录把 H3 0.08 元显示成精确成本。追溯 operation_costs.record_video_generation_cost，数值实际由冻结配置单价乘用量得到；改为 ESTIMATED，页面已实测显示“估算 0.08”。顺带修复视频查询对 projects 的内连接导致无项目的独立任务计数为 1、列表为空，改为左连接与正确空值。两个 PG 断言 RED→GREEN；内容资产/管理员权限/计费专项 88 项通过。没有修改历史金额或供应商实付。
- R44：真实复刻页 Prompt 虽要求保持首帧人物，逐镜头主体却仍重复“穿浅色条纹衬衫的男子”。编译模板升级 h3.prompt.v6，逐镜头主体统一引用“已确认首帧中的人物”，不再插入原人物 subject 外观字段；动作、机位、场景和台词继续来自原结构。新增断言确认旧服饰不出现且动作/背景/台词保留，RED→GREEN，CW030 共 46 项通过。历史已提交任务/锁定快照不改写，新编译采用新模板；新付费视频效果仍须另验。
- R45：返回成熟分镜工作区时，已完成首帧仍被 resume 并标 busy，readOnly 变化触发上游加载/清空绑定，再触发首帧恢复；实测表现为“已等待 27446 秒”及多个图片预览失败。已完成结果改为普通读取并恢复预选，不进入生成中；仍在运行的任务继续恢复轮询，过期绑定不恢复。新增 busy/resume 断言 RED→GREEN，FirstFrameSelection 与 AnalysisWorkspace 63 项通过。原项目页面回归继续核验。
- CI：旧提交 55d3a77 的 Windows/Secret 成功，Linux 唯一失败为 test_generation_capacity_claim_is_atomic_across_oral_and_generation_workers 的 BrokenBarrierError。视频 worker 先持有容量行锁，在 read_runtime_limits 测试屏障等待；口播 worker 隔离阶段在等待同一行锁，造成测试自身死锁。仅把混合并发屏障移至两个连接建立后、进入 worker 前；不改生产锁、不加长等待、不删除容量/账务断言。整个 test_postgres_migrations.py 的 24 项通过。

本轮前一完整门为前端 1449、后端四物理 PG 2154 passed/1 原有 skip；R43—R45 后再次执行最终代码的完整静态与 PG 门，结果回填后才提交。无新供应商生成及客户积分消费。


## 2026-09-15 06:47：R34—R45 当前提交验收记录

完整静态门通过：100 个前端测试文件、1450 项测试；秘密扫描、TypeScript/Biome、E2E lint、Tauri fmt/check、Ruff（344 文件格式）及 Mypy（147 源文件）通过。四个物理隔离 PostgreSQL 分片全部 exit 0：487 + 516 + 612 + 540 = 2155 passed，1 项既有 skipped；不把跳过计为通过。

真实前端继续操作：原项目恢复后角色参考和已确认候选可见，不再显示数万秒的首帧加载；点击“编译视频生成提示词”，模板升级为 h3.prompt.v6，原人物衬衫描述从生成提示词移除，动作、运镜、场景及台词保留。随后点击“锁定 Prompt”，显示 LOCKED、就绪 3/3。未点击付费创建。06:47 数据库只读核对余额 781、冻结 0，最新计费仍为 9 月 14 日 23:20 的 H3 任务，素材仍为已归档 b1676a5c；编译和锁定无新增扣费。

本机原始证据：86-workspace-restored.txt、87-prompt-v6-locked.txt、88-prompt-v6-locked.json。77—80 早期 AX 导出仅包含差异占位，不能作为完整页面证据；相关操作观察以本轮记录为准。81—85 已重新保存完整 AX。

代码自检覆盖归档前后权限与会话 fencing、并发幂等、不重复计费、独立任务空项目、时间换算、提示词版本冻结及首帧恢复状态；未发现当前改动的新阻断缺陷。此为开发者自检，不冒称独立人工评审。PR 当前提交的远程三门禁仍须独立核验。

整体验收仍未完成：爆款 viral/cover 与 viral/prepared 权限扩展待用户确认；Hifly/Metaso 实际账单登录核对、历史 UNKNOWN 费用、音频输入口播、H3 扩展模式及新版提示词生成效果、链接成功链路和下载最终落盘尚未完成。现有 4 秒样本只证明短样本链路，不能证明原视频全长/全部旁白复现。费用累计人民币尚未闭合，继续付费测试前须先核账；保留累计 1000 积分/100 元上限。用户已授权验收完成、CI 通过后正常 PR squash 合并 main；本次先提交已验证修复，不宣称整体完成或部署。


## 2026-09-15 08:05：R46—R50 真实复测与爆款列表重设计

- R46：真实 AUDIO 口播已被供应商生成，但原归档路径 oral/results 不在既有 COS 业务目录内，任务停在 ARCHIVE_FAILED、预扣 50 未结算。成片写入改为 generation-results/oral，声音试听改为 materials/oral/voices。前端点击原任务“重试归档”后成功，不重提供应商、不重复预扣；68 项口播专项通过。
- R47：后台手动精选只更新 homepage_featured，遗漏 collection_published，导致客户普通列表返回 200/0 条。只有通过媒体/封面就绪和可见性检查的手动展示才同步发布到普通目录；取消首页展示保留目录可见性。22 项管理接口专项通过；真实重新展示后客户列表出现 1 条。
- R48（用户新增设计要求）：后台爆款表格长标题挤压其他列，时间/数字频繁换行，删除按钮过于突出。沿用黑金设计，改为四列固定信息层级、两行标题、四项互动数据对齐、归档及首页状态集中、当前页统计、独立详情展开、可聚焦滚动区和固定表头。完整标题/ID/北京时间/存储地址保留；删除移到详情且仍需原因确认。未添加假的缩略图、假统计、批量删除或额外付费动作。搜索 ID、详情展开已真实操作；4 项交互回归通过。当前截图窗口实测 1186×706，页面 scrollWidth=1186、表格宽约 813，无页面横向溢出；viewport 请求 1024 未生效，已 reset，不能据此宣称 1024 实测通过。
- R49：切换 COS 后，旧视频准备记录仍指向 local 目录；独立环境恢复目录又改变缓存范围，导致“归档就绪”但预览返回 409。预览命中持久化的历史 local 记录时，仅在非生产环境按任务 ID/原范围/尝试号重建并核验原对象，逐块校验大小及 SHA256，复制至当前 COS，再用旧状态/范围/URI/尝试号 CAS 更新指针。复制失败保留原指针及原文件；并发新状态不覆盖；生产继续拒绝 local。6 项针对性回归含根目录变化、伪造 URI、上传失败、并发更新和生产阻断。真实管理预览现加载 68.266667 秒、720×1280 视频；原本地文件保留。
- R50：非生产客户鉴权模式配置 loopback 自定义端口时，封面 URL 构建被误当桌面专属并返回 LOCAL_API_BASE_URL_INVALID/503，使列表再次失败。明确 loopback HTTP 仅可在非客户生产环境使用；不修改客户鉴权、会话或生产 HTTPS 要求。成功/外部地址/路径/生产阻断 5 项回归通过；真实客户列表恢复。

### 四端账务与费用边界

音频任务 2febd9a1-2c2b-4c2f-a0ad-5e4df4d83a82；供应商任务 nfTJAZHy2DbcV3WW7TjNyw；操作 85da73bf-5a44-41a2-bca8-5e0b5b4f1262；COS 资产 7dbaf39f-253f-435b-a037-9daf7af732b8。视频 24 秒、864×1040、4102034 字节；前端播放至 ended=true/currentTime=24。07:29:52 原预扣 50 实扣 48、释放 2，余额 733、冻结 0、累计 267；管理端与个人中心各显示同一预扣/结算/退回，总流水 46 条。没有为归档重试新增供应商任务。

飞影实际账单：9/14 500+10+82，9/15 07:17 AUDIO 新增 217，合计 809 供应商积分。只读充值报价 7500 积分/50 元对应约 5.3933 元替代购买成本，非本轮付款。秘塔实际 API 用量 40.8+5.7=46.5 积分，按其展示的 4399 元/500000 积分折算约 0.4091 元；Apilio 9/14 共 11 笔合计 0.880704 算力单位，9/15 查询无记录。供应商币种与现金支付并未完全统一，不将账户充值或历史会员购买全额算作测试成本。后续预算按 Apilio 每单位暂占 10 元，并另留约 20 元未知/小额调用余量，当前保守占用上取整 35 元（预算占用，非实际账单），仍以用户累计 100 元/1000 客户积分为上限。没有充值、支付或对外发布。

### 云权限操作依据

已获用户逐项授权：追加 viral/cover/*、viral/prepared/*；因 CAM 上限 5 版，另获本轮明确授权删除已备份非当前版本 1。完整 JSON 备份已解析验证，控制台只勾选版本 1 删除并保存。当前为版本 6（2026-09-15 07:28:53），保留版本 2—5、原五目录、Put/Get/Head/Delete；没有删除桶对象或开放匿名访问。管理端七目录连接测试通过，既有视频自动补齐封面并展示成功。

### 证据及验收范围

本机原始证据目录 outputs/business-remediation-20260914：97 策略版本 6；98 口播成功和钱包；99 管理结算；100 个人中心；101 爆款展示行；102 改版前、103 改版后、104 展开详情截图。图片使用当前页面实测，不把历史截图冒充当前验收。自动化当前专项：口播 68、管理 22、存储/爆款/管理组合 141；新增目录恢复用例 6 单独通过。前端交互 4 通过、类型和 Biome 检查通过；本次完整静态及四 PostgreSQL 分片正在执行，未完成项不记通过。

评审为开发者自检：重点检查幂等扣费、COS 目录边界、历史文件不删除、失败指针保全、并发 CAS、只读账号、删除确认及表格完整信息访问。未冒称独立评审。仍在 PR #108 同一 worktree，未合并部署；整体业务仍有 H3 新 Prompt 全长/扩展模式、链接导入成功链路、下载落盘与部分个人中心/异常分支待复测，不能宣称全部业务完成。

08:15 收尾：完整静态门及前端 1451 项通过；四物理 PG 分片 2169 passed / 1 原有 skipped。表格搜索、清空、2/8 页、详情、预览通过；客户视频播放完毕、收藏及取消收藏通过，余额 733 / 冻结 0。最终截图 106、数据快照 105 已保存。提交前开发者自检通过，继续等待新提交远程三门禁和上述剩余业务验收；不得据此标整体完成。

## 2026-09-15：R51 视频号互动补采与十五秒复测

用户明确授权继续使用已配置的 TikHub API 补齐视频号字段。真实单条核对证明搜索历史值只有点赞 180，评论/分享/收藏为空；详情接口可返回四项统计。后台缺失原因是管理列表未接入既有按需补采服务，而非仅 CSS 隐藏。新增管理员“补齐本页互动”，每条独立幂等请求、串行显示进度；普通 GET/翻页不调用供应商，只读审计员不可触发。24 小时成功缓存、10 分钟失败冷却沿用；真正的 0 保留为 0，接口缺失保持 null，成功响应的缺项显示“未提供”，失败不清空旧值。列表显示最后补采时间。

补采使用已有 TikHub 视频详情 POST /api/v1/wechat_channels/v2/fetch_video_detail（raw=false）：like_count→点赞、comment_count→评论、forward_count→分享、fav_count→收藏。按官方公开契约 https://api.tikhub.io/openapi.json 核对，其价格说明为每请求 0.01 美元；不把库内配置的单价乘次数当成供应商人民币实付。保存供应商返回的精确字符串 object_id，避免64位ID经JavaScript舍入，并供后续在搜索 export_id 过期后继续获取；搜索更新仍保留该元数据。未新增用户积分收费，实际调用继续进入平台 billing_operations/billing_attempts，操作同时进入审计日志。

实际批量操作暴露管理请求默认 5 秒超时、短于上游建议 30 秒：已仅为互动补采配置 45 秒前端等待，普通管理请求超时不变；网络状态不明时同条记录在当前页面保留幂等键。新增模拟6秒响应测试先失败后通过。评审另修复供应商等待期间的锁顺序：使用同视频 PostgreSQL 事务咨询锁串行化补采，不持有视频行锁等待外部接口；采集器可同时更新视频行。并发专项通过。当前自检未发现新增阻断问题，不冒称独立人工评审。

完整静态门已通过：前端100文件/1453项，秘密扫描、类型/Biome、E2E lint、Tauri fmt/check、Ruff及Mypy通过；最终后端四物理隔离PG分片498+523+614+540=2175 passed/1原有skipped。早期静态失败为新增测试的mock返回类型过窄，已修正；不把失败或skip记为通过。表格上一提交06f55d7的远程Secret/Linux/Windows NSIS三门禁全绿；本次R51新提交另核验CI。

真实证据：原始单条接口108、失败样本定向复查111，管理列表回填点赞277/评论3/分享798/收藏279（源点赞在两次查询间变化）；第二次点击没有新增供应商调用，数据库、管理行和审计相符。现有150条视频号按25条/页持续复测，前四页分别21/20/23/13条完整，4/5/2/12条暂未获得有效统计；剩余页及冷却后的失败复测继续进行，不能把已点击补采标为全部完成。本次最多225次TikHub调用（含诊断及失败复测），按每美元暂占10元的保守预算连同既有调用及新H3合计占用上限60元；这是预算留量而非实付账单，仍遵守累计100元/1000客户积分上限。

十五秒H3：前端选择15秒、重新编译 h3.prompt.v6 并锁定，再提交单任务81368a32-2d29-41d5-b018-885d08096dd0，操作6edd2b36-229c-4da2-b14f-e02de28db198。08:18:45预扣150、08:20:12结算150；实际播放15.083333秒、768×1344、ended=true。个人中心48条流水与管理端对应，余额583/冻结0/累计417；秘塔实际新增视频153及Context-IR5.7积分，累计205.2。证据107/109/112—115已保存在本机。此处仅确认播放、造型观察及账务链路，未据此宣称全部台词和原视频逐帧不变。

关于自动转存：正式采集流程会在同一任务中搜索入库、补统计、下载/解密视频及封面并写COS，成功才发布目录；生产compose已有worker-viral。当前独立联调只启动普通生成worker，且采集关键词为0、历史两平台队列均已结束，因此历史“待转存”不会自动上传。此为当前测试环境配置/进程缺口，需受控补做采集到云归档验收；不能把开关开启、互动补采或“展示到首页”当成视频转存完成。存量媒体记录目前仅2条抖音成功，其中1条COS迁移已验证；视频号云归档尚须真实打通。

整体任务继续在PR108既有worktree内；H3其他模式、链接导入、视频号真实归档、下载落盘、部分异常分支及最终成本核对仍待完成。用户授权全部验收及当前CI通过后正常squash合并main，不部署、不充值付款、不对外发布。
