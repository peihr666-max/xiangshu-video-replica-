# FIX-W13-20260912 — 管理员恢复会话最小权限

任务/工作包：第一组 W13；原评估 ADM-03、ADM-04。

Owner / Reviewer：Codex（01a094e2-9d59-73e3-8b2d-280723c19e7e）/ 独立子代理 review_w13（复审通过）。

分支 / 基线 SHA：fix/w13-admin-recovery-20260912；origin/main 55220f7e5a84b06e748f98324bfed141f2e6fb94。独立 worktree 位于仓库上级 `.worktrees/FIX-W13-20260912`；从主分支创建。

上游规格段落：上线前评估 ADM-03/04；去重后第一部分 W13。UC 客户登录由 CW-077 既有负责人开发，本任务限定后台恢复会话。

改动文件：server/app/admin_auth_routes.py；client/src/AdminApp.tsx、AdminApp.test.tsx、api.admin.ts、generated/api.ts；server/tests/pg_test_kit.py 及十二个受影响后台/设备/会话权限测试文件；本任务登记和证据。

失败测试或回归锁定：新增恢复会话路由拒绝、审计员本人设密、历史恢复会话十分钟有效期测试先红后绿。恢复期间撤销、停用、降权均拒绝改密。独立评审发现等待行锁跨过有效期时 now() 固定在事务开始，新增真实 PG 行锁测试确认旧实现失败，改为取得锁后读取 clock_timestamp() 后通过。

实现结果：exchange 会话最长十分钟，只能查询会话、退出及设置本人密码；admin/auditor 均可设置本人密码，审计员正常登录后业务写权限仍被拒绝。设置密码事务重新锁定并检查角色、会话撤销及实时有效期，然后更新密码并撤销该账号所有会话。刷新页面识别 auth_method，恢复会话继续进入密码设置页。

验证命令与通过数：隔离 PG16（15513）上十二文件回归 420 passed；补充撤销/停用/降权竞态 3 passed；独立评审修复后 test_admin_auth.py 全专项 98 passed（含真实行锁过期测试）。Linux 前端 AdminApp/api.admin 专项 39 passed、tsc 通过。完整质量门及当前 PR CI 待完成。

证据层级：CODE_PRESENT + 专项通过；完整 AUTOMATED_VERIFIED 待质量门。

安全与可观测性：恢复权限按 HTTP 方法和路由模板明确白名单；保留既有 UA 会话绑定、CSRF 和审计；不使用真实凭据。业务测试改为通过真实会话服务创建正常 password 会话，认证专项仍调用真实交换/密码路由，不覆盖授权依赖。测试资源助手只允许登记的测试库。

迁移与回滚：无需 schema 迁移。响应新增 auth_method 字段；既有 auth_method 数据直接使用，历史 exchange 会话同样受十分钟上限约束。OpenAPI 在隔离环境重新生成，只迁入本任务两个响应类型的机器生成字段，未混入主分支既有的无关类型漂移。前后端应配套发布；回退认证代码会恢复旧的过宽恢复权限。

外部授权记录：用户明确授权每五任务一组开发、独立 worktree、代码评审、PR 及合并，并授权独立只读评审子代理。未执行生产发布或真实外部服务调用。

未测试项：第二部分由用户团队完成的前后端及管理端人工联合调试、生产浏览器部署环境。

Lore 提交 SHA：不适用；Git 提交及 PR 记录为准。

## 并行交接

W12 与本任务均涉及 test_admin_profit_routes.py：合并时保留 W12 的每日售价回归和本任务的正常密码会话夹具。共享文档由当前集成人按任务追加真实状态。CW-077 客户登录在制工作区保持原状。本任务不新增迁移 head。

## 独立评审

review_w13 首轮发现一项 P2（锁等待后过期校验），已通过数据库实时钟修复并补充红绿测试；独立复审通过，无未解决必须修复项；最终质量门结论待回填。

## 最终完整门禁复验

完整本地质量门通过：服务端 2881 passed、1 原有 TLS 场景跳过；前端 1347 passed；secret、Biome、TypeScript、e2e lint、Tauri fmt/check、ruff、format、mypy 均通过。服务端四个独占 PG16 分片分别为 794、616、674、797 passed，各退出码 0，覆盖清单检查通过。最终日志为 `W13-verified-shard-0.log` 至 `-3.log` 与 `W13-main-static.log`，保存在工作区 `outputs/remediation-20260912/`。PG 使用临时内存盘，默认同步与事务配置保持开启，不是生产容量或持久性验收。

先前中断、环境或旧夹具失败记录保留，不记为通过；当前证据层级为 AUTOMATED_VERIFIED（本地）。独立评审通过，PR 当前 SHA 的 CI 和正常 squash 合并仍待完成；所有人工联合调试留第二部分。

主干整合：上述完整门验证已合入 main@37a2633 的 W13 代码。独立只读复核确认管理员 auth_method、生成类型及管理员密码测试会话均保留；客户新密码策略未覆盖管理员最少 12 位要求。测试子进程通过 PYTHONPATH 固定到当前验证副本，CLI_SOURCE_CHECK 通过；未使用缓存目录旧源码作为当前服务端证据。
