# FIX-R04-20260912 免费额度重试与刷新

来源：2026-09-12 上线前评估，R-04 / ADM-07。基线：89d0ba41beac6773a67f6342fcb9d50cfada1cea。

执行者：Codex，session 01a094e2-9d59-73e3-8b2d-280723c19e7e；独立Reviewer待分配。用户已要求依报告逐项修改。本任务为新的整改维护ID，不复用既有CW编号。

状态：CODE_PRESENT + 专项验证；本地全量质量门、独立评审、PR CI、合并与真实环境验收仍待完成。没有把旧SHA或其他任务通过结果计入本任务。

修改：冻结待确认业务意图（金额、来源单、原因、幂等键）；超时/断网/5xx/408/409/429不释放，关闭重开确认仍按原请求重放；同步锁阻止重复确认；请求不明时禁止编辑原输入。成功响应更新余额，刷新客户列表与360订单/流水/调账；列表请求代次避免旧请求覆盖刷新。

红灯：3个真实组件+真实api.admin适配器故障模型用例失败。绿灯：组件专项16 passed；后台和api.admin共25文件168 passed；Biome改动文件通过、tsc -b通过。集成模型包含真实5秒超时、网络响应丢失、503；服务端用内存幂等账本模型，非真实PG，不据此宣称真实资金验收通过。

资源：独立Vitest worktree，Node依赖只读使用已有依赖集合；不占其他任务PG。新测试文件 CustomersPage.free-grant.test.tsx 由Vitest默认发现，服务端分片不需要变更。


日志：工作区外 outputs/remediation-20260912 下 R02-/R04-前缀文件；三份原报告保持不变。完整执行表：该目录修改工作任务清单与并行方案.md。

文件：client/src/admin/CustomersPage.tsx, client/src/admin/CustomersPage.free-grant.test.tsx。

回退：撤回本任务代码修改；无schema变更。资金历史数据不随代码回退自动修正，须另行授权对账。未执行生产部署或外部付费操作。

后续：完整本地质量门→独立评审→普通PR/CI→按授权合并→main验证；未完成前不释放claim或删除worktree。
