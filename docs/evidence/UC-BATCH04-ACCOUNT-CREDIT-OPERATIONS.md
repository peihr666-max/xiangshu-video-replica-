# UC-BATCH-04 账号积分运营与旧账号兼容

日期：2026-09-13。范围 UC-16—20，包含第三批明确结转的历史余额策略。企业员工/成员权限延期。

## 已实现

- 后台客户列表和同条件 CSV 覆盖注册账号；账号积分摘要关联软件及各 Token 消费，只有脱敏凭据元数据。
- 积分赠送 FREE_GRANT、无收款补偿 CREDIT_COMPENSATION 共用账号钱包和原生管理员写契约；整数、钱包上限、幂等、原因及审计。无收款积分金额为零，不计充值收入。
- 原生管理员 Cookie 支付设置，不依赖旧控制代理。漏单核验必须读取原账号订单、向外部网关查已付款证据，复用同一个入账函数；已付回调和补发不能双加。
- 已认证旧激活账号首次设置不少于六位密码，沿用原 user_id、钱包、项目、会话与历史；现有密码和 API Key 不可使用该入口。
- 管理员版本化配置历史余额保留/比例转换，逐账号预览和确认。钱包锁、预期余额/策略版本、一次性记录、差额账本；待结算、新积分混合交易或可能补付订单拒绝自动转换。
- 前端来源标签、余额刷新、转换入口及登录设置已经连接真实 API，重新生成 OpenAPI TypeScript。

## 测试先行与自检

RED：注册账号未出现在后台、赠送归类 INTERNAL、补偿来源拒绝、原生支付及账号查账接口 404；旧账号密码和转换接口 404。实现后对应 PG 测试通过。新 UI 组件先记录缺失导入再实现。

通过真实 PG 专项与升级矩阵 78 项（uc4-matrix.log，65.48 秒）：核验补发再回调只增加一次、未支付/金额不符/核验期间撤销管理员不增加积分、普通账号/审计员禁止写、整数输入、转换保留/比例/向下取整、防重复并发、不可删除转换审计、混合/预扣/版本漂移阻止转换；CW-056 和离线历史迁移兼容。

完整前端首次在 Windows 限制 2 workers 运行：1360 passed / 1 failed，失败为 AdminApp 测试仍拦截旧支付设置地址。已更新原生地址断言并复验 AdminApp/CustomersPage 38 passed。最终完整前端重跑 89 个文件、1361 passed（07:19，88.44 秒），TypeScript/Biome/e2e lint、迁移 manifest 与含新暂存文件的秘密扫描通过。Linux/Tauri/后端完整门仍待完成；不能用前端全绿替代。

Python ruff/check 与 mypy（137 模块）曾通过；随后微调缓存头、兼容文案和大额无收款积分边界回归，最终全门仍需重验。自检修复：发放重试键绑定实际请求；账户余额及时刷新；客户不显示内部审批单号/原因；大额赠送只受积分上限限制；公共流水新增 CONVERSION 的所有消费者补齐标签。

## 数据库

新增唯一迁移 20260913T0630_account_credit_operations，父 20260912T2330_customer_credit_pricing；已发布迁移无改动。新 legacy_credit_policy 与 wallet_credit_conversions；PG 真实测量 82 表、965 列、156 FK、255 CHECK、20 触发器。指纹 072869b3947a0856e656b50833fc701135ad07d34b6216e008b15288b3a4fd3a。manifest 和各 head 常量按实测更新；离线历史导入只接受未配置默认策略和空转换表。存在转换或补偿历史时拒绝破坏性下迁。

## 真实页面与当前门禁状态

从 main@40e261c 新建本批 worktree；Vite 5184/API 18084，独立新容器 uc-batch04-preview-pg / uc_batch04_preview，不迁移其他预览数据库。浏览器已经实际验证：公共工作台 → 头像登录门 → 注册六位密码 → 主界面 → 用户中心。未出现设备管理页，Token 入口集中。

全量 Linux 静态门默认 Vitest 并发引发本机 Docker/WSL 资源拥堵，引擎持续 500，专用容器 stop 亦失败；后续页面数据超时不能计为验收通过。待获准恢复共享 Docker 后，以 VITEST_MAX_WORKERS=2 运行完整静态门和四个独立物理 PG 分片，再重验真实后台加分/客户流水/价格及页面截图。未执行全局 Docker 重启。

## §14 验收模板

任务/工作包：UC-BATCH-04 / UC-16—20。
上游规格：本会话注册/个人中心/多 Token 共用积分、后台积分及价格设置、旧账号兼容、每五任务 PR 合并要求。
改动文件：账号运营/迁移/转换路由与测试、时间戳迁移，后台及用户中心组件和类型，既有计账与充值/列表，head/离线迁移/测试分片及证据账本。
失败测试或回归锁定：见测试先行与自检记录。
实现结果：UC-16/17 代码及专项已实现；UC-18 完整套件、UC-19 完整页面、UC-20 最终门禁/PR/合并尚未完成。
验证命令与通过数：见上；未运行或未完成项不计通过。
证据层级：专项 AUTOMATED_VERIFIED；不声明整批、staging、真实付费链路或生产放行。
安全与可观测性：真实管理员会话、CSRF、写契约、原因审计、无秘密回显、钱包归属与幂等、版本防漂移。
迁移与回滚：见数据库段；历史交易不可变。
外部授权：用户允许开发/评审/PR/合并；真实付款、付费 Provider、生产 COS、公网发布未执行。
未测试项：Docker 恢复后的最终全量门与页面联调；真实商户支付和付费生成/生产部署。
提交 SHA：待完成本地收尾后登记。
