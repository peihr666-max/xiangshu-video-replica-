# UC-BATCH-04 账号积分运营与旧账号兼容

> 2026-09-13 交付状态：本地实现、自检、1910 项后端与 1362 项前端验证及主要页面联调完成。用户已明确允许向既有公开仓库 peihr666-max/xiangshu-video-replica- 推送、提交 PR 并在 CI 通过后合并；此前公开目的地审批阻断已解除。最终远程门禁和合并以本分支关联 PR 记录为准。

日期：2026-09-13。范围 UC-16—20，包含第三批结转的历史余额策略。企业成员权限延期。

## 交付结果

- 后台注册账号与旧激活账号统一列表、CSV、账号钱包及 Token 消费查账。明文 Token、密码不在后台显示。
- 赠送 FREE_GRANT 和无收款补偿 CREDIT_COMPENSATION 进入同一账号钱包，账面支付金额为零。权限、正整数、上限、原因、确认、幂等和审计沿用原生管理员契约。
- 客户详情的订单/流水使用原生管理员 Cookie 接口，修复旧代理未配置时详情 503；支付设置同样使用原生入口。全局旧控制接口的部署改造不纳入本批完成声明。
- 漏到账核验关联原账号原订单，先查外部网关，再重验管理员会话、权限、订单归属，复用统一结算。未付款、金额不符或核验期间撤权不会加分；补发后回调不重复入账。
- 旧激活账号在有效旧会话下首次设置不少于六位的密码，保留 user_id、钱包、项目、历史和原会话。API Token 和已有密码账号不能使用初始化入口。
- 后台版本化配置历史余额保留/按比例转换；逐账号预览和确认，一次性转换记录与差额 CONVERSION 流水。混合新积分交易、待结算、可补付订单、余额或策略漂移均拒绝自动转换。
- 用户中心显示赠送/补偿来源、历史转换筛选，切换页签重新读取账号余额；修复后台余额更新后重复渲染卡片，以及充值金额按钮样式覆盖。

## 基线与整合评审

从 main@40e261c 创建 feat/account-center-batch-04；实现提交 6800586，联调修复 d04fb5b。期间主分支 PR #91 合入 3492bc3，使用 463d9e9 整合。六处 modify/delete 冲突仅为本批迁移 head 断言与主分支已退休测试的冲突，保留主分支删除；按最新发现的 91 个测试文件重建四分片。没有改回旧 SQLite 运行方式或恢复内部打包代码。最终提交归属与 PR Commits 在推送和合并前分别核对。

本地自检覆盖管理员读写边界、跨账号归属、重试键、钱包锁顺序、支付幂等、价格快照、转换不可逆历史、生成类型契约、页面唯一入口与原生接口。真实页面发现的问题均有修复和专项锁定；当前无未解决的本批阻断项。远程三门禁及合并结果以本分支关联 PR 的最终提交为准，不以本地通过替代。

## 测试证据

| 检查 | 最终结果 |
| --- | --- |
| Linux npm run check:static（整合主分支后） | 通过；89 个前端文件，1362 passed；secret、Biome、TypeScript、e2e lint、cargo fmt/check、ruff/format、mypy 135 模块 |
| PostgreSQL 全量，四个物理隔离容器 | 1910 passed，1 项原有 TLS skip；91 个文件清单全覆盖 |
| 四分片计数 | 526 / 602 / 453 / 329 passed；skip 位于分片 1 |
| 最后增加的三路并发及账号迁移专项 | 30 passed（包含三路并发）；已纳入上述全量，不重复相加 |
| 旧激活账号筛选及账号专项修复 | 94 passed（整合前）；旧列表/CSV 9 个失败已修复 |
| 新增最后样式/并发文件及迁移 manifest | Biome、ruff/format、manifest check 通过 |

关键并发实测：100 初始积分 + 25 管理员赠送 + 20 支付结算 − 60 Token 消费 = 85；钱包 85/0 与流水可用/冻结增量 85/0 一致，Token 消费 60。并发使用真实 PG 独立连接；仅外部支付确认输入为本地测试数据，不发生真实收款。

红绿证据：原生客户订单/流水原先 404；重复 React key 导致同一账号出现两个查账卡片；后台加分后切换个人中心页签仍显示旧余额。分别新增失败断言后修复，最后余额测试 1 failed/8 passed → 9 passed。

失败历史保留：首次 Linux 默认并发造成 Docker/WSL 资源拥堵。用户明确允许后执行 Docker Desktop restart；恢复本批专用环境并限制 VITEST_MAX_WORKERS=2、CARGO_BUILD_JOBS=2、容器 2 CPU/2 GB。第一次完整后端发现旧激活账号列表兼容 9 个失败；修复后专项通过。主分支更新时停止旧基线复跑，整合后重新全量。新基线唯一失败来自复制容器残留的空 packaging_tools 目录，清理空目录后完整分片 0 复验 526 passed；不改测试断言。一次补验命令因清单 CRLF 未执行测试，改用逐行 strip 的清单读取后运行；未将该次空运行算作通过。一次静态命令缺 cargo PATH，补齐后整合基线完整静态门再次通过。

## 真实前后台链路

正式 Vite 5184 / API 18084，使用独立 uc-batch04-preview-pg 的 uc_batch04_preview；不是 review 页，不拦截前端业务接口，不注入假余额。重启后实际测试：

1. 公共工作台 → 头像登录门 → 六位密码注册 → 自动返回主界面 → 头像进入个人中心。
2. 默认 Token → 新建测试 Token → 更新到 V2 → 确认撤销，默认 Token 仍有效；所有操作保留同一 user_id 和钱包。列表只显示脱敏前缀。
3. 后台登录 → 注册客户详情 → 赠送 500 → 补偿 50；余额 550，两个零金额订单；详情中只有一个账号查账卡片。
4. 个人中心消费记录显示“积分赠送 +500”“积分补偿 +50”，不暴露内部来源单号/原因；页签切换后顶部余额 550。
5. 后台发布积分价格 V1（768P=3/秒、2K=7/秒、口播=11/次、1 元=100 积分），实际客户价格页一致；充值弹窗显示 100 元约 10000 积分及当前余额 550。未点击生成真实支付二维码。
6. 后台历史余额“保留原数值”策略 V0→V1，原因/确认提交成功；不会自动批量修改现有钱包。
7. 退出回公共工作台 → 再次头像登录 → 返回主界面 → 个人中心，账号及 550 积分保留；账号设置显示已有密码，客户中心无设备管理。

截图为实际 854×693 视口：用户中心流水表和充值弹窗，归档于项目上级“账号体系与用户中心方案-2026-09-12/第四批验收证据”。截图只代表捕获视口。手机窄屏、完整桌面旧会话密码迁移、真实商户支付和付费生成未在本轮浏览器实测；相关组件/PG 自动测试不是这些外部验收的替代品。

## 数据库与回滚

唯一新增迁移 20260913T0630_account_credit_operations，父 20260912T2330_customer_credit_pricing。真实 PG：82 表、965 列、156 FK、255 CHECK、20 触发器，指纹 072869b3947a0856e656b50833fc701135ad07d34b6216e008b15288b3a4fd3a。manifest、head、升级矩阵同步；已发布迁移无改动。离线历史导入仅接受默认未配置策略与空转换表。存在转换策略/转换记录/补偿历史后拒绝破坏性下迁；回滚采用兼容代码和数据库备份恢复方案，不删除历史账本。

## §14 验收模板

任务/工作包：UC-BATCH-04 / UC-16—20。
上游规格：本会话四批账号清单、注册和个人中心、多 Token 共享积分、后台积分/计价/历史余额配置。
改动文件：账号运营、密码初始化、转换路由与测试；时间戳迁移；前后台账号/支付组件、CSS 和类型；计账/充值/列表；head、离线工具、分片及账本。
失败测试或回归锁定：见测试证据，包含真实页面发现的三项回归。
实现结果：UC-16/17 功能完成；UC-18 真实 PG 全量/并发完成；UC-19 本地正式页面主要链路完成，外部及未实测场景明确列出；UC-20 本地门和自检完成，远程门禁与合并以关联 PR 为准。
验证命令与通过数：见测试表，专项不重复累计。
证据层级：AUTOMATED_VERIFIED + 本地真实页面验证；不声明 STAGING_VERIFIED、REAL_CHAIN_VERIFIED 或 PRODUCTION_GO。
安全与可观测性：管理员 Cookie/CSRF、原因、审计、幂等、账号钱包归属、Token 元数据与历史版本。
迁移与回滚：见数据库段，历史交易不可变。
外部授权：开发、PR、合并与 Docker 重启已授权；未执行真实收款、付费 Provider、生产 COS 或公网发布。
未测试项：真实商户/付费供应商/生产部署、手机视口与桌面存量凭据迁移实测。
提交和合并证据：本分支关联 PR 最终 head、三门禁记录和 squash SHA；本地验收日志与报告同步归档至项目上级第四批验收证据。
两个 Token 竞争专项另按清单精确复验：100 积分、每个预扣 80，只有一个成功，钱包剩余 20/冻结 80（1 passed，1.69 秒）。仅调整测试价格输入，未改计费实现；此用例已包含在全量计数中，不额外累计。
