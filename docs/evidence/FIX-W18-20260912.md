# FIX-W18-20260912 — 上传定稿与流式下载

任务/工作包：第一组 W18；原评估 BE-R01、BE-R02。

Owner / Reviewer：Codex（01a094e2-9d59-73e3-8b2d-280723c19e7e）/ 独立子代理 review_w12；最终复审通过，无未解决必修项。

分支 / 基线 SHA：fix/w18-storage-streaming-20260912；origin/main 55220f7e5a84b06e748f98324bfed141f2e6fb94。独立 worktree 从主分支创建。

上游规格段落：去重后第一部分 W18；实际 COS、部署下载代理和浏览器联合调试归第二部分。

改动文件：server/app/storage.py、server/app/media.py、server/app/media_routes.py、server/app/materials.py、server/app/material_routes.py、server/app/character_identity.py、server/app/upload_cleanup.py、server/tests/test_storage.py、server/tests/test_media.py、server/tests/test_character_identity_api.py、server/tests/test_cw058_content_asset_pg_matrix.py。

失败测试或回归锁定：先证明可覆盖已完成上传、HEAD与GET之间替换、声明和实际长度不符、取消下载未关闭流；清理代码存在前，新增 PG 回收用例失败。独立评审额外发现 COS release_conn、legacy video 身份、全部上传消费者、删除后回收登记以及分页游标归属，逐项修复并补回归。

实现结果：COS 上传签名绑定类型及长度（浏览器自动设置 Content-Length）；参考视频、通用素材、人物授权书/来源图按实际流量验证，在独立 verified-uploads 路径存储已验证字节，完成写入经资产锁和状态校验。已完成参考资产重试保留既有 verified URI，跨项目复用只复制内容元数据。下载最大512 MiB，按1 MiB块流式发送，支持单段Range与416，完成、错误和断开时释放上游响应及连接池。

过期回收：新增 PostgreSQL 命令 `python -m app.upload_cleanup`，默认预览，`--apply` 才修改。按上传意向到期加1小时宽限回收；现有 append-only 上传意向审计保留非签名 source URI 和 expires_at，项目/人物删除后仍能找到暂存对象与候选。清理先提交 EXPIRED 再执行存储I/O，所有完成写入拒绝 EXPIRED；任何已完成资产引用保护对象。每页最多1000个登记，每资产最多100候选，错误返回重试游标，重复执行幂等。返回 next_asset_id 非 null 时继续传 `--after-asset-id`，并原样传 next_object_asset_id / next_object_cursor 到 `--object-asset-id` / `--object-cursor`；本轮结束后下一轮从空游标开始，涵盖迟到上传与新登记。只读预览同样返回分页；不自动配置定时任务或桶生命周期。没有完整上传有效期记录的旧资产不自动删除。

验证命令与通过数：Linux/Python3.12，W18专属PG16@15518；存储和内容资产矩阵53 passed；评审问题PG专项9 passed；分页边界1 passed；早期media+storage62 passed。计数为各次专项结果，存在重叠，不相加。7个改动模块mypy、11个文件ruff通过。完整本地质量门及PR CI待完成；日志位于工作区 outputs/remediation-20260912/W18-*.log。

证据层级：CODE_PRESENT + 专项通过；完整 AUTOMATED_VERIFIED 待质量门。

安全与可观测性：不记录签名URL；回收报告只输出计数与内部分页标识；候选枚举和删除范围由代码限定验证前缀，客户端没有 verified-uploads PUT 授权。默认不删除，真实COS回收和生命周期配置由用户在第二部分人工验收。

迁移与回滚：无数据库迁移，复用现有资产元数据和不可变审计登记；不依赖其他任务的迁移head。回滚代码会恢复原上传覆盖风险；已生成对象不随代码回滚删除。

外部授权记录：用户授权分组开发、独立worktree、独立子代理评审、提交PR和合并。本次只操作隔离PG和Fake/本地测试存储，未执行真实COS删除、付费Provider或部署。

未测试项：第二部分真实浏览器COS签名头、实际对象大小/Range/中断、反向代理缓冲行为、生产桶清理预览与生命周期人工联调。

Lore提交SHA：不适用，以Git提交和PR记录为准。

## 完整质量门进展

完整静态门通过：前端 82 文件 1344 passed，TypeScript、Biome、e2e lint、Tauri、ruff、format、mypy 全部通过。临时盘容量不足的首次加速分片已中断并保留日志；扩容后完整四分片合计 2855 passed、19 failed、1 原有生产 TLS 场景跳过。

19 项失败中 17 项来自待合入主分支的固定日期夹具（独立 PR #78）；另外两项为旧测试契约：跨项目复用不应复制原上传授权元数据、COS 模拟流必须具备读取和关闭语义。已更新 `server/tests/test_media.py` 与 `server/tests/test_storage_cross_instance.py`，明确验证内容元数据保留、三项授权/回收字段剔除以及 API/Worker 两次读取后关闭。没有修改生产代码迁就测试；独立复审 PASS，两文件完整复测 67 passed。最终全绿口径仍等待主分支日期夹具合入后的复测和当前 PR CI。

## 最终完整门禁复验

完整本地质量门通过：服务端 2875 passed、1 原有 TLS 场景跳过；前端 1344 passed；secret、Biome、TypeScript、e2e lint、Tauri fmt/check、ruff、format、mypy 均通过。服务端四个独占 PG16 分片分别为 804、616、674、781 passed，各退出码 0，覆盖清单检查通过。最终日志为 `W18-complete-shard-0.log` 至 `-3.log` 与 `W18-static-final.log`，保存在工作区 `outputs/remediation-20260912/`。PG 使用临时内存盘，默认同步与事务配置保持开启，不是生产容量或持久性验收。

先前中断、环境或旧夹具失败记录保留，不记为通过；当前证据层级为 AUTOMATED_VERIFIED（本地）。独立评审通过，PR 当前 SHA 的 CI 和正常 squash 合并仍待完成；所有人工联合调试留第二部分。

主干整合复验：完整门在 main@8ae7305 基线上通过，之后合入账号主干 37a2633。独立只读评审确认媒体授权与设备会话绑定、对象验证及流式下载顺序完整保留；当前整合代码的存储、媒体、人物身份、内容资产 PG 矩阵及跨实例五个文件 141 passed，ruff/format/mypy 通过。日志为 W18-main-verified.log、W18-main-backend-static.log。首次专项因清理后缺少临时父目录失败；补齐目录准备后重跑通过，未修改生产逻辑或放宽断言。

发行入口主干整合（main@4d2e598）：已保留数据库入口 fail-closed 和钱包提示前置修复；当前受影响专项 155 passed、退出码 0，ruff/format/mypy 均通过。日志为 `W18-release-main-verified.log` 和 `W18-release-main-static.log`。独立只读复核 PASS；生产功能未因测试入口变化而放宽，所有真实环境联合调试仍留第二部分。
