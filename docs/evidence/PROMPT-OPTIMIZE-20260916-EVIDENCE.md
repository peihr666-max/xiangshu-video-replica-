# PROMPT-OPTIMIZE-20260916 · H3 拆解与可选提示词优化

## 任务记录（§14）

- 任务/工作包：H01—H15，非新增 CW 编号。
- Owner / Reviewer：Codex 当前会话 / 执行者代码自检；独立 PR 评审待安排，未冒称独立评审通过。
- 分支 / 基线 SHA：`feat/prompt-optimize-20260916` / `f0b45b2b2558487adb8981635153cc7afb5da925`。
- 上游规格段落：[实施清单及三份规范](../prompt-spec/H3提示词功能实施工作清单-2026-09-16.md)。
- 改动文件：`analysis*`、`h3_prompts`、`prompt_context`、`prompt_optimizer*`、`prompt_rules/*`、`generation`、`independent`、既有 worker/账务；共享 PromptEditor/hook、三个生成入口、模板与草稿、API 类型与专项测试。
- 失败测试或回归锁定：先将 studio/live 的隐式编译断言改为最终文本提交，4 项 RED 后 GREEN；旧异步按钮等待期间覆盖文字与撤销覆盖修改由新组件测试锁定。
- 实现结果：一次拆解返回源事实和 H3 候选；按五种模式共用规则；可选异步优化复用 worker；当前文本在已有建批事务中冻结。原同步实现被替换，未新增第二套队列。
- 验证命令与通过数：下节持续回填。
- 证据层级：AUTOMATED_VERIFIED（本地全量与修复专项完成，PR 三门禁待触发）；未进行真实付费调用。
- 安全与可观测性：查询归属、资产授权、上下文摘要、幂等键、租约 CAS、不确定状态不重发；优化科目独立，失败退客户预扣，已发生供应商成本保留。
- 迁移与回滚：追加未发布 `20260916T2000_prompt_optimization_receipts`，一张任务表与分析上下文列；旧客户端版本号请求保持兼容。代码回退需停止新任务消费；数据库降级会删除新任务记录，只能在备份及队列排空后由部署方安排。
- 外部授权记录：用户明确授权接手原会话、代码修改、评审、CI 与 PR；未授权合并、部署或真实付费探针。
- 未测试项：真实 Gemini/H3 生成效果、生产账务、L2VA 供应商能力；未进行视觉浏览器实测。
- 实现提交：`f8c037e6f06aa69fbefadf593094a096f2d1c206`；无 Lore 工具调用。
- 主线同步：开发期间 main 合入发布账号扫码 PR #123（`3e120752`）；在同一任务分支合并，仅四份账本追加记录冲突，分别保留两个任务记录。后端源码无主线增量，不重复后端全量。

## 行为与验收映射

| 规格范围 | 实现与证据 |
|---|---|
| A01—A08 拆解 | `analysis-h3.v1`、元数据覆盖/缺失时探测、单次请求、无隐式修复；事实有效但候选无效仍保存；`test_h3_prompts.py` |
| E01—E07 编辑器 | 顶部右侧图标、继续编辑、主动应用迟到结果、安全撤销、Unicode 上限；`PromptEditor.test.tsx` |
| M01—M08 模式/素材 | 真实首尾帧角色与类型内编号、用途输入、绑定变化提示、L2VA 生成门禁；规则与独立创建测试 |
| P01—P07 协议 | POST 202 / GET 归属，冻结上下文，重放/冲突/并发、过期恢复；`test_prompt_optimizer.py` |
| V01—V04 版本 | 新请求直接生成文本快照，旧版本请求可读可用；`test_customer_chain_e2e.py` 两种参数路径，模板模式、规则版本和素材角色随文本保存 |
| S01—S05 费用/权限 | 复用客户写围栏与资产权限；无默认售价；失败释放预扣，未知不重发；优化专项与既有 PG 账务矩阵 |
| G01—G02 生成 | 新 studio / 旧项目 / 旧工作区直接提交当前文本，保留首帧确认、参数限制及原幂等恢复；前端与客户链路测试 |

## 代码自检发现及处理

| 级别 | 问题 | 处理与回归 |
|---|---|---|
| P1 | 后台重新编译覆盖用户编辑 | 三个提交入口均改为当前文本；后端 XOR 契约与同事务快照；直接建批链路测试 |
| P1 | 迟到优化结果覆盖新稿、撤销丢失后续编辑 | 编辑序号/上下文/会话守卫，结果候选显式应用；组件测试 |
| P1 | 网络未知重试可能再次创建优化 | 持久化原请求/幂等键和 task id，恢复仅重放/查询原任务；组件恢复测试 |
| P1 | 对账仍使用原同步 receipt 终态，过期任务预扣无法释放 | 更新为异步终态；发送前/发送后两种过期测试 |
| P1 | 新文本请求刷新后被旧恢复校验丢弃 | 恢复记录改为文本/版本二选一；建批 5xx 重开测试 |
| P1 | 旧工作区未读取 READY H3 正文，快速换素材或清空可能丢失本地稿 | 提取共用读取函数，旧入口采用已验收分析；编辑立即持久化并保留主动清空标记；新增 4 项回归先 RED 后 GREEN |
| P1 | 旧项目预览显示原稿但提交修订稿 | 预览绑定同一当前文本；编辑完成保留当前稿 |
| P2 | 拆解规则的 ORBIT 等枚举不被解析器接受 | 同步运动枚举、生成 API 类型；规则契约测试 |
| P2 | 并发拆解请求插入冲突后可能复用其他上下文 | 在插入前及冲突恢复后均核对冻结上下文，冲突返回 409 |
| P1 | 新迁移 CRLF 与 Git 入库 LF 导致文件摘要不一致 | 只规范本任务未发布迁移为 LF，重算 manifest；核对工作区与 Git 索引字节哈希一致 |
| P2 | 旧迁移结构摘要仍按同步表计算 | 从隔离 PG 空库升级实测新 counts/digest；迁移守卫和升级矩阵 |
| P2 | 既有前端测试在异步加载/保存完成前断言 | H3 开关测试等待按钮可用；云草稿测试等待成功提示；保留原业务断言 |
| P2 | 爆款历史素材缺少实际帧率和画幅 | paid call 前探测授权源视频；失败明确提示，无猜测元数据 |

## 验证记录

- Windows 专项：新编辑器 6 项；受影响旧创作页面 154 项；最终 Linux 前端全量 104 文件 / 1584 项通过；H3 纯函数、单次请求、台词与不确定性保护 10 项通过。
- Windows 静态：TypeScript、Ruff、格式与 mypy 155 个应用文件通过；generated API 更新后补齐一个既有上传测试夹具字段。
- Linux 最终静态门通过：前端 104 文件 / 1584 项、TypeScript、secret、e2e lint、Rust fmt/check、Ruff/format、mypy 155 文件。隔离 PG 首轮全量已完成：2387 passed / 12 failed / 1 skipped，耗时 54m19s；12 项初始失败已全部修复并复验通过（82.91s），新增及关键链路 26 项全部通过（56.31s）；迁移 manifest、Ruff 与格式复验通过。既有跳过为 production TLS 分支，由纯配置测试覆盖。首轮静态门曾通过，但镜像源码覆盖保留了一份已删除的旧测试（多 12 项）；最终门改为先清除容器内旧源码、再复制当前 worktree。只以最终源码结果为验收依据。之前失败保留在本机日志，不以局部通过替代全量门禁。
- 最终日志：`linux-static-final2.log`、`backend-full.log`、`backend-retest.log`；全量只执行一轮，随后只跑失败项与新增/受影响专项。
- 本机证据目录：`outputs/h3-prompt-implementation-20260916/`（工作区父目录），含接手备份、逐轮测试日志、schema 探测和 OpenAPI。
- 测试资源：本任务 `h3-prompt-pg-20260916`、`h3-prompt-tests`、`h3-prompt-static`、`h3-prompt-retest` 均 `--rm`，无命名卷，复用原镜像；收尾清理自身容器，不保留测试数据库。

## 限制与产品边界

结构通过不等于视觉效果已经验证。模型对动作/光线等语义的准确性仍需样片验收；程序不会宣称全量语义正确。音频引用按用户说明保留，不把未传输的音轨当成已识别内容。L2VA 提示词可编辑优化，生成服务能力未验证时明确拒绝。已有显式编译/历史版本接口作为兼容路径保留，生成按钮不再调用它们。PR 交付不代表已经合并或部署。
