# 定时采集、人工精选与共享云媒体

状态：AUTOMATED_VERIFIED；本地完整静态门及全量失败修复复验完成，两位独立终审通过；PR 三门禁与合并待核验。

## 追加采集计费需求

用户要求后台统一采集按实际接口请求数量向每个客户收取爆款数据费用。已新增独立批次、客户及逐请求账单关联，保存单次价格快照；HTTP 计量可关联批次，供应商成本只记平台请求一次。经营统计支持关联供应商筛选，单客户行明确公共成本未分摊；管理页补充采集批次、确认/结果不明的调用次数与客户收费明细。数据库读取、云存储不扣分。

2026-09-14 用户确认：仅向已激活且未停用客户逐请求扣分，余额不足记录失败，不透支、不自动补扣。调度器已接通独立批次创建与单次售价/客户快照，采集 Worker 按持久化已确认请求补齐客户账单。每个 (request_id,user_id) 的扣分、资金来源和结果在同一事务提交；失败凭证阻止后续充值自动补扣。资格沿管理端现有账户状态：正常 self_register 账号已处于 ACTIVE；激活码账号须有 ACTIVE 绑定；内部无激活证据账号排除。结算复查并锁定激活码和用户行，停用与扣分互斥；已停用记 SKIPPED_INACTIVE，不在重新启用后补扣。

供应商实际调用单独持久化；每次确认请求才向名单内客户结算 1 次，搜索分页/视频号详情依真实 transport 次数，缓存命中不计费。处理中记录超过 10 分钟上限进入未知结果，保留供应商成本待补录且不向客户收费；迟到响应不能重新结算旧请求。成本仅记平台请求一次，经营统计区分供应商调用记录数、客户收费笔数与账务记录数。单客户利润不冒充分摊公共成本后的利润，批次总账提供整体利润。

新增计量、自动扣分、COMMIT 回执丢失、改写/ASR 已知成本和钱包积分单位均有先红后绿回归。最终相关后端专项 104 项通过（test_usage_billing、test_cw043_viral_import_pg、test_cw030_worker_pg_matrix、test_asr_provider），管理及钱包页面 17 项通过。专项集合与早期测试重叠，不相加。使用真实独占 PostgreSQL 和替身 HTTP，没有真实客户扣款或供应商调用。

完整 check:static 已通过：96 个前端文件 / 1418 项测试、secret、e2e lint、Rust fmt/check、Ruff/format 和 mypy 146 个源文件；空库 schema 实测并同步 manifest 与 CW056 常量：90 表、1064 列，digest `0a6489a4af081348915575cb7c3ef76a11e0199f3f5714137226ed2f6526d419`。两位独立代理终审无遗留 P1/P2。唯一一轮服务端全量已运行：2010 passed、19 failed、1 既有 TLS skip（958.51 秒）。19 条失败定位为新表名冻结清单遗漏及离线链遍历缺方言保护；修复后进一步补齐旧归档工具的 PG 专用表/列登记，保持非空目标拒绝，受影响 173 项专项全通过（158.09 秒），全部原失败关闭；未重复全量，最终当前代码全量由 PR CI 再验证，PR 及合并尚待完成，日志归档在仓库外 outputs/viral-cloud-media-20260913。

## 最终业务流程

1. 管理员在“爆款视频”运行控制中设置平台、分类、关键词和每词最大数量。最多 20 个关键词、每词 1–50 条；搜索接口实际返回不足时以返回数量为准。空关键词配置不会发起采集。
2. 管理员选择“每天”或“每周”。独立 `worker-viral` 以 PostgreSQL 时间检查到期日；首次配置启动首轮，之后每 1/7 天一轮。多个进程通过单例行锁只建立一组任务；每批冻结关键词配置。更改周期时重新计算下次执行时间，开关切换不重置周期。
3. 每个关键词的搜索结果去重写入 PG，保存标题、作者、时长、互动统计、发布时间、分类、源 ID 等首页数据。单关键词失败继续其他关键词，成功步骤记录 checkpoint。
4. 服务端转存封面；视频号详情补齐统计并取得解密参数，分块解密到临时文件，再上传管理员配置的主存储。PG 保存已验证的永久 storage_uri；不保存会过期的下载签名作为唯一播放依据。
5. 全平台批次完成才发布普通爆款列表并更新固定七日内容窗口。失败保留上轮列表，15 分钟后最多续做两次；下个周期开始新批次。不存在可用旧批次时普通列表保持空并显示采集状态。
6. 管理员在独立“爆款视频库”页面查看全部采集记录（含待转存/失败记录）、统计、云存储地址及云端预览，按平台和标题/作者/ID 搜索、分页。新记录 `homepage_featured=0`，经过人工筛选并确认才显示在首页；人工选中的已归档旧视频可以继续展示，不被定时批次替换。取消精选即时影响后续首页读取。
7. 删除采用 PG tombstone；管理列表、前台列表/收藏/详情/播放/新导入均不可见，后续 upsert 保留删除状态，不重复转存。已有项目独立副本和共享存储对象保留；每次精选/取消/删除通过管理员写契约、CSRF、原因、幂等及审计。已签发短期下载地址依原有效期到期。
8. 首页、列表、统计和点击播放读取数据库；播放对已存对象生成新签名，封面走已归档对象。缺素材返回可重试状态，不因点击偷偷调用源接口。列表不发布未转存的视频或封面。
9. “用于复刻/提取文案”从共享成品生成用户项目独立副本。跨用户、跨进程共享下载/解密结果；项目副本包含导入 attempt，迟到清理不会删除新副本。
10. 用户主动粘贴链接解析是单独业务，保留原计费契约。媒体准备失败完成失败凭证与退款，重放不重复请求。云存储和已入库列表读取不向用户扣分；后台供应商调用按上述规则记录一次平台成本，并向符合资格的客户逐项结算。

## 本聊天全部需求核对

| 需求 | 实现位置与核对结果 |
| --- | --- |
| 取消套餐收费，前端各功能逐项扣分 | 原 BILLING-20260913 已在 main；统一 usage_billing，缺科目配置不扣分 |
| 视频 768P/2K 分开按秒；数字人口播、ASR 按秒 | billing_catalog 独立科目与实际时长结算，保留受理价格快照 |
| 人物形象图、首帧、任务图片按张 | character / first_frame 按交付图片数量结算；实际重试分别记成本 |
| 视频分析、改写、抖一抖链接解析按次 | analysis / rewrite / link_resolution 成功结算，失败退回用户积分 |
| 所有涉及收费的业务接口由管理员配置成本、售价 | BillingRatesManager + billing_routes；权限/原因/版本/幂等/审计，COS/ZPay 禁止非零收费 |
| 每次请求及日、周、月、多月、年成本收入利润 | BillingEconomics + billing_reports；北京时间及任意日期范围，资金来源未知/成本未知保持待核对 |
| 视频号解密及云存储方案 | 服务端解密、流式临时文件、共享存储原件、独立项目副本；PG 存永久 storage_uri |
| 每天或每周按管理员关键词自动采集 | viral_collection + 独立 worker-viral，配置冻结、失败续做、避免重复调度 |
| 首页元数据、统计和归档视频地址保存数据库 | viral_store、viral_media_preparation；播放时从永久对象生成新签名 |
| 客户点击只读数据库/已归档视频 | viral_routes / cached_only 导入，不触发搜索或解密回源 |
| 后台专门视频库，人工筛选才上首页 | ViralVideosPage；默认未精选，人工精选/取消/删除，独立 homepageVideos |
| 后台采集按真实接口次数向客户收取费用 | 每次调用平台成本 + 每位客户逐次扣分 + 独立批次账单；不按视频条数或套餐收费 |
| 已激活且未停用，余额不足不透支、不补扣 | 批次成员快照、结算资格锁、终态失败/停用跳过凭据、重启补齐未处理成员 |
| 视频重复反馈 | 用户已确认是不同视频，误判已撤回；保留平台+源 ID 去重，不虚构额外去重缺陷 |
| 独立 worktree、评审、PR 和合并 | 独立 worktree、两位终审、本地完整门及失败修复完成；PR/合并待核验 |

系统审计补齐：客户钱包的余额/充值/流水统一为积分，充值按 points_per_yuan，消费名称使用 service_name；改写已响应但截断/格式失败仍保留一次供应商成本，ASR 文本无效但有实际时长证据时保留成本并退用户积分；项目导入 COMMIT 回执丢失时保留已发布副本。

## 部署入口

默认客户 compose 增加 `worker-viral`，普通四个生成 Worker 不认领采集任务。开发入口为 `npm run dev:viral-collection`；既有 PG/bootstrap 与生产安全检查保持生效。新增进程后连接池预算 60 ≤ 120。正常 rollout 更新采集进程；回滚停止采集进程，防止旧镜像不识别新参数，后续成功发布再恢复。

本任务只修改交付代码，没有启动真实采集服务，没有接触联合调试运行中的容器、用户数据库或真实 COS。上线后需使用已配置的主存储和数据源、启动独立采集进程并配置关键词。

## 实现与评审

- 下载与上传使用分块文件流；本机仅服务端临时文件，完成或异常即清理。媒体前缀仅缓存 128 KiB，视频最大 512 MiB；封面限制 10 MiB。
- 媒体对象按存储命名空间、处理版本及 attempt 隔离；PG 租约续期、条件更新发布、过期接管。已完成对象的认领使用读取快照 CAS，防止完成瞬间重复下载。
- 发布结果以云端 HEAD 的长度和 SHA256 校验；写数据库结果不确定时保留不可变对象，避免删掉已发布素材。不设置共享媒体自动删除策略。
- 保留既有 URL/重定向/固定 DNS 地址/SSRF 检查、Windows ffprobe 文件关闭规则。
- 独立评审逐项修复：完成瞬间重复认领、媒体异常漏退款、未准备视频进入列表、采集占用生成 Worker、关键词失败阻断后续项、暂停后新发详情请求。
- 追加需求评审修复：checkpoint 仅在事务成功后替换内存状态；首页精选独立保存到 homepageVideos，普通榜单/解析与首页异步响应不会相互覆盖；本地管理预览转换成签名 HTTP；管理列表仅含定时采集支持的抖音/视频号；隐藏视频不能误设置为首页展示。

## 验证记录

早期下载/上传接口缺失的红测 12 项；完成瞬间重复认领另有确定性红测。共享/退款/采集扩展 196 项通过，进度提交失败回归 7 项通过。新增人工精选、删除和每日/每周需求后，真实 PG 专项 75 项通过，前端管理页/关键词/首页加载 55 项通过；上述集合重叠，不相加。mypy 145 文件、TypeScript 检查通过。集成最新 main ce798f1 后，完整前端首次发现 3 个旧 fixture 断言不符：两条首页 source 缺少精选标记，一条钱包入口仍断言秒单位。只修正 fixture 与新积分口径，相关 91 项通过，后续完整静态门 1418 项全绿；未放宽首页生产过滤。本轮服务端全量及后续失败修复见上方；修复仅涉及未发布 revision、结构冻结清单和旧归档工具 PG 结构登记。没有运行历史数据迁移或环境切换。CI 结果待回填。

## §14 任务记录

```text
任务/工作包：VIRAL-CLOUD-MEDIA-20260913，定时采集、人工精选与共享云媒体
Owner / Reviewer：Codex 01a0983e-6373-7200-9655-dcb2d6298a06 / 独立只读代理 /root/viral_storage_review、/root/billing_review（均通过）
分支 / 基线 SHA：feat/viral-cloud-media-20260913 / 初始84b4d712ed55438ff7d00b9e348bef12ec1a6354，最终rebase到ce798f1ca85091261a2e3af1b517458e7fc04703
上游规格段落：本任务用户确认的云主存储、每日/每周关键词采集、人工精选/删除及按实际请求逐客户扣分要求；AGENTS 标准工作流及任务账本 §12.16
改动文件：见客户版代码开发清单 V3 的本任务文件映射及本 PR diff
失败测试或回归锁定：流式解密/上传缺失、完成认领竞争先红后绿；补 PG 发布条件、失败续采、工作进程隔离、暂停、统计更新、退款及管理端配置回归
实现结果：每日/每周采集、预解密云归档、人工精选与删除、前端仅读 PG/云成品、项目副本、逐请求批次账单及成本收入利润
验证命令与通过数：npm run check:static（1418 前端）；一次 pytest 全量（2010 passed/19 failed/1 既有 TLS skip），修复后受影响173项全通过；详见上方记录与原始日志
证据层级：AUTOMATED_VERIFIED（本地完整门及所有失败修复已验证；CI/真实链路另记）
安全与可观测性：管理写契约/原因/幂等/审计、PG fencing、失效对象不发布、无真实密钥输出；仅独占 Docker 网络和 PG16 测试库
迁移与回滚：仅追加 20260913T1600_shared_viral_media；已发布 revision 不改；无存量数据迁移或实际环境切换。回滚停止新采集进程，共享云对象与用户项目副本保留
外部授权记录：用户已授权独立 worktree、编写代码、独立评审、提交 PR 及合并；按最终每周采集要求修改。本任务未调用真实付费源、COS 或发布生产
未测试项：真实数据源响应、真实 COS 权限/吞吐、生产周期调度；自动化使用替身供应商与真实独立 PG
Lore 提交 SHA：最终见 PR head 与 squash SHA
```
