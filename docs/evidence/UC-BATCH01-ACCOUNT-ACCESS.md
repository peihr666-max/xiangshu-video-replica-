# UC 第一批：账号入口与会话（UC-01—05）

状态：本地代码评审和完整回归通过（AUTOMATED_VERIFIED），PR / CI / 合并待执行。下文早期记录保留追溯，以末尾最终验证为准。

用户授权：2026-09-12 要求从最新主分支创建 worktree，每 5 个任务一批，完成代码后评审、提交 PR、合并至主分支，再开始下一批。本批基线 `55220f7`，分支 `feat/account-center-batch-01`。CW-077 原有 WIP 迁入该批，同一 Owner，原工作目录仅用于保全。

## 已冻结规则

- 使用选定黑金分栏登录/注册页面和项目正式 `client/public/studio/brand.png`。
- 软件公开主界面复用实际工作台代码；无账号可浏览入口，业务操作和个人中心需要账号登录；成功注册自动登录，登录成功返回主界面。
- 客户密码不少于 6 位，使用 salted scrypt；不要求激活码，不向 URL、日志或 Web Storage 写密码和凭据。
- 用户追加确认：不限制设备数量，同一账号多设备同时在线，互不踢下线。每设备独立 session、epoch、lease；退出/失效只影响该会话。管理员停用仍应使所属账号不可操作。
- 账号积分共享；充值兑换比例、各模型积分价格、历史余额转换策略全部由后台设置（第三/四批实现）。订单与任务固化计价快照；不依据页面示例值执行历史转换。
- 所有 Token 共用同一账号钱包；默认 Token、多 Token 管理与用户中心属于第二批。第一批 UC-05 只交付本批会话迁移与类型，后续子迁移随所属功能交付。

## 四批排程

| 批次 | 工作项 | 主要交付 |
| --- | --- | --- |
| 01 | UC-01—05 | 规则、注册登录、真实页面门禁、独立设备会话、迁移和契约 |
| 02 | UC-06—10 | 选定用户中心、默认/多 Token、轮换、管理交互、权限 |
| 03 | UC-11—15 | 后台积分计价、Token 来源、共享钱包并发、账单、自助充值 |
| 04 | UC-16—20 | 后台加分、历史账号迁移、跨端联调、质量门及交付报告 |

每批从合并完成后的最新 main 创建；不堆叠未合并分支。单元/集成测试随功能完成，最终全面联调不代替每批检查。

## 目前证据

- 客户 6 位密码测试先失败后通过。
- 旧登录实现的新测试先失败（请求 ID、可空设备字段等），修复后账号→资料→续期→退出链路通过。
- 无限制多设备测试先失败 `DEVICE_SLOTS_FULL`，改造后通过：连续 7 台设备登录、并行有效、本机退出不影响其他设备。
- 隔离 PG 注册和登录专项 35 项通过（迁入批次前）；迁入后及后续修复仍需重新验证。
- 登录页面新增两项组件测试通过（6 位密码提交、确认密码不一致不发送请求）；完整页面和浏览器联调待完成。

## 验证与交付待办

2026-09-12 用户追加工作台视觉修正：移除右上角重复注册登录按钮、账号输入框焦点内框、上传图标对齐、纵向间距、移除解析约 60 秒提示；新增小红书链接真实解析/媒体导入范围。解析及媒体单测 RED：9 failed / 5 passed；实现后 GREEN：14 passed（仅新增专项，不代表完整导入与迁移验证通过）。迁移追加 `20260912T1910_xiaohongshu_link_import`，完整 PG 路由导入验证和 head/manifest 同步待完成。

用户同时要求个人中心融合选定效果图与现有功能。已生成并归档新版设计稿 `账号体系与用户中心方案-2026-09-12/03-用户中心-融合现有功能改版.png`（仓库外项目方案目录）；真实现有页面与代码已核对，实施仍归第二批 UC-06—10。设计示例不视为真实账号数据或实现完成证据。

迁移 manifest / head 矩阵、既有单在线测试改为新契约、类型生成、签名资源会话绑定、账号停用与重试专项、真实 API 浏览器回归、全量静态与 PG 检查、评审记录、PR / CI / squash SHA 均待补齐；不以本文件替代证据。

## 本轮更新

用户追加确认：个人中心的前端页面和后端接口一次性开发完成。第二批必须交付可真实操作的账号资料、默认/多 Token、轮换与撤销、现有钱包与充值/消费记录整合，保留通知、发布账号和设备管理；不得以静态页面、模拟余额或占位按钮作为完成。后续独立后台计价任务不能代替第二批前后端联调。

- PostgreSQL 账号/会话/设备回归：首轮 180 passed / 1 failed；失败为小红书测试缺少运行设置种子，补齐后完整解析→导入→Worker→账号项目/素材专项通过。
- 追加管理员解绑/吊销与两设备签名资源回归：先复现审计表外键错误（2 failed），修复无激活码审计后，与小红书专项合计 3 passed。另一个设备的会话和资源签名保持有效。
- Linux 静态质量门完整通过：secret、Biome、TypeScript、全部前端 1346 tests、e2e lint、cargo fmt/check、Ruff/format、mypy 129 modules。一次钱包面板时序测试在并行负载下失败，复跑完整门通过；未修改该测试或业务实现。
- 全量后端 pytest 正在运行，未标通过；CI/PR/合并尚待执行。
- 实际浏览器注册自动登录和头像跳转通过，桌面/手机视觉回归见根目录 design-qa.md。
- 当前新库结构摘要：30356748a0be0ac36a980d56f35e698a011aed2a863363faa9ab88a8a373ef92；head 20260912T1910_xiaohongshu_link_import；79 表 / 938 列，迁移清单已重新生成。

## §14 证据模板

任务/工作包：UC 第一批 UC-01—05 / CW-077。
Owner / Reviewer：当前 Codex / 本地代码自检，PR 正式评审待提交。
分支 / 基线 SHA：feat/account-center-batch-01 / 55220f7e5a84b06e748f98324bfed141f2e6fb94。
上游规格段落：用户本会话关于六位密码、公开入口、无限设备同时在线、每五项一批的明确指令优先；其余复用客户 V3 会话与写围栏规格。
改动文件：customer_auth/session/device 系列、账号入口和工作台、admin 设备统计、viral 链接/导入、media 签名绑定、ops 探针，两个追加迁移与对应测试/类型/证据。
失败测试或回归锁定：详见本文件各轮 RED/GREEN；新增多设备心跳探针先失败（误报双在线），修复为按 session 身份和同设备 epoch 检查。
实现结果：入口、账号会话及本批用户修正已实现；第二批个人中心新版尚未完成，不能提前计为交付。
验证命令与通过数：Linux npm run check:static 通过（1346 前端用例）；后端完整四分片 2872 passed / 1 skipped；跳过项为既有 production TLS 重复覆盖，详见最终验证。
证据层级：AUTOMATED_VERIFIED（本地）；未达到 STAGING_VERIFIED 或 PRODUCTION_GO。
安全与可观测性：凭据摘要存储、加密短时幂等、限流、设备独立撤销、资源签名绑定 session+epoch、运维告警适配。
迁移与回滚：仅新增未发布迁移；下迁前检查多会话、无激活码审计/会话历史及小红书数据，拒绝有损回退。
外部授权记录：用户已授权 worktree→开发→评审→PR→合并；未调用真实付费供应商、真实支付、生产 COS 或生产库。
未测试项：真实供应商和支付商户链路、生产部署；PR 三门禁与最终合并待完成。
Lore 提交 SHA：当前工作区未提交，待提交后回填。

## 最终提交前验证

- 完整后端四个物理隔离 PostgreSQL 分片：794 + 616 + 673 + 789 = **2872 passed / 1 skipped**，最终最长 319.90 秒。唯一跳过 `test_bootstrap_accepts_valid_pg_all_environments[production]`：既有 production TLS 要求由 `test_db_pg.py` 配置测试覆盖；未新增或放行 PG skip。
- 最终浏览器回归 **4 passed**：真实 Vite / FastAPI / PG，访客头像门禁、六位密码注册返回主界面、真实设备列表、三设备同时在线且退出一台不影响另外两台、真实 PENDING 充值订单不提前加分。仅外部支付二维码展示被桩替代，没有真实支付。
- CI 浏览器用例已由旧激活码单在线验收迁至注册及独立多设备契约；原激活码与配对兼容由服务端专项保留验证。
- 收尾 Ruff / format（349 files）/ mypy（129 modules）、e2e Biome、迁移 guard、秘密扫描均通过；完整 Linux static 含 1346 前端用例通过。
- 全量过程中发现并修复旧秒数时期的同账号抢占预期、运维误报、可空管理员审计；最终又发现固定发布日期越过七天过滤窗口，测试视频改为相对昨天，业务过滤规则不变；XHS 文案断言同步。对应 viral 专项 75 passed 后复跑上述完整四分片全绿。
- 运行环境修复记录：Windows 绑定卷导致旧轮次过慢，改用 Linux volume 与临时 tmpfs；补齐 CI 标准 ffmpeg 和真实基线 Git 索引后重跑。早期被中止、环境报错的轮次不计通过。
- 本地评审：核对密码错误/账号停用、加密幂等、密钥轮换、同设备恢复、跨设备签名隔离、撤销、迁移下行保护、用户真实页面。无未解决的本批阻断项。
- 测试日志归档于本任务工作区 `.verify/backend-final/`、`.verify/linux-static.log`，浏览器人工截图于 `.verify/`；日志和测试凭据不提交到仓库。
- 下一批必须从本批合并后的 main 开发完整个人中心前后端，当前旧个人中心不能视为该设计已交付。

## PR 主分支同步

PR #79 创建后检测到 main 新合入 #78（8ae7305），故工作流尚未触发。逐段保留双方文档登记，日期夹具采用主分支的相对昨天实现及新增过期/未来日期过滤回归；本任务业务代码未被覆盖。合并冲突解决后复验受影响测试，再推送触发三门禁。
