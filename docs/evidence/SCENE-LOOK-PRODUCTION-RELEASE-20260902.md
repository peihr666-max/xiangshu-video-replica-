# 场景造型结果恢复生产发布门禁

- 候选提交：`87661a141931b17fdf91576e56da4b2e2b068039`
- 候选 Git tree：`9be6fee6c1867a3ccd69800e1294bc6afa37f13c`
- 版本：`0.1.12`
- 目标环境：`https://video.zszhj.cn`

## 发布范围

- 场景造型只调用一次图片服务，不再执行 Gemini 质检或自动重生成。
- 接收供应商图片链接后下载图片，并沿现有存储适配器归档到生产云存储。
- 场景任务按人物身份恢复到“场景造型”页签，不再显示为人物基准任务。
- 仅允许可信 Apilio 输出域名使用代理假 IP 网段，不放宽其他下载地址的 SSRF 防护。

## 合并前证据

- 服务端：`1481 passed, 4 skipped`。
- 前端：`617 passed`。
- Ruff、Mypy、Biome、TypeScript、Rust 与硬编码密钥扫描通过。
- 定向场景回归验证：单次 Provider 调用、零 Gemini 质检调用、身份隔离和任务恢复均通过。

## 生产切换门禁

1. 发布文件 SHA-256 与候选 Git tree 一致。
2. 切换前备份 Compose、Web 站点和 PostgreSQL 数据库，并验证备份可读取。
3. 保持数据库迁移 head 为 `053_activation_code_archive`。
4. 依次滚动更新双 API 和四 Worker，再原子切换 Web 静态资源。
5. 线上验证 `/health`、客户入口、管理登录门禁及场景任务恢复接口。

## 回滚边界

- 失败时恢复旧 Compose、旧镜像和旧 Web 站点。
- 本次没有新增迁移；数据库 head 不发生变化。
- 保留失败发布目录、日志和数据库备份，不执行破坏性清理。

## 未覆盖风险

- 真实付费 Apilio 与生产 COS 的场景图片链路需要在生产切换后执行一次受控验收。
- Windows 安装包仍为未签名发布物，不将本次服务器部署冒充签名安装包验收。
