# FE-VIDEO-PREVIEW-20260913：统一竖屏预览并兼容横屏

## 需求与范围

用户确认所有视频预览采用竖屏风格，横屏完整显示，上下用同一画面的模糊背景填充。预览框统一 9:16，主画面 contain，按预览高度限制宽度；只改变展示，不转换文件，也不覆盖用户生成比例。范围为客户前端及共用任务结果入口，无 API、计费、迁移、依赖和 CI 修改。

## 系统排查与落实

| 入口 | 排查发现 | 落实 |
| --- | --- | --- |
| 工作台推荐/进行中任务 | 竖框海报 cover 裁切 | 共用完整海报及模糊填充 |
| 爆款列表/详情 | 卡片播放 cover，详情 contain；海报规则不同 | 同一视频组件，保留原播放/单飞/重试事件 |
| 素材库卡片/详情 | 视频与图片共用 140px 高区域 | 视频卡片单独 9:16，普通图片保留现状 |
| 参考生视频/首帧/口播分身预览 | 16:9、13:7 横框 | 视频用途图片也使用竖框及完整前景 |
| 参考素材选择器 | 通用图片高度/视频参考 16:9 | 视频资源经 Media 使用共用组件，参考行 72px 宽竖图 |
| 源帧/首帧候选 | 4:5/9:16，窄屏又覆盖为 16:9，首帧 cover | 共用完整画面，单列宽度限制 240px |
| 任务中心缩略图/详情 | 86×77、16:10 | 54px 宽竖图及高度受限的 9:16 详情 |
| 成片舞台/运维结果播放器 | aspect-ratio 与 max-height 同时限制导致实际比例变化 | 宽度由高度推算，保留自绘控制条与原生控件 |
| 发布草稿/预览/封面 | 132×96、196px 高及 78px 高 cover | 72×128 草稿、9:16 预览和封面选项 |

代码搜索确认全部原生 `<video>` 入口已归入 VideoPreview；普通人物照片、图片素材、声音、图表继续按各自用途显示。无内容的页面不能当作实际视频播放验收。

## 交互与性能自检

只保留一个视频解码/声音源，用小尺寸 canvas 复制当前帧并做 CSS 模糊，无像素读取/导出；精确 9:16 跳过背景绘制。暂停/结束/错误/卸载时取消帧回调，切换签名地址时重置背景但保留播放器节点及外部 ref。装饰性背景从无障碍树隐藏。播放失败继续交给原页面重试逻辑处理。

首次完整前端检查 1379 passed / 2 failed：新组件初版按 src 重建导致既有刷新测试持有旧节点，已修复并新增保持节点回归；既有生成测试在报价按钮仍禁用时点击，调整为等待启用，保留原提交参数断言。首次封面失败恢复专项定位旧 CSS 类，更新到实际前景元素，继续验证错误移除及 URL 更新恢复。失败日志保留，不计作通过。

## 验证与可观察证据

专项组件及内容页面 89 passed；刷新/复刻专项 122 passed。本机 Chromium 已真实播放合成 640×360、360×640 MP4，两个 280×497.775 容器、前景 contain，横屏 canvas 可见且竖屏 canvas 隐藏，暂停有效。页面截图与 DOM 尺寸、完整门禁日志归档于仓库上级 outputs/video-preview-20260913。review 页面只用于展示验证，不代表真实业务接口或付费生成验收。完整本地质量门通过：前端 93 文件 / 1384 passed，后端 1914 passed / 1 原有 TLS skip（601.66 秒），Linux secret/Biome/TypeScript/e2e lint/cargo fmt+check/Ruff/format/mypy 全通过。后端完整快照之后仅改 3 个前端布局文件，最终静态门已覆盖。PR 三门禁与合并结果以远程记录为准。

## §14 任务证据

```text
任务/工作包：FE-VIDEO-PREVIEW-20260913
Owner / Reviewer：Codex / 本轮代码自检和 PR 门禁，未冒称独立人工评审
分支 / 基线 SHA：feat/video-preview-portrait-20260913 / c28fb7c42f43a2b1eedbe97cdc317027e37644fd
上游规格段落：用户本轮视频预览排查与横屏模糊填充确认；AGENTS 标准工作流
改动文件：见客户版代码开发清单对应文件冻结
失败测试或回归锁定：组件单解码/ref/错误恢复/背景绘制/暂停卸载/竖屏跳过；首次失败及修复见上文
实现结果：9:16 完整前景 + 同源模糊背景；各类预览共用
验证命令与通过数：专项 89、122；最终完整前端 1384、后端 1914 passed / 1 原有 TLS skip、Linux 静态门通过；远程门禁以 PR Checks 为准
证据层级：AUTOMATED_VERIFIED；完整本地质量门与浏览器验证通过，未声明生产验收
安全与可观测性：装饰背景无焦点/无读屏内容，不上传媒体，不改 API 和凭据
迁移与回滚：无迁移，可通过正常 PR 回滚前端展示层
外部授权记录：本会话用户已授权新 worktree、实现/评审/PR/合并；无真实发布或收费调用
未测试项：最终 Windows 安装包与真实平台媒体链接、生产部署；不据此宣称 PRODUCTION_GO
Lore 提交 SHA：不适用；Git 提交与合并另记
```

## 最终代码自检

自检覆盖全部视频入口、横竖屏比例、装饰层焦点/读屏、单播放器与回调释放、签名链接刷新、封面失败恢复、响应式优先级和生成比例保留。浏览器复验修正口播预览宽度被通用 studio-media 覆盖的问题，并补齐首尾帧选择按钮内预览。真实成片舞台实测 223.2×396.8、播放/暂停/拖到 4 秒结尾正常；控制条始终处于背景之上。临时 MP4 及验证页面已归档后移出工作树，不进入正式产物。

窄窗口补验：独立 Chromium 标签页设置为 390×844，发布页文档宽度 375px（含滚动条差值）、9 个预览均约 9:16 且 contain，无横向溢出；恢复窗口尺寸后关闭临时验证标签。记录 narrow-publishing.json。
