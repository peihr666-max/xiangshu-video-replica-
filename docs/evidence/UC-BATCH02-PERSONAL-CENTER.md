# UC 第二批：完整个人中心前后端（UC-06—10）

状态：开发中。基线 main@37a2633，前置 PR #79 三门禁全绿且已合并。本批实现五页签个人中心、资料编辑、默认与多个 Token、更新/撤销、权限及账号停用检查，真实钱包和记录整合。设计参考为项目方案目录 03-用户中心-融合现有功能改版.png。新页面不得以静态数据或占位按钮作为验收。

Token 采用稳定逻辑组与不可覆盖的凭据版本记录；更新只撤销旧版本，保留账务历史。默认初始化、创建和轮换在会话事务内幂等完成。所有 Token 共享账号钱包；后续积分计价后台子任务仍按计划独立批次完成。

## 验证记录

待测试先行、实现、真实浏览器和规定质量门完成后逐项登记。


## 最新产品修正与实际验证

用户要求消除重复页面、个人中心不展示设备：移除账号概览，只保留 Token 管理、消费记录、充值记录、发布账号、账号设置；顶部保留账号/积分卡，充值按钮只出现一次。通知归账号设置，设备区与设备入口移除，多设备并行登录的后端能力不变。

RED → GREEN：个人中心初始接口 4 项失败后通过；权限/撤销竞态增量先 1 failed / 5 passed，修复后 6 passed；原 CW-078 与新增个人中心共 38 passed。新增界面去重先 2 failed / 3 passed，精简后 5 passed。既有工作区与个人中心上一轮合计 87 passed；收尾完整前端及全量 PostgreSQL 另行登记。

本地真实页面完成：注册六位密码→自动登录→主页→头像；默认 Token 仅首次创建，手动创建、轮换版本 2、撤销后按钮失效且另一枚有效；昵称保存并在顶部更新；消费/充值记录真实空状态、发布账号真实读取；通知偏好服务端持久化。充值弹窗使用服务端价格与积分，未调用真实支付商。桌面与手机布局见根目录 design-qa.md 及本地截图。

浏览器预览凭据使用既有内存存储，整页刷新后需重新登录；原生客户端仍使用既有 DPAPI。开发热更新期间曾触发会话失效，停止热更新重登后接口与心跳继续正常；未将临时凭据改存 localStorage。

## §14 证据模板

任务/工作包：UC-BATCH-02 / UC-06—10。
Owner / Reviewer：Codex / 本地代码自检，PR 门禁待完成。
分支 / 基线 SHA：feat/account-center-batch-02 / 37a263390b3ca03cbc1713e1f61ff9e7f165166e。
上游规格段落：本会话用户的账号门禁、积分、多 Token、无限并行设备、个人中心前后端一起完成，以及页面去重/隐藏设备指令。
改动文件：CustomerCenterPage、API 契约、StudioWorkspace/MainPages、充值积分文案，api_key_routes/service/customer_fence/recharge_routes，新增迁移及对应测试/清单。
失败测试或回归锁定：见上方 RED/GREEN；显式覆核跨账号 404、禁用账号 401、缺少 scope 403、撤销后事务内重验、默认幂等和轮换旧凭据无效。
实现结果：本批个人中心与管理接口已实现并实际联调；积分计价、计费任务 Token 来源、后台加分/迁移在后续批次实现。
验证命令与通过数：38 项后端专项、5 项新增组件通过；完整 Linux static 与四个物理隔离 PG 全量运行中。
证据层级：CODE_PRESENT + 专项 AUTOMATED_VERIFIED，完整门禁待通过；不声明 STAGING_VERIFIED 或 PRODUCTION_GO。
安全与可观测性：一次性凭据与加密短时幂等恢复；账号、Token 行锁内复验；单组单活跃版本、历史版本保留；审计不含明文或摘要。
迁移与回滚：新增 20260912T2200，79 表/942 列，指纹 dc3c17b82a60c760c5388377af4250fea3950ba0fda2b99ad2bcd612271250cb；一旦存在 Token 生命周期/归属流水拒绝有损 downgrade。
外部授权记录：用户已授权 worktree→开发→评审→PR→合并。未访问真实支付/生产 COS/生产数据库。
未测试项：真实商户支付与生产发布；后续批次业务未提前验收。
Lore 提交 SHA：待提交。
