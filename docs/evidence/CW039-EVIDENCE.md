# CW039-EVIDENCE — CW-039 手册定版（2026-09-12，演练维持 GA 冻结）

> 任务：CW-039（owner 决策 D3 拆分：手册定版本批次完成 / 真实演练维持 GA 冻结；D5：本手册同时作为 042-b 物理删除批次的前置交付）。
> 分支 `feat/customer-v3-w6-physical-exit`，基线 4d2e598。

## 交付

**手册正本：`docs/切换回滚手册.md`**（定版=权威程序文档，重大拓扑变更须同步修订重冻结）：

1. 系统形态与回滚对象（应用/数据库/桌面端/历史工具四层，逐层回滚载体）。
2. 空白重建路径五步（CW-032 交付包 + bootstrap-base-image + provision-empty-customer；CW-042-b 后 `VIDEO_REPLICA_SETTINGS_KEY` 必须显式供给——本地 keystore 已退役）。
3. 回滚场景矩阵五类：应用回滚（git revert/镜像标签/签名版本回退）、Schema 回滚（首选 PITR，downgrade 受数据丢失守卫约束是设计行为）、数据回滚（PITR + 零态核对）、发布回滚（CW-024 legacy-backup）、历史导入回滚（operator 制品，弃库重建）。
4. 备份与恢复要点（PITR 载体、恢复隔离纪律、密钥纪律）。
5. 新库上线零态核对五项（**CW-038 收窄项并入**：无悬挂 RESERVE/无未完任务/账本零差额/无悬挂外部回执/迁移 head 唯一）。
6. 演练义务清单（CW-033 drill / CW-048 故障与 RTO/RPO / CW-051 切换——全部 GA 冻结，不因本文档视为已完成）。

## 边界（诚实登记）

- 本任务**不包含**真实副本演练：GA 冻结锚点不变（CW-001 §5.5 / CW-005 §7），待演练环境授权后与 CW-033/CW-048 同窗执行；手册 §6 明文「不因本文档视为已完成」。
- 手册中的重建/回滚程序来自已合入 main 的契约与证据（CW-032/CW-024/CW-060/CW-033），非新验证；其实物执行归现场验收组（CW-046~052）。
- CW-038 的账务核对逻辑以 SQL 检查单形式并入手册 §5，其执行属上线窗口操作，不在本任务跑库核验（无生产库可核）。
