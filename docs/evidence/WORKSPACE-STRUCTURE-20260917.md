# WORKSPACE-STRUCTURE-20260917：目录与开发导航整理

基线：`58e147dce49683361c25e9c09c03ba7a6f43b8f7`。用户确认范围为目录、资料和开发导航，保留业务源码目录。执行者完成自检，独立评审以 PR 为准。

## 变更

| 原位置 | 新位置 |
| --- | --- |
| 顶层六个 `前端页面V*` 目录 | `docs/design/reviews/` 下保留原名 |
| `design-previews/` | `docs/design/previews/` |
| `design_prototypes/` | `docs/design/prototypes/` |
| 顶层五张 `viral-*.png` | `docs/design/references/` |
| `商务方案/` | `docs/business/` |
| 已跟踪的 `design-qa.md` | `docs/evidence/design-qa-20260917.md` |
| 根目录 `dist/` 中 10 个历史 Skill 发行文件 | `archive/skill-releases/` |

共迁移 168 个文件、16 个顶层入口。审核包保持版本边界，历史发行包保持原始字节。同步有效的相对链接和当前设计基线路径；历史证据中的个人机器绝对路径保留。新增文档总索引、设计索引、开发代码地图和相对路径的编辑器 workspace；README 修正过期目录与“同一时间只开一个分支”的描述。

业务源码、依赖锁文件、构建配置、数据库迁移、CI 和部署入口没有迁移。`.gitignore` 去重并补充 mypy 缓存、新设计目录的压缩包与预览选择产物。

## 验证

- V1.3 图册检查通过：28 个页面，图片与 HTML 引用完整。
- V1.4 审核器测试 7/7 通过。该旧测试要求在图册目录执行；首次从仓库根执行有 1 项相对路径失败，导航命令已修正后复验通过，未修改测试代码。
- 迁移前文件逐项存在性、非 Markdown 源文件 SHA256、既有链接目标、新导航链接、图册 SHA256 与 workspace 路径检查通过。
- 现有秘密扫描通过；差异空白检查通过。
- 本轮为资料迁移与导航维护，没有启动数据库或 Docker，也没有声称业务、真实链路或生产部署验收通过。CI 状态以 PR 当前提交为准。

## 恢复与后续

可以按本提交的逆向变更恢复资料目录，业务运行不依赖迁入路径。开发者从[开发导航](../development/README.md)进入，后续设计包继续归到 `docs/design/`。资料归档不意味着历史任务重新验收或提高生产等级。
