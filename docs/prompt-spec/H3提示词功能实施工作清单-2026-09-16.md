# H3 提示词功能实施工作清单

日期：2026-09-16。状态：用户已授权本会话接手；原 Claude 会话已停止、17 个改动文件已备份。代码实现与自检完成，本地门禁已通过，提交 PR 与 CI 中。

依据：[功能与提示词规范 v1.1](./MiniMaxH3多模式提示词与AI优化开发规范-2026-09-16.md)、[拆解指令 v4.1](./视频拆解提示词v4-开发替换稿-2026-09-16.md)、[现有代码审查](./提示词AI优化-现有代码审查与最小改造方案-2026-09-16.md)。实现以三份文件的用户确认版本为准。

## 一、交付目标

上下文齐全时，一次视频分析返回分镜事实和对应模式的 H3 提示词，校验通过即可生成。所有在用提示词编辑入口提供可选 AI 优化，支持继续编辑和安全撤销。生成请求采用当前框内文本，不能在后台重新编译并覆盖。保留现有资产授权、确认首帧、幂等、钱包与生成能力开关。

只精简本功能被替代的链路；历史版本读取、旧客户端请求与仍在使用的入口继续兼容。保留现有 PostgreSQL、worker、Gemini 传输和账务设施，不引入新队列或另一套提示词版本库。

## 二、已核实的开工情况

- [x] 已读取仓库 AGENTS、开工清单与排班/认领规则。
- [x] 已刷新远程引用；origin/main 为 `f0b45b2b2558487adb8981635153cc7afb5da925`。
- [x] 已核对本地 worktree、远程分支及开放 PR。只读 GitHub REST 查询成功，查询时开放 PR 数为 0。GitHub 连接器返回 403，不能将该连接器作为后续 PR 提交成功的依据。
- [x] 已确认主仓库存在未提交的 `analysis.py`、`generation.py` 和未跟踪的 H3 指南/证据；属于既有成果，本会话未修改、未迁移。
- [x] 发现 `.worktrees/PROMPT-OPTIMIZE-20260916`，分支 `feat/prompt-optimize-20260916`，基线同 main。
- [x] 已读取共享认领 `xiangshu-video-replica-/.git/codex-task-claims/PROMPT-OPTIMIZE-20260916/claim.txt`：Owner 为 Claude Code 会话 `fbeae78e`，任务是“AI 优化提示词（同步接口＋按次计费＋图标入口）”。该 worktree 存在未提交业务代码，不能当成空目录接管。
- [x] 用户明确授权本会话接手，已匹配会话 fbeae78e 停止 PID 17736；接手前完整备份原改动。
- [x] 复用原独立 worktree 和分支，更新共享 claim；Owner 为当前 Codex 会话，评审为执行者自检，未冒称独立评审。

## 三、逐项实施与验收

以下每项按“先失败回归测试 → 修改 → 专项验证 → 更新证据”推进。编号是本次工作项，不冒充仓库 CW 任务编号。

| 状态 | 编号 | 工作项与主要文件 | 完成条件 |
|---|---|---|---|
| [x] | H01 | 冻结请求、响应、模式和元数据契约；`api.ts`、`studio/types.ts`、服务端请求 schema | 五模式与真实资产职责一致；分析包装与独立优化输出分开；未知字段、空白和长度统一验证 |
| [x] | H02 | 当前文本直接建批；`generation.py`、`generation_routes.py` | `prompt_text` 与旧 `prompt_version_id` 互斥；在既有事务冻结文本、版本、素材、批次和预扣；保留权限与首帧确认；不调用编译器生成另一份文本 |
| [x] | H03 | 前端生成链路精简；`studio/live.ts`、`ProjectDetailFlow.tsx`、`StudioWorkspace.tsx` | 去掉新路径的 createScript→compile→revise→lock 串联；有/无确认文案均使用最终框内文本；普通手写稿不强制优化 |
| [x] | H04 | 共用 H3 模式规则与验证器 | 覆盖 T2VA/I2VA/FL2VA/L2VA/Ref2VA；按类型编号；来源分析视频不自动成为生成参考；冲突和无法绑定返回诊断，不编造素材 |
| [x] | H05 | 分析任务输入与单次模型响应；`analysis.py`、`analysis_routes.py`、worker 对应执行入口 | 冻结真实元数据与目标上下文；同次返回 `analysis-h3.v1`；模型调用数为 1；取消隐式付费 JSON 修复；旧版本可读 |
| [x] | H06 | 分析结果验证、保存与显示 | 分开校验源事实/目标文本；保留有效分析；服务端给出 READY/NEEDS_CONTEXT/NEEDS_REVIEW/INVALID；ACTION_BEAT 不变成切镜；台词不重不漏；不暗中缩放时间 |
| [x] | H07 | 可选优化任务与追加迁移；优化服务/路由、`main.py`、`generation_worker.py` | 创建返回 202，查询校验归属；复用 worker 租约；同 key 同请求复用、不同请求 409；发送后不确定禁止自动重发；网络调用在事务外 |
| [x] | H08 | 优化用量与对账；`billing_catalog.py`、`usage_billing.py` 及既有 reconcile | 独立业务科目；无启用售价不扣客户费；供应商成本与客户扣费分开；崩溃、失败、不确定均可追踪，不遗留预扣；不建立视频批次 |
| [x] | H09 | 共享提示词编辑器及异步 hook | 图标位于右上角，有可访问名称；空文本/只读禁用；成功回填、失败保留原文；等待时编辑/切项目/换账号不被迟到结果覆盖；撤销不丢后续编辑 |
| [x] | H10 | 接入所有在用入口；`CreationPages.tsx`、`GenerationLauncher.tsx`、旧项目流 | 复刻、文生、图生、参考页以及仍在使用的旧入口共用逻辑；优化不触发生成；不建立相互覆盖的第二份文本 |
| [x] | H11 | 分析回填、云草稿恢复与模板；`state.ts`、`live.ts`、`SavedPromptImporter`、saved_prompt 请求 | 新分析可更新未手改稿，手改稿需主动应用；保存模板不清除草稿编辑状态；刷新仍保留当前稿；模板导入携带模式/角色元数据，无权资产不自动加入 |
| [x] | H12 | 独立生成与边界统一；`independent.py`、模式解析、参考映射 | 4001–7000 Unicode 码点可通行，emoji 两端计数一致；未知标签明确诊断；@N 转换在预览可见；尾帧模式不退化为 T2V，能力未验证时保持不可生成 |
| [x] | H13 | 删除已被替代且无调用方的代码 | 中文分镜渲染退出默认生成稿，仅保留事实展示/旧数据回退；不删除历史兼容与账务安全检查；用调用方检索确认删除范围 |
| [x] | H14 | 代码评审与验收映射 | 对规格 A01–A08、E01–E07、M01–M08、P01–P07、V01–V04、S01–S05、G01–G02 逐项登记测试/证据；评审重点覆盖竞态、权限、幂等、账务、兼容和文本覆盖 |
| [ ] | H15 | 质量门、提交、CI 与 PR | 专项通过；按仓库规则完成静态门及隔离 PG 全量门，或记录适用的本机 runner CI 路径；仅提交本任务文件；push 后建立一个 PR，检查当前 head 的 secret/Linux/Windows 门禁，失败实质修复 |

执行顺序：H01 → H02/H03 → H04/H05/H06 → H07/H08 → H09/H10/H11/H12 → H13 → H14 → H15。依赖存在时顺序实施，不用多个相互依赖的 PR 绕过 main 基线约定。

## 四、现有在制实现的差异核查

以下为接手前的差异核查记录；其后已获用户授权并完成相应修改。

| 位置/观察 | 与确认规格的差额 | 接手后的处理 |
|---|---|---|
| `prompt_optimizer_routes.py` 当前是同步 `POST /api/prompts/optimize` | 缺少规格中的异步创建/查询契约，页面需要长时间等待同步请求 | 复用可用的授权/媒体解析/幂等代码，将执行接入现有 worker；不同时保留两套新接口作为产品主路径 |
| `PromptOptimizeButton.tsx` 请求结束后直接 `onOptimized` | 等待期间修改文字或切换上下文时，旧结果可能覆盖新稿 | 增加编辑序号、上下文快照、卸载/账号隔离与待应用结果 |
| 同一按钮的撤销仅判断 `previous` 是否存在 | 优化后再手改仍可撤销，可能丢掉新修改 | 只在当前文字和上下文仍匹配本次结果时允许一键撤销 |
| 当前模式枚举 `T2V/I2V/FL2V/R2V` | 尾帧模式尚未纳入；不能仅按已有枚举宣称五模式完成 | 共用模式解析，补齐 L2VA 契约并明确生成能力限制 |
| 新增 receipt 表及计费注册 | 有复用价值，但任务状态/输入快照/结果恢复需对照异步规格重新核验 | 只保留一张满足最终契约的业务操作表；不得直接在未经评审的迁移上再叠一张重复表 |
| 主线仍是原分析函数和隐藏修复调用 | 未完成“拆解直接输出 H3”的主目标 | H05/H06 明确实现，不以按钮完成代替完整交付 |

## 五、验证与评审记录要求

1. 前后端专项使用 fake provider；验证真实请求正文及调用次数，而非仅验证 UI 出现按钮。
2. PG 测试必须使用本任务隔离资源。容器加 `--rm`，镜像用固定 tag，结束清理本任务资源。未运行/跳过不得记通过。
3. 代码评审完成后执行仓库要求的质量门；分片清单新增测试文件必须登记，避免新测试未进入 CI。
4. 评审发现的每项问题记录严重度、位置、修复与复验；独立评审按仓库流程安排，不冒称自检即独立评审。
5. 提交前重新 fetch 并核对 `origin/main..HEAD` 仅包含本任务提交。PR 描述给出具体行为、验证结果、剩余限制与回退方式。
6. 本次授权包括代码修改、评审、提交和 PR/CI，不自动扩大为合并、生产部署或真实付费模型调用。真实效果验收与自动化验收分别记录。

## 六、当前产物与未完成项

实现、验证和边界见 [实施与评审证据](../evidence/PROMPT-OPTIMIZE-20260916-EVIDENCE.md)。

H01—H13 为实现完成；不代表真实供应商效果验收通过。H14 已完成执行者自检及验收映射；H15 本地门禁已通过，待提交和 PR/CI 状态回填。文字内容的事实正确性仍需用户核对，程序只确定性检查结构、标签、台词和切镜时间等可比较项。音频参考只使用用户填写的用途说明，不宣称模型已听取音轨。L2VA 仅提供编辑与优化，生成仍受能力门禁限制。
