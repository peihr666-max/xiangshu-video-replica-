{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "download-events",
  "description": "Allow the local main window to receive actual video download completion events.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
