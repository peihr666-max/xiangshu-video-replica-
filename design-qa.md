# 文/图生视频布局复核

final result: passed

本轮目标是用户 2026-09-16 提供的 `codex-clipboard-8d0c304f-d565-4f3e-a014-557eb0e118e1.png` 的内容区布局。实现截图：[桌面实际页面](docs/evidence/creation-workflow-ui/frames-desktop.png)。已将两张图在同一次图像输入中对照，而非仅凭代码判断。

- 对照尺寸：参考图 1737×905；浏览器请求同尺寸，实际 CSS 1738×906（宿主像素取整），截图 1734×903；不存在倍密度缩放差异。状态均为已添加首帧、尾帧为空、768P / 8 秒 / 16:9 / 1 个。
- 先前 P2：首尾帧区过高导致参数落到底栏下方。修复了图片区、提示文案、输入框和区块间距。重新截图后，四个下拉框底部约 803px，操作栏顶部 812px，首屏无控件遮挡。
- 布局：左侧首尾帧并排、描述、四参数；右侧跨行大预览；底部粘性操作栏。手机 390×844 下为纵向区块、参数两列，文档宽度 386px，无横向溢出。
- 字体：使用产品既有中文字体栈及字号，01 / 02 / 03 和主次文本层级清楚；不按参考图复原旧导航字号。
- 颜色：沿用黑灰背景、金色选择与主按钮、灰色辅助文本，预览卡片用边界区分。
- 图片：使用现有审核素材验证真实上传图片呈现，不生成或替换用户的业务素材。院落图片与参考图中的现代别墅不同，是数据差异；两处预览内容一致且没有拉伸。
- 文案：保留已确认的三项主导航、AI 视频名称和无面包屑；不恢复参考图中的旧四导航。描述上限仍按实际 7000 字能力，非图中的 500 字。开放提示由真实能力决定。
- 交互：分辨率改变后底部摘要同步；模式来回切换保留参数；参考页继续使用已有布局且没有新增底栏。保存与提交权限门禁沿用原实现。

无待解决 P0 / P1 / P2。范围为桌面内容区布局修正，品牌侧栏沿用此前确认版本；未调用真实供应商。
