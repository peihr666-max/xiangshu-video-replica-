# 文/图生视频布局复核

final result: passed

## 2026-09-17 参考生视频统一布局

本次用户要求参考页与已确认图生页一致，取 [frames-desktop.png](docs/evidence/creation-workflow-ui/frames-desktop.png) 为布局基准，与新截图 [reference-split-desktop.png](docs/evidence/creation-workflow-ui/reference-split-desktop.png) 在同次图像输入对照。此决定替代下方历史记录中保留参考页旧布局的约定。

- 复用同一左右网格、标题编号、四参数区和底栏，字体、颜色、间距沿用已有样式；图片仍来自审核素材，未改成背景占位图。
- 初次检查发现素材库按钮单独占一行导致参数被底栏挡住。已移到素材标题行右侧，重新检查参数底部约 786px，底栏顶部 812px；问题已修复。
- 单图及双图状态的素材区高度受控，点击第二张图后正确选中并切换右侧预览，删除测试添加项后恢复初始素材。未触发真实上传或生成。
- 底部摘要显示“参考生视频”，分辨率改变时同步；大加号、素材用途和数量限制说明保留。
- 390×844 手机下参数两列、工具行回到正常文档流，文档宽 386px，没有横向溢出。
- 无待解决 P0/P1/P2；相关 204 项前端测试通过。本次只统一布局，无服务端变更。

## 上一轮文/图模式记录

本轮目标是用户 2026-09-16 提供的 `codex-clipboard-8d0c304f-d565-4f3e-a014-557eb0e118e1.png` 的内容区布局。实现截图：[桌面实际页面](docs/evidence/creation-workflow-ui/frames-desktop.png)。已将两张图在同一次图像输入中对照，而非仅凭代码判断。

- 对照尺寸：参考图 1737×905；浏览器请求同尺寸，实际 CSS 1738×906（宿主像素取整），截图 1734×903；不存在倍密度缩放差异。状态均为已添加首帧、尾帧为空、768P / 8 秒 / 16:9 / 1 个。
- 先前 P2：首尾帧区过高导致参数落到底栏下方。修复了图片区、提示文案、输入框和区块间距。重新截图后，四个下拉框底部约 803px，操作栏顶部 812px，首屏无控件遮挡。
- 布局：左侧首尾帧并排、描述、四参数；右侧跨行大预览；底部粘性操作栏。手机 390×844 下为纵向区块、参数两列，文档宽度 386px，无横向溢出。
- 字体：使用产品既有中文字体栈及字号，01 / 02 / 03 和主次文本层级清楚；不按参考图复原旧导航字号。
- 颜色：沿用黑灰背景、金色选择与主按钮、灰色辅助文本，预览卡片用边界区分。
- 图片：使用现有审核素材验证真实上传图片呈现，不生成或替换用户的业务素材。院落图片与参考图中的现代别墅不同，是数据差异；两处预览内容一致且没有拉伸。
- 文案：保留已确认的三项主导航、AI 视频名称和无面包屑；不恢复参考图中的旧四导航。描述上限仍按实际 7000 字能力，非图中的 500 字。开放提示由真实能力决定。
- 交互：分辨率改变后底部摘要同步；模式来回切换保留参数；参考页继续使用已有布局且没有新增底栏。保存与提交权限门禁沿用原实现。

无待解决 P0 / P1 / P2。范围为桌面内容区布局修正，品牌侧栏沿用此前确认版本；未调用真实供应商。

## 2026-09-17 生成画面比例联动复核

按用户最新澄清，以参数所选比例控制图片区，不再固定为横屏。两种模式共享同一比例逻辑；新草稿默认 9:16，既有草稿保留原值。已在实际页面核对 9:16 / 16:9 / 1:1 / 21:9 的尺寸，并观察图片完整显示与虚化衬底。首帧候选另随其图片画幅设置。

文/图页空尾帧与已有首帧均为 90×160，参数下拉框底部 805px，底栏顶部 812px，无控件遮挡；390px 手机页面内容宽 386px。参考页缩略图与大预览同步。截图：[参考页](docs/evidence/creation-workflow-ui/reference-ratio-portrait.png)、[文图页](docs/evidence/creation-workflow-ui/frames-ratio-portrait.png)。完整前端 1631 项通过；无待解决 P0/P1/P2。本次尺寸数据和截图来自审核环境，不代表真实生成结果。

## 2026-09-17 最终修正：上传图片决定显示框

用户最终明确选择“跟随上传图片：横图自动变横框”，替代前一节输出参数联动。空占位 9:16，图片加载后用实际宽高比；已验证输出参数 9:16 → 1:1 时横图保持横框。首尾帧外部固定横框已移除，尾帧真实占位为 90×160，已上传横图为 284.3×160，大预览 715.8×402.8。预览标签改为“原图比例”，区分输出参数。390px 手机无横向溢出。截图：[文图](docs/evidence/creation-workflow-ui/frames-native-ratio.png)、[参考](docs/evidence/creation-workflow-ui/reference-native-ratio.png)。最终全量 1634 项通过，收尾标签/边框专项 107 项通过，无待解决 P0/P1/P2；旧竖框截图仅保留追溯。
