# 前端完善实施进度与逐项 Code Review

实施分支：`fix/frontend-completion-no-confirmation-20260907`

基线 HEAD：`aab50997323f73efb6d63de61ea07005ae0ca4a5`

审计快照：`452478f7222629e2e6f08854ef653ebdd37b9455`

## FP-00 基线核对

- 审计快照是实施基线的祖先，已进入当前主线。
- 审计快照之后与本批前端缺口有关的核心前端文件没有被其他提交替换；后续业务差异仅涉及 `server/app/db_pg.py`、`server/app/viral_media.py`、`server/app/viral_routes.py`。
- 客户账号、设备和管理端补充项映射现有 T29–T34；逐角色 E2E 映射 T34-E2E；质量门禁映射 T35；真实供应商链路映射 T40；灰度及 Go/No-Go 映射 T41–T42。
- FP-01–20、FP-22、FP-27–30 没有可直接复用的正式 TXX，当前保留 FP 编号实施，不自行新增 TXX；每项以独立提交和评审记录追踪。
- 全量门禁以最终提交/PR 候选为单位执行；逐项开发阶段执行专项测试、类型检查、格式检查和独立 Code Review。

## 逐项记录

| 任务 | 状态 | 红灯证据 | 修复后验证 | Code Review | 提交 |
|---|---|---|---|---|---|
| FP-01 | 代码完工，待全仓门禁/真实上传验收 | `MainPages.test.tsx`：3 failed / 34 passed，确认项目 ID 被误写为来源资产 ID | `MainPages.test.tsx` + `CreationPages.test.tsx`：61 passed；TypeScript、Biome、diff check 通过 | 两轮阻断问题已修，最终 APPROVE | `5b91089` |
| FP-02 | 代码完工，达到 `AUTOMATED_VERIFIED` | 新增恢复与重试用例初始 2 failed / 23 passed | 5 个相关测试文件 143 passed；TypeScript、Biome、diff check、变更秘密扫描通过 | 四轮 BLOCK 问题均修复，第五轮 APPROVE | `8d13f60` |
| FP-03 | 代码完工，达到 `AUTOMATED_VERIFIED` | 首帧交接、迟到响应、失效与重试用例初始 5 failed | `CreationPages.test.tsx` + `state.test.ts`：53 passed；TypeScript、Biome、diff check、变更秘密扫描通过 | 首轮 3 项 BLOCK 均修复，第二轮 APPROVE | `7f08678` |
| FP-04 | 代码完工，达到 `AUTOMATED_VERIFIED` | 失败后重试与过期响应用例初始 2 failed | `CreationPages.test.tsx`：48 passed；TypeScript、Biome、diff check、变更秘密扫描通过 | 三轮生命周期 BLOCK 均修复，第四轮 APPROVE | `f3ae7e9` |
| FP-05 | 代码完工，达到 `AUTOMATED_VERIFIED` | 前端类型、重复、超限、能力关闭、旧草稿和最终提交用例初始 6 failed；后端重复/超限用例初始 1 failed；恢复失败与重试用例追加后初始 2 failed | 前端 3 个相关测试文件 95 passed；后端 `test_independent_creation.py` 15 passed；TypeScript、Biome、Ruff、格式、mypy、diff check、变更秘密扫描通过 | 前两轮分别阻断异步上传/草稿恢复边界和恢复失败误整理问题，全部修复后第三轮 APPROVE | `193986d` |
| FP-06 | 代码完工，达到 `AUTOMATED_VERIFIED` | 报价失败、恢复绕过、重复点击、旧响应覆盖、卸载隔离、重挂载幂等及变价重试用例分批复现，新增回归均在对应修复前失败 | 前端 7 个相关测试文件 219 passed；后端报价/幂等/计费 6 passed；TypeScript、Biome、diff check、变更秘密扫描通过 | 前三轮阻断报价门禁、异步上下文、跨重挂载冻结请求及变价换键问题，全部修复后第四轮 APPROVE | `1b13ab1` |
| FP-07 | 代码完工，达到 `AUTOMATED_VERIFIED` | 真实供应商坏图、不可裁剪拼合图、伪造源图与 WebP 上传回归初始 6 failed；评审继续复现 PNG CRC/IEND/IHDR、解压上限、结构合法但不可解码图片及随包 FFmpeg 能力缺口 | 后端 `test_simple_character.py` 81 passed；前端 `CharacterLibrary.test.tsx` 26 passed；Ruff、格式、mypy、TypeScript、Biome、bash 语法、diff check、变更秘密扫描通过；精简 FFmpeg Docker 实际构建成功并通过 PNG/JPEG/WebP 解码 smoke | 前三轮 BLOCK 分别发现图片完整性、真实解码和随包 FFmpeg 能力问题，全部修复后第四轮 APPROVE | `f633907` |
| FP-09 | 本机开发库存只读盘点完成；生产库存与历史处置待指定/确认 | 只读查询确认 5 个已发布人物版本中 1 个拼合图标记为 `local_placeholder`，且被 1 个项目、2 条当前参考选择和 4 条历史 JSON 快照引用；35 张批准视图均缺独立切片来源标记 | SQLite `mode=ro&immutable=1` + `query_only` 断言通过；资产 133、版本 5、批准视图/审核各 35、外键违规 0；盘点前后数据库 SHA-256 一致且 WAL 为 0 | 首轮 BLOCK 3 项盘点完整性问题，补切片来源、JSON 引用和 0 元授权上限后第二轮 APPROVE | `12281f7` |
| FP-10 | 代码完工并合入统一分支，达到 `AUTOMATED_VERIFIED` | 搜索、上下文切换、加载更多失败、A→B→A、失效游标和迟到首屏失败回归分批先红 | 分项前端 168 passed；统一集成前端 8 文件 365 passed、后端相关 287 passed/25 PG skipped；TypeScript、Biome、Ruff、格式、mypy、diff check、变更秘密扫描通过 | 前五轮分别阻断搜索语义、单快照、失效游标和迟到错误竞态；全部补回归修复后第六轮 APPROVE | `d28ebd8`、`3794f0c`、`32d9e81`、`af6176a`、`fcd70de`、`ae474a0`；集成 `bae76ca` |
| FP-11 | 代码完工并合入统一分支，达到 `AUTOMATED_VERIFIED` | 同源重挂载、缺平台原生 ID、Worker 归属变化、永久失败重放、畸形成功与第 120 次轮询终态回归分批先红 | 分项前端完整检查 75 文件 1037 passed、后端 78 passed；统一集成前端 365、后端 287 passed/25 PG skipped；静态检查通过 | 前三轮 BLOCK 分别阻断幂等键生命周期、永久失败分类/畸形成功和末次轮询终态；全部补回归后第四轮 APPROVE | `fb93453`、`8dce49a`、`9408222`、`5982bee`；集成 `bae76ca` |
| FP-12 | 代码完工并合入统一分支，达到 `AUTOMATED_VERIFIED` | 文案定位、跨账号/跨素材草稿恢复、重挂载保存、并发保存与迟到响应回归分批先红 | 分支前端 6 文件 234 passed、后端文案改写 26 passed；统一集成前端 9 文件 378 passed；TypeScript、Biome、Vite build、Ruff、格式、mypy、diff check、变更秘密扫描通过 | 分支多轮阻断草稿归属、保存队列和测试隔离；与 FP-25 联合复核关闭角色切换门禁后最终 APPROVE | `b9a77ef`～`ba62202`；集成 `de2dcdc`、`4aa8ddb`、`b1af102` |
| FP-13 | 代码完工，达到 `AUTOMATED_VERIFIED`；真实供应商验收待 FP-38 | 场景照片未写入图片 ID、切换人物残留旧图、缓存照片服务端失效与 A→B→A 异步授权回归分批先红；评审继续复现未知 404 误冻结和 React 提交后的被动 effect 窗口 | `PeoplePages.test.tsx` + `state.test.ts`：50 passed；TypeScript、Biome、diff check、变更秘密/调试扫描通过 | 前三轮 BLOCK 均补回归修复，第四轮以 StrictMode、提交时序与错误分类独立复核后 APPROVE | `2fdb14e`、`68088c6`、`98e19fa`、`b2dede6`；集成 `88fc7df` |
| FP-14 | 代码完工，达到 `AUTOMATED_VERIFIED`；真实 COS、浏览器与供应商验收待 FP-38 | 音频用途/时长合同、深页选择、上传取消/迟到和用途交叉使用回归分批先红；首轮评审继续发现无用途音频绕过、TypeScript 错误和声音样本缺少取消隔离 | 分支前端 274 passed、后端 70 passed；统一集成前端联合 322 passed、后端联合 295 passed；构建、TypeScript、Biome、Ruff、格式、mypy、依赖审计、diff check、变更秘密扫描通过 | 首轮 BLOCK 3 项均补回归修复，第二轮独立复核上传生命周期、单一用途、可信时长和深页选择后 APPROVE | `ba39df1`、`b1e878b`；集成 `22584de` |
| FP-15 | 代码完工，达到 `AUTOMATED_VERIFIED`；真实成片待 FP-38 | 字幕开关到创建请求的回归初始失败：开启和关闭都未影响 `createOralTask` 参数 | 前端口播交互/API 131 passed；后端 Hifly/口播 82 passed；TypeScript、Biome、diff check、变更秘密扫描和依赖审计通过 | 首轮独立复核 UI→API→Worker→Hifly、AUDIO 隔离、幂等指纹与迟到响应后 APPROVE | `7a424a3` |
| FP-17 | 代码完工，达到 `AUTOMATED_VERIFIED`；生产规模查询计划待 FP-37 | 四类首屏截断、旧响应形状、total 查询、场景翻页失败和场景版本全库排序回归分批先红 | 分支 Node 24 前端完整检查 73 个文件 1066 passed、后端相关 273 passed；统一集成前端联合 322 passed、后端联合 295 passed；TypeScript、Biome、Ruff、格式、mypy、diff check、变更秘密扫描通过 | 前两轮 BLOCK 分别阻断路由层全量场景切片/失败重试和全库版本排序；改为页内查询后第三轮 APPROVE | `ce16216`、`e2796c2`、`c07a0cf`；集成 `943b09b` |
| FP-18 | 代码完工，达到 `AUTOMATED_VERIFIED` | 第21条流水与订单历史翻页初始 1 failed；评审追加复现分页失败错页、旧摘要恢复已关闭订单、创建迟到插入旧页及初始化清除操作错误 | 前端 72 个测试文件 1022 passed；客户钱包/API 3 个相关测试文件 122 passed；后端 `test_customer_recharge.py` 24 passed；TypeScript、Biome、diff check、变更秘密扫描通过 | 前两轮共 BLOCK 4 项 P2，全部补回归修复后第三轮 APPROVE | `addf93d` |
| FP-19 | 代码完工，达到 `AUTOMATED_VERIFIED`；真实 COS 与弱网浏览器验收待 FP-38 | 首/次页三类媒体预览、未知照片数、末页删除和可见页签名成本回归先红；首轮评审实测 60 条历史素材在启动时产生 61 次签名，并发现音视频错误重签与详情复用测试缺口 | 分支及统一集成前端 4 个专项文件 170 passed；后端 `test_materials.py` 19 passed；TypeScript、Biome、Ruff、格式、mypy、diff check、新增行秘密扫描通过 | 首轮 BLOCK 批量预签和三类媒体测试缺口；返修后第二轮复核启动、素材页/选择器 6+1 成本、迟到错误和详情复用后 APPROVE | `d5cf5ee`、`57cfa25`；集成 `d296081` |
| FP-20 | 代码完工并合入统一分支，达到 `AUTOMATED_VERIFIED` | 账号切换沿用旧收藏、被动 effect 前旧请求写入、A→B→A 按钮锁死、旧游标误用于新账号及两种真实查询临时排序均先红 | 分项前端 69、后端 51 passed；SQLite 两种查询无临时 B-Tree；统一集成前端 365、后端 287 passed/25 PG skipped；静态检查通过 | 前两轮分别 BLOCK 三类账号竞态/索引方向与 A→B→A/旧游标窗口；全部补回归后第三轮 APPROVE | `72c3c90`、`28e532e`、`e430e5d`；集成 `bae76ca` |
| FP-21 | 代码完工，达到 `AUTOMATED_VERIFIED`；真实浏览器历史栈与逐角色验收待 FP-35/37 | Analytics 的 task/batch ID、首屏外详情、类型化 URL、Admin intent、busy 历史恢复、returnTo 残留、同 ID 换账号缓存及口播 OpenAPI 合同均先红 | 分支及统一集成前端 7 个专项文件 269 passed；后端口播专项 51 passed；client build、TypeScript、Biome、Ruff、格式、mypy、OpenAPI 一致性与新增行秘密扫描通过 | 首轮 BLOCK Admin/历史/返回/账号缓存/OpenAPI 共 5 项；补回归返修后第二轮 APPROVE | `7750993`、`232d9e0`；集成 `a4367f1` |
| FP-22 | 代码完工并合入统一分支，达到 `AUTOMATED_VERIFIED`；PG 实库与生产规模查询待 FP-37 | 独立批次遗漏、文本时间边界、运行态遗漏、产出口径重复、跨账号迟到写入及北京时间午夜拆分统计日回归分轮先红 | 分项后端 31、前端 106 passed；统一集成前端 108、后端 31 passed；Vite build、TypeScript、Biome、Ruff、格式、mypy、OpenAPI 一致性、diff check 通过 | 前两轮分别 BLOCK 5 项归属/时间/口径/竞态问题与单次请求跨日竞态；全部补回归后第三轮 APPROVE | `efba744`、`11f259e`、`6c3ff6b`；集成 `875551a` |
| FP-23 | 代码完工，达到 `AUTOMATED_VERIFIED` | 共享客户上下文、迟到列表响应、A→B→A 迟到写操作、失败记录跳转和口播失败归集回归在修复前失败；首轮评审继续发现供应商错误泄漏、凭证混用和仪表盘口径缺口 | 前端 73 个测试文件 1041 passed；后台管理与 PostgreSQL 仪表盘 27 passed；TypeScript、Biome、Ruff、格式、mypy、diff check、变更秘密扫描通过 | 首轮 BLOCK 4 项均修复，第二轮复核脱敏、竞态、统计与 API 契约后 APPROVE | `6043e12` |
| FP-24 | 代码完工，达到 `AUTOMATED_VERIFIED` | 资料失败/重试、网络与租约分离、退出、暂不处理、租约到期和会话异步竞态回归分批先红；评审追加复现旧 401 错误体与旧凭据读取竞态 | 前端 72 个测试文件 1018 passed；相关 5 个测试文件 160 passed；TypeScript、Biome、diff check、变更秘密扫描通过；延迟响应夹具类型修正后前端全量 1032 passed | 首轮 BLOCK 3 项、第二轮 BLOCK 2 项并全部补回归修复，第三轮以 5 项独立内存模拟复核后 APPROVE；测试类型修正另经独立复核 APPROVE | `e5191c3`、`c8356fd`（测试类型） |
| FP-25 | 代码完工并合入统一分支，达到 `AUTOMATED_VERIFIED` | 后端角色矩阵首跑 3 failed / 5 passed；前端审计员写控件、角色切换旧请求与已打开选择器回归分批先红 | 分支前端 5 文件 219 passed、Studio 62 passed；统一集成前端 9 文件 378 passed、后端 7 文件 239 passed；TypeScript、Biome、Vite build、Ruff、格式、mypy、diff check、变更秘密扫描通过 | 后端首轮 APPROVE；前端三轮 BLOCK 与合并级一轮 BLOCK 均补回归修复，最终 FP-12/25 联合复核 APPROVE | 后端 `35a6fe3`；前端 `30721c8`、`e2e45b5`、`16ba612`、`e852c5c`；集成 `2e69346`、`4aa8ddb`、`b1af102` |
| FP-26 | 代码完工，达到 `AUTOMATED_VERIFIED`；发布账号与正式发布继续暂缓 | 固定余额、加载失败误显示为 0、正式账户固定资料与发布状态回归分批先红；首轮评审继续发现账户概览资料失败缺少重试入口和候选基线不一致 | 统一集成线 6 个账户/工作台测试文件 144 passed；TypeScript、Biome、diff check、变更秘密/调试扫描通过 | 首轮 BLOCK 两项均在统一集成基线补回归修复，第二轮独立复核钱包隔离、资料重试、暂缓发布边界后 APPROVE | `1ce4db6`、`21816fe` |
| FP-27 | 代码完工，达到 `AUTOMATED_VERIFIED` | 设置页初始 3 failed / 4 passed，后端初始因缺少 Hifly tester 无法收集；评审继续复现供应商认证误触发本地退出、畸形状态码/余额误成功、4301 位状态码异常及超安全整数余额错误显示 | 前端 72 个测试文件 1032 passed，其中 `SettingsPanel.test.tsx` 15 passed；后端设置/Hifly/控制台 136 passed；TypeScript、Biome、Ruff、格式、mypy、diff check、变更秘密扫描通过 | 前四轮 BLOCK 问题均补回归修复，第五轮 APPROVE | `b06da2d` |
| FP-30 | 代码完工并合入统一分支，达到 `AUTOMATED_VERIFIED`；PG 实库与真实付费链路待 FP-37/38 | 假 native ID、媒体未验先建项目、付费重放、崩溃收据、账号竞态、DNS 重绑定、非常规端口及 PG 事务回滚回归分轮先红 | 分项后端 136、前端 213 passed；统一集成前端 365、后端 287 passed/25 PG skipped；合并级扩大后端套件 373 passed/141 PG skipped；迁移往返、OpenAPI、TypeScript、Biome、Ruff、格式、mypy通过 | 前四轮分别 BLOCK 6、3、2、1 项安全/事务问题，第五轮功能评审 APPROVE；合并级评审阻断旧 head 断言，返修后复审 APPROVE | `c7d83a7`、`0e4f8b1`、`e0a5c51`、`cbe721a`、`39f0ede`；集成 `bae76ca`；合并复核 `c66168ec` |

## 当前进度与剩余任务

更新时间：2026-09-08。当前统一修复分支已有 FP-00、FP-01～07、FP-10～15、FP-17～27、FP-30 的完成证据；FP-09 完成本机只读盘点，历史处置等待 Q02。

- 本轮新增统一集成：FP-12 文案改写与云端草稿、FP-25 审计员前后端只读门禁已经完成；联合专项前端 9 文件 378 passed、后端角色矩阵 7 文件 239 passed、文案改写后端 26 passed，最终独立 Code Review 均为 APPROVE。
- 当前可直接编码且不依赖新产品口径的已知修复项已全部完成。FP-35 逐页面/逐角色验收和 FP-36 最终自动化门禁必须在剩余范围冻结后执行，避免对尚未确认的模块给出虚假完成结论。
- 需要确认或仍暂缓：FP-08、FP-16、FP-28、FP-29、FP-31～34，以及 FP-09 历史资产写入处置。
- 最终验收与发布出口：FP-35～40；FP-37～40 还需要 Q08 的环境、账号、费用与生产动作范围，当前不声明 `REAL_CHAIN_VERIFIED` 或 `PRODUCTION_GO`。

跨 Workspace 去重审计已确认：FP-08、FP-16、FP-28 没有完整候选；FP-29 的局部通知代码 `86ed324` 已在当前祖先链，但尚未按 FP-29 映射、验收和评审；FP-31～33 在 `c5-publish` 隔离工作区存在后端、前端和证据候选，需在发布范围解冻后逐项差异审计；FP-34 的经营数据基础 `9005a58` 已在当前祖先链，但收入/利润业务口径仍待确认。候选存在只代表可复用起点，必须补齐任务合同、验收和独立 Code Review 后才能标记完成。

## FP-02 交付说明

- 复刻页重挂载读取已保存分镜、分析、脚本和 Prompt；增加加载、失败与重试。
- 项目切换、新上传、手动拆解及 StrictMode 均隔离迟到响应；Prompt 保存与历史恢复的乱序结果不会覆盖新内容。
- 同项目未保存编辑优先于历史版本，主动清空也可跨页面和云端草稿保留；旧版云端草稿兼容缺失的编辑标记；新项目统一清除旧项目文本归属。
- 未执行真实登录、跨设备和生产环境验收；这些证据归入 FP-35/37，不在本项中声明完成。
- 无数据库迁移，可直接 revert 本项提交。

## FP-03 交付说明

- 人物置换确认首帧时，草稿同步保存资产 ID 和确认版本 ID；同资产产生新确认版本时也会更新，撤销确认会立即清除交接。
- 视频生成页在内存素材中缺少该首帧，或素材存在但没有可用 URL 时，会按同一资产 ID 读取签名预览并合并回 `data.assets`，使预览与后续 i2v 校验共用同一 ID。
- 首帧加载期间禁止提交；失败后可重试；历史视频任务不再遮挡加载错误；首帧 A 切换为 B 后，A 的迟到响应不会覆盖 B。
- 切换项目、人物 IP 或目标图时，统一清除旧首帧 ID、确认版本和确认状态；手动更换首帧也会使旧版本归属失效。
- Code Review 首轮发现并阻断 3 个边界问题：撤销/新版本同步、已有无 URL 素材恢复、历史任务遮挡错误；修复并补回归后第二轮 APPROVE。
- 未执行真实用户登录、付费 Provider 视频生成和生产环境验收；这些证据属于 FP-35/37/38，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移，可直接 revert 本项提交。

## FP-04 交付说明

- “重试匹配人物参考”现在会明确推进请求修订号，对同一项目、人物版本和源帧组合发起第二次有效请求，成功后回填匹配结果并清除错误。
- 请求忙态、同步排队锁和进行中请求归属共同阻止连续点击风暴；同一 request key 的 Promise 可复用，避免 StrictMode 重挂载重复调用外部接口。
- effect 只依赖项目 ID、人物版本 ID、源帧版本 ID 和已匹配版本 ID；同项目 ID 的对象刷新不会取消请求。
- 人物或源帧只在业务版本 ID 真实变化时才清空下游并重新匹配；同版本重复回调不会重复请求，也不会清除已确认首帧。
- 覆盖 A 请求未完成时 A→B→A、A 已失败后 A→B→A、同 ID 项目对象刷新、旧 A/B 迟到响应隔离、连续重试等回归。
- Code Review 前三轮分别阻断了取消后锁死、失败组合返回、同版本重复回调问题；全部修复并补回归后第四轮 APPROVE。
- 未执行真实用户与生产环境匹配服务验收；这些证据属于 FP-35/37，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移，可直接 revert 本项提交。

## FP-05 交付说明

- 参考生视频的素材库选择和本机上传只接受 PNG/JPEG 图片；素材选择排除视频、音频、已选图片，并按服务端 `max_reference_images` 动态限制数量。
- 上传回调使用最新草稿状态；离开页面、切换草稿或上传期间达到上限时，迟到结果不会覆盖新草稿或突破数量限制。
- 草稿恢复和最终提交共用参考图校验，识别缺失/非图片、重复和超限引用；旧草稿必须先显式整理，确认弹窗打开后也不能绕过提交校验。
- 能力读取失败和草稿素材读取失败均有独立错误与重试状态；草稿素材读取失败时保留原引用，不暴露“整理参考图”，切换草稿后旧重试响应失效。
- 后端继续通过请求模型限制最大数量，并补充重复参考图拒绝；没有放宽素材归属、类型、模式和数量校验。
- Code Review 前两轮分别阻断了异步上传闭包/草稿恢复、能力重试与恢复失败误整理问题；修复并补回归后第三轮 APPROVE。
- 未执行真实用户登录、生产素材和付费 Provider 生成验收；这些证据属于 FP-35/37/38，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移，可直接 revert 本项提交。

## FP-06 交付说明

- 复刻、项目详情、来源分析、独立视频和数字人口播统一使用后端实时报价；报价加载、失败、参数不一致或恢复记录切换期间均禁止进入付费提交，并提供明确重试入口。
- 成熟生成批次的正常创建与结果未知恢复都必须取得对应请求的有效报价；恢复记录即使生成参数相同也重新报价，避免复用普通编辑态的旧报价。
- 复刻和项目详情在建批响应不确定时冻结完整请求与幂等键；离页后重新进入同一用户、项目和业务输入会直接重放，存稿、编译和锁定不会重复执行。
- 独立视频和数字人口播按真实提交 body 计算幂等指纹；重新报价金额变化不会换键，提示词等业务参数变化才换键。连续点击、关闭重开弹窗、页面卸载和迟到响应均有隔离回归。
- Code Review 前三轮分别阻断恢复绕过报价、旧异步响应、准备期参数变化、跨重挂载重复付费和报价金额误入幂等指纹；全部修复后第四轮 APPROVE。
- 未执行真实用户余额、付费供应商和生产环境验收；这些证据属于 FP-35/37/38，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移，可直接 revert 本项提交。

## FP-07 交付说明

- 人物源图统一校验内容类型、文件结构、尺寸上限和实际解码能力；同步创建、异步排队、重新生成和场景造型共用同一套入口校验，坏图不会先写入对象存储或数据库。
- PNG 解析校验块顺序、CRC、IEND、IHDR 参数和受限解压；JPEG、WebP 在结构校验后交给隔离的 FFmpeg 进程解码一帧，带 15 秒超时并在工具缺失时显式失败。
- 真实图片供应商一旦被选择，调用失败、空结果、伪图片或不可裁剪拼合图均返回可重试错误；不会再生成确定性占位切片并写成 `APPROVED` / `PUBLISHED`。
- 明确未配置供应商或测试 fake provider 时仍保留 `local_placeholder` 模式，产物带可识别来源；真实供应商成功结果的七个发布视图全部来自同一张真实拼合图裁剪。
- 精简 FFmpeg 构建补齐静态 zlib、pipe、PNG/JPEG/WebP 解码、图片管道输入和 null 输出；构建阶段用三种真实图片执行与服务端相同的 smoke 命令，避免开发机完整 FFmpeg 掩盖正式包能力缺失。
- WebP 仅扩展人物 `source` 资产，授权证明仍只允许原有 PDF/JPEG/PNG；未执行真实付费图片供应商、Windows NSIS 实机和生产存储验收，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移，可直接 revert 本项提交。

## FP-09 交付说明

- [本机开发库存只读盘点报告](./FP-09-本机开发库存只读盘点报告.md) 绑定 `server/data/app.db` 的文件大小、修改时间和 SHA-256；全程使用 SQLite 只读、immutable 和 query-only 模式，盘点前后数据库哈希一致且没有新增 WAL 内容。
- 当前本机开发库有 5 个已发布人物版本：4 个拼合图标记为 `image_provider`，1 个标记为 `local_placeholder`；占位版本关联 7 张批准视图，并被 1 个项目主人物、2 条当前参考选择及 4 条冻结 JSON 快照引用。
- 35 张批准视图均能追溯到原生成资产，但这些历史生成资产全部缺少 `view_content_source`；报告将其全部列为切片来源未知，没有根据拼合图来源推断切片真实性。
- 已分别给出标记不可用、取得源图后真实重生成和仅保留隔离 DEV 样例三种处置方案。当前付费授权上限为 0 元；Q02 和具体金额授权前不执行删除、覆盖、扣费、供应商调用或生产访问。
- 本报告只覆盖本机开发 SQLite 快照，不代表客户生产 PostgreSQL、COS 对象内容或其他机器库存已经盘清；无数据或业务代码变更，可直接 revert 本项提交。

## FP-10 交付说明

- 爆款全部列表与收藏列表使用服务端游标加载更多；搜索继续只过滤当前已加载内容，清空搜索后恢复当前上下文最后确认的游标。
- 分页快照按范围、平台、排序和分类分区保存；A→B→A 可恢复 A 的已确认位置，各上下文不会串用游标。
- 加载更多失败保留最后成功内容和游标；服务端明确返回 `VIRAL_CURSOR_INVALID` 时只清除当前上下文坏游标，并自动无游标重读首屏。
- 首屏、收藏和加载更多的成功、失败与收尾均校验请求 ID、重载修订号和分页上下文；搜索或筛选切换后的迟到结果不能污染新页面。
- 六轮独立 Code Review 逐步阻断本地搜索语义、分页恢复、多上下文保存、失效游标和迟到失败竞态；全部补回归后 APPROVE。
- 本项已随 FP-11、FP-20、FP-30 整体合入统一分支；冲突处理后前端 8 文件 365 项、后端 287 项通过，25 项 PG 用例因 fixture 未启动跳过。
- 未执行真实抖音/视频号来源、生产账号和生产 PostgreSQL 验收；当前不声明 `REAL_CHAIN_VERIFIED`。

## FP-11 交付说明

- 爆款“提取文案”和“复刻”统一以当前账号、平台、原生视频 ID 和用途发起幂等导入；同源重挂载安全重放，提文案与复刻互不串键。
- 正式入口缺少平台原生 ID、媒体不可用、仅音频或能力不匹配时留在当前页面给出明确反馈，不创建伪项目或把错误来源写入草稿；审核示例继续走本地数据。
- 导入完成后将真实项目和来源资产交给文案或分析流程；切换来源、账户、页面或请求代次后，迟到结果不能覆盖当前草稿。
- Worker 在准备和完成写入阶段均复查任务租约、项目归属与来源可用性；项目转移、删除和来源删除标为永久失败，同一旧键不能重新变回处理中。
- 幂等键仅在成功、可重试失败、网络结果未知和卸载时保留；永久失败、4xx、轮询超时和缺项目/资产的畸形成功按精确 expected key 清除，避免迟到旧操作误删新键。
- 第 120 次轮询返回成功或失败时仍按终态处理，只有最后一次仍未终结才按超时换键，避免已成功任务被重复导入。
- 前三轮独立 Code Review 分别阻断永久键、错误可重试分类/畸形成功及末次轮询边界；修复后第四轮 APPROVE。
- 本项已随 FP-10、FP-20、FP-30 整体合入统一分支；未调用真实短视频数据源、ASR、分析供应商或生产对象存储，不声明 `REAL_CHAIN_VERIFIED`。

## FP-12 交付说明

- 文案改写请求绑定当前账号、项目、来源资产和人物 IP 版本；切换任一业务归属后，旧请求、旧草稿和迟到响应均不能覆盖新上下文。
- 页面从后端恢复最新改写任务和云端草稿，草稿保存采用串行队列；快速连续编辑、重挂载和乱序完成只允许最新有效内容成为最终状态。
- 自动保存和手动保存共同校验保存操作代次、账号、草稿范围与当前角色权限；从 customer 切换为 auditor 后，已排队定时器和在途保存不能继续写入或显示成功通知。
- 保存失败保留本地编辑并提供明确重试；成功、失败和收尾只更新发起时仍为当前的请求，避免旧操作清除新错误或新忙态。
- 分支评审逐步阻断草稿定位、队列竞态和测试隔离问题；与 FP-25 合并后补齐角色权限代次，最终 FP-12/25 联合 Code Review 均为 APPROVE。
- 本项未调用真实改写供应商，也未执行生产账号跨设备和浏览器人工验收；这些证据属于 FP-35/37/38，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移；功能提交和合并提交均可独立 revert。

## FP-13 交付说明

- 场景照片进入口播分身时，在同一次草稿更新中写入人物 ID 和图片 ID；口播创建只读取当前草稿中的图片 ID，不再依赖页面内临时对象。
- 切换人物会清除旧人物的图片、分身和声音；只有本次更新明确提供新图片时才保留，避免人物 B 误用人物 A 的照片。
- 缓存照片若被服务端明确判定不存在或无权访问，会冻结旧选择并提示重新选择；普通路由 404 和未知 404 保持可重试，不被误判为资产失效。
- 选择上下文版本、操作 ID 和布局阶段生命周期共同隔离 A→B→A、授权弹窗、创建分身、成功/失败收尾、组件卸载及 React 提交窗口中的迟到响应。
- 照片选择列表仅展示当前人物的非拼合图片；缓存照片不可用时保留清晰的重新选择入口。
- 前三轮独立 Code Review 分别阻断人物/图片归属、错误分类和异步生命周期问题；全部补回归修复后，第四轮 APPROVE。
- 未调用真实图片或口播供应商，未执行生产数据验收；这些证据属于 FP-38，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移；集成提交可直接 revert。

## FP-14 交付说明

- 完整口播音频与声音克隆样本都复用统一素材上传合同，显示进度并支持取消；AbortSignal、操作代次、人物上下文和卸载门禁共同阻止取消或离页后的迟到完成写入草稿。
- 完整口播音频上传完成后写入真实 `audio_asset_id`，AUDIO 模式不需要 TTS 声音；上传失败、未完成、取消或用途不匹配时不能提交。
- 素材选择器通过服务端搜索和分页访问首屏外云音频，按用途过滤并读取真实签名预听地址；无权限资产不会获得可提交用途。
- 新音频必须声明单一用途，并由服务端 ffprobe 写入可信时长标记；历史缺用途或未验证时长的音频只读展示，`allowed_uses=[]`，不能用于口播或声音克隆。
- 口播和声音克隆提交端分别复查 owner、媒体类型、完成态、用途和时长；声音样本按 5～180 秒合同校验，不把完整口播音频误作克隆样本。
- 首轮 Code Review 阻断无用途绕过、TypeScript 门禁和声音样本上传生命周期；补测试修复后第二轮 APPROVE。合并时统一了 `live` 模块测试 Mock，联合专项全绿。
- 未执行真实 COS 大文件上传、真实浏览器音频元数据兼容和真实供应商验收；这些证据属于 FP-38，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移；两笔功能提交及集成提交均可 revert。

## FP-17 交付说明

- 人物列表使用绑定账号、角色和查询范围的服务端游标；9 人数据可从 8 条首屏继续读取第 9 人，并返回真实 total。
- 场景列表使用当前人物和场景类型范围内的 total、稳定排序和 LIMIT/OFFSET；13 套场景按 12+1 到达，第二页失败保留 12/13 与重试入口，成功后按资产 ID 去重追加。
- 场景 SQL 先分页当前人物的 persona，再仅查询当前页 persona 的最新发布版本和完整发布视图；移除对全库发布版本的窗口排序，并用 200 条无关发布版本锁定查询结构。
- 普通批次使用游标与 scoped total，口播任务使用独立 offset/total；两类记录保留独立标识、历史入口和加载状态，21 条数据均可跨首屏到达。
- 筛选、隐藏及末页变空后会按真实 total 回退，不形成空白死页；轮询刷新保留已加载深页，一类请求失败不清空另一类历史。
- 前两轮 Code Review 分别阻断路由层全量切片、失败后无法重试和全库版本排序；改为数据库页内查询后第三轮 APPROVE。
- 未在生产规模 PostgreSQL 执行 `EXPLAIN ANALYZE`，也未执行真实大数据量浏览器验收；这些证据属于 FP-35/37，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移；三笔功能提交及集成提交均可 revert。

## FP-18 交付说明

- 客户额度流水和充值订单 API 透传服务端现有 `limit/offset` 参数；前端每页 20 条，可访问第21条及更旧记录。
- “最近充值订单”继续作为快捷区并隐藏已关闭订单；“查看全部充值记录”展开独立分页历史，完整历史保留 CLOSED 状态。
- 摘要、流水和完整订单历史使用独立请求序号；流水翻页不重复读取钱包或最近订单，迟到结果不能覆盖更新后的订单状态。
- 页码只使用成功响应返回的 offset；翻页失败保留最后成功内容和页码，并在对应区域提供重试，不把旧数据标成新页。
- 创建或关闭订单后重新读取最近订单和用户当前成功打开的历史页；异步创建迟到不会把新订单插入第二页等旧闭包位置。
- 摘要加载错误、分页错误和用户操作错误分别保存；初始化请求迟到不会清除后来发生的关闭失败，支付已确认也不会因视图刷新失败被误报为支付状态查询失败。
- 后端现有客户路由已按 session 用户过滤并返回 total/limit/offset；本项未修改账务、支付状态判定和数据库结构，未执行真实支付或生产账号验收。
- Code Review 前两轮共阻断 4 项 P2，全部修复后第三轮 APPROVE；无数据库迁移，可直接 revert 本项提交。

## FP-19 交付说明

- 先对素材库联调 Workspace 的 `2cb8a17`、`b3114c2`、`87e03a7`、`435adbb` 做差异审计，只复用 `b3114c2` 的“当前页按需签名”方向；没有整提交搬运，也没有覆盖已集成的 FP-14 音频用途与 FP-17 分页合同。
- 工作台启动只读取素材元数据；项目来源和草稿明确引用的少量资产仍按 ID 恢复，不再为最多 60 条历史图片逐条获取短期签名。
- 素材库和图片选择器按每页 6 条获取预览；首屏 6 条、第二页仅增加实际存在的 1 条。翻页、账号或请求代次变化后，旧签名响应不能覆盖当前页。
- 图片、视频和音频的媒体加载错误都有明确失败状态与用户触发的单次重签；同一素材只保留一个在途请求，旧 URL 的迟到错误不会清掉新 URL，详情复用当前有效预览且不重复签名。
- 仅有 `generation_task_id` 的直出成片可取得预览；删除末页唯一素材后返回有效页。人物照片数使用服务端场景 `total`，未加载时显示未知，不再用 0 冒充。
- 首轮 Code Review 实测旧启动路径在 60 条素材下发生 61 次签名，并阻断音视频错误与详情测试缺口；返修补红灯后，第二轮独立复核与统一集成专项均通过。
- 全仓秘密脚本仍会命中基线测试中的假值 `older-token`；本项新增行秘密扫描无命中，该既有门禁问题不属于 FP-19 变更。
- 未执行真实 COS 签名过期、公网弱网和浏览器媒体兼容验收；这些证据归入 FP-38，本项不声明 `REAL_CHAIN_VERIFIED`。无数据库迁移，可直接 revert 两笔功能提交及集成提交。

## FP-20 交付说明

- 收藏、取消和收藏列表继续复用现有 `viral_video_favorites`、账号归属、平台加原生视频 ID 稳定主键以及游标分页；没有新建第二套收藏存储。
- 收藏 mutation 同时绑定账号与操作代次。账号切换、A→B→A、组件卸载或旧请求迟到后，旧操作不能回滚新账号、发通知、调用 `onSavedChange`、写共享状态或永久禁用按钮。
- 首屏、详情和“全部爆款/我的收藏”深页请求都把账号与完整分页上下文纳入单调代次；当前上下文首屏尚未成功确认前，加载更多不会沿用旧账号或旧筛选的游标。
- 保存失败恢复原显示，重复点击只保留一个逻辑写操作；来源下架后收藏记录继续返回并以 `hidden` 状态解释，不伪装成仍可复刻的在线来源。
- 未发布的 `079_viral_favorite_page_index` 追加两种索引，分别覆盖无平台和平台等值条件下的 `created_at DESC` 稳定 keyset 排序；SQLite `078 → 079 → 078 → head` 往返及两种真实 SQL 查询计划均无临时 B-Tree。
- 前两轮 Code Review 依次阻断账号 effect 窗口、详情/深页迟到、索引方向、A→B→A 锁死和新账号首屏前旧游标；补红灯返修后第三轮独立复核 APPROVE。
- 本项已随 FP-10/11/30 整体合入统一分支。未启动 PostgreSQL fixture，因此生产规模 `EXPLAIN (ANALYZE, BUFFERS)` 留给 FP-37；未执行双授权设备重新登录、刷新和跨账号浏览器验收，不声明 `REAL_CHAIN_VERIFIED`。

## FP-21 交付说明

- 普通生成详情以 `generation_batch + batch_id` 定位，口播详情以 `oral_task + task_id` 定位；Analytics 不再把 generation task ID 当作批次 ID。地址只包含白名单页面、对象类型、对象 ID、返回页和明确筛选，不包含 session、设备或后台凭据。
- 首屏或当前已加载页之外的普通批次复用 `getGenerationBatch` 按 ID 读取，口播复用现有 owner-scoped 详情接口；不存在、无权限和服务失败分别进入受控状态并可重试，不依赖前 20 条内存列表。
- Studio 详情对象、必要选择和 `returnTo` 可随刷新、直接深链接、Back/Forward 恢复；busy 写操作期间只暂存最后一个历史路由，操作结束后应用。普通导航清除不再适用的旧选择和返回页，各详情入口显式声明来源。
- 详情与预览以账号、后端类型和对象 ID 组成完整上下文；同 ID 换账号时，未完成 owner 验证前不展示旧缓存，切账号、卸载或对象变化后的迟到成功/失败不能更新共享数据。
- 后台 7 个模块和失败生成记录 intent 使用规范 hash；同模块清除筛选及前进后退会重置对应子页状态和 offset。旧 `projects`、`characters`、`wallet` 别名继续可用，未知路由安全回退。
- 口播列表和详情共享 Pydantic 响应模型，详情声明 404 并从 fresh OpenAPI 生成客户端类型；读取仍以 `task_id + actor.id` 过滤，外账号统一返回不存在。
- 首轮 Code Review 阻断 Admin intent、busy 历史、returnTo、账号缓存和 OpenAPI 五项；补红灯返修后第二轮 APPROVE，统一集成后的前后端专项和静态检查再次全绿。
- 未在真实 Chromium 验证原生 `popstate/hashchange` 顺序、Live 长写操作期间连续历史跳转，也未执行 customer/auditor/admin 真实会话 UAT；这些证据归入 FP-35/37。本项无数据库迁移，可直接 revert 两笔功能提交和集成提交。

## FP-22 交付说明

- 工作台经营报表直接聚合当前账号拥有的普通生成批次、独立批次和数字人口播任务；没有项目 ID 的独立批次按 `created_by` 归属纳入，其他账号数据不会混入。
- 今日、近 7/30 天、趋势和响应 `generated_at` 共用请求入口采样的同一个 UTC 时刻，并以北京时间自然日计算；请求恰好跨越北京时间零点时也不会把卡片和趋势拆到不同日期。
- SQLite 文本时间统一解析 `Z`、显式时区和空格格式后再比较；PostgreSQL 使用 `timestamptz` 参数。运行中统计覆盖 `SUBMITTING`、`GENERATING`、`ARCHIVING`，失败和产出按普通批次、独立批次、口播分别给出清晰字段。
- 前端图表绑定账号与请求代次；A→B→A、组件提交窗口和迟到成功/失败不能覆盖当前账号报表。加载失败保留独立重试入口，不用假数据或固定数字填充。
- 前两轮 Code Review 依次阻断归属、文本时间、运行态、重复口径、账号竞态和单请求跨日问题；全部补确定性回归后第三轮 APPROVE，合入统一分支后的前端 108 项、后端 31 项及静态检查再次通过。
- PostgreSQL fixture 未启动，未在生产规模数据执行查询计划和真实角色浏览器验收；这些证据归入 FP-37，本项不声明 `REAL_CHAIN_VERIFIED`。无数据库迁移，可直接 revert 三笔功能提交和集成提交。

## FP-23 交付说明

- 客户管理的设备与会话面板共用同一个客户选择；从客户列表、会话列表或客户 ID 查询切换时，两块内容同步更新，不再出现设备属于 A、会话属于 B 的混合上下文。
- 设备与会话列表使用请求序号丢弃迟到响应；下线设备、结束会话和加秒操作绑定客户上下文代次，即使发生 A→B→A 切换，旧 A 操作完成后也不会覆盖新 A 页面、关闭新弹窗或触发额外刷新。
- 仪表盘“失败任务待处理”直接进入已应用 `FAILED` 条件的生成记录；失败数以相同的上海自然日七日窗口统计普通视频和数字人口播失败。
- 生成记录增加数字人口播类型、只读错误详情和供应商任务凭证；结果资产与供应商任务 ID 分开返回，后台没有新增代客重试入口。
- 口播供应商原始错误信息不返回管理端；接口只按任务状态输出固定安全分类文案，包含 Bearer token 或签名 URL 的恶意测试数据不会出现在响应中。
- 首轮 Code Review 阻断了错误信息泄漏、共享上下文回传、口播失败统计及供应商凭证混用，全部补回归修复后第二轮 APPROVE。
- 本项没有调用真实 Hifly、付费任务或生产数据；未执行生产 PostgreSQL 和真实角色人工验收，因此不声明 `REAL_CHAIN_VERIFIED` 或 `PRODUCTION_GO`。
- 无数据库迁移，可直接 revert 本项提交。

## FP-24 交付说明

- 客户资料读取失败会显示明确错误和重试入口；资料未加载时禁用保存，重试结果按请求序号隔离，旧工作区异步读取完凭据后不会再发请求。
- 心跳运行态分别记录网络可达性、最后成功心跳和服务端租约截止时间；网络失败不伪造租约失效，租约到期会按本机时钟即时更新设备在线状态。
- 手动与自动心跳共用请求序号和会话代次门禁；乱序结果、退出后的结果、旧会话 401 以及延迟解析的错误体均不能覆盖或结束新会话。
- 个人中心接通现有后端退出接口；重复点击只提交一次，退出后清除 session token 并保留长期设备凭据，网络失败也能安全回到登录页。
- “暂不处理”只在当前页面折叠待确认请求并显示说明，不调用不存在的拒绝、审批或删除接口；重新进入设备管理仍可继续处理。
- 解绑在弹窗确认前后及异步读取设备凭据后均复查最新租约；列表刷新错误与操作错误分离，后台刷新不会抹掉租约失效提示。
- Code Review 前两轮共阻断 5 项会话与租约竞态，全部修复后第三轮 APPROVE；未执行真实桌面双设备和生产会话人工验收，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移，可直接 revert 本项提交。

## FP-25 交付说明

- 后端人物重命名/IP 资料、口播授权/分身/声音/确认、Studio 草稿和已存文案删除均在领域写入口最前面执行审计员门禁；原有首帧、人物参考和场景人物门禁继续复用同一规则。
- 角色矩阵验证审计员写请求返回 `403 / ROLE_FORBIDDEN` 且业务表无副作用，同时 customer 对自有人物、口播、首帧、人物参考、场景人物和 Studio 草稿的原有写路径保持可用。
- 前端人物页、创作页和工作台按实时 `currentUser.role` 进入只读；人物与场景分页、首帧签名预览等读取仍可用，创建、上传、删除、改名、确认、生成、保存和历史文案选择等写入口同时具备控件禁用和 handler guard。
- customer→auditor→customer 角色切换会推进权限代次并使旧自动保存、手动保存、文案提取、口播/视频生成回调失效；口播和视频使用独立提交尝试号，旧 finally 不会解锁后来的新请求。
- 审计员切入时会关闭已打开的人物选择器，选择器提交时也重新读取当前角色；历史文案与“更换人物”在只读态不会触发草稿写入或打开选择流程。
- 后端证据线首轮 APPROVE；前端经过三轮 BLOCK 返修，合并级复核再阻断两个历史控件和选择器角色窗口，补回归后最终 APPROVE。统一集成专项前端 378 项、后端 239 项通过。
- PostgreSQL fixture、真实 customer/auditor/admin 会话和逐页面浏览器验收仍归 FP-35/37；本项不声明 `REAL_CHAIN_VERIFIED`。无数据库迁移，可直接 revert 对应提交。

## FP-26 交付说明

- 正式客户工作区通过当前 session 读取客户钱包，内部工作区继续使用内部钱包接口；两种身份不会混用凭据。
- 钱包状态区分未查询、查询中、真实 `0 秒`、成功余额和读取失败；失败可重试，不再用固定数值或 0 掩盖请求失败。
- 钱包请求绑定用户与修订号；切换账户、重试和组件卸载后，旧响应不能覆盖当前账户状态。
- 正式账户概览中的用户名和加入时间来自客户资料；资料读取失败会显示“重试资料查询”，成功后清除错误，复用现有资料请求的迟到响应隔离。
- 发布账号、待发布数量和正式发布继续显示明确暂缓状态；授权和发布按钮保持禁用，没有合入或调用暂缓的 C5 发布能力。
- 首轮 Code Review 阻断资料重试入口和错误基线；候选在当前统一集成线重新落地、补回归并验证后，第二轮 APPROVE。
- 未执行真实客户账号、生产钱包或发布平台验收；这些证据属于 FP-35/37，发布链路仍受 FP-31～33 的确认与解冻约束。
- 无数据库迁移，可直接 revert 两笔提交。

## FP-27 交付说明

- 管理设置页新增 Hifly 数字人口播配置；API Key 继续使用现有加密设置仓库存储，读取和保存响应只返回掩码，输入框不会回填密钥明文。
- “只读检查”只调用 Hifly 账户余额接口，不创建分身、声音、口播或其他收费任务；未配置、认证失败、超时、供应商失败和成功余额均有独立状态。
- Hifly 自身的 401/403 和无效 token 统一映射为业务认证失败，不触发本系统的 `SESSION_EXPIRED_EVENT`，工作台和控制台会话均可留在当前页修正凭据后重试。
- 供应商响应严格校验 JSON、状态码和余额类型；布尔值、浮点数、无穷大、超长整数、负余额、缺失余额及超出 JavaScript 安全整数范围的余额全部失败收敛，避免误报成功或显示被舍入的金额。
- 客户、员工和审计员不能读取或修改管理员供应商设置；只读操作员页面会禁用密钥输入、保存和连接检查。
- Code Review 前四轮依次阻断会话误退出、畸形响应误成功、超长整数异常和余额精度问题，全部补回归后第五轮 APPROVE。
- 本项未使用真实 Hifly 凭据，未执行生产网络、真实余额或任何付费生成；这些证据保留给获准后的 FP-38，本项不声明 `REAL_CHAIN_VERIFIED`。
- 无数据库迁移，可直接 revert 本项提交。

## FP-30 交付说明

- 正式链接入口只接受规范抖音原生视频 ID；先通过既有豆易豆网关解析，再对媒体执行协议、主机、标准端口、逐跳 DNS、公网固定 IP、Host/SNI/证书、MIME、魔数、大小及 ffprobe 校验。媒体完整预取并进入受控缓存后才创建项目，不留下失败半成品。
- Provider 签名不随重定向外泄；禁止 HTTPS 降级、私网或混合 DNS、非常规端口、循环和超限重定向。连接固定到本次验证的公网 IP，消除 DNS 预查与实际连接之间的重绑定窗口。
- `080_viral_link_resolution_receipts` 使用 PREPARED 租约、REQUEST_SENT 和终态区分安全接管与结果未知。可能已经发出付费请求时不自动重调；并发、进程崩溃和终态落库前崩溃均以同一收据阻止重复付费。
- PREPARED、REQUEST_SENT 和最终结果使用独立受管事务；客户 PostgreSQL 路径不在 psycopg transaction context 内显式提交或回滚。REQUEST_SENT 过期时先提交 UNCERTAIN，再于事务外返回受控 503，响应与数据库状态一致。
- Workbench 链接、上传和渲染状态绑定账号与操作代次；A→B→A、迟到上传、迟到解析和换账号后的按钮状态不会复用旧账号项目或来源。
- 前四轮 Code Review 依次阻断付费幂等/伪来源/媒体验证、崩溃租约/DNS 重绑定、PG 事务/非常规端口和 UNCERTAIN 回滚；全部补红灯返修后第五轮 APPROVE。
- 本项与 FP-10/11/20 整体合入统一分支；合并级 Code Review 确认共享文件保留 FP-19 可见页签名和 FP-21 稳定详情导航，并阻断 11 处仍指向 078 的“当前迁移 head”测试断言。返修 `c66168ec` 后复审 APPROVE，前端冲突相关 336 项通过，后端完整相关套件 373 项通过、141 项 PG skipped；TypeScript、Biome、Ruff、格式、mypy通过。
- 未启动 PostgreSQL 实库、未调用真实豆易豆付费服务，也未执行真实浏览器逐角色链路；这些证据归入 FP-37/38，不声明 `REAL_CHAIN_VERIFIED`。迁移 080 尚未发布，当前可按 080→079 回退。
