# 当前文案工坊页面复核

日期：2026-09-07。入口：本机 Vite 开发服务 `http://127.0.0.1:5176/review/v1.4#studio/copy`。使用 Codex 浏览器读取页面及点击控件，未连接业务 API。页面明确标注“示例审核 · 不调用业务接口”，以下证据只证明当前 React 页面交互。

| 顺序 | 操作 | 页面观察 |
| --- | --- | --- |
| 1 | 打开文案工坊 | 显示原文、可编辑二创文案、作品名称、终稿 V3、IP 卡片、按 IP 二创、更换人物、确认终稿、保存版本、用于数字人口播；未见返回来源按钮、原文纠错入口、提取任务进度区、字数/预计口播时长。 |
| 2 | 保存版本→我的文案 | 列表一条“张工 · 建房预算 已确认 · V3”；示例提示只保留本次工作区。 |
| 3 | 点击该文案 | 可访问性树无变化，仍在我的文案 Tab；需要手动切回文案改写。 |
| 4 | 切回改写→用于数字人口播 | URL 转为 `#studio/oral`，显示同一标题、同一正文、终稿 V3、当前人物/声音/分身；未点击生成或费用确认。 |
| 5 | 点击去文案工坊修改 | URL 回到 `#studio/copy`，正文仍在；工坊仍未出现“返回口播/返回来源”的专用按钮。 |
| 6 | 修改正文为审查用第二版，保存→我的文案 | 仍只有同一条记录，变为“草稿 · V3”，没有 V4 或旧稿历史。 |

保存版本的数据库覆盖行为另外由 `studio_drafts.py` 的 upsert 和专项测试证明；本记录不能证明云端保存、真实提取、真实 AI 改写、付费口播生成成功。
