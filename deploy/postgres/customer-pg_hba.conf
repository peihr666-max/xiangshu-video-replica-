# Container-local administration remains available to pg_dump/restore.
local all all trust
# No unencrypted TCP connections, even on the private compose network.
hostnossl all all all reject
hostssl all all all scram-sha-256
